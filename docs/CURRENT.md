# Current — NEXAMed

Trạng thái hiện tại của dự án. Cập nhật file này mỗi khi trạng thái thay đổi đáng kể (không phải log từng commit — đó là việc của `CHANGELOG.md`).

## Giai đoạn

Sprint 1 — Nền tảng (tuần 1-2 theo `docs/product/plan.md`). S1-01, S1-02, S1-03, S1-04, S1-04b, S1-04c, S1-05, S1-06, S1-07, S1-08 đã xong. Còn S1-09.

## Đã có

- `CLAUDE.md` — quy tắc và ràng buộc dự án (đã bổ sung quy định viết code hướng tái sử dụng).
- `.claude/docs/` — 7 tài liệu kỹ thuật đã chốt.
- `docs/product/prd.md`, `docs/product/plan.md` — đã có nội dung đầy đủ.
- `docs/ERD.md` — sơ đồ quan hệ dữ liệu đầy đủ.
- **S1-01 xong**: monorepo pnpm (`apps/api` NestJS, `apps/web` React+Vite, `packages/core`, `packages/shared`), cấu trúc thư mục theo `.claude/docs/project-structure.md`, ESLint boundary rule chặn `packages/core` import NestJS/Prisma/React/`process.env`, CI (`.github/workflows/ci.yml`).
- **S1-02 xong**: `apps/api/prisma/schema.prisma` với 7 bảng nền tảng (`tenant`, `tenant_setting`, `room`, `user_account`, `user_role`, `code_sequence`, `audit_log`), migration đầu tiên (`prisma/migrations/20260807090207_init`), script kiểm 8 cột bắt buộc (`scripts/check-mandatory-columns.mjs`, nối vào CI), Zod env validation ở `apps/api/src/config` (đã kiểm chứng: API crash đúng khi thiếu `DATABASE_URL`/`JWT_SECRET`/`ENCRYPTION_KEY`, chạy được khi đủ), `docker-compose.yml` gốc cho Postgres local dev, `apps/api/.env.example`. `packages/shared` có enum 5 vai trò (`roles.ts`).
- **Postgres nâng lên bản 18** (từ 16) để dùng `uuidv7()` thật cho mọi `id` — xem `docs/DECISIONS.md` #009.
- **S1-03 xong**: role Postgres riêng `nexamed_app` (không superuser/BYPASSRLS/DELETE) cho API runtime, tách khỏi role `nexamed` (chỉ dùng cho migration qua `MIGRATE_DATABASE_URL` + `apps/api/scripts/with-migrate-url.mjs`); RLS thật trên 6 bảng có `tenant_id`; `CHECK (version >= 1)`; `PrismaService` + `UnitOfWorkService` (`apps/api/src/infrastructure/persistence`, mở transaction + `SET LOCAL app.current_tenant_id`); `TenantContextMiddleware` (tạm đọc header, chờ JWT ở S1-04 — xem `docs/DECISIONS.md` #012) wire global vào `AppModule`. **Đã xác minh thật bằng integration test** (`tenant-isolation.spec.ts`, 4/4 pass trên Postgres thật): cách ly tenant đúng, không set context thì lỗi thay vì rò dữ liệu, không xoá được, không insert xuyên tenant được.
- **RBAC + Data Scope xong (S1-04b)** — thay hoàn toàn mô hình "5 vai trò cứng" theo yêu cầu chủ dự án (`docs/DECISIONS.md` #013-#016): bảng `department`, `role`, `permission` (danh mục toàn hệ thống, 23 permission), `role_permission` (ma trận `role × permission → data_scope`, 4 mức `none`/`personal`/`department`/`global`, không có `branch`), migration `*_rbac_data_scope` (RLS + CHECK + REVOKE cho bảng mới). Ma trận mặc định + logic seed đặt ở `packages/core/src/rbac/permissions.ts` (nguồn sự thật, dùng chung seed script/guard/web sau này). `seedDefaultRolesForTenant`/`seedPermissionCatalog` ở `apps/api/src/infrastructure/persistence/`. **Đã xác minh thật** (`rbac.spec.ts`, 4/4 pass): seed đúng 5 vai trò/tenant, RLS cách ly `role`/`role_permission` theo tenant, `permission` đọc được nhưng không ghi được qua role app, `doctor.encounter.read = global` đúng thiết kế (không break-glass cho tiền sử — khớp PRD ENC-01 P0).
- `docs/design/` — có `UI_GUIDELINE.md`, `AI_AVOID_RULES.md`.
- `docs/product/future-modules-reference.md` — tài liệu tham khảo (chưa chốt) cho Dược/kho, Viện phí, danh mục dùng chung, ý tưởng luồng khám mở rộng — dùng khi tới v2/v2.1/v3, đã ghi rõ mâu thuẫn với schema/state machine v1 đã chốt, không tự áp dụng.
- **Auth xong (S1-04)** — JWT access token 15 phút + refresh token httpOnly cookie xoay vòng mỗi lần refresh, Argon2id, khoá tài khoản tạm sau 5 lần sai/15 phút (`docs/DECISIONS.md` #019-#020). Bảng mới `user_session` (rotation chain + phát hiện reuse — dùng lại refresh token đã rotate thì thu hồi toàn bộ phiên của user), `user_account` thêm `failed_login_count`/`last_failed_login_at`/`locked_until`, migration `*_auth_sessions`. Module `apps/api/src/modules/iam/` (`AuthController`: `POST /auth/login|refresh|logout`, `AuthService`, `TokenService`, `SessionRepository`, `UserAccountAuthRepository`) + `packages/core/src/iam/lockout.ts` (nguồn sự thật ngưỡng khoá, unit test 9/9) + `packages/core/src/errors/` (khởi đầu lớp lỗi nghiệp vụ — `DomainError` + 5 lỗi auth cụ thể). `TenantContextMiddleware` đổi hẳn sang đọc JWT access token thật (không còn header tạm — hoàn thành cam kết ở `docs/DECISIONS.md` #012). Thêm `JwtAuthGuard` (xác thực tối thiểu, viết + test sẵn, **chưa áp vào controller nào** vì chưa có controller nghiệp vụ), `ResponseInterceptor`/`DomainExceptionFilter` (response envelope `{data,meta}`/`{error}` theo `architecture.md` — áp dụng lần đầu vì đây là controller thật đầu tiên của hệ thống), rate limit riêng cho `/auth/login` (`@nestjs/throttler`, 10 request/phút/IP). **Đã xác minh thật** (`auth.spec.ts`, 9/9 pass trên Postgres thật; `jwt-auth.guard.spec.ts`, 5/5 pass): login đúng/sai mật khẩu, khoá tài khoản đúng ngưỡng, tài khoản vô hiệu hoá, sai `tenantId` bị coi như sai thông tin đăng nhập (không lộ tồn tại xuyên tenant), refresh rotation, phát hiện reuse thu hồi toàn bộ phiên, logout, token hỏng/rác.

- **Break-glass xong (S1-04c)** — `POST /api/v1/break-glass` (yêu cầu access token — nơi dùng thật đầu tiên của `JwtAuthGuard`): xác thực lại mật khẩu (tái dùng `UserAccountAuthRepository.findById` + argon2, tái dùng `InvalidCredentialsError` — không tạo lỗi mới) + lý do bắt buộc → tạo `break_glass_session` (bảng đã có sẵn từ S1-04b, **không cần migration mới**) → ghi `audit_log` (`break_glass.request`) → gọi `NotificationPort.send` (no-op). Thời hạn phiên đọc `tenant_setting` key `break_glass_duration_minutes`, mặc định 120 phút nếu tenant chưa cấu hình. `packages/core/src/iam/break-glass.ts` (hàm thuần `computeExpiresAt`/`isSessionActive`, unit test 3/3) + `packages/core/src/ports/notification.port.ts` (`NotificationPort` — chỉ 1/6 port của S1-06 được kéo trước, giống cách S1-04 đã kéo trước một phần `errors/`) + adapter no-op (`apps/api/src/infrastructure/notification/noop.adapter.ts`). `BreakGlassService.tryConsume()` — primitive sẵn sàng cho guard `data_scope` thật của S2 (kiểm tra phiên còn hiệu lực + ghi `audit_log` `break_glass.access`), tự mở transaction riêng, chưa có guard nào gọi tới. **Đã xác minh thật** (`apps/api` `break-glass.spec.ts`, 6/6 pass trên Postgres thật + kiểm thủ công qua curl): đúng mật khẩu tạo phiên đúng hạn mặc định/tuỳ chỉnh, sai mật khẩu không tạo phiên không gọi notify, `tryConsume` đúng entity trong hạn mới `granted:true`, entity khác/hết hạn thì `false`, không có access token bị 401.

- **Audit log xong (S1-05)** — không có gì mới ở bảng/quyền DB (đã xong từ S1-03). Hai cơ chế ghi tách theo loại thao tác (`docs/DECISIONS.md` #021 — lý do: interceptor HTTP không thể tham gia transaction Prisma mà service tự mở/đóng kín bên trong): **ghi** → gọi tường minh `writeAuditLog(tx, ...)` trong transaction service, dời từ `modules/iam/` sang `apps/api/src/infrastructure/persistence/audit-log.helper.ts` (hạ tầng dùng chung, mọi module domain tương lai gọi được mà không phải import xuyên qua `iam`); **xem** → `AuditViewInterceptor` + decorator `@AuditView('entityType')` (`apps/api/src/common/`), tự mở transaction riêng sau khi handler trả về thành công, không ghi khi handler lỗi, lỗi ghi audit tự nổi lên thành response lỗi (không nuốt). `.claude/docs/security-audit.md` mục Audit log đã cập nhật quy ước này cho S2 theo. **Đã xác minh thật** (`audit-view.interceptor.spec.ts`, 3/3 pass trên Postgres thật) — chưa áp vào controller nào (chưa có controller nghiệp vụ, S2). Toàn bộ 28 test cũ (auth/break-glass/rbac/tenant-isolation/jwt-auth-guard) vẫn pass nguyên sau khi dời helper.

- **`packages/core` hoàn thiện xong (S1-06)** — khung entity thuần (`packages/core/src/entity/base-entity.ts`): `BaseEntity` (8 cột bắt buộc), `AppendOnlyEntity` (audit_log/break_glass_session), `SignableEntity` (dữ liệu lâm sàng ký được, `supersedesId`/`amendmentReason`) + hàm thuần `isSigned()` (nguồn sự thật duy nhất cho quy tắc "đã ký thì bất biến", unit test 2/2). Đủ 6 port ở `packages/core/src/ports/` — 5 port còn lại (`NotificationPort` đã có từ S1-04c): `StoragePort` (save/read/delete, adapter v1 `LocalDiskStorageAdapter` ghi thật ra disk dưới `STORAGE_DIR/<tenantId>/<key>`, có chặn path traversal), `EventBusPort` (publish/subscribe, adapter `InMemoryEventBusAdapter` — đồng bộ, handler lỗi ném lên để service rollback transaction, đăng ký handler là wiring lúc khởi động không phải state theo request), `SignaturePort` (adapter no-op trả `signaturePayload: null`), `InsuranceGatewayPort` (adapter no-op ném `NOT_IMPLEMENTED`, đúng như bảng port/adapter ở `project-structure.md` — chưa có service nào gọi, ngoài phạm vi v1), `PatientIdentityPort` (adapter `SameTenantPatientIdentityAdapter` trả chính `patient.id`, khớp `global_patient_ref` luôn null ở v1). Đăng ký DI qua `apps/api/src/infrastructure/ports.module.ts` (`@Global()`, tách khỏi `NOTIFICATION_PORT` vẫn đăng ký riêng trong `IamModule` như cũ — không đụng lại), wire vào `AppModule`. Thêm `STORAGE_DIR` vào `env.schema.ts`/`.env.example` (mặc định `./storage`, gitignore). **Đã xác minh thật**: `base-entity.spec.ts` (2/2), `local-disk.adapter.spec.ts` (4/4, ghi/đọc/xoá file thật + chặn key chứa `..`), `in-memory.adapter.spec.ts` (3/3); build thật rồi chạy `node dist/main.js` — log xác nhận `PortsModule dependencies initialized` và `Nest application successfully started` qua NestJS DI container thật (khác các test trước chỉ `new Service(...)` tay, không qua Nest). Không có folder `patient-identity/` sẵn trong cây thư mục minh hoạ ở `project-structure.md` (chỉ liệt kê storage/eventbus/signature/insurance/notification) — tạo thêm theo đúng pattern "một thư mục cho mỗi port" đã có, không phải đổi cấu trúc đã chốt.

- **Web app shell xong (S1-08)** — hỏi chốt luồng/menu với chủ dự án trước khi code (theo yêu cầu mới: luôn hỏi trước khi xây mỗi chức năng): menu sidebar kết hợp theo luồng nghiệp vụ **và** ẩn/hiện theo vai trò; có Dashboard chung dạng empty-state (không bịa số liệu, vì patient/appointment/encounter chưa tồn tại tới S2-S3); Quản trị là mục riêng trong sidebar (không gộp avatar), chỉ hiện với `clinic_admin`/`system_admin`; chỉ hiện menu module đã có backend thật (Đặt lịch/Tiếp nhận/Khám bệnh/Kê đơn chưa hiện, thêm dần đúng sprint); màu/token theo `.claude/docs/ui-guidelines.md` (không theo bảng hex/Geist ở `docs/design/UI_GUIDELINE.md` — tài liệu đó chỉ tham khảo layout/menu chung, không phải màu). `tenantId` nạp runtime qua `apps/web/public/config.json` (gitignore, có `config.example.json`) — sửa trực tiếp lúc deploy on-prem, không cần rebuild (chốt xong điểm treo ở `docs/DECISIONS.md` #020).
  Backend mở rộng nhỏ đi kèm (`docs/DECISIONS.md` #022): `loginResponseSchema.user` thêm `roles`; thêm `GET /auth/me` (`JwtAuthGuard`, lần dùng thật thứ hai sau break-glass) để web khôi phục danh tính/vai trò lúc reload trang (chỉ còn refresh cookie, chưa có `user` trong bộ nhớ).
  Frontend: Tailwind v4 qua `@tailwindcss/vite` (không cần `postcss.config.js`/`tailwind.config.js` riêng — khác giả định Tailwind v3 ban đầu), `react-router-dom` v7, `@phosphor-icons/react`; API client tối thiểu tự viết (`apps/web/src/shared/api/client.ts`, chỉ đủ cho auth flow, S1-09 thay hẳn bằng client sinh từ OpenAPI); Zustand auth store (session state, không phải server entity data); `LoginPage`/`RequireAuth`/`AppBootstrap`; `AppShell`/`Sidebar`/`UserMenu`; `DashboardPage`/`AdminPage` (empty state); `Button`/`EmptyState` dùng chung ở `shared/ui`.
  Phát hiện + sửa 1 race condition lúc kiểm bằng trình duyệt thật: React StrictMode (dev) gọi effect 2 lần khiến `AppBootstrap` bắn 2 request `/auth/refresh` gần đồng thời, có thể đụng cơ chế phát hiện reuse-token (S1-04) — chặn bằng `useRef` guard (chỉ ảnh hưởng dev, StrictMode không chạy ở production build, nhưng vá cho chắc).
  **Đã xác minh thật**: `pnpm -w lint/typecheck/build` sạch; 45 test `apps/api` pass; chạy `pnpm dev` thật + Playwright (headless Chromium — môi trường chưa có project skill `run`, tự cài để dùng) chụp ảnh xác nhận đúng luồng: chưa đăng nhập → `/login`; đăng nhập đúng → Dashboard đúng token màu (sidebar `bg-slate-900`), thấy Tổng quan + Quản trị (user test có vai trò `clinic_admin`); F5 giữ nguyên phiên (refresh+`/me` hoạt động); đăng xuất → `/login`.

## Đang chờ

- Guard đọc `role_permission` theo `data_scope` thật trong request, tự trả `403 { breakGlassAvailable: true }` khi bị chặn — schema/seed xong, `BreakGlassService.tryConsume()` sẵn sàng; áp vào controller thật là việc của S2 (khi có controller nghiệp vụ đầu tiên). Khi có controller đầu tiên, cũng là lúc gắn `AuditViewInterceptor`/`@AuditView` vào các route GET tương ứng.
- ADM-07 (P1): UI cấu hình ma trận phân quyền.
- S1-09: api client sinh từ OpenAPI thay cho bản tối giản tự viết ở S1-08, TanStack Query setup với cache key có `tenantId`.
- Menu Đặt lịch/Tiếp nhận/Khám bệnh/Kê đơn: thêm vào sidebar khi module backend tương ứng ra đời (S2-S4) — khung `Sidebar.tsx` đã sẵn sàng nhận thêm mục.

## Lưu ý môi trường

- Máy dev hiện chạy Node v26.5.0, khác Node 20 LTS ghi trong `CLAUDE.md`/`package.json.engines`. Chưa gây lỗi (chỉ warning), nhưng cần dùng đúng Node 20 khi triển khai thật.
- Docker đã cài trên máy dev. Migration `20260807090207_init` **đã được áp thật lên Postgres 18.4** qua `prisma migrate deploy` và xác minh bằng insert thử — `id` sinh ra đúng UUID v7 (time-ordered, ví dụ `019fdc38-6d2f-7ff2-...`). Không còn là "chưa test".
- **Postgres container map ra cổng 5433, không phải 5432 mặc định** — máy dev này có sẵn một Postgres native (Windows service `postgres.exe`, không liên quan tới dự án) đang chiếm 5432. `DATABASE_URL` trong `.env.example`/`.env` đã trỏ sang `5433`. Nếu đổi máy dev khác không có xung đột này, có thể đổi lại 5432 trong `docker-compose.yml`.
- UUID v7 (`DECISIONS.md` #009) và cột `audit_log` (`DECISIONS.md` #007) đã chốt xong, không còn treo.
- Mật khẩu role `nexamed_app` đang hard-code trong migration (`'nexamed_app'`) — chỉ dùng được cho local dev/CI, **bắt buộc đổi** khi triển khai on-prem thật (S4-05), xem `docs/DECISIONS.md` #010.