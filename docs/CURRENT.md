# Current — NEXAMed

Trạng thái hiện tại của dự án. Cập nhật file này mỗi khi trạng thái thay đổi đáng kể (không phải log từng commit — đó là việc của `CHANGELOG.md`).

## Giai đoạn

Sprint 1 — Nền tảng đã xong toàn bộ 11 việc (S1-01 → S1-09). **Sprint 2 — Bệnh nhân + Đặt lịch đã xong toàn bộ** (tuần 3-4 theo `docs/product/plan.md` mục 5): S2-01, S2-02, S2-03, S2-05, S2-06, S2-07, S2-08, S2-09, S2-10 (cả 3 gate cuối sprint đã đạt, xem `docs/TASK.md`). S2-04 (BHYT) lùi lại sau Sprint 3 theo quyết định đã chốt, không chặn.

**Sprint 3 (Khám bệnh) — S3-01 (danh mục ICD-10) đã xong, mở khoá một phần** — module `icd10_catalog` hiện thực đầy đủ (Chương I, 924 mã, trang tra cứu 3 cột cascade), xem `docs/DECISIONS.md` #056 và mục "Đã có" bên dưới. Vẫn CHƯA mở khoá hoàn toàn phần "Khám bệnh" (S3-05→07: API màn khám, bộ chọn chẩn đoán, ghi chú SOAP) — cần thêm component chọn chẩn đoán, chưa xây. Trong lúc chờ, chủ dự án đã cho làm nhiều việc khác (hiệu chỉnh hồ sơ bệnh nhân #034-#036, danh mục dùng chung #037, danh mục hành chính #038, redesign trang Danh mục/Cấu hình #039-#040, backfill role_permission tự động, trang "Thông tin phòng khám" #041) — xem mục "Đã có" bên dưới. **Phần "Tiếp nhận" của Sprint 3 (REC-01→03: check-in, hàng đợi, sinh hiệu) đã làm xong, sau đó thiết kế lại theo 3 vòng góp ý chủ dự án** (2026-08-13, `docs/DECISIONS.md` #042 rồi #043) — không phụ thuộc ICD-10 nên không cần chờ. Bản hiện tại: "Danh sách tiếp nhận" (chỉ encounter, theo ngày) tách khỏi "Tiếp nhận bệnh nhân" (form tạo encounter trực tiếp, không qua appointment) và "Hàng đợi khám" (trang riêng cho bác sĩ). Phần "Khám bệnh" (SOAP/chẩn đoán/đơn thuốc) vẫn treo.

**Danh mục dùng chung (`reference_catalog`) — Dân tộc/Quốc tịch — đã xong** (mục "Việc đang treo" ở bản ghi trước đã được tiếp tục và hoàn thành trong phiên này). Chủ dự án xác nhận đảo ngược `docs/DECISIONS.md` #034 (tự cung cấp 2 file dữ liệu thật — 54 dân tộc + 30 quốc tịch — thay vì để AI tự soạn) và muốn xây thành master data **quản lý được** qua UI (thêm/sửa/ẩn), không chỉ xem như phác thảo ban đầu. Chi tiết đầy đủ ở `docs/DECISIONS.md` #037 và mục "Đã có" bên dưới.

**Danh mục hành chính `province`/`ward` (Tỉnh/Phường-Xã, sáp nhập hành chính 2025) — đã xong**. Chủ dự án gửi file dữ liệu chính thức (Bộ Nội vụ), hỏi chốt thiết kế 2 bảng (không gộp phẳng) và phạm vi (read-only, tích hợp luôn vào `patient.address_json`) trước khi code. Chi tiết đầy đủ ở `docs/DECISIONS.md` #038 và mục "Đã có" bên dưới.

**"Phòng làm việc hôm nay" (#054) + "Tầng phòng" (#055) — đã xong.** Chủ dự án gửi tài liệu tham khảo mô hình định tuyến theo phòng (HIS); dùng EnterPlanMode + AskUserQuestion chốt 3 điểm trước khi code (bác sĩ tự chọn phòng lúc đăng nhập; hàng đợi khám GIỮ NGUYÊN lọc theo `doctor_id`, không đổi RBAC; tự ẩn hoàn toàn ở quy mô 1-3 bác sĩ). Bảng mới `doctor_room_session`. Ngay sau đó chủ dự án yêu cầu thêm quản lý Tầng/Bàn khám-Ghế, tích hợp chung 1 màn hình Quản trị (không tách pill riêng) — bảng mới `floor` (tùy chọn)/`exam_station` (bắt buộc thuộc 1 phòng), cả hai THUẦN mô tả, không đổi đơn vị điều phối (vẫn ở cấp `room`). Mục sidebar mới "Tầng phòng" trong "Cấu hình hệ thống". Chi tiết đầy đủ ở `docs/DECISIONS.md` #054/#055.

**Module `icd10_catalog` (S3-01, mở khoá một phần) — đã xong, hiện có ĐỦ Chương I-XXII (15.844 mã).** Chủ dự án gửi file danh mục ICD-10 chính thức theo nhiều đợt trong cùng phiên — Chương I trước (924 mã), rồi II-V, rồi toàn bộ VI-XXII còn lại. Dùng EnterPlanMode + AskUserQuestion chốt trước khi code (2 agent Explore khảo sát `geo`/`reference_catalog`/`ConfigScreenShell` trước, plan lưu tại `C:\Users\Administrator\.claude\plans\sparkling-scribbling-stallman.md`). File gốc trên đĩa hoá ra đã đúng UTF-8 sẵn (không hề mojibake — lỗi chỉ sinh ra lúc dán vào khung chat), copy byte-for-byte (`cp` + `diff` xác nhận khớp tuyệt đối) vào `docs/data/icd10-chuong-<số>.md` (22 file) thay vì chạy bước sửa mojibake đã dự trù. Schema mở rộng đầy đủ (`chapter_code`/`chapter_name`/`block_code`/`block_name`/`group_code`/`group_name`/`gender_restriction`/`usage_restriction`/`who_note`, đúng khuôn `province`/`ward`) — migration `20260819110000_icd10_catalog` (chỉ chạy một lần, không đổi khi nạp thêm chương). Dữ liệu sinh bằng script dùng-một-lần đọc TOÀN BỘ file khớp mẫu `icd10-chuong-*.md` (`packages/core/src/icd10/data.ts`), tách ký hiệu chéo `†`/`*` (hệ thống dagger/asterisk của WHO) khỏi `code`. **2 vấn đề kỹ thuật phát sinh thật khi dữ liệu tăng lên hàng chục nghìn dòng, đã sửa**: (1) mảng object literal 15.844 phần tử làm TypeScript lỗi `TS2590` (vượt ngưỡng độ phức tạp kiểm tra kiểu) — chuyển sang nhúng JSON string + `JSON.parse` lúc chạy; (2) `chapterCode` là số La Mã ("I".."XXII"), sắp theo thứ tự CHUỖI ở DB cho kết quả sai (`"IX" < "V"` theo alphabet) — thêm `packages/core/src/icd10/roman-numeral.ts` (`romanToInt`, có unit test) để `Icd10Service.listChapters` tự sắp lại đúng thứ tự Chương thật ở tầng ứng dụng. Module `apps/api/src/modules/icd10/` (4 endpoint: chapters/groups/codes/tìm kiếm, dùng lại quyền `patient.read`). Trang tra cứu mới "Danh mục Chuyên môn" (`/admin/catalog-clinical`, thay `ComingSoonPage`) — `ConfigScreenShell` pill phẳng "ICD-10", 3 cột cascade (Chương | Nhóm gộp theo Khối | Bảng mã chi tiết) + ô tìm nhanh, badge cảnh báo mềm giới tính/hạn chế bệnh chính (không chặn). Chi tiết đầy đủ ở `docs/DECISIONS.md` #056.
  **Đã xác minh thật**: `packages/core` `data.spec.ts` (9/9, kiểm đúng số dòng từng Chương I-XXII + không còn ký hiệu `†`/`*`) + `roman-numeral.spec.ts` (3/3), `apps/api` `icd10-http.spec.ts` (11/11, HTTP e2e trên Postgres thật, seed đủ 22 chương, xác nhận thứ tự Chương trả về ĐÚNG số La Mã) — tổng 296 test `apps/api` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. Reseed dev DB thật xác nhận đủ 15.844 dòng đúng số lượng từng chương. Playwright qua Chrome thật (dùng thẳng Chrome cài sẵn trên máy, `executablePath`, do Playwright không tự tải được Chromium bundled trong môi trường này — tải bị treo ở bước giải nén, nghi antivirus, thử 3 lần): đăng nhập → Danh mục Chuyên môn → chọn Chương I → chọn Nhóm A00 → đúng 4 mã hiện ra; gõ ô tìm nhanh hoạt động, không lỗi console. **Vẫn CHƯA mở khoá hoàn toàn phần "Khám bệnh"** — S3-05→07 (API màn khám, ghi chú SOAP, bộ chọn chẩn đoán dùng danh mục này) chưa xây.
  **Hạn chế đã biết, CHỦ ĐỘNG để nguyên theo yêu cầu chủ dự án**: tìm nhanh dùng `contains` trên `search_key` đã bỏ dấu — với từ khoá ngắn (2-3 ký tự) có thể khớp nhầm chuỗi con không liên quan về nghĩa (ví dụ gõ "tả" → "ta" khớp cả vào "lao ở t**a**i"). Không phải vấn đề tốc độ (đã có GIN trigram index, 15.844 dòng vẫn rất nhỏ với Postgres) mà là vấn đề mức độ liên quan ngữ nghĩa của kết quả — sửa được bằng ràng buộc độ dài từ khoá tối thiểu, ưu tiên khớp đầu từ, hoặc dùng `similarity()`/ranking của `pg_trgm` thay vì `contains` thuần. Chưa chặn gì (đây chỉ là trang tra cứu tham khảo, chưa phải bộ chọn chẩn đoán chính thức của S3-06/07) — cân nhắc sửa khi làm bộ chọn chẩn đoán thật.

**ADM-07 (Vai trò tuỳ biến + ma trận phân quyền) — đã xong, ngoài kế hoạch Sprint 3 (`docs/DECISIONS.md` #057).** Chủ dự án yêu cầu làm trước trong lúc chờ mở khoá phần "Khám bệnh". Mockup Artifact duyệt qua 2 vòng (bố cục CRUD Xem/Thêm/Sửa/Xoá/Đặc quyền mở rộng theo ảnh tham khảo) trước khi code. Trang "Vai trò & Phân quyền" mới trong "Danh mục Tổ chức và Nhân sự". Kéo theo ripple bắt buộc: `user_account` đổi gán vai trò từ `roleNames` (enum 5 tên cố định) sang `roleIds` (uuid thật) để vai trò tuỳ biến gán được cho tài khoản. Xem chi tiết đầy đủ ở mục "Đã có" bên dưới.

**S3-05 (API màn hình khám) — đã xong (`docs/DECISIONS.md` #059), phần "Khám bệnh" MỞ KHOÁ MỘT PHẦN.** Bảng `diagnosis`/`clinical_note` hiện thực đúng khuôn đã đặc tả sẵn từ trước (migration `20260820120000_clinical_diagnosis_note`, không đổi cột nào so với `docs/ERD.md`). 4 endpoint mới trên `apps/api/src/modules/encounter/`: `GET /encounters/:id/consultation` (gộp tiền sử + dị ứng + sinh hiệu + chẩn đoán + SOAP trong một request, đúng yêu cầu S3-05), `PUT .../diagnoses` (thay toàn bộ danh sách, ép đúng 1 `PRIMARY`), `PUT .../clinical-note` (lưu cả 4 mục SOAP một lần, bản nháp — ký/đính chính để dành Sprint 5), `POST .../complete` (`IN_CONSULTATION→COMPLETED`, chỉ cần đúng 1 chẩn đoán chính, **không** đợi Kê đơn Sprint 4 — đã hỏi và chốt với chủ dự án). Không thêm permission mới (RBAC cho `diagnosis`/`clinical_note` đã seed sẵn từ S1-04b, tự vá qua `syncRolePermissionsForAllTenants()`). **Đã xác minh thật**: `encounter-http.spec.ts` thêm 17 test (27/27 tổng file) — tổng 333/333 test `apps/api` pass (1 flake tiền-nhiệm không liên quan ở `clinic-profile-http.spec.ts`, xác nhận qua chạy lại riêng, không phải regression). `pnpm -w typecheck` sạch toàn workspace. OpenAPI + web codegen đã sinh lại.
  **Phần web (S3-06/07) — ĐÃ XONG (2026-08-20, 2 phiên liên tiếp).** Phiên 1: code theo đúng bố cục mockup đã chốt ở #059 (trang cuộn dọc 1 khung, 4 tab neo bấm-để-cuộn-tới, tab 2-4 "Sắp ra mắt"), banner bệnh nhân tách 2 tầng + sinh hiệu neo phải (sửa vỡ dòng 1366px), sidebar tự thu gọn khi vào màn khám (`shared/layout/sidebar.context.tsx` mới), panel tiền sử đổi thẻ theo mockup gốc. Phiên 2 (chủ dự án dùng thử, yêu cầu đổi trực tiếp — **thay thế quyết định "SOAP 4 mục" ở #059**, xem `docs/DECISIONS.md` #060): **`clinical_note.section` đổi từ 4 mục SOAP sang 8 mục nhóm "Tiền sử" (bản thân/gia đình/dị ứng) và "Thăm khám" (lý do khám*/quá trình bệnh lý/chuẩn đoán*/khám toàn thân/khám bộ phận, *bắt buộc)** — migration `20260820150000_clinical_note_sections_v2`, gộp lại thành 1 khung "THÔNG TIN KHÁM LÂM SÀNG" duy nhất (ô nhập rút gọn, lưới 3 cột). "Tiền sử dị ứng" ghi thẳng `patient.allergyNote` qua `PATCH /patients/:id` (ripple RBAC: thêm `doctor.patient.update=global`). Sinh hiệu bổ sung/đo lại được ngay trong màn khám (`VitalSignsDialog.tsx` mới, nới guard `recordVitalSigns` sang cả `IN_CONSULTATION` — bug thật phát hiện khi test). Ngưỡng cảnh báo nhiệt độ 39.0°C→38.5°C, thêm phân loại BMI theo WHO Châu Á - TBD (emerald/amber/rose). Tab 1 đổi tên "Khám & Chẩn đoán". `apps/web/src/features/encounter/`, `shared/ui/Textarea.tsx` (prop `dense` mới). Chi tiết đầy đủ ở `docs/CHANGELOG.md` 2026-08-20 và `docs/DECISIONS.md` #059/#060.

**S3-09 (test + đo hiệu năng màn hình khám) — đã xong, Sprint 3 hoàn tất toàn bộ.** Bổ sung 3 test cách ly/scope còn thiếu cho 3/4 endpoint S3-05 (`.claude/docs/multi-tenancy.md`: mỗi endpoint chạm dữ liệu phải có test tenant A/B — `consultation`/`diagnoses`/`complete` đã có sẵn từ trước, `clinical-note` thì thiếu). `apps/api/scripts/perf-consultation.ts` (mới, thủ công không chạy CI, cùng khuôn `perf-patient-search.ts` S2-02) đo đúng 4 truy vấn tuần tự của `getConsultationDetail()` qua role app + RLS thật với bệnh nhân có 20 lượt khám cũ (PRD mục 5). **Đã xác minh thật**: 338/338 test `apps/api` pass (30/30 `encounter-http.spec.ts`), `pnpm -w typecheck/build` sạch (lint chỉ còn 3 lỗi cấu hình ESLint plugin có sẵn từ trước, không liên quan). Đo thật trên dev DB: tổng 33ms — ĐẠT xa ngưỡng "dưới 2 giây" (ENC-01, gate cuối Sprint 3).

**Danh mục "Nghề nghiệp" (ngoài kế hoạch, `docs/DECISIONS.md` #061)** — chủ dự án yêu cầu trực tiếp: thêm category `OCCUPATION` vào `reference_catalog` (migration `20260820160000_reference_catalog_occupation`, không seed cứng — `clinic_admin` tự thêm qua UI, đúng khuôn `PATIENT_SOURCE`/`EXAM_TYPE`), pill "Nghề nghiệp" mới ở trang "Danh mục hành chính". Đảo ngược tiếp phần `occupation` của #034. `patient.occupation` đổi từ input text tự do sang `Combobox` load từ danh mục này ở `PatientFormFields.tsx` (dùng chung cho cả Thêm/Sửa bệnh nhân lẫn Tiếp nhận bệnh nhân). **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch, 338 test `apps/api` không regress, Playwright qua Chrome thật xác nhận CRUD danh mục + Combobox load/chọn đúng ở cả 2 form.

**Màn hình khám — chỉnh dải sinh hiệu + đảo ngược lần 2 quy tắc nhãn trường toàn app (`docs/DECISIONS.md` #062)** — chuỗi phản hồi trực tiếp trên bản chạy thật: dải sinh hiệu có đường kẻ phân cách + chấm trạng thái (khôi phục từ #060) + nền đậm hơn; panel "Tiền sử & lịch sử khám" hẹp `360px`→`280px`; sửa 2 đường viền header lệch nhau; "Ghi chú dị ứng"→"Tiền sử dị ứng". **Nhãn trường (`label`) toàn app đổi từ nhạt/nhỏ (`text-xs font-medium text-slate-500`, chốt 2026-08-14) sang đậm/to (`text-sm font-semibold text-slate-800`, không còn ngoại lệ "dense")** — quy tắc mới ghi vào `.claude/docs/ui-guidelines.md` mục 4.1c, bắt buộc cho form phát sinh sau này. **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch, 338 test `apps/api` không regress (1 flake môi trường không liên quan ở `appointment-http.spec.ts`, xem chi tiết #062), Playwright qua Chrome thật.

**Mở rộng ADM-01 — "Danh mục quản lý tài khoản" đầy đủ hồ sơ nhân sự (`docs/DECISIONS.md` #063, ngoài kế hoạch, chủ dự án yêu cầu trực tiếp).** `user_account` thêm 10 cột (mã nhân viên tự sinh, SĐT/email cá nhân+công ty, học vị/chức danh/trạng thái+hình thức làm việc — 4 mã tham chiếu `reference_catalog` mới, được ký HSBA chỉ metadata, bắt buộc đổi mật khẩu lần đầu — **enforce thật**, chặn điều hướng tới khi đổi xong qua `RequireAuth.tsx`). `reference_catalog` thêm 4 category (`ACADEMIC_TITLE`/`STAFF_POSITION`/`EMPLOYMENT_STATUS`/`EMPLOYMENT_TYPE`) + cột `deactivates_account` (chỉ có ý nghĩa với `EMPLOYMENT_STATUS` — tách khỏi `code` vì `code` sửa được qua UI, so khớp chuỗi sẽ hỏng ngầm) — chọn "Nghỉ việc" tự động `is_active=false` cho tài khoản. 4 category nhân sự này + Khoa/Phòng đều **tự sinh mã**, không nhập tay (phản hồi giữa chừng của chủ dự án). Module `department`/`department_type` (mới, `department` có bảng từ S1-04b nhưng chưa từng có UI) — Khoa/Phòng phân loại 2 cấp qua "Loại Khoa/Phòng" (tùy chọn, THUẦN tổ chức), UI 2 cột tham khảo "Tầng phòng" (#055). Field "Vai trò" ở form tài khoản đổi giao diện sang `MultiSelectCombobox` nhưng vẫn gửi `roleIds` thật — **đã kiểm chứng RBAC hoạt động đúng bằng Playwright** (không chỉ suy luận từ code): tài khoản chỉ gán vai trò "Điều dưỡng" → đăng nhập → gọi thẳng API cần quyền quản trị → `403` thật từ backend, không phải do frontend ẩn giúp. Rà soát mã hoá mật khẩu toàn hệ thống theo yêu cầu chủ dự án: đã dùng Argon2id từ trước, đạt chuẩn OWASP, không cần sửa. **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch, 352 test `apps/api` (351 pass ổn định, 1 flake tiền-nhiệm không liên quan), Playwright qua Chrome thật xác nhận toàn bộ luồng (tạo tài khoản → bắt đổi mật khẩu lần đầu → tự động vô hiệu hoá khi "Nghỉ việc" → CRUD danh mục/Khoa-Phòng → RBAC thật).

**"Hàng đợi khám" — mô hình "Hàng đợi ảo" theo Bác sĩ/Khoa — ĐÃ CODE XONG (`docs/DECISIONS.md` #064, ngoài kế hoạch, kiến trúc đã chốt qua mockup từ phiên trước).** Hàng đợi không phải bảng riêng — chỉ là filter trên `encounter`: `doctorId` đổi nullable + thêm `departmentId` bắt buộc (migration `20260820210000_encounter_virtual_queue`, backfill dữ liệu cũ, mỗi tenant tự có 1 "Khoa chung" mặc định seed lúc tạo tenant). "Bệnh nhân của tôi" = `doctorId=actor`, "Hàng chờ chung · Khoa X" = `departmentId=Khoa actor AND doctorId IS NULL` (cờ `includeDepartmentPool` tường minh, không đổi hành vi "Danh sách tiếp nhận" mặc định). "Nhận ca" (mở rộng `POST /encounters/:id/start`) set `doctorId` lúc pull atomic, chống trùng chỉ fallback (ghi có điều kiện + lỗi `ENCOUNTER_ALREADY_CLAIMED` riêng cho UI hiện toast lịch sự) — không WebSocket. Web: `ReceptionDoctorQueuePage.tsx` viết lại thành board 3 cột (Đang chờ/Đang khám/Đã khám hôm nay); `ReceptionIntakeForm.tsx` thêm khu vực Điều phối (toggle "Theo bác sĩ cụ thể"/"Theo Khoa", tự ẩn khi tenant chỉ 1 Khoa); `EncounterConsultationPage.tsx` thêm dải hàng chờ thu gọn (ẩn/hiện được) cạnh Phiếu khám.
2 bug thật phát hiện lúc code, không thuộc phạm vi #064 nhưng sửa cùng lúc vì chạm đúng chỗ: (1) check-in từ lịch hẹn có sẵn trước đây không lưu 7 trường "Thông tin tiếp nhận"/"Chỉ định dịch vụ khám" dù Tiếp nhận trực tiếp đã lưu đúng từ #052; (2) `ReceptionIntakeForm`/`ReceptionDoctorQueuePage` ban đầu dùng API tra Khoa yêu cầu `user_account.read` (chỉ admin có) — lễ tân/bác sĩ thật sẽ bị 403, chỉ lộ ra khi kiểm bằng Playwright với đúng vai trò (không lộ khi test bằng tài khoản admin) — sửa bằng endpoint tự-phục vụ mới `GET /departments/options` (gắn `reference_catalog.read`, đúng khuôn `GET /appointments/doctors` #030).
**Đã xác minh thật**: 368/368 test `apps/api` (28 file, +16 test mới cho "Nhận ca"/routing/pool), `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright qua Chrome thật (tenant/tài khoản dev tạo riêng cho lần kiểm — 2 bác sĩ cùng Khoa + 1 Khoa khác): tiếp nhận theo Khoa (không chọn bác sĩ) → bác sĩ cùng Khoa thấy đúng ticket trong "Hàng chờ chung" → "Gọi khám — nhận ca này" → chuyển đúng `IN_CONSULTATION`, điều hướng sang màn khám, dải hàng chờ ẩn/hiện đúng → bác sĩ thứ hai không còn thấy ticket đã bị nhận. Không lỗi console ngoài dự kiến.

## Đã có

- `CLAUDE.md` — quy tắc và ràng buộc dự án (đã bổ sung quy định viết code hướng tái sử dụng).
- `.claude/docs/` — 7 tài liệu kỹ thuật đã chốt.
- `docs/product/prd.md`, `docs/product/plan.md` — đã có nội dung đầy đủ.
- `docs/ERD.md` — sơ đồ quan hệ dữ liệu đầy đủ.
- **S1-01 xong**: monorepo pnpm (`apps/api` NestJS, `apps/web` React+Vite, `packages/core`, `packages/shared`), cấu trúc thư mục theo `.claude/docs/project-structure.md`, ESLint boundary rule chặn `packages/core` import NestJS/Prisma/React/`process.env`, CI (`.github/workflows/ci.yml`).
- **S1-02 xong**: `apps/api/prisma/schema.prisma` với 7 bảng nền tảng (`tenant`, `tenant_setting`, `room`, `user_account`, `user_role`, `code_sequence`, `audit_log`), migration đầu tiên (`prisma/migrations/20260807090207_init`), script kiểm 8 cột bắt buộc (`scripts/check-mandatory-columns.mjs`, nối vào CI), Zod env validation ở `apps/api/src/config` (đã kiểm chứng: API crash đúng khi thiếu `DATABASE_URL`/`JWT_SECRET`/`ENCRYPTION_KEY`, chạy được khi đủ), `docker-compose.yml` gốc cho Postgres local dev, `apps/api/.env.example`. `packages/shared` có enum 5 vai trò (`roles.ts`).
- **Postgres nâng lên bản 18** (từ 16) để dùng `uuidv7()` thật cho mọi `id` — xem `docs/DECISIONS.md` #009.
- **S1-03 xong**: role Postgres riêng `nexamed_app` (không superuser/BYPASSRLS/DELETE) cho API runtime, tách khỏi role `nexamed` (chỉ dùng cho migration qua `MIGRATE_DATABASE_URL` + `apps/api/scripts/with-migrate-url.mjs`); RLS thật trên 6 bảng có `tenant_id`; `CHECK (version >= 1)`; `PrismaService` + `UnitOfWorkService` (`apps/api/src/infrastructure/persistence`, mở transaction + `SET LOCAL app.current_tenant_id`); `TenantContextMiddleware` (tạm đọc header, chờ JWT ở S1-04 — xem `docs/DECISIONS.md` #012) wire global vào `AppModule`. **Đã xác minh thật bằng integration test** (`tenant-isolation.spec.ts`, 4/4 pass trên Postgres thật): cách ly tenant đúng, không set context thì lỗi thay vì rò dữ liệu, không xoá được, không insert xuyên tenant được.
- **RBAC + Data Scope xong (S1-04b)** — thay hoàn toàn mô hình "5 vai trò cứng" theo yêu cầu chủ dự án (`docs/DECISIONS.md` #013-#016): bảng `department`, `role`, `permission` (danh mục toàn hệ thống, 23 permission), `role_permission` (ma trận `role × permission → data_scope`, 4 mức `none`/`personal`/`department`/`global`, không có `branch`), migration `*_rbac_data_scope` (RLS + CHECK + REVOKE cho bảng mới). Ma trận mặc định + logic seed đặt ở `packages/core/src/rbac/permissions.ts` (nguồn sự thật, dùng chung seed script/guard/web sau này). `seedDefaultRolesForTenant`/`seedPermissionCatalog` ở `apps/api/src/infrastructure/persistence/`. **Đã xác minh thật** (`rbac.spec.ts`, 4/4 pass): seed đúng 5 vai trò/tenant, RLS cách ly `role`/`role_permission` theo tenant, `permission` đọc được nhưng không ghi được qua role app, `doctor.encounter.read = global` đúng thiết kế (không break-glass cho tiền sử — khớp PRD ENC-01 P0).
- `docs/design/` — có `UI_GUIDELINE.md`, `AI_AVOID_RULES.md`.
- `docs/product/future-modules-reference.md` — tài liệu tham khảo (chưa chốt) cho Dược/kho, Viện phí, danh mục dùng chung, ý tưởng luồng khám mở rộng — dùng khi tới v2/v2.1/v3, đã ghi rõ mâu thuẫn với schema/state machine v1 đã chốt, không tự áp dụng.
- **Auth xong (S1-04)** — JWT access token 15 phút + refresh token httpOnly cookie xoay vòng mỗi lần refresh, Argon2id, khoá tài khoản tạm sau 5 lần sai/15 phút (`docs/DECISIONS.md` #019-#020). Bảng mới `user_session` (rotation chain + phát hiện reuse — dùng lại refresh token đã rotate thì thu hồi toàn bộ phiên của user), `user_account` thêm `failed_login_count`/`last_failed_login_at`/`locked_until`, migration `*_auth_sessions`. Module `apps/api/src/modules/iam/` (`AuthController`: `POST /auth/login|refresh|logout`, `AuthService`, `TokenService`, `SessionRepository`, `UserAccountAuthRepository`) + `packages/core/src/iam/lockout.ts` (nguồn sự thật ngưỡng khoá, unit test 9/9) + `packages/core/src/errors/` (khởi đầu lớp lỗi nghiệp vụ — `DomainError` + 5 lỗi auth cụ thể). `TenantContextMiddleware` đổi hẳn sang đọc JWT access token thật (không còn header tạm — hoàn thành cam kết ở `docs/DECISIONS.md` #012). Thêm `JwtAuthGuard` (xác thực tối thiểu, viết + test sẵn, **chưa áp vào controller nào** vì chưa có controller nghiệp vụ), `ResponseInterceptor`/`DomainExceptionFilter` (response envelope `{data,meta}`/`{error}` theo `architecture.md` — áp dụng lần đầu vì đây là controller thật đầu tiên của hệ thống), rate limit riêng cho `/auth/login` (`@nestjs/throttler`, 10 request/phút/IP). **Đã xác minh thật** (`auth.spec.ts`, 9/9 pass trên Postgres thật; `jwt-auth.guard.spec.ts`, 5/5 pass): login đúng/sai mật khẩu, khoá tài khoản đúng ngưỡng, tài khoản vô hiệu hoá, sai `tenantId` bị coi như sai thông tin đăng nhập (không lộ tồn tại xuyên tenant), refresh rotation, phát hiện reuse thu hồi toàn bộ phiên, logout, token hỏng/rác.

- **Break-glass xong (S1-04c)** — `POST /api/v1/break-glass` (yêu cầu access token — nơi dùng thật đầu tiên của `JwtAuthGuard`): xác thực lại mật khẩu (tái dùng `UserAccountAuthRepository.findById` + argon2, tái dùng `InvalidCredentialsError` — không tạo lỗi mới) + lý do bắt buộc → tạo `break_glass_session` (bảng đã có sẵn từ S1-04b, **không cần migration mới**) → ghi `audit_log` (`break_glass.request`) → gọi `NotificationPort.send` (no-op). Thời hạn phiên đọc `tenant_setting` key `break_glass_duration_minutes`, mặc định 120 phút nếu tenant chưa cấu hình. `packages/core/src/iam/break-glass.ts` (hàm thuần `computeExpiresAt`/`isSessionActive`, unit test 3/3) + `packages/core/src/ports/notification.port.ts` (`NotificationPort` — chỉ 1/6 port của S1-06 được kéo trước, giống cách S1-04 đã kéo trước một phần `errors/`) + adapter no-op (`apps/api/src/infrastructure/notification/noop.adapter.ts`). `BreakGlassService.tryConsume()` — primitive sẵn sàng cho guard `data_scope` thật của S2 (kiểm tra phiên còn hiệu lực + ghi `audit_log` `break_glass.access`), tự mở transaction riêng, chưa có guard nào gọi tới. **Đã xác minh thật** (`apps/api` `break-glass.spec.ts`, 6/6 pass trên Postgres thật + kiểm thủ công qua curl): đúng mật khẩu tạo phiên đúng hạn mặc định/tuỳ chỉnh, sai mật khẩu không tạo phiên không gọi notify, `tryConsume` đúng entity trong hạn mới `granted:true`, entity khác/hết hạn thì `false`, không có access token bị 401.

- **Audit log xong (S1-05)** — không có gì mới ở bảng/quyền DB (đã xong từ S1-03). Hai cơ chế ghi tách theo loại thao tác (`docs/DECISIONS.md` #021 — lý do: interceptor HTTP không thể tham gia transaction Prisma mà service tự mở/đóng kín bên trong): **ghi** → gọi tường minh `writeAuditLog(tx, ...)` trong transaction service, dời từ `modules/iam/` sang `apps/api/src/infrastructure/persistence/audit-log.helper.ts` (hạ tầng dùng chung, mọi module domain tương lai gọi được mà không phải import xuyên qua `iam`); **xem** → `AuditViewInterceptor` + decorator `@AuditView('entityType')` (`apps/api/src/common/`), tự mở transaction riêng sau khi handler trả về thành công, không ghi khi handler lỗi, lỗi ghi audit tự nổi lên thành response lỗi (không nuốt). `.claude/docs/security-audit.md` mục Audit log đã cập nhật quy ước này cho S2 theo. **Đã xác minh thật** (`audit-view.interceptor.spec.ts`, 3/3 pass trên Postgres thật) — chưa áp vào controller nào (chưa có controller nghiệp vụ, S2). Toàn bộ 28 test cũ (auth/break-glass/rbac/tenant-isolation/jwt-auth-guard) vẫn pass nguyên sau khi dời helper.

- **`packages/core` hoàn thiện xong (S1-06)** — khung entity thuần (`packages/core/src/entity/base-entity.ts`): `BaseEntity` (8 cột bắt buộc), `AppendOnlyEntity` (audit_log/break_glass_session), `SignableEntity` (dữ liệu lâm sàng ký được, `supersedesId`/`amendmentReason`) + hàm thuần `isSigned()` (nguồn sự thật duy nhất cho quy tắc "đã ký thì bất biến", unit test 2/2). Đủ 6 port ở `packages/core/src/ports/` — 5 port còn lại (`NotificationPort` đã có từ S1-04c): `StoragePort` (save/read/delete, adapter v1 `LocalDiskStorageAdapter` ghi thật ra disk dưới `STORAGE_DIR/<tenantId>/<key>`, có chặn path traversal), `EventBusPort` (publish/subscribe, adapter `InMemoryEventBusAdapter` — đồng bộ, handler lỗi ném lên để service rollback transaction, đăng ký handler là wiring lúc khởi động không phải state theo request), `SignaturePort` (adapter no-op trả `signaturePayload: null`), `InsuranceGatewayPort` (adapter no-op ném `NOT_IMPLEMENTED`, đúng như bảng port/adapter ở `project-structure.md` — chưa có service nào gọi, ngoài phạm vi v1), `PatientIdentityPort` (adapter `SameTenantPatientIdentityAdapter` trả chính `patient.id`, khớp `global_patient_ref` luôn null ở v1). Đăng ký DI qua `apps/api/src/infrastructure/ports.module.ts` (`@Global()`, tách khỏi `NOTIFICATION_PORT` vẫn đăng ký riêng trong `IamModule` như cũ — không đụng lại), wire vào `AppModule`. Thêm `STORAGE_DIR` vào `env.schema.ts`/`.env.example` (mặc định `./storage`, gitignore). **Đã xác minh thật**: `base-entity.spec.ts` (2/2), `local-disk.adapter.spec.ts` (4/4, ghi/đọc/xoá file thật + chặn key chứa `..`), `in-memory.adapter.spec.ts` (3/3); build thật rồi chạy `node dist/main.js` — log xác nhận `PortsModule dependencies initialized` và `Nest application successfully started` qua NestJS DI container thật (khác các test trước chỉ `new Service(...)` tay, không qua Nest). Không có folder `patient-identity/` sẵn trong cây thư mục minh hoạ ở `project-structure.md` (chỉ liệt kê storage/eventbus/signature/insurance/notification) — tạo thêm theo đúng pattern "một thư mục cho mỗi port" đã có, không phải đổi cấu trúc đã chốt.

- **Web app shell xong (S1-08)** — hỏi chốt luồng/menu với chủ dự án trước khi code (theo yêu cầu mới: luôn hỏi trước khi xây mỗi chức năng): menu sidebar kết hợp theo luồng nghiệp vụ **và** ẩn/hiện theo vai trò; có Dashboard chung dạng empty-state (không bịa số liệu, vì patient/appointment/encounter chưa tồn tại tới S2-S3); Quản trị là mục riêng trong sidebar (không gộp avatar), chỉ hiện với `clinic_admin`/`system_admin`; chỉ hiện menu module đã có backend thật (Đặt lịch/Tiếp nhận/Khám bệnh/Kê đơn chưa hiện, thêm dần đúng sprint); màu/token theo `.claude/docs/ui-guidelines.md` (không theo bảng hex/Geist ở `docs/design/UI_GUIDELINE.md` — tài liệu đó chỉ tham khảo layout/menu chung, không phải màu). `tenantId` nạp runtime qua `apps/web/public/config.json` (gitignore, có `config.example.json`) — sửa trực tiếp lúc deploy on-prem, không cần rebuild (chốt xong điểm treo ở `docs/DECISIONS.md` #020).
  Backend mở rộng nhỏ đi kèm (`docs/DECISIONS.md` #022): `loginResponseSchema.user` thêm `roles`; thêm `GET /auth/me` (`JwtAuthGuard`, lần dùng thật thứ hai sau break-glass) để web khôi phục danh tính/vai trò lúc reload trang (chỉ còn refresh cookie, chưa có `user` trong bộ nhớ).
  Frontend: Tailwind v4 qua `@tailwindcss/vite` (không cần `postcss.config.js`/`tailwind.config.js` riêng — khác giả định Tailwind v3 ban đầu), `react-router-dom` v7, `@phosphor-icons/react`; API client tối thiểu tự viết (`apps/web/src/shared/api/client.ts`, chỉ đủ cho auth flow, S1-09 thay hẳn bằng client sinh từ OpenAPI); Zustand auth store (session state, không phải server entity data); `LoginPage`/`RequireAuth`/`AppBootstrap`; `AppShell`/`Sidebar`/`UserMenu`; `DashboardPage`/`AdminPage` (empty state); `Button`/`EmptyState` dùng chung ở `shared/ui`.
  Phát hiện + sửa 1 race condition lúc kiểm bằng trình duyệt thật: React StrictMode (dev) gọi effect 2 lần khiến `AppBootstrap` bắn 2 request `/auth/refresh` gần đồng thời, có thể đụng cơ chế phát hiện reuse-token (S1-04) — chặn bằng `useRef` guard (chỉ ảnh hưởng dev, StrictMode không chạy ở production build, nhưng vá cho chắc).
  **Đã xác minh thật**: `pnpm -w lint/typecheck/build` sạch; 45 test `apps/api` pass; chạy `pnpm dev` thật + Playwright (headless Chromium — môi trường chưa có project skill `run`, tự cài để dùng) chụp ảnh xác nhận đúng luồng: chưa đăng nhập → `/login`; đăng nhập đúng → Dashboard đúng token màu (sidebar `bg-slate-900`), thấy Tổng quan + Quản trị (user test có vai trò `clinic_admin`); F5 giữ nguyên phiên (refresh+`/me` hoạt động); đăng xuất → `/login`.

- **S1-09 xong** — api client sinh từ OpenAPI thay cho bản `fetch` tối giản tự viết ở S1-08, TanStack Query cache-key infra (xem `docs/DECISIONS.md` #023). `apps/api/scripts/generate-openapi.ts`: dựng `OpenAPIRegistry` (`@asteasolutions/zod-to-openapi` v7, tương thích `zod@^3`) trực tiếp từ Zod schema đã có ở `@nexamed/shared` — không có contract thứ hai phải đồng bộ tay — xuất `apps/api/openapi/openapi.json` (5 endpoint: `/auth/login|refresh|logout|me`, `/break-glass`, đúng envelope `{data,meta}`/`{error}`). `packages/shared/src/auth.ts` thêm `refreshResponseSchema`/`logoutResponseSchema` làm nguồn cho hai endpoint trước đây trả object ad-hoc không có schema. `apps/web`: `openapi-typescript` sinh `src/shared/api/openapi-schema.d.ts` từ file JSON trên (không cần API đang chạy); `src/shared/api/client.ts` viết lại bằng `openapi-fetch` (giữ nguyên `ApiError`, việc bóc envelope, cách đính `Authorization` bearer qua middleware thay vì tham số mỗi lời gọi); `src/shared/api/query-keys.ts` — factory cache key `[tenantId, domain, ...]` đúng `.claude/docs/architecture.md`, chưa có nơi dùng thật (chờ S2 có server entity data qua `patient`/`appointment`) nhưng sẵn sàng. Cả `openapi.json` lẫn `openapi-schema.d.ts` đều **commit vào repo** (không gitignore) — `apps/web` phải build độc lập, không phụ thuộc `apps/api` đang chạy hay build trước. `auth.api.ts` đổi chữ ký `getMe()` (bỏ tham số `accessToken`). **Đã xác minh thật**: `pnpm -w lint/typecheck/build` sạch; 45 test `apps/api` + 14 test `packages/core` pass nguyên (không đổi hành vi backend); chạy `pnpm dev` thật + Playwright xác nhận qua ảnh chụp: đăng nhập → Dashboard; F5 giữ nguyên phiên qua client mới; đăng xuất → `/login`; không lỗi console ngoài dự kiến.

- **S2-01 xong (module `patient`)** — CRUD (tạo/xem/sửa; không có xoá, không có yêu cầu PRD) + mã hoá `national_id`. Cũng là lúc hiện thực **`PermissionGuard` thật** (việc treo từ Sprint 1) vì đây là controller nghiệp vụ đầu tiên — xem chi tiết cơ chế guard ở `.claude/docs/security-audit.md` mục Data Scope, chi tiết đầy đủ implementation ở `docs/TASK.md` S2-01. Trước khi viết migration, đã hỏi và xác nhận với chủ dự án vá lệch `docs/ERD.md` ↔ `.claude/docs/data-model.md` cho bảng `patient` (thêm `address_json`, `identity_verified_at` — xem `docs/DECISIONS.md` #024). `IamModule` đổi thành `@Global()` để `PermissionGuard` (ở `CommonModule`) resolve đúng `BreakGlassService` khi được dùng từ module khác (patient hôm nay, mọi module domain sau này). `AuditViewInterceptor` đã có nơi dùng thật đầu tiên (`GET /patients/:id`). **Đã xác minh thật**: `patient-http.spec.ts` (10/10, HTTP e2e — CRUD, trùng CCCD → 409, optimistic lock, permission guard chặn đúng + break-glass sẵn sàng, cách ly tenant qua guard+RLS → 404 không phải 403), `code-sequence.repository.spec.ts` (3/3), `pii-encryption.spec.ts` (5/5), `data-scope.spec.ts`/`format-display-code.spec.ts` (`packages/core`). Tổng 63 test `apps/api` + 20 test `packages/core` pass, không regress từ Sprint 1. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại cho 4 endpoint `/patients*` mới.

- **S2-02 xong (tìm kiếm bệnh nhân, PAT-02)** — `GET /api/v1/patients?q=...` tìm theo tên (không dấu, không phân biệt hoa/thường), số điện thoại, mã bệnh nhân, cùng một ô nhập. `packages/core/src/search/strip-vietnamese-diacritics.ts` chuẩn hoá `q` ở tầng ứng dụng, khớp đúng hàm SQL `nexamed_unaccent_lower()` (bọc `unaccent()` thành IMMUTABLE) sinh cột `patient.search_key` (Postgres `GENERATED ALWAYS AS (...) STORED`, migration `20260811055006_patient_search_s2_02`, bật thêm extension `pg_trgm`/`btree_gin`). GIN kết hợp `(tenant_id, search_key gin_trgm_ops)` đúng `docs/ERD.md` mục 5. Lúc scaffold migration, Prisma tự đề xuất xoá nhầm `patient_tenant_id_phone_idx` (index raw-SQL không khai báo trong schema.prisma bị coi là "drift") — đã phát hiện và bỏ dòng đó, bài học cho các migration sau trên bảng có index raw-SQL-only. **Đã đo hiệu năng thật với 50.000 bản ghi** (`apps/api/scripts/perf-patient-search.ts`, chạy tay, không vào CI): cả 3 kiểu tìm dưới 35ms — đạt mục tiêu "<1 giây" của PRD với biên độ lớn. Ở quy mô này Postgres planner chọn Seq Scan thay vì GIN theo mặc định (bảng đủ nhỏ để nằm gọn trong shared buffers — quyết định đúng của planner, không phải lỗi); đã xác nhận GIN index tồn tại/dùng được thật bằng cách ép `enable_seqscan=off`. **Đã xác minh thật**: `patient-http.spec.ts` thêm 6 test tìm kiếm (16/16 tổng), `strip-vietnamese-diacritics.spec.ts` (5/5). Tổng 69 test `apps/api` + 25 test `packages/core` pass. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại.

- **S2-03 xong (chống trùng mềm, PAT-03)** — `GET /api/v1/patients/check-duplicate?fullName=&dob=`: endpoint riêng gọi TRƯỚC khi tạo hồ sơ (đã hỏi và chốt cách thiết kế với chủ dự án — không suy diễn: chọn phương án endpoint check riêng thay vì gộp cảnh báo vào response tạo mới, để không đổi contract `POST /patients` đã test kỹ). Khớp CHÍNH XÁC tên đã chuẩn hoá (`searchKey`, tái dùng cột generated + `stripVietnameseDiacritics` của S2-02) + ngày sinh — khác `contains` của tìm kiếm PAT-02, vì mục đích là phát hiện trùng gần tuyệt đối chứ không gợi ý theo từ khoá. Không chặn, không tự tạo/sửa gì — chỉ trả danh sách hồ sơ nghi trùng để client tự quyết định (chặn CCCD đã có sẵn từ S2-01 qua DB constraint C3, không thuộc phạm vi việc này). Route `check-duplicate` phải khai báo trước `:id` trong controller (NestJS khớp theo thứ tự khai báo). **Đã xác minh thật**: `patient-http.spec.ts` thêm 6 test (22/22 tổng pass) — trùng tên không dấu/khác hoa thường + đúng dob, khác dob thì không trùng, gần giống nhưng không khớp tuyệt đối thì không trùng, gọi check không tạo bản ghi mới, cách ly tenant, thiếu tham số → 400. 75 test `apps/api` + 25 test `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại cho endpoint mới.
- **S2-05 xong (module `appointment`, APP-01/02/03)** — exclusion constraint `EXCLUDE USING gist` ở tầng DB (C2, `docs/ERD.md` mục 4) chặn thật hai lịch hẹn trùng khung giờ cùng bác sĩ, kể cả hai request đồng thời — **đã đạt gate cuối Sprint 2** ("một thành công, một `APPOINTMENT_SLOT_CONFLICT`", xác minh bằng `Promise.all`, không chỉ chạy tay). `appointment.room_id` nullable (chưa có module `tenant`/cấu hình phòng ở S2-07). Exclusion constraint cần hàm SQL riêng đánh dấu `IMMUTABLE` bọc `scheduled_at + interval` (Postgres từ chối toán tử `STABLE` trong index) — cùng kỹ thuật `nexamed_unaccent_lower()` đã dùng ở S2-02. Lỗi vi phạm exclusion constraint không có mã Prisma riêng (`PrismaClientUnknownRequestError`, khác `P2002`/`P2003` đã quen ở patient module) — phát hiện thật qua chạy test, không đoán từ tài liệu (chi tiết `docs/DECISIONS.md` #026).
  `AppointmentService` là nơi đầu tiên lọc `data_scope=personal` THẬT (`doctor.appointment.* = personal`) thay vì coi tương đương `global` như `patient` — bác sĩ chỉ đặt/xem lịch của chính mình, đặt hộ bác sĩ khác → 403, xem lịch bác sĩ khác → 404. Khuôn mẫu này sẽ tái dùng cho `encounter`/`vital_sign`/`clinical_note` ở S3. **Đã xác minh thật**: `appointment-http.spec.ts` (15/15 pass) — gate đồng thời, khác bác sĩ/giờ liền kề không xung đột, scope personal đúng, cách ly tenant, permission, tham chiếu không tồn tại (422), thiếu trường (400). Toàn bộ 90 test `apps/api` pass, không regress. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại.
- **S2-06 xong (huỷ lịch + walk-in, APP-04/06)** — hỏi và chốt với chủ dự án trước khi code: check-in thật (tạo `encounter`) KHÔNG làm ở S2-06, để dành cho module Tiếp nhận ở Sprint 3 (đúng quyết định "không đi đường tắt walk-in tối thiểu" đã ghi lúc S2-02). Walk-in (APP-06 phần "tạo lịch") không cần code mới — `appointment.source='walk_in'` đã có sẵn từ S2-05, dùng thẳng `POST /appointments` hiện có. `POST /api/v1/appointments/:id/cancel`: bắt buộc `cancelReason` + `version` (optimistic locking), chỉ huỷ được lịch còn `SCHEDULED` (`AppointmentNotCancellableError`, 409, cho lịch đã huỷ/không đến/đã chuyển đổi), tôn trọng `data_scope=personal` giống `getAppointment` (404 cho lịch ngoài phạm vi bác sĩ). Huỷ xong thì khung giờ đó hết bị exclusion constraint C2 chặn (đã xác minh: đặt lại được ngay). **Đã xác minh thật**: `appointment-http.spec.ts` thêm 10 test (25/25 tổng pass). Toàn bộ 100 test `apps/api` pass, không regress. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại.
- **S2-07 xong (tài khoản+vai trò ADM-01, cấu hình phòng khám ADM-02 trừ mẫu in)** — hỏi và chốt phạm vi trước khi code (bỏ "mẫu in", lùi tới S4-04/PRE-04). Theo đúng ranh giới module đã chốt ở `.claude/docs/architecture.md`: "tài khoản, vai trò" thuộc module `iam` (mở rộng, không tạo module `tenant` mới như tên gọi ở `plan.md`), "cấu hình phòng khám, phòng" thuộc module `clinic` (mới) — khớp ghi chú sẵn có trong code từ S1-04/S1-08 đã dự báo trước việc này.
  Sửa một lỗ hổng schema thật trước khi code: `user_role` UNIQUE đổi sang PARTIAL (`WHERE deleted_at IS NULL`) — unique thường sẽ chặn gán lại đúng vai trò đã từng bị gỡ. Tiện sửa luôn 1 bug đi kèm (`findRoleNamesForUser` thiếu filter `deletedAt`, chưa từng lộ vì trước đó không ai soft-delete `user_role`).
  `POST/GET/GET:id/PATCH /api/v1/users` + `POST /api/v1/users/:id/reset-password` — **lần đầu tiên `SessionRepository.revokeAllForUser()` (viết sẵn từ S1-04) được gọi thật**: đổi vai trò/vô hiệu hoá/đặt lại mật khẩu đều thu hồi toàn bộ phiên đang mở, đã xác minh bằng HTTP thật (đăng nhập lấy cookie thật → admin thao tác → cookie cũ bị từ chối). `POST/GET/PATCH /api/v1/rooms`, `GET/PATCH /api/v1/clinic-settings` (giờ làm việc theo ngày trong tuần + độ dài slot, đọc/ghi `tenant_setting`).
  **Đã xác minh thật**: `user-account-http.spec.ts` (14/14), `clinic-http.spec.ts` (13/13). Toàn bộ 127 test `apps/api` pass, không regress. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại.
- **Kế hoạch Tiếp nhận (Sprint 3) đã chốt sơ bộ với chủ dự án** (chưa code): xây đủ `appointment` (S2-05/06) + màn hình lịch (S2-09) trước Tiếp nhận, đúng thứ tự `plan.md` — không đi đường tắt "walk-in tối thiểu". Snapshot thẻ BHYT lúc check-in (`encounter.insurance_snapshot`) tạm thời luôn `self_pay=true` cho tới khi làm S2-04 (lùi lại sau Sprint 3, không chặn Tiếp nhận). Schema `appointment`/`encounter`/`vital_sign` dùng đúng những gì đã chốt trong `docs/ERD.md`, không có trường mới nào phát sinh thêm ngoài đó.

- **S2-08 xong (web bệnh nhân: danh sách, tạo mới, chi tiết/sửa, PAT-01→03)** — hỏi và chốt workflow trước khi code: chi tiết sửa TẠI CHỖ (không modal), luồng chống trùng kiểm tra LÚC bấm Lưu (gọi `check-duplicate` trước `POST /patients`, không debounce theo từng ký tự gõ), nghi trùng chặn submit lần đầu và hiện cảnh báo kèm "Vẫn tạo mới"/"Huỷ". Form dùng chung `PatientFormFields` cho cả tạo mới và sửa, lưới 2 cột theo `ui-guidelines.md`. `ErrorBanner`/`Skeleton`/`useDebouncedValue` đặt ở `shared/` với chủ đích tái dùng ngay.
  Phát hiện + sửa 2 bug lúc kiểm bằng trình duyệt thật: `Button.tsx` override `className` thô bị nuốt bởi class mặc định (nút "Huỷ" vô hình, chữ trắng nền trắng) → thêm `variant` (`primary`/`secondary`/`danger`); `QueryClient` mặc định retry cả lỗi `404 NOT_FOUND` (id không tồn tại vẫn thử lại 3 lần trước khi hiện empty state) → tắt retry riêng cho `NOT_FOUND`.
  **Đã xác minh thật qua trình duyệt** (Playwright headless Chromium, chụp ảnh từng bước): đăng nhập → danh sách + tìm kiếm đúng; tạo hồ sơ mới thành công; tạo lại đúng tên+ngày sinh → cảnh báo trùng đúng, "Huỷ" không tạo bản ghi; sửa hồ sơ tại chỗ lưu đúng giá trị mới; không lỗi console ngoài dự kiến. `pnpm -w lint/typecheck/build` sạch; 127 test `apps/api` pass, không regress.

- **Fix hạ tầng dùng chung sau khi dùng thử (docs/DECISIONS.md #028)** — để trang yên rồi thao tác lại báo "Không tải được dữ liệu" (access token 15 phút hết hạn giữa chừng, không có gì làm mới) và gõ tìm kiếm giao diện nháy liên tục (mỗi từ khoá mới xoá trắng bảng hiện có). Sửa ở `shared/api/client.ts` (tự phát hiện 401, làm mới token qua `/auth/refresh`, thử lại request cũ đúng 1 lần, dedupe nhiều 401 đồng thời) và `patient.queries.ts` (`placeholderData: keepPreviousData`, giữ kết quả cũ khi đổi từ khoá). Áp dụng cho MỌI module domain sau này qua cùng client/mẫu query, không riêng patient.

- **App Shell v2 — sidebar thu gọn, topbar/breadcrumb, List Screen Pattern (docs/DECISIONS.md #027, #029)** — chủ dự án yêu cầu làm lại giao diện tham khảo sản phẩm HIS khác; duyệt qua mockup Artifact trước khi code. Đổi tên "Bệnh nhân" → "Danh sách bệnh nhân", gộp vào nhóm sidebar mới **"Tiếp nhận và Đặt lịch"** (sẽ chứa thêm "Lịch hẹn"/S2-09, "Tiếp nhận"/S3 khi backend tương ứng ra đời). Bỏ nút "Thêm bệnh nhân" (tạo mới chuyển sang gắn với luồng Đặt lịch/Tiếp nhận, chưa xây — điểm vào cụ thể quyết định khi làm S2-09/S3). Sidebar thêm vùng logo + nút thu gọn (`w-60`⇄`w-16`). `TopBar.tsx` mới (breadcrumb dạng chip thay nút "← Quay lại" + lời chào/avatar mở dropdown đăng xuất) thay hẳn `UserMenu.tsx` (đã xoá). Danh sách bệnh nhân viết lại theo List Screen Pattern (`ui-guidelines.md` mục 9): rộng gần hết chiều ngang (bỏ `max-w-[1400px]` — chỉ áp dụng riêng màn hình danh sách, form vẫn giữ `max-w-4xl`), bảng CSS Grid (không phải `<table>`, để phối hợp với `@tanstack/react-virtual` định vị `position: absolute`) cố định chiều cao cuộn nội bộ, cuộn vô hạn giữ cursor pagination (`PATIENT_LIST_LIMIT` 20→50), mở hồ sơ bằng double-click mã bệnh nhân (không click cả hàng) + Tab/Enter tương đương bàn phím, nút "Sửa hồ sơ" chỉ hiện với vai trò có `patient.update` (`receptionist`/`clinic_admin`).
  **2 bug hạ tầng phát hiện qua chủ dự án dùng thử, đã sửa + ghi quy tắc bắt buộc ở `ui-guidelines.md` mục 8.3** (tránh lặp lại ở màn hình sau): breadcrumb tồn đọng (stale) ở `DashboardPage`/`AdminPage` vì quên gọi `useBreadcrumb` (context cố ý không tự xoá khi unmount, tránh nháy trắng — nên trang quên khai báo sẽ hiện nguyên breadcrumb trang trước); icon nhóm "Tiếp nhận và Đặt lịch" lúc sidebar thu gọn bấm không phản hồi gì (`onClick` có điều kiện chặn khi `collapsed`, quên tính trường hợp bấm chính icon đó để mở lại) — sửa để bấm icon nhóm lúc thu gọn sẽ mở lại sidebar và mở luôn nhóm.
  **Đã xác minh thật qua Playwright**: toàn bộ luồng điều hướng (mở/đóng nhóm sidebar, thu gọn/mở lại qua icon, double-click mở chi tiết, breadcrumb đổi đúng qua Tổng quan/Danh sách bệnh nhân/Quản trị). `pnpm -w lint/typecheck/build` sạch toàn workspace; 127 test `apps/api` pass, không regress (thuần web).

- **S2-09 xong (web Lịch hẹn — lưới + danh sách, APP-01)** — nhiều vòng hỏi/chốt lại theo mockup Artifact (chi tiết đầy đủ ở `docs/DECISIONS.md` #030, #031): kéo-chọn chỉ để đặt mới; tạo nhanh bệnh nhân ngay trong form; lưới cột=bác sĩ/hàng=khung giờ bước 30 phút chọn 1 ngày; bổ sung thêm sau khi xem mockup: đổi/dời lịch (`PATCH /appointments/:id/reschedule`, tái dùng khuôn `cancel()` + exclusion constraint C2), cảnh báo trễ hẹn (chỉ gợi ý thị giác, giữ ranh giới APP-05/Sprint 5), đường kẻ "bây giờ", gợi ý bác sĩ trống lịch trong form đặt nhanh (dùng dữ liệu ngày đã tải sẵn, không gọi API mới); xác nhận **không** làm "ca làm việc riêng từng bác sĩ" (cần bảng mới ngoài `docs/ERD.md` đã chốt).
  Backend mở rộng: `packages/core/src/date/vietnam-day-range.ts` (util VN UTC+7, cùng kỹ thuật `format-display-code.ts`); 2 port mới `DoctorDirectoryPort`/`ClinicConfigReaderPort` (module `appointment` đọc dữ liệu `iam`/`clinic` qua port, đúng mẫu `PatientReaderPort` ở `coding-standards.md`, **không** import chéo module) — giải quyết luôn lỗ hổng quyền thật phát hiện lúc thiết kế (lễ tân không có `user_account.read`/`clinic_config.read`, đã chốt KHÔNG mở rộng ma trận mà thêm 2 endpoint chiếu tối thiểu `GET /appointments/doctors|schedule-config` gắn quyền `appointment.read` sẵn có). `GET /appointments` thêm filter `date` + join `patientFullName`/`patientCode` (chỉ ở `list()`).
  Frontend: `apps/web/src/features/appointment/` (feature module đầy đủ theo khuôn `patient.*`), `apps/web/src/features/patient/PatientPicker.tsx` (mới, dùng chung — tìm + tạo nhanh bệnh nhân, tái dùng nguyên `PatientFormFields`). `Sidebar.tsx`/`router.tsx` thêm "Lịch hẹn".
  **Đã xác minh thật**: `appointment-http.spec.ts` thêm 13 test (38/38, tổng 140 test `apps/api` + 30 `packages/core` pass, không regress). `pnpm -w lint/typecheck/build` sạch. Playwright qua dữ liệu demo thật (5 bác sĩ, nhiều lịch hẹn tạo qua API): lưới/danh sách/đặt/huỷ/sửa lịch đều đúng. 2 bug thật phát hiện + sửa lúc kiểm bằng trình duyệt (không phải review code): giới hạn `limit` chế độ lưới (200) vượt trần schema đã có (`max(100)`) → 400; đường kẻ "bây giờ" ban đầu cài làm 1 phần tử CSS Grid chiếm trọn hàng thân bảng khiến toàn bộ cột giờ/bác sĩ (auto-placement) bị đẩy xuống hàng kế tiếp, lưới trông trống trơn dù DOM vẫn đúng — sửa bằng để mỗi cột tự vẽ đoạn kẻ riêng (bài học ghi ở `docs/DECISIONS.md` #031, áp dụng cho mọi overlay phủ nhiều cột CSS Grid sau này).

- **Đổi mô hình đặt lịch sang "lead capture" ngay sau khi dùng thử S2-09 (`docs/DECISIONS.md` #032, `docs/ERD.md` v1.4)** — chủ dự án yêu cầu: đặt lịch **không** còn tạo/gắn hồ sơ `patient` — chỉ ghi Họ tên/SĐT/Lý do khám trực tiếp lên `appointment` (`full_name`/`phone`/`reason` cột mới; `patient_id` đổi nullable, để sẵn cho Tiếp nhận thật gắn sau, Sprint 3 chưa xây). Mỗi lịch có `booking_code` riêng (mã đặt lịch khách trình lúc đến, sinh qua `code_sequence` đúng khuôn `patient_code`, prefix `LH`). `GET /appointments/lookup?phone=` tự điền tên theo lần đặt gần nhất khi lễ tân gõ SĐT + đếm số lần huỷ, cảnh báo spam khi ≥5 lần (chỉ cảnh báo, không chặn). `POST /appointments/:id/checkin` (tái dùng quyền `appointment.update`) chuyển thẳng `SCHEDULED → CONVERTED` (không sinh trạng thái mới, chưa tạo `encounter`/gắn `patient_id` — chỉ đổi trạng thái + thông báo, đúng đề nghị của chủ dự án cho tới khi Tiếp nhận thật ra đời).
  `AppointmentQuickCreatePanel.tsx` bỏ hẳn `PatientPicker` (giữ nguyên file, không xoá), thay bằng form SĐT/Họ tên/Lý do khám + banner spam + dialog xác nhận đặt lịch (nội dung đúng nguyên văn chủ dự án yêu cầu). `AppointmentDetailPanel.tsx` thêm nút Check-in + dialog thông báo. Migration `20260812012715_appointment_lead_capture` viết tay (không dùng `prisma migrate diff`, tránh drift giả với exclusion constraint C2).
  **Đã xác minh thật**: `appointment-http.spec.ts` viết lại toàn bộ (50/50 pass, 152 test `apps/api` tổng không regress); `pnpm -w lint/typecheck/build` sạch; Playwright qua dữ liệu thật xác nhận đúng dialog xác nhận, tự điền tên theo SĐT, banner spam, check-in chuyển trạng thái. 1 bug bundler thật (Rollup không dò được named export hằng số giá trị thuần từ `packages/shared` qua `vite build`, dù Node `require()` thấy đúng) — xử lý bằng chuyển hằng số ngưỡng spam sang khai báo tại `apps/web` (đúng vị trí kiến trúc thật, không chỉ né bug — xem `docs/DECISIONS.md` #032 để biết chi tiết + ghi chú cho lần sau).

- **Fix Lịch hẹn + S2-10 (test cách ly tenant, đạt gate cuối cùng của Sprint 2)** — chủ dự án báo bug thật: chế độ Danh sách không lọc theo ngày đã chọn (`AppointmentListView` gọi query thiếu tham số `date`, trong khi backend đã hỗ trợ kết hợp `date`+`cursor` từ S2-09) — đã sửa (`useAppointmentsListQuery(date)`); kèm 2 việc UX theo yêu cầu (badge "N lịch hẹn trong ngày" chuyển xuống ngang hàng nút "Hôm nay", đổi màu nổi bật, hiện cho cả 2 chế độ; phím tắt F2 + tooltip cho nút "Đặt lịch"). Sau đó làm S2-10: rà soát + bổ sung 7 test cách ly tenant còn thiếu ở `appointment-http.spec.ts` (+4: `/doctors`, `/schedule-config`, `/lookup?phone=`, danh sách) và `user-account-http.spec.ts` (+3: danh sách, `PATCH :id`, `reset-password` xuyên tenant) — **không tìm thấy bug thật**, code đã lọc `tenantId` đúng từ đầu, chỉ thiếu test xác nhận theo `.claude/docs/multi-tenancy.md`. `clinic-http.spec.ts` đã đủ từ S2-07, không cần thêm.
  **Đã xác minh thật**: 159/159 test `apps/api` pass (152 cũ + 7 mới, không regress), `pnpm -w typecheck` sạch toàn workspace, Playwright xác nhận đổi ngày ở Danh sách cập nhật đúng số dòng + badge.

- **Định hướng đa chuyên khoa (nhi/sản/nha) — chốt "specialty pack" + khung tối thiểu (`docs/DECISIONS.md` #033)** — chủ dự án hỏi khả năng thương mại hoá một source cho nhiều loại phòng khám. Phân tích khả thi đầy đủ ở `docs/product/multi-specialty-analysis.md` (mới, tài liệu chưa chốt triển khai). Đã hỏi và chốt phạm vi: **chỉ chuẩn bị khung tối thiểu**, giữ nguyên Sprint 3 theo kế hoạch (không xây hẳn 1 gói thật ngay, không đảo lộn roadmap). Đã làm phần không phụ thuộc `encounter` (bảng chưa được code, Sprint 3 chưa bắt đầu): thêm `encounter.specialty` (mặc định `'general'`) vào đặc tả thiết kế (`.claude/docs/data-model.md`, `docs/ERD.md` v1.5 — không migration/DB thật nào chạy); `packages/core/src/specialty/registry.ts` (mới) — `SpecialtyPack`/`createSpecialtyRegistry()`, hoàn toàn trơ (chỉ có gói `general`), chưa có specialty pack thật nào đăng ký.
  **Đã xác minh thật**: `registry.spec.ts` 5/5 pass, 35/35 test `packages/core` không regress, `pnpm -w typecheck` sạch toàn workspace.

- **Mở rộng form Thêm/Sửa hồ sơ bệnh nhân (PAT-01) — ảnh đại diện, CCCD ngày/nơi cấp, dân tộc/quốc tịch/nghề nghiệp, số bảo hiểm, Khu phố, Thông tin người thân (`docs/DECISIONS.md` #034)** — chủ dự án yêu cầu hiệu chỉnh lại giao diện, dừng Sprint 3 (đang bị chặn bởi thiếu file danh mục ICD-10) để làm việc này trước. Dùng Plan mode: khám phá backend/frontend hiện có rồi hỏi 4 điểm chốt (ảnh làm thật ngay qua `StoragePort`; dân tộc/quốc tịch/nghề nghiệp nhập tự do không danh mục DB; địa chỉ Tỉnh/Xã giữ text tự do; người thân 1 bộ trên `patient`, không tách bảng) trước khi viết migration.
  Migration `20260812150000_patient_profile_fields` (viết tay, 11 cột nullable — `photo_key`, `national_id_issued_at/place`, `ethnicity`, `nationality`, `occupation`, `insurance_number`, `relative_full_name/relationship/phone/address`); `address_json` thêm `neighborhood`, bỏ `district` khỏi UI (giữ trong schema/DB tương thích ngược, khớp cải cách hành chính 2 cấp Tỉnh→Xã 2025).
  **Lần đầu hệ thống có upload/serve file thật** — dựng signed-URL tối giản (`apps/api/src/infrastructure/storage/signed-url.ts`, HMAC tự chứa `{tenantId,key,exp}`) + endpoint công khai `GET /api/v1/files/:token` (không `JwtAuthGuard`, chữ ký là lớp xác thực), đúng yêu cầu "phục vụ qua signed URL có hạn" đã ghi từ S1-06 trong `security-audit.md` nhưng chưa từng dùng tới. `POST /patients/:id/photo` (multipart, kiểm magic byte qua `packages/core/src/storage/sniff-image-extension.ts`, giới hạn 3MB → 413).
  **Bố cục form đổi qua 3 vòng chỉnh trực tiếp trên bản dựng thật** (không phải chốt trước): mỗi nhóm trường bọc khung viền + badge xanh nổi trên viền (đồng nhất cho cả "Thông tin hành chính" lẫn "Thông tin người thân" — ban đầu người thân chỉ có divider mảnh, chủ dự án phản hồi "nhạt" nên đổi sang cùng khuôn box+badge); gap lưới thu hẹp 2 lần (`gap-6` → `gap-3`); trang Thêm mới/Chi tiết bỏ hẳn `max-w-4xl`, kéo full-width `p-6` theo đúng yêu cầu "giống bố cục Danh sách bệnh nhân" — **override** quy tắc "form giữ max-w" đã chốt ở `ui-guidelines.md` mục 9 cũ, đã cập nhật lại mục 9 (ghi ngoại lệ) + thêm mục 9b (Boxed Section Form Pattern) mô tả pattern mới để form khác sau này tái dùng đúng.
  **Đã xác minh thật**: `signed-url.spec.ts` (6/6), `sniff-image-extension.spec.ts` (5/5, `packages/core`), `patient-http.spec.ts` mở rộng (33/33, tổng 176 test `apps/api` không regress) — upload hợp lệ, sai magic byte → 400, quá 3MB → 413, version sai → 409, ảnh cũ bị xoá thật (URL cũ 404). `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật: tạo hồ sơ đủ field mới → lưu → vào chi tiết → upload ảnh → F5 vẫn thấy ảnh → sửa field mở rộng → lưu đúng, không console error ngoài dự kiến.

- **Danh mục dùng chung `reference_catalog` — Dân tộc/Quốc tịch, quản lý được qua UI (`docs/DECISIONS.md` #037)** — đảo ngược #034 cho riêng 2 field này (`occupation` không đổi). Chủ dự án tự cung cấp 2 file dữ liệu thật (`Danh_sach_54_dan_toc_Viet_Nam.md`, `Master_Data_Quoc_Tich_ISO3166.md`) thay vì để AI tự soạn — giải quyết đúng gốc rễ lý do #034 từng chặn. Đã dựng mockup Artifact trước khi code, chốt qua hỏi-đáp: lưu `code` (không lưu `name`) trên `patient.ethnicity`/`patient.nationality`; quyền quản lý `clinic_admin` (permission mới `reference_catalog.manage`, giới hạn lý thuyết đã ghi rõ — không cách ly đa-tenant); IA menu "Quản trị" đổi thành nhóm cha/con (giống "Tiếp nhận và Đặt lịch"), mục con "Danh mục dùng chung" → `/admin/reference-catalog` (trang riêng, tab theo category); "Xoá" là soft (`is_active=false`) — phát hiện role DB `nexamed_app` bị revoke `DELETE` toàn cục từ migration nền tảng, không thể xoá cứng dù muốn.
  Bảng mới `reference_catalog` (toàn hệ thống, không `tenant_id`, không `version` — khác `permission`: **không** revoke INSERT/UPDATE vì `clinic_admin` sửa qua chính API). Seed đầy đủ 54 dân tộc + 30 quốc tịch (không phải placeholder) tại `packages/core/src/reference-catalog/data.ts`. Module `apps/api/src/modules/reference-catalog/` (CRUD đầy đủ, `PermissionGuard` chuẩn — không lệch pattern như dự tính ban đầu, vì xác nhận được dự án chưa có tenant production nào nên thêm permission mới không cần backfill). `AdminPage.tsx` (thuần `EmptyState`, không còn dùng) đã xoá cùng route `/admin`.
  **Đã xác minh thật**: `reference-catalog-http.spec.ts` (10/10), `packages/core/src/reference-catalog/data.spec.ts` (2/2) — tổng 191 test `apps/api` + 42 test `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật (dữ liệu thật, không mock): sidebar nhóm Quản trị mở đúng → trang Danh mục dùng chung hiện đúng 54/30 dòng theo tab → thêm/sửa/xoá(ẩn)/khôi phục một mục qua UI đều phản ánh đúng ngay → form Thêm bệnh nhân có dropdown đúng dữ liệu, mặc định Quốc tịch = Việt Nam (`VNM`) → mở hồ sơ "cũ" có giá trị tên tự do trước #037 (tạo qua API để mô phỏng) → dropdown giữ nguyên giá trị cũ, không bị xoá trắng. Không console error ngoài dự kiến.
  **Phát hiện thật lúc kiểm bằng trình duyệt (không phải bug code, là đặc điểm cơ chế RBAC đã biết từ trước)**: tenant dev đã seed từ trước (trước khi thêm 2 permission mới) không tự có `role_permission` cho `reference_catalog.*` — `seedDefaultRolesForTenant()` chỉ chạy lúc **tạo tenant**, không có cơ chế backfill permission mới cho tenant đã tồn tại. Đã vá thủ công cho tenant dev bằng script một lần (không phải thay đổi code) — ghi lại ở mục "Đang chờ" vì đây sẽ là vấn đề thật khi phòng khám pilot đã cài đặt và dự án cần thêm permission mới sau này.

- **Danh mục hành chính `province`/`ward` (Tỉnh/Phường-Xã, sáp nhập hành chính 2025) — đã xong (`docs/DECISIONS.md` #038)** — chủ dự án gửi file dữ liệu chính thức Bộ Nội vụ (`docs/data/Danh-muc-Phuong-xa_moi.md`, 34 tỉnh + 3321 phường/xã), hỏi và chốt thiết kế 2 bảng (không gộp 1 bảng phẳng như file gốc) trước khi code, xác nhận thêm: **read-only** (khác `reference_catalog` — không endpoint quản lý qua UI, dữ liệu hành chính nhà nước) và tích hợp luôn vào `patient.address_json` (đảo ngược tiếp phần Tỉnh/Xã của #034).
  Bảng mới `province`/`ward` (toàn hệ thống, không `tenant_id`, không `version`/`is_active` — cùng bản chất `icd10_catalog`; REVOKE `INSERT`/`UPDATE` khỏi `nexamed_app`, chỉ seed script ghi được). `ward.code` (8 chữ số) duy nhất toàn quốc, dùng thẳng làm PK. Seed thật tại `packages/core/src/geo/data.ts`. Quyền dùng lại `patient.read` (không thêm permission mới, tránh lặp vấn đề backfill role_permission ghi ở trên). Module `apps/api/src/modules/geo/` (`GET /geo/provinces`, `GET /geo/wards?provinceCode=` — bỏ trống trả toàn bộ, dùng dựng bảng tra code→tên cho cột Địa chỉ ở Danh sách bệnh nhân).
  Web: `PatientFormFields.tsx` — 2 input text (Tỉnh/Phường-Xã) → Combobox cascading (đổi Tỉnh tự xoá Phường/Xã đã chọn). Tách `withLegacyValueOption()` từ `PatientFormFields.tsx` ra `shared/ui/Combobox.tsx` (dùng chung cho cả ethnicity/nationality lẫn province/ward — trùng lặp lần 2, tách theo `CLAUDE.md`).
  **Sửa dữ liệu theo yêu cầu chủ dự án lúc rà lại**: 4 tên tỉnh dùng viết tắt "Tp" trong file gốc (Hải Phòng, Đà Nẵng, Hồ Chí Minh, Cần Thơ) đổi thành "Thành phố" đầy đủ; `seed-geo.ts` phần `province` đổi sang upsert (thay vì bỏ qua trùng) để tên sửa đồng bộ được khi seed lại.
  **Đã xác minh thật**: `packages/core/src/geo/data.spec.ts` (4/4), `apps/api/src/modules/geo/geo-http.spec.ts` (7/7) — tổng 198 test `apps/api` + 46 test `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật (dữ liệu thật): Combobox cascading đúng (34 tỉnh, đúng số phường/xã lọc theo tỉnh, đổi tỉnh tự xoá phường/xã cũ); tạo hồ sơ đủ địa chỉ → Danh sách bệnh nhân hiện đúng tên đã resolve từ mã, không phải mã thô.

- **Trang "Danh mục" + "Cấu hình hệ thống" — mẫu Two-panel Config Screen (`docs/DECISIONS.md` #039, #040)** — chủ dự án gửi 2 ảnh tham khảo phần mềm phòng khám khác cho phong cách trang Cấu hình, dựng mockup Artifact 2 kiểu (Kiểu 1 danh sách phẳng / Kiểu 2 thanh nhóm + thẻ) trước khi code, chốt Kiểu 2 — nhưng chốt rõ mẫu này KHÔNG bắt buộc cho mọi trang Quản trị tương lai, phân bố menu do chủ dự án quyết định khi tạo từng menu mới (`.claude/docs/ui-guidelines.md` mục 10). Route `/admin/reference-catalog` đổi thành `/admin/catalog` (nhãn sidebar "Danh mục"), gộp `reference_catalog` (Dân tộc/Quốc tịch) và `geo` (Tỉnh/Thành, Phường/Xã — vẫn chỉ đọc đúng #038) chung 1 pill "Danh mục dùng chung" (4 mục). `ClinicHoursPane` (giờ làm việc + slot, dùng `GET/PATCH /clinic-settings` có sẵn từ S2-07, lần đầu có UI web) ban đầu đặt chung trang "Danh mục", sau đó tách hẳn sang trang riêng "Cấu hình hệ thống" (`/admin/system-config`) theo yêu cầu chủ dự án — nhóm sidebar "Quản trị" nay có 2 mục con.
  Nhiều vòng chỉnh trực tiếp trên bản chạy thật (không dựng mockup lại): bỏ pill "Danh mục hành chính" riêng (gộp hẳn vào "Danh mục dùng chung"); form "Giờ làm việc" đổi từ `max-w-2xl`/`max-w-3xl` (để trống nhiều bên phải) sang full-width theo Boxed Section Form Pattern (mục 9b, 2 khung viền+badge "Giờ làm việc theo tuần"/"Độ dài Slot"); **trích `ConfigScreenShell`** (`apps/web/src/shared/ui/ConfigScreenShell.tsx`) sau khi chủ dự án phát hiện trang "Cấu hình hệ thống" mới không đồng bộ bố cục với "Danh mục" — lần dùng thứ hai của khuôn pill+list+content nên tách theo `CLAUDE.md`, cả 2 trang cùng dùng để LUÔN đồng nhất (kể cả khi chỉ có 1 pill/1 mục — đã hỏi và chốt không tự ẩn cột trái/pill bar dù ít lựa chọn). `GeoPane` mode Phường/Xã mặc định chọn sẵn Thành phố Hồ Chí Minh (mã `29`) thay vì để trống chờ chọn.
  **Điều tra "lưới lịch hẹn không áp đúng giờ đã cấu hình" theo báo cáo chủ dự án**: xác minh bằng Playwright + curl trực tiếp trên dữ liệu thật — không có bug thật trong `ClinicHoursPane`/`AppointmentGridView`/`resolveDayHours()`, chỉ là dữ liệu `tenant_setting` bị lệch tạm thời do chính kịch bản test Playwright của tôi trong phiên này chọn nhầm ô giờ (`input[type=time] >> nth=N` khớp nhầm hàng khi layout đổi sang lưới 2 cột) — đã khôi phục đúng giá trị gốc và xác minh lại đúng cách (sửa giờ 1 ngày cụ thể qua UI → lưới lịch hẹn hiện đúng khung giờ đó ngay). Riêng việc lưới cần cuộn khá nhiều để thấy hết 1 ngày dài (9-10 tiếng, `ROW_HEIGHT_PX=92`/30 phút một hàng từ S2-09) là hành vi có sẵn từ trước, chưa phải bug — đang chờ chủ dự án xác nhận có muốn điều chỉnh mật độ hiển thị hay không.
  **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch toàn workspace (trừ 1 lỗi ESLint có sẵn từ trước, không liên quan). Không migration/schema/API contract nào đổi. Playwright qua trình duyệt thật + dữ liệu thật: cả 2 trang cấu hình đồng nhất bố cục, CRUD Dân tộc/Quốc tịch hoạt động, Phường/Xã mặc định TP.HCM có dữ liệu ngay, sửa giờ làm việc lưu đúng và phản ánh đúng trên lưới lịch hẹn.

**Fix nhỏ đi kèm — nhãn giờ đóng cửa ở lưới lịch hẹn**: chủ dự án xem trên màn hình phản hồi "chỉ hiển thị đến 16h30 trong khi giờ làm việc là 7h30-17h" — đúng là mỗi nhãn giờ trên lưới chỉ đánh dấu ĐẦU 1 khung 30 phút (`generateSlotLabels`, S2-09), nên hàng cuối "16:30" trông như lưới dừng ở đó dù khung đó thực chất kéo dài tới đúng giờ đóng cửa. Thêm 1 nhãn biên `range.close` (ví dụ "17:00") ngay dưới hàng cuối cùng trong `AppointmentGridView.tsx` — cuộn tới đáy giờ thấy rõ mốc giờ đóng cửa thật, xác nhận lưới đã hiển thị đủ. Không đổi `resolveDayHours()`/dữ liệu, chỉ thêm 1 nhãn hiển thị. Đã xác minh qua Playwright (cuộn tới đáy, thấy đúng "17:00").

- **Backfill `role_permission` cho tenant cũ — tự động lúc API khởi động**: đã hỏi và chốt cơ chế trước khi code (3 lựa chọn: script chạy tay / endpoint quản trị / tự động lúc khởi động) — chọn tự động. `apps/api/src/infrastructure/persistence/sync-role-permissions.ts` (`syncRolePermissionsForTenant`/`syncRolePermissionsForAllTenants`) — với mỗi tenant, chỉ THÊM dòng `role_permission` còn thiếu so với `DEFAULT_ROLE_PERMISSIONS` cho vai trò `is_system_default=true`, không sửa/xoá dòng đã có (an toàn nếu ADM-07 sau này cho `clinic_admin` tuỳ biến) và không đụng vai trò tuỳ biến. Gọi 1 lần trong `main.ts` (`bootstrap()`, sau khi tạo `app`, trước `app.listen()`) — không chạy trong test (test không qua `main.ts`). Không migration/schema nào đổi (`role_permission` đã ghi được bởi `nexamed_app` từ trước). Yêu cầu permission mới phải có sẵn trong bảng `permission` trước (vẫn qua `pnpm db:seed`, `nexamed_app` không có quyền ghi bảng đó) — nếu chưa, dòng đó bị bỏ qua thay vì crash cả API.
  **Đã xác minh thật**: `sync-role-permissions.spec.ts` (4/4, Postgres thật) — tenant seed đủ thì không thêm gì; xoá thử 1 dòng mô phỏng tenant cũ → vá đúng lại, đúng `data_scope` theo ma trận, gọi lại lần 2 không tạo trùng; không đụng vai trò tuỳ biến; `ForAllTenants` chỉ vá đúng tenant thiếu, cách ly tenant khác. Kiểm thật thêm trên DB dev thật (không phải test container): xoá `clinic_admin/audit_log.read` của tenant dev → chạy `node dist/main.js` → log `[startup] Đồng bộ role_permission: thêm 1 dòng còn thiếu (...)` → xác nhận DB đã phục hồi đúng `dataScope=global`. Tổng 202 test `apps/api` pass (198 cũ + 4 mới), không regress. `pnpm -w typecheck/lint/build` sạch (trừ 1 lỗi ESLint có sẵn từ trước, không liên quan).

- **Lưới lịch hẹn cuộn nhiều — đã hỏi và chốt**: giữ nguyên `ROW_HEIGHT_PX=92`/bước 30 phút (không đổi mật độ dữ liệu — quyết định thiết kế đã chốt lúc S2-09), chỉ tối ưu khoảng trống khác quanh lưới. `AppointmentSchedulePage.tsx`: gộp 2 hàng công cụ cũ (nút "Đặt lịch" riêng + hàng điều hướng ngày/legend/toggle) thành 1 hàng, `p-4`→`p-3`/`gap-3`→`gap-2.5`; `AppointmentGridView.tsx`: giảm padding dọc header cột bác sĩ (`py-2.5`→`py-2`). Nhường thêm vùng nhìn thấy cho thân lưới trước khi phải cuộn, không đụng dữ liệu/logic giờ.
  **Đã xác minh thật qua trình duyệt** (Playwright, dữ liệu dev thật): hàng công cụ gộp hiển thị đúng (điều hướng ngày + badge bên trái, legend + toggle Lưới/Danh sách + nút "Đặt lịch" bên phải), không vỡ layout; bấm "Đặt lịch" mở đúng panel đặt lịch nhanh; chuyển "Danh sách" đúng dữ liệu; thu hẹp xuống 1366x768 (ngưỡng tối thiểu theo CLAUDE.md) vẫn không tràn/vỡ hàng công cụ; không console error ngoài dự kiến. `pnpm -w typecheck/lint/build` sạch (trừ 1 lỗi ESLint có sẵn từ trước, không liên quan).

- **Trang "Thông tin phòng khám" — mở rộng `tenant`, upload logo, sửa bug ảnh vỡ có sẵn từ #034 (`docs/DECISIONS.md` #041)** — mục con mới trong pill "Cấu hình phòng khám" (`/admin/system-config`), đặt trước "Giờ làm việc & Slot" theo yêu cầu chủ dự án, mặc định mở đầu tiên. Migration `20260813120000_tenant_profile_fields` thêm 6 cột trên `tenant` (`phone`/`email`/`currency`/`taxCode` đã có/`timezone`/`logo_key`/`print_logo_key`) — không dùng `tenant_setting`, không permission mới (dùng lại `clinic_config.*`). `currency`/`timezone` chỉ lưu giá trị hiển thị, chưa nối vào logic tính toán/ngày giờ hệ thống. `apps/api/src/modules/clinic/clinic-profile.{controller,service,repository}.ts` (mới) — cùng khuôn upload ảnh đại diện bệnh nhân #034 (magic byte, signed URL, xoá file cũ). `apps/web/src/features/clinic/ClinicInfoPane.tsx` (mới).
  **2 bug phát hiện lúc dùng thử, đã sửa**: (1) ảnh vỡ (broken image icon) — `photoUrl`/`logoUrl` là đường dẫn tương đối, `<img>` phân giải theo origin trang web thay vì API → sửa bằng `resolveApiUrl()` dùng chung (`shared/api/client.ts`), áp dụng lại cho cả `PatientAvatarUpload.tsx` (bug có sẵn từ #034, không phải do tính năng mới); (2) upload ảnh hoạt động độc lập với chế độ Sửa gây khó hiểu — đổi sang ẩn nút "Chọn ảnh" cho tới khi bấm "Sửa", áp dụng cho cả 2 nơi (patient avatar + clinic logo), ghi thành quy tắc bắt buộc ở `.claude/docs/ui-guidelines.md` mục 9b.
  **Đã xác minh thật**: `clinic-profile-http.spec.ts` 18/18 (tổng 220 test `apps/api`), `pnpm -w typecheck/lint/build` sạch, Playwright qua trình duyệt thật (CRUD đầy đủ, upload logo hiển thị đúng ảnh thật, gating Sửa đúng ở cả 2 màn hình).

- **Tiếp nhận bệnh nhân (Sprint 3, phần 1: REC-01→03) — đã xong (`docs/DECISIONS.md` #042)** — check-in thật (tạo `encounter` + gắn `patient` + chuyển `appointment→CONVERTED`, atomic), trang "Tiếp nhận" mới (hàng đợi, `/reception`), nhập sinh hiệu có cảnh báo ngoài ngưỡng theo tuổi (không chặn lưu), nút "Bắt đầu khám"/"bỏ về". Không phụ thuộc ICD-10 nên làm được trước phần Khám bệnh (vẫn treo). Module mới `apps/api/src/modules/reception/` + `apps/api/src/modules/encounter/` (chia sẻ Repository qua Nest `exports` trong 1 transaction — quyết định kiến trúc mới, xem #042). Vá ma trận RBAC (3 permission `encounter.*` mới, nurse đổi personal→global). Endpoint `POST /appointments/:id/checkin` cũ đã **gỡ bỏ**, thay bằng `POST /reception/check-in`.
  **Bổ sung giữa phiên theo yêu cầu chủ dự án**: số điện thoại được phép trùng thật sự (VD: mẹ dùng chung SĐT cho nhiều con) — chỉ CCCD mới chặn. `GET /patients/by-phone` mới: cảnh báo mềm ở form Thêm/Sửa khách hàng (không chặn lưu), và ở trang Tiếp nhận dùng để lễ tân **chọn** đúng khách hàng khi tra theo SĐT lịch hẹn.
  **Đã xác minh thật**: `encounter-state-machine.spec.ts` (5/5), `vital-sign-thresholds.spec.ts` (10/10), `reception-http.spec.ts` (19/19), `encounter-http.spec.ts` (9/9), `patient-http.spec.ts` +4 test `by-phone` — tổng 246/246 test `apps/api` pass, chạy lặp lại 3 lần để xác nhận hết flaky sau khi sửa 1 race condition thật phát hiện lúc test (`syncRolePermissionsForTenant` đổi `create()` từng dòng sang `createMany({skipDuplicates:true})` — tránh Postgres abort cả transaction khi 2 tiến trình chèn cùng lúc). `pnpm -w lint/typecheck/build` sạch toàn workspace.

- **Thiết kế lại "Tiếp nhận" sau 3 vòng góp ý chủ dự án — đã xong (`docs/DECISIONS.md` #043)** — bản #042 (hàng đợi gộp appointment+encounter, "khách vãng lai" tạo appointment+encounter cùng lúc) bị bác bỏ hoàn toàn. Bản mới: **3 màn hình tách biệt** — "Danh sách tiếp nhận" (`/reception`, CHỈ encounter đã tiếp nhận, theo ngày, không gộp lịch hẹn chưa tới), "Tiếp nhận bệnh nhân" (`/reception/new`, form tạo `encounter` TRỰC TIẾP với `appointment_id=NULL` — khách không qua đặt lịch trước, `POST /reception/direct`), "Hàng đợi khám" (`/reception/doctor-queue`, trang riêng cho bác sĩ, lọc `doctorId` tường minh vì `encounter.read` vẫn `global`). Check-in từ lịch hẹn có sẵn chuyển hẳn thành popup trên panel chi tiết Lịch hẹn (`AppointmentDetailPanel.tsx`), không còn ở trang Tiếp nhận. 2 danh mục cấu hình mới **tái dùng `reference_catalog`** (thêm category `PATIENT_SOURCE`/`EXAM_TYPE` + cột `price`, không tạo bảng/module riêng) — "Loại khám" mang giá tham khảo, **snapshot** vào `encounter.exam_type_code/name/price` lúc tạo (không JOIN sống), chỉ lưu để hiển thị, KHÔNG tính viện phí (ngoài phạm vi CLAUDE.md). Migration mới `20260813160000_reception_direct_and_catalog_extension`. RBAC không đổi (tái dùng `encounter.create`).
  **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch (lint có 1 lỗi cấu hình ESLint plugin tiền-nhiệm không liên quan, ở file không đụng tới). `reception-http.spec.ts` viết lại (25/25 pass, thêm describe `POST /reception/direct` + `GET /reception/list` với `doctorId`). Toàn bộ 252 test `apps/api`+`packages/core` pass (`geo-http.spec.ts` có 1 race điều kiện tiền-nhiệm khi chạy song song nhiều file test — pass 100% khi chạy riêng, không liên quan thay đổi lần này, đã xác nhận qua nhiều lần chạy).

- **"Danh sách tiếp nhận" về đúng nghĩa THUẦN theo dõi trạng thái — đã xong (`docs/DECISIONS.md` #044)** — chủ dự án phát hiện trang này vẫn còn giữ nút Sinh hiệu/Bắt đầu khám/+Tiếp nhận copy từ thiết kế cũ, mâu thuẫn với quyết định #043. Đã sửa: "Bắt đầu khám" chuyển hẳn sang "Hàng đợi khám" (đúng đối tượng thao tác); "Sinh hiệu" bỏ hẳn khỏi trang này, chuyển sang nhập NGAY LÚC TIẾP NHẬN (thiếu thì bác sĩ tự bổ sung sau ở form phiếu khám); nút "+Tiếp nhận bệnh nhân" bỏ (đã có mục sidebar). **Hợp nhất 2 biểu mẫu tiếp nhận thành 1 component** `ReceptionIntakeForm.tsx` (bệnh nhân, lý do, nguồn khách, loại khám, bác sĩ, sinh hiệu tuỳ chọn) — dùng cho cả trang "Tiếp nhận bệnh nhân" (`mode='direct'`) lẫn popup check-in trên panel Lịch hẹn (`mode='checkin'`, thay hẳn dialog tối giản cũ, bác sĩ/giờ cố định theo lịch hẹn). Backend: `checkInRequestSchema` mở rộng khớp `registerReceptionRequestSchema` (bắt buộc kèm loại khám, tuỳ chọn kèm sinh hiệu — `intakeVitalSignFieldsSchema` dùng chung với `recordVitalSignRequestSchema`); `checkIn()`/`registerDirect()` đều tạo kèm `vital_sign` trong cùng transaction nếu có nhập. `POST /reception/encounters/:id/vital-signs` (REC-02/03) giữ nguyên phía backend làm hạ tầng bổ sung sau, chỉ gỡ trigger UI (`VitalSignForm.tsx` xoá — 0 caller còn lại).
  **Đã xác minh thật**: `reception-http.spec.ts` (25/25, thêm assertion tạo `vital_sign` kèm check-in), `encounter-http.spec.ts` (9/9, vá helper `checkInFreshEncounter()` riêng — payload cũ thiếu loại khám bắt buộc mới). Tổng 252/252 test `apps/api`+`packages/core` pass (không regress, `geo-http.spec.ts` cũng pass lần này). `pnpm -w typecheck/build` sạch toàn workspace, OpenAPI + web type đã sinh lại.

- **Sidebar — nhóm "Khám bệnh" mới, chuyển "Hàng đợi khám" vào đây** — dọn chỗ sidebar trước khi làm màn hình Khám bệnh thật (S3-05→07, vẫn chặn bởi ICD-10). "Tiếp nhận và Đặt lịch" còn 4 mục; "Khám bệnh" (mới, cùng điều kiện vai trò `DOCTOR_QUEUE_ROLES`) chỉ có "Hàng đợi khám", route/code giữ nguyên `/reception/doctor-queue` / `features/reception/` — thuần đổi vị trí sidebar, không đổi module. Sửa kèm `useBreadcrumb` của `ReceptionDoctorQueuePage.tsx` (lệch nhóm cũ, phát hiện lúc kiểm bằng trình duyệt). **Đã xác minh thật qua Playwright**: thứ tự nhóm đúng, vào thẳng `/reception/doctor-queue` chỉ nhóm "Khám bệnh" tự mở (không cả 2 nhóm cùng mở dù chung tiền tố `/reception`), breadcrumb đúng "Khám bệnh › Hàng đợi khám". `pnpm --filter @nexamed/web run typecheck` sạch, `pnpm -w lint` chỉ còn 1 lỗi ESLint có sẵn từ trước không liên quan.

- **Đặt lịch nhanh — thiết kế lại theo yêu cầu chủ dự án**: `AppointmentQuickCreatePanel.tsx` chia 2 nhóm Boxed Section (Thông tin hành chính/Thông tin đặt lịch), rộng 480px (~33% màn 1440px), thêm ngày hẹn editable + tìm SĐT khớp `patient` thật (khoá SĐT/Họ tên/Mã BN khi khớp, hiện kèm ngày sinh DD/MM/YYYY để đối chiếu, không tự điền), bỏ ô Thời lượng (dùng thẳng cấu hình slot), thêm "Người tạo lịch" (readonly, tên user đăng nhập). Không đổi API contract (`patientId` vẫn không gửi lên, đúng #032). Thêm `shared/format/date.ts` (`formatDobDisplay`, DD/MM/YYYY) áp dụng cho mọi nơi hiển thị `patient.dob` dạng chỉ-xem — KHÔNG áp dụng được cho `<input type="date">` (trình duyệt tự hiển thị theo locale OS, chưa có ô nhập ngày tự chế). Giá trị input toàn app đổi sang đậm/rõ (`font-semibold text-[15px]`, xem `.claude/docs/ui-guidelines.md` mục 4.1c) thay `text-sm` thường. Thêm màu nhấn phụ `brand-teal` (#099078, `apps/web/src/app/index.css` `@theme`) cho các chip lựa chọn (Nguồn đặt lịch/Bác sĩ), tách khỏi `blue-600`. Sửa 1 bug thật: "Nguồn đặt lịch" mặc định `'phone'` khiến tô màu ngay khi mở form dù chưa bấm — đổi mặc định `null`, bắt buộc bấm chọn mới submit được. Đã xác minh qua Playwright (search/khoá SĐT, đổi ngày, đặt lịch thành công, màu sắc đúng theo trạng thái). Hover chọn Bác sĩ đổi sang kết hợp 2 màu thương hiệu (viền `blue-400` + nền `brand-teal-tint`, xem ui-guidelines mục 2.1). Bỏ `shadow-sm` trang trí trên các thẻ đã tô nền đặc (đã chọn) theo phản hồi "nhìn AI tạo" — nguyên tắc mới ghi ở ui-guidelines mục 2.2 (không đổ bóng cho phần tử không thực sự nổi lớp). **Đã xây `shared/ui/TimeInput.tsx`** (mặt nạ `HH:mm`, chuẩn bắt buộc mới — ui-guidelines mục 4.1d) thay toàn bộ `<input type="time">` trong app (Đặt lịch nhanh, dời lịch, tiếp nhận, giờ làm việc phòng khám) — đảm bảo 24h tuyệt đối, không còn phụ thuộc locale trình duyệt/OS. Đã xác minh qua Playwright: gõ `0930` → `09:30`, gõ sai `9999` tự kẹp về `23:59` lúc rời ô.

- **Header danh sách đậm/nổi bật + "Danh sách tiếp nhận" đổi 8 cột + cuộn ngang/dọc ẩn thanh cuộn** — theo phản hồi trực tiếp: (1) header mọi bảng đổi `font-bold text-slate-800` (trước `font-semibold`/`font-medium` tuỳ file, không đồng nhất) — áp cho `PatientListPage`/`GeoPane`/`ReferenceCatalogPane`/`ReceptionListPage`/`AppointmentListView` (ui-guidelines mục 9 bổ sung). (2) `ReceptionListPage.tsx` đổi đúng 8 cột theo yêu cầu (Mã lượt khám/Họ tên/Năm sinh/SĐT/Địa chỉ/Ngày giờ tiếp nhận/Trạng thái/Người tiếp nhận, bỏ cột Bác sĩ) — 3 cột chưa có nguồn dữ liệu (Năm sinh/Địa chỉ/Người tiếp nhận, `ReceptionListItem` chưa có field) hiện `—` chờ khớp sau; cột cố định px + `min-width` tổng để `overflow-x-auto` cuộn ngang được khi không đủ chỗ (ui-guidelines mục 9c, kỹ thuật mới). (3) Bug thật: khung cuộn dọc thiếu `overflow-x-hidden` sinh thêm 1 thanh cuộn ngang RIÊNG (đặc tính CSS: khai `overflow-y` mà bỏ trống `overflow-x` thì trình duyệt tự suy ra `auto`) — đã sửa + ghi cảnh báo. (4) Thanh cuộn giờ ẨN mặc định, chỉ hiện khi rê chuột (class `scroll-hover` mới, `apps/web/src/app/index.css`) — áp cho mọi khung cuộn trong danh sách (`ReceptionListPage` cả 2 trục, `PatientListPage`/`AppointmentListView`/`GeoPane`). **Đã xác minh qua Playwright với 111 dòng dữ liệu test thật** (seed qua API, không phải mock): bảng không vỡ, header/thân bảng cuộn ngang khớp nhau tuyệt đối, không còn 2 thanh cuộn. `pnpm -w typecheck` sạch; lint chỉ còn lỗi cấu hình có sẵn không liên quan.

- **Trang "Danh mục hành chính" (đổi tên từ "Danh mục") — tách "Danh mục dùng chung" thành pill riêng từng danh mục, sửa bug breadcrumb tĩnh, thêm 4 sub-menu "Sắp ra mắt" (`docs/DECISIONS.md` #045, #046)** — chủ dự án yêu cầu trực tiếp (không mockup): 6 danh mục (Dân tộc/Phường-Xã/Tỉnh-Thành/Quốc tịch/Nguồn khách hàng/Loại khám) trước gộp chung 1 pill nay mỗi cái là 1 pill riêng ngang hàng; `ConfigScreenShell` thêm chế độ "pill phẳng" (không cột trái) khi pill không có `items` — nội dung sát menu chính. Sửa kèm bug thật (chủ dự án phát hiện lúc dùng thử): breadcrumb khai tĩnh một lần lúc mount, bấm đổi pill không cập nhật — sửa cho cả `CatalogAdminPage` lẫn `ClinicConfigPage` (đoạn cuối breadcrumb lấy theo pill/mục đang active). Thêm 4 mục sidebar "Sắp ra mắt" (Tổ chức và Nhân sự, Chuyên môn, Cận lâm sàng, Dược và Vật tư) — ngoại lệ có chủ đích với quy tắc "không dựng menu cho tính năng chưa xây" (đã hỏi qua `AskUserQuestion` trước khi làm), chỉ đặt chỗ UI (`ComingSoonPage` dùng chung), không mở rộng phạm vi v1 — Cận lâm sàng/Dược và Vật tư vẫn ngoài phạm vi v1 theo `CLAUDE.md`. Không migration/schema/API nào đổi.
  **Đã xác minh thật**: `pnpm -w typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật (tài khoản `dev.admin`): sidebar đúng thứ tự/nhãn, 6 pill Danh mục hành chính không còn "Danh mục dùng chung", không còn cột trái, breadcrumb đổi đúng theo pill/mục ở cả 2 trang, 4 trang "Sắp ra mắt" hiện đúng, không lỗi console ngoài dự kiến.

- **Tiêu đề tab trình duyệt động (`docs/DECISIONS.md` #047)** — tái dùng nguyên `BreadcrumbProvider` có sẵn (không thêm API riêng ở từng trang): tab hiện `"{menu cha} - {trang/tab đang chọn}"` (ví dụ `Tiếp nhận và Đặt lịch - Lịch hẹn`), về `"NEXAMed"` khi chưa đăng nhập/không có breadcrumb. Đã xác minh thật qua Playwright đọc `document.title` ở nhiều trang.

- **Trang 404 thương hiệu + redirect `/admin` → `/admin/catalog` (`docs/DECISIONS.md` #048)** — chủ dự án báo lỗi thật: gợi ý lịch sử trình duyệt dạng `/admin` (rút gọn từ `/admin/catalog`) ra trang trắng mặc định xấu của `react-router`. Thêm route `{path:'admin', element:<Navigate to="/admin/catalog"/>}` xử lý đúng ca cụ thể; thêm `NotFoundPage.tsx` gắn ở `path:'*'` (mục cuối `children` của route `/`, trong `AppShell`) bắt mọi URL không khớp khác — render kèm sidebar/breadcrumb như trang thường, không đứng riêng. Đã xác minh thật qua Playwright cả 4 trường hợp (redirect, 404 trong `/admin/*`, 404 ngẫu nhiên, chưa đăng nhập vẫn về đúng `/login`).

- **Cột mã định danh: bỏ gạch chân mã bấm được, đổi mã không bấm được sang màu đen (`docs/DECISIONS.md` #049)** — chủ dự án hỏi lý do `patientCode` gạch chân, chốt: mã bấm được (`patientCode`/`bookingCode`) chỉ còn màu xanh + con trỏ tay, bỏ `underline decoration-dotted`; mã không bấm được (`encounterNo`) đổi `text-blue-700` → `text-slate-800` (đen, tránh hiểu nhầm là link). Đã xác minh thật qua Playwright ở cả 3 danh sách (bệnh nhân, lịch hẹn, tiếp nhận).

- **Tăng độ đậm chữ dữ liệu trong bảng danh sách + gộp cột "Thời gian" 2 dòng ở Lịch hẹn (`docs/DECISIONS.md` #050)** — chủ dự án phản hồi chữ "mờ nhạt, không có điểm nhấn", yêu cầu chỉnh "tầm 400"; đo thật bằng `getComputedStyle` phát hiện chữ ĐÃ ở 400 sẵn (áp literal 400 sẽ vô tác dụng) — nâng lên `font-medium` (500) cho ô dữ liệu chính/phụ ở 5 bảng danh sách (bệnh nhân, lịch hẹn, tiếp nhận, danh mục hành chính × 2), giữ nguyên placeholder/badge/cột mã/metadata thứ yếu không đổi (giữ phân cấp thị giác). Đồng thời Lịch hẹn (chế độ Danh sách): bỏ cột "Thời lượng", đổi "Ngày giờ" → "Thời gian" (dời ra sau "Mã đặt lịch", trước "Họ tên"), hiển thị 2 dòng giờ đậm xanh/ngày nhỏ nhạt theo mẫu chủ dự án gửi. Đã xác minh thật qua Playwright.

- **Căn lề bảng danh sách, màu nhấn cột "Thời gian", dọn sạch `font-mono` (`docs/DECISIONS.md` #051)** — tiêu đề cột LUÔN canh giữa; dữ liệu canh trái cho cột Tên (Họ tên, Bác sĩ)/Địa chỉ, còn lại canh giữa, áp cho 5 bảng danh sách. Cột "Thời gian" ở Lịch hẹn đổi màu giờ sang `text-brand-teal` (#099078). Phát hiện + sửa dứt điểm nguyên nhân gốc slashed zero: `font-variant-numeric: lining-nums` (sửa lần đầu #CHANGELOG 2026-08-15(2)) KHÔNG tắt được số 0 gạch chéo của font monospace — phải bỏ hẳn `font-mono`; quét lại thấy sót 3 nơi (`AppointmentDetailPanel.tsx`, `ReferenceCatalogPane.tsx`, `GeoPane.tsx`), đã dọn sạch, xác nhận `grep -r "font-mono" apps/web/src` rỗng. Đã xác minh thật qua Playwright.

- **Thiết kế lại "Tiếp nhận bệnh nhân" — hiện đủ field bệnh nhân thay `PatientPicker`, tra trùng SĐT/CCCD qua popup, thêm Loại tiếp nhận/Hình thức khám/Ưu tiên khám/Chỉ định dịch vụ khám (`docs/DECISIONS.md` #052)** — mockup Artifact duyệt qua nhiều vòng chỉnh trên bản chạy thật. Bỏ `PatientPicker` khỏi màn Tiếp nhận, hiện thẳng `PatientFormFields` — gõ SĐT/CCCD tự tra trùng, bật popup xác nhận (`PatientMatchDialog.tsx`, mới, thay banner phẳng cũ — áp dụng nhất quán cho cả PAT-03) thay vì tự khoá âm thầm; chọn xong khoá riêng 4 field định danh (CCCD/Mã BN/Họ tên/Ngày sinh), còn lại sửa được, sửa xong tự PATCH hồ sơ gốc. 4 danh mục mới tái dùng `reference_catalog` (Loại tiếp nhận/Hình thức khám/Lý do ưu tiên/Loại giá dịch vụ, không seed sẵn — `clinic_admin` tự tạo). `encounter` thêm 7 cột (Ưu tiên khám bắt buộc kèm lý do; nhóm "Chỉ định dịch vụ khám" — Loại giá dịch vụ/Đơn vị/Số lượng/Thành tiền — chỉ lưu hiển thị, KHÔNG tính viện phí, "Thanh toán sau" chỉ là cờ UI không lưu DB). Endpoint mới `GET /patients/by-national-id`. Giới tính bệnh nhân không còn mặc định "Nữ" (áp dụng luôn cho Thêm/Sửa bệnh nhân, đúng quy tắc `ui-guidelines.md` 4.1c). **Đã xác minh thật**: 258/258 test `apps/api` (27 `reception-http.spec.ts` + 4 `by-national-id` mới), `pnpm -w typecheck/lint/build` sạch, Playwright qua trình duyệt thật (tạo mới/khớp SĐT/khớp CCCD/bảng dịch vụ tính đúng).

- **Panel "Chi tiết lịch hẹn" — tách "Sửa lịch" khỏi "Dời lịch", "Tiếp nhận" điều hướng trang, popup "Tiếp nhận thành công" (`docs/DECISIONS.md` #053)** — "Dời lịch" (`POST /appointments/:id/reschedule`, thay `PATCH .../reschedule` cũ S2-09) tạo `appointment` MỚI cho ngày/giờ/bác sĩ đã chọn (kế thừa fullName/phone/reason/roomId/durationMinutes/source từ lịch cũ), lịch cũ chuyển `status='RESCHEDULED'` (cột mới `rescheduled_from_id` trỏ về, migration `20260818150000_appointment_reschedule_status`). "Sửa lịch" khôi phục lại (`PATCH /appointments/:id`, tại chỗ, chỉ trong ngày — không có ô chọn ngày) tồn tại song song. `DoctorAvailabilityList.tsx` (mới, `apps/web/src/features/appointment/`) — danh sách bác sĩ trống/bận dùng chung cho cả Đặt lịch nhanh lẫn Dời lịch/Sửa lịch (trùng lặp lần 2, tách theo CLAUDE.md). Nút "Tiếp nhận" đổi từ mở popup (`ReceptionIntakeDialog.tsx`, đã xoá) sang điều hướng `/reception/new` kèm `location.state.checkin` — đảo ngược #043/#044. `ReceptionIntakeForm.tsx`: nộp "Lưu tiếp nhận" (`mode='direct'`) hiện popup "THÀNH CÔNG" thay vì điều hướng ngay, nút "Tiếp nhận mới" xoá form ở lại trang; footer đổi "Huỷ"→"Quay lại", thêm nút "Nhập lại" (`resetForm()`, giữ nguyên fullName/phone/doctorId từ `checkin` khi ở `mode='checkin'`). **Đã xác minh thật**: `appointment-http.spec.ts` viết lại 2 describe (`PATCH :id` 7 test + `POST :id/reschedule` 8 test, 56/56 pass), tổng 259 test `apps/api` không regress (trừ race điều kiện tiền-nhiệm đã biết ở `geo-http.spec.ts` khi chạy song song toàn suite). `pnpm -w typecheck/lint/build` sạch. Playwright qua trình duyệt thật: Dời lịch tạo đúng lịch mới + lịch cũ hiện "Đã dời lịch"; Sửa lịch lưu tại chỗ đúng cùng mã; Tiếp nhận điều hướng đúng kèm prefill; popup "THÀNH CÔNG" đúng mẫu tham khảo.

- **ADM-07 (Vai trò tuỳ biến + ma trận phân quyền) — đã xong (`docs/DECISIONS.md` #057)** — mockup Artifact duyệt qua 2 vòng (chốt bố cục CRUD Xem/Thêm/Sửa/Xoá/Đặc quyền mở rộng theo ảnh tham khảo chủ dự án gửi) trước khi code. Trang "Vai trò & Phân quyền" mới (`/admin/catalog-organization`, thay `ComingSoonPage`, pill trong `ConfigScreenShell`): cột trái danh sách 5 vai trò mặc định + vai trò tuỳ biến, cột phải bảng ma trận (hàng = tính năng nhóm theo 6 khu vực, cột = phạm vi dữ liệu Không/Cá nhân/Khoa-phòng/Toàn bộ). `clinic_admin` tạo/đổi tên/ẩn được vai trò tuỳ biến; 5 vai trò mặc định chỉ sửa ma trận. Backend mới `apps/api/src/modules/iam/role*.ts` (6 endpoint `/roles*`, dùng chung quyền `role_permission.manage`, không permission mới). **Ripple bắt buộc sang `user_account`**: `roleNames` (enum 5 tên cố định) → `roleIds` (uuid thật) trong `createUserAccountRequestSchema`/`updateUserAccountRequestSchema`, `currentUserSchema.roles` nới từ enum sang `string[]` — nếu không đổi, vai trò tuỳ biến tạo ra không gán được cho ai. Migration `20260820090000_role_management` (UNIQUE `role(tenant_id,name)` đổi thường→PARTIAL). **Giới hạn đã biết, chấp nhận ở v1**: Sidebar.tsx vẫn ẩn/hiện menu theo tên cứng 5 vai trò hệ thống — vai trò tuỳ biến không tự có đúng menu tương ứng dù quyền backend đúng.
  **Đã xác minh thật**: `role-http.spec.ts` (18/18), `user-account-http.spec.ts` cập nhật toàn bộ payload (18/18) — tổng 315 test `apps/api` + 73 `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua Chrome thật: toàn bộ luồng tạo/đổi tên/ẩn vai trò, sửa/lưu ma trận, tên vai trò hệ thống hiển thị tiếng Việt — không lỗi console.

## Đang chờ

- Đổi contract bất kỳ endpoint nào phải chạy lại `pnpm --filter @nexamed/api run openapi:generate` rồi `pnpm --filter @nexamed/web run api:codegen` — không sửa tay `openapi.json`/`openapi-schema.d.ts`.
- S2-04 (BHYT) lùi lại sau Sprint 3 theo quyết định đã chốt, không chặn.
- "Ca làm việc riêng từng bác sĩ" (lưới lịch hẹn chỉ hiện bác sĩ có ca hôm đó, cấu hình lịch phân ca trước) — vẫn **chưa làm** (`docs/DECISIONS.md` #030 mục 5). Khác — đã làm hướng liên quan gần: "phòng làm việc hôm nay" (#054, bác sĩ TỰ chọn phòng lúc đăng nhập, không cấu hình lịch trước) + "Tầng phòng" (#055, Tầng/Phòng/Bàn khám). Không thay thế #030 — nếu quay lại làm lưới lịch hẹn lọc theo ca, đọc cả 3 quyết định để tránh chồng chéo.
- Đa chuyên khoa (nhi/sản/nha): chưa xây gói thật nào — chỉ mới có khung tối thiểu (`docs/DECISIONS.md` #033). Khi làm S3-06 (màn hình khám), component nhận cấu hình trường qua props, không hard-code riêng một chuyên khoa. Chọn gói đầu tiên khi có khách hàng thật trả tiền, không dựng khung trước — xem `docs/product/multi-specialty-analysis.md` mục 9.

## Lưu ý môi trường

- Máy dev hiện chạy Node v26.5.0, khác Node 20 LTS ghi trong `CLAUDE.md`/`package.json.engines`. Chưa gây lỗi (chỉ warning), nhưng cần dùng đúng Node 20 khi triển khai thật.
- Docker đã cài trên máy dev. Migration `20260807090207_init` **đã được áp thật lên Postgres 18.4** qua `prisma migrate deploy` và xác minh bằng insert thử — `id` sinh ra đúng UUID v7 (time-ordered, ví dụ `019fdc38-6d2f-7ff2-...`). Không còn là "chưa test".
- **Postgres container map ra cổng 5433, không phải 5432 mặc định** — máy dev này có sẵn một Postgres native (Windows service `postgres.exe`, không liên quan tới dự án) đang chiếm 5432. `DATABASE_URL` trong `.env.example`/`.env` đã trỏ sang `5433`. Nếu đổi máy dev khác không có xung đột này, có thể đổi lại 5432 trong `docker-compose.yml`.
- UUID v7 (`DECISIONS.md` #009) và cột `audit_log` (`DECISIONS.md` #007) đã chốt xong, không còn treo.
- Mật khẩu role `nexamed_app` đang hard-code trong migration (`'nexamed_app'`) — chỉ dùng được cho local dev/CI, **bắt buộc đổi** khi triển khai on-prem thật (S4-05), xem `docs/DECISIONS.md` #010.
