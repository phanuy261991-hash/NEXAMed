# Current — NEXAMed

Trạng thái hiện tại của dự án. Cập nhật file này mỗi khi trạng thái thay đổi đáng kể (không phải log từng commit — đó là việc của `CHANGELOG.md`).

## Giai đoạn

Sprint 1 — Nền tảng đã xong toàn bộ 11 việc (S1-01 → S1-09). **Sprint 2 — Bệnh nhân + Đặt lịch đã xong toàn bộ** (tuần 3-4 theo `docs/product/plan.md` mục 5): S2-01, S2-02, S2-03, S2-05, S2-06, S2-07, S2-08, S2-09, S2-10 (cả 3 gate cuối sprint đã đạt, xem `docs/TASK.md`). S2-04 (BHYT) lùi lại sau Sprint 3 theo quyết định đã chốt, không chặn.

**Sprint 3 (Khám bệnh) — S3-01 (danh mục ICD-10) đã xong, mở khoá một phần** — module `icd10_catalog` hiện thực đầy đủ (Chương I, 924 mã, trang tra cứu 3 cột cascade), xem `docs/DECISIONS.md` #056 và mục "Đã có" bên dưới. Vẫn CHƯA mở khoá hoàn toàn phần "Khám bệnh" (S3-05→07: API màn khám, bộ chọn chẩn đoán, ghi chú SOAP) — cần thêm component chọn chẩn đoán, chưa xây. Trong lúc chờ, chủ dự án đã cho làm nhiều việc khác (hiệu chỉnh hồ sơ bệnh nhân #034-#036, danh mục dùng chung #037, danh mục hành chính #038, redesign trang Danh mục/Cấu hình #039-#040, backfill role_permission tự động, trang "Thông tin phòng khám" #041) — xem mục "Đã có" bên dưới. **Phần "Tiếp nhận" của Sprint 3 (REC-01→03: check-in, hàng đợi, sinh hiệu) đã làm xong, sau đó thiết kế lại theo 3 vòng góp ý chủ dự án** (2026-08-13, `docs/DECISIONS.md` #042 rồi #043) — không phụ thuộc ICD-10 nên không cần chờ. Bản hiện tại: "Danh sách tiếp nhận" (chỉ encounter, theo ngày) tách khỏi "Tiếp nhận bệnh nhân" (form tạo encounter trực tiếp, không qua appointment) và "Hàng đợi khám" (trang riêng cho bác sĩ). Phần "Khám bệnh" (SOAP/chẩn đoán/đơn thuốc) vẫn treo.

**Danh mục dùng chung (`reference_catalog`) — Dân tộc/Quốc tịch — đã xong** (mục "Việc đang treo" ở bản ghi trước đã được tiếp tục và hoàn thành trong phiên này). Chủ dự án xác nhận đảo ngược `docs/DECISIONS.md` #034 (tự cung cấp 2 file dữ liệu thật — 54 dân tộc + 30 quốc tịch — thay vì để AI tự soạn) và muốn xây thành master data **quản lý được** qua UI (thêm/sửa/ẩn), không chỉ xem như phác thảo ban đầu. Chi tiết đầy đủ ở `docs/DECISIONS.md` #037 và mục "Đã có" bên dưới.

**Danh mục hành chính `province`/`ward` (Tỉnh/Phường-Xã, sáp nhập hành chính 2025) — đã xong**. Chủ dự án gửi file dữ liệu chính thức (Bộ Nội vụ), hỏi chốt thiết kế 2 bảng (không gộp phẳng) và phạm vi (read-only, tích hợp luôn vào `patient.address_json`) trước khi code. Chi tiết đầy đủ ở `docs/DECISIONS.md` #038 và mục "Đã có" bên dưới.

**"Phòng làm việc hôm nay" (#054) + "Tầng phòng" (#055) — đã xong.** Chủ dự án gửi tài liệu tham khảo mô hình định tuyến theo phòng (HIS); dùng EnterPlanMode + AskUserQuestion chốt 3 điểm trước khi code (bác sĩ tự chọn phòng lúc đăng nhập; hàng đợi khám GIỮ NGUYÊN lọc theo `doctor_id`, không đổi RBAC; tự ẩn hoàn toàn ở quy mô 1-3 bác sĩ). Bảng mới `doctor_room_session`. Ngay sau đó chủ dự án yêu cầu thêm quản lý Tầng/Bàn khám-Ghế, tích hợp chung 1 màn hình Quản trị (không tách pill riêng) — bảng mới `floor` (tùy chọn)/`exam_station` (bắt buộc thuộc 1 phòng), cả hai THUẦN mô tả, không đổi đơn vị điều phối (vẫn ở cấp `room`). Mục sidebar mới "Tầng phòng" trong "Cấu hình hệ thống". Chi tiết đầy đủ ở `docs/DECISIONS.md` #054/#055.

**Module `icd10_catalog` (S3-01, mở khoá một phần) — đã xong, hiện có ĐỦ Chương I-XXII (15.844 mã).** Chủ dự án gửi file danh mục ICD-10 chính thức theo nhiều đợt trong cùng phiên — Chương I trước (924 mã), rồi II-V, rồi toàn bộ VI-XXII còn lại. Dùng EnterPlanMode + AskUserQuestion chốt trước khi code (2 agent Explore khảo sát `geo`/`reference_catalog`/`ConfigScreenShell` trước, plan lưu tại `C:\Users\Administrator\.claude\plans\sparkling-scribbling-stallman.md`). File gốc trên đĩa hoá ra đã đúng UTF-8 sẵn (không hề mojibake — lỗi chỉ sinh ra lúc dán vào khung chat), copy byte-for-byte (`cp` + `diff` xác nhận khớp tuyệt đối) vào `docs/data/icd10-chuong-<số>.md` (22 file) thay vì chạy bước sửa mojibake đã dự trù. Schema mở rộng đầy đủ (`chapter_code`/`chapter_name`/`block_code`/`block_name`/`group_code`/`group_name`/`gender_restriction`/`usage_restriction`/`who_note`, đúng khuôn `province`/`ward`) — migration `20260819110000_icd10_catalog` (chỉ chạy một lần, không đổi khi nạp thêm chương). Dữ liệu sinh bằng script dùng-một-lần đọc TOÀN BỘ file khớp mẫu `icd10-chuong-*.md` (`packages/core/src/icd10/data.ts`), tách ký hiệu chéo `†`/`*` (hệ thống dagger/asterisk của WHO) khỏi `code`. **2 vấn đề kỹ thuật phát sinh thật khi dữ liệu tăng lên hàng chục nghìn dòng, đã sửa**: (1) mảng object literal 15.844 phần tử làm TypeScript lỗi `TS2590` (vượt ngưỡng độ phức tạp kiểm tra kiểu) — chuyển sang nhúng JSON string + `JSON.parse` lúc chạy; (2) `chapterCode` là số La Mã ("I".."XXII"), sắp theo thứ tự CHUỖI ở DB cho kết quả sai (`"IX" < "V"` theo alphabet) — thêm `packages/core/src/icd10/roman-numeral.ts` (`romanToInt`, có unit test) để `Icd10Service.listChapters` tự sắp lại đúng thứ tự Chương thật ở tầng ứng dụng. Module `apps/api/src/modules/icd10/` (4 endpoint: chapters/groups/codes/tìm kiếm, dùng lại quyền `patient.read`). Trang tra cứu mới "Danh mục Chuyên môn" (`/admin/catalog-clinical`, thay `ComingSoonPage`) — `ConfigScreenShell` pill phẳng "ICD-10", 3 cột cascade (Chương | Nhóm gộp theo Khối | Bảng mã chi tiết) + ô tìm nhanh, badge cảnh báo mềm giới tính/hạn chế bệnh chính (không chặn). Chi tiết đầy đủ ở `docs/DECISIONS.md` #056.
  **Đã xác minh thật**: `packages/core` `data.spec.ts` (9/9, kiểm đúng số dòng từng Chương I-XXII + không còn ký hiệu `†`/`*`) + `roman-numeral.spec.ts` (3/3), `apps/api` `icd10-http.spec.ts` (11/11, HTTP e2e trên Postgres thật, seed đủ 22 chương, xác nhận thứ tự Chương trả về ĐÚNG số La Mã) — tổng 296 test `apps/api` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. Reseed dev DB thật xác nhận đủ 15.844 dòng đúng số lượng từng chương. Playwright qua Chrome thật (dùng thẳng Chrome cài sẵn trên máy, `executablePath`, do Playwright không tự tải được Chromium bundled trong môi trường này — tải bị treo ở bước giải nén, nghi antivirus, thử 3 lần): đăng nhập → Danh mục Chuyên môn → chọn Chương I → chọn Nhóm A00 → đúng 4 mã hiện ra; gõ ô tìm nhanh hoạt động, không lỗi console. **Vẫn CHƯA mở khoá hoàn toàn phần "Khám bệnh"** — S3-05→07 (API màn khám, ghi chú SOAP, bộ chọn chẩn đoán dùng danh mục này) chưa xây.
  **Hạn chế đã biết, CHỦ ĐỘNG để nguyên theo yêu cầu chủ dự án**: tìm nhanh dùng `contains` trên `search_key` đã bỏ dấu — với từ khoá ngắn (2-3 ký tự) có thể khớp nhầm chuỗi con không liên quan về nghĩa (ví dụ gõ "tả" → "ta" khớp cả vào "lao ở t**a**i"). Không phải vấn đề tốc độ (đã có GIN trigram index, 15.844 dòng vẫn rất nhỏ với Postgres) mà là vấn đề mức độ liên quan ngữ nghĩa của kết quả — sửa được bằng ràng buộc độ dài từ khoá tối thiểu, ưu tiên khớp đầu từ, hoặc dùng `similarity()`/ranking của `pg_trgm` thay vì `contains` thuần. Chưa chặn gì (đây chỉ là trang tra cứu tham khảo, chưa phải bộ chọn chẩn đoán chính thức của S3-06/07) — cân nhắc sửa khi làm bộ chọn chẩn đoán thật.

**ADM-07 (Vai trò tuỳ biến + ma trận phân quyền) — đã xong, ngoài kế hoạch Sprint 3 (`docs/DECISIONS.md` #057).** Chủ dự án yêu cầu làm trước trong lúc chờ mở khoá phần "Khám bệnh". Mockup Artifact duyệt qua 2 vòng (bố cục CRUD Xem/Thêm/Sửa/Xoá/Đặc quyền mở rộng theo ảnh tham khảo) trước khi code. Trang "Vai trò & Phân quyền" mới trong "Danh mục Tổ chức và Nhân sự". Kéo theo ripple bắt buộc: `user_account` đổi gán vai trò từ `roleNames` (enum 5 tên cố định) sang `roleIds` (uuid thật) để vai trò tuỳ biến gán được cho tài khoản. Xem chi tiết đầy đủ ở mục "Đã có" bên dưới.

**S3-05 (API màn hình khám) — đã xong (`docs/DECISIONS.md` #059), phần "Khám bệnh" MỞ KHOÁ MỘT PHẦN.** Bảng `diagnosis`/`clinical_note` hiện thực đúng khuôn đã đặc tả sẵn từ trước (migration `20260820120000_clinical_diagnosis_note`, không đổi cột nào so với `docs/ERD.md`). 4 endpoint mới trên `apps/api/src/modules/encounter/`: `GET /encounters/:id/consultation` (gộp tiền sử + dị ứng + sinh hiệu + chẩn đoán + SOAP trong một request, đúng yêu cầu S3-05), `PUT .../diagnoses` (thay toàn bộ danh sách, ép đúng 1 `PRIMARY`), `PUT .../clinical-note` (lưu cả 4 mục SOAP một lần, bản nháp — ký/đính chính để dành Sprint 5), `POST .../complete` (`IN_CONSULTATION→COMPLETED`, chỉ cần đúng 1 chẩn đoán chính, **không** đợi Kê đơn Sprint 4 — đã hỏi và chốt với chủ dự án). Không thêm permission mới (RBAC cho `diagnosis`/`clinical_note` đã seed sẵn từ S1-04b, tự vá qua `syncRolePermissionsForAllTenants()`). **Đã xác minh thật**: `encounter-http.spec.ts` thêm 17 test (27/27 tổng file) — tổng 333/333 test `apps/api` pass (1 flake tiền-nhiệm không liên quan ở `clinic-profile-http.spec.ts`, xác nhận qua chạy lại riêng, không phải regression). `pnpm -w typecheck` sạch toàn workspace. OpenAPI + web codegen đã sinh lại.
  **Phần web (S3-06/07) — ĐÃ XONG (2026-08-20, 2 phiên liên tiếp).** Phiên 1: code theo đúng bố cục mockup đã chốt ở #059 (trang cuộn dọc 1 khung, 4 tab neo bấm-để-cuộn-tới, tab 2-4 "Sắp ra mắt"), banner bệnh nhân tách 2 tầng + sinh hiệu neo phải (sửa vỡ dòng 1366px), sidebar tự thu gọn khi vào màn khám (`shared/layout/sidebar.context.tsx` mới), panel tiền sử đổi thẻ theo mockup gốc. Phiên 2 (chủ dự án dùng thử, yêu cầu đổi trực tiếp — **thay thế quyết định "SOAP 4 mục" ở #059**, xem `docs/DECISIONS.md` #060): **`clinical_note.section` đổi từ 4 mục SOAP sang 8 mục nhóm "Tiền sử" (bản thân/gia đình/dị ứng) và "Thăm khám" (lý do khám*/quá trình bệnh lý/chuẩn đoán*/khám toàn thân/khám bộ phận, *bắt buộc)** — migration `20260820150000_clinical_note_sections_v2`, gộp lại thành 1 khung "THÔNG TIN KHÁM LÂM SÀNG" duy nhất (ô nhập rút gọn, lưới 3 cột). "Tiền sử dị ứng" ghi thẳng `patient.allergyNote` qua `PATCH /patients/:id` (ripple RBAC: thêm `doctor.patient.update=global`). Sinh hiệu bổ sung/đo lại được ngay trong màn khám (`VitalSignsDialog.tsx` mới, nới guard `recordVitalSigns` sang cả `IN_CONSULTATION` — bug thật phát hiện khi test). Ngưỡng cảnh báo nhiệt độ 39.0°C→38.5°C, thêm phân loại BMI theo WHO Châu Á - TBD (emerald/amber/rose). Tab 1 đổi tên "Khám & Chẩn đoán". `apps/web/src/features/encounter/`, `shared/ui/Textarea.tsx` (prop `dense` mới). Chi tiết đầy đủ ở `docs/CHANGELOG.md` 2026-08-20 và `docs/DECISIONS.md` #059/#060.

**S3-09 (test + đo hiệu năng màn hình khám) — đã xong, Sprint 3 hoàn tất toàn bộ.** Bổ sung 3 test cách ly/scope còn thiếu cho 3/4 endpoint S3-05 (`.claude/docs/multi-tenancy.md`: mỗi endpoint chạm dữ liệu phải có test tenant A/B — `consultation`/`diagnoses`/`complete` đã có sẵn từ trước, `clinical-note` thì thiếu). `apps/api/scripts/perf-consultation.ts` (mới, thủ công không chạy CI, cùng khuôn `perf-patient-search.ts` S2-02) đo đúng 4 truy vấn tuần tự của `getConsultationDetail()` qua role app + RLS thật với bệnh nhân có 20 lượt khám cũ (PRD mục 5). **Đã xác minh thật**: 338/338 test `apps/api` pass (30/30 `encounter-http.spec.ts`), `pnpm -w typecheck/build` sạch (lint chỉ còn 3 lỗi cấu hình ESLint plugin có sẵn từ trước, không liên quan). Đo thật trên dev DB: tổng 33ms — ĐẠT xa ngưỡng "dưới 2 giây" (ENC-01, gate cuối Sprint 3).

**Danh mục "Nghề nghiệp" (ngoài kế hoạch, `docs/DECISIONS.md` #061)** — chủ dự án yêu cầu trực tiếp: thêm category `OCCUPATION` vào `reference_catalog` (migration `20260820160000_reference_catalog_occupation`, không seed cứng — `clinic_admin` tự thêm qua UI, đúng khuôn `PATIENT_SOURCE`/`EXAM_TYPE`), pill "Nghề nghiệp" mới ở trang "Danh mục hành chính". Đảo ngược tiếp phần `occupation` của #034. `patient.occupation` đổi từ input text tự do sang `Combobox` load từ danh mục này ở `PatientFormFields.tsx` (dùng chung cho cả Thêm/Sửa bệnh nhân lẫn Tiếp nhận bệnh nhân). **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch, 338 test `apps/api` không regress, Playwright qua Chrome thật xác nhận CRUD danh mục + Combobox load/chọn đúng ở cả 2 form.

**Màn hình khám — chỉnh dải sinh hiệu + đảo ngược lần 2 quy tắc nhãn trường toàn app (`docs/DECISIONS.md` #062)** — chuỗi phản hồi trực tiếp trên bản chạy thật: dải sinh hiệu có đường kẻ phân cách + chấm trạng thái (khôi phục từ #060) + nền đậm hơn; panel "Tiền sử & lịch sử khám" hẹp `360px`→`280px`; sửa 2 đường viền header lệch nhau; "Ghi chú dị ứng"→"Tiền sử dị ứng". **Nhãn trường (`label`) toàn app đổi từ nhạt/nhỏ (`text-xs font-medium text-slate-500`, chốt 2026-08-14) sang đậm/to (`text-sm font-semibold text-slate-800`, không còn ngoại lệ "dense")** — quy tắc mới ghi vào `.claude/docs/ui-guidelines.md` mục 4.1c, bắt buộc cho form phát sinh sau này. **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch, 338 test `apps/api` không regress (1 flake môi trường không liên quan ở `appointment-http.spec.ts`, xem chi tiết #062), Playwright qua Chrome thật.

**Mở rộng ADM-01 — "Danh mục quản lý tài khoản" đầy đủ hồ sơ nhân sự (`docs/DECISIONS.md` #063, ngoài kế hoạch, chủ dự án yêu cầu trực tiếp).** `user_account` thêm 10 cột (mã nhân viên tự sinh, SĐT/email cá nhân+công ty, học vị/chức danh/trạng thái+hình thức làm việc — 4 mã tham chiếu `reference_catalog` mới, được ký HSBA chỉ metadata, bắt buộc đổi mật khẩu lần đầu — **enforce thật**, chặn điều hướng tới khi đổi xong qua `RequireAuth.tsx`). `reference_catalog` thêm 4 category (`ACADEMIC_TITLE`/`STAFF_POSITION`/`EMPLOYMENT_STATUS`/`EMPLOYMENT_TYPE`) + cột `deactivates_account` (chỉ có ý nghĩa với `EMPLOYMENT_STATUS` — tách khỏi `code` vì `code` sửa được qua UI, so khớp chuỗi sẽ hỏng ngầm) — chọn "Nghỉ việc" tự động `is_active=false` cho tài khoản. 4 category nhân sự này + Khoa/Phòng đều **tự sinh mã**, không nhập tay (phản hồi giữa chừng của chủ dự án). Module `department`/`department_type` (mới, `department` có bảng từ S1-04b nhưng chưa từng có UI) — Khoa/Phòng phân loại 2 cấp qua "Loại Khoa/Phòng" (tùy chọn, THUẦN tổ chức), UI 2 cột tham khảo "Tầng phòng" (#055). Field "Vai trò" ở form tài khoản đổi giao diện sang `MultiSelectCombobox` nhưng vẫn gửi `roleIds` thật — **đã kiểm chứng RBAC hoạt động đúng bằng Playwright** (không chỉ suy luận từ code): tài khoản chỉ gán vai trò "Điều dưỡng" → đăng nhập → gọi thẳng API cần quyền quản trị → `403` thật từ backend, không phải do frontend ẩn giúp. Rà soát mã hoá mật khẩu toàn hệ thống theo yêu cầu chủ dự án: đã dùng Argon2id từ trước, đạt chuẩn OWASP, không cần sửa. **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch, 352 test `apps/api` (351 pass ổn định, 1 flake tiền-nhiệm không liên quan), Playwright qua Chrome thật xác nhận toàn bộ luồng (tạo tài khoản → bắt đổi mật khẩu lần đầu → tự động vô hiệu hoá khi "Nghỉ việc" → CRUD danh mục/Khoa-Phòng → RBAC thật).

**"Hàng đợi khám" — mô hình "Hàng đợi ảo" theo Bác sĩ/Khoa — ĐÃ CODE XONG (`docs/DECISIONS.md` #064, ngoài kế hoạch, kiến trúc đã chốt qua mockup từ phiên trước).** Hàng đợi không phải bảng riêng — chỉ là filter trên `encounter`: `doctorId` đổi nullable + thêm `departmentId` bắt buộc (migration `20260820210000_encounter_virtual_queue`, backfill dữ liệu cũ, mỗi tenant tự có 1 "Khoa chung" mặc định seed lúc tạo tenant). "Bệnh nhân của tôi" = `doctorId=actor`, "Hàng chờ chung · Khoa X" = `departmentId=Khoa actor AND doctorId IS NULL` (cờ `includeDepartmentPool` tường minh, không đổi hành vi "Danh sách tiếp nhận" mặc định). "Nhận ca" (mở rộng `POST /encounters/:id/start`) set `doctorId` lúc pull atomic, chống trùng chỉ fallback (ghi có điều kiện + lỗi `ENCOUNTER_ALREADY_CLAIMED` riêng cho UI hiện toast lịch sự) — không WebSocket. Web: `ReceptionDoctorQueuePage.tsx` viết lại thành board 3 cột (Đang chờ/Đang khám/Đã khám hôm nay); `ReceptionIntakeForm.tsx` thêm khu vực Điều phối (toggle "Theo bác sĩ cụ thể"/"Theo Khoa", tự ẩn khi tenant chỉ 1 Khoa); `EncounterConsultationPage.tsx` thêm dải hàng chờ thu gọn (ẩn/hiện được) cạnh Phiếu khám.
2 bug thật phát hiện lúc code, không thuộc phạm vi #064 nhưng sửa cùng lúc vì chạm đúng chỗ: (1) check-in từ lịch hẹn có sẵn trước đây không lưu 7 trường "Thông tin tiếp nhận"/"Chỉ định dịch vụ khám" dù Tiếp nhận trực tiếp đã lưu đúng từ #052; (2) `ReceptionIntakeForm`/`ReceptionDoctorQueuePage` ban đầu dùng API tra Khoa yêu cầu `user_account.read` (chỉ admin có) — lễ tân/bác sĩ thật sẽ bị 403, chỉ lộ ra khi kiểm bằng Playwright với đúng vai trò (không lộ khi test bằng tài khoản admin) — sửa bằng endpoint tự-phục vụ mới `GET /departments/options` (gắn `reference_catalog.read`, đúng khuôn `GET /appointments/doctors` #030).
**Đã xác minh thật**: 368/368 test `apps/api` (28 file, +16 test mới cho "Nhận ca"/routing/pool), `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright qua Chrome thật (tenant/tài khoản dev tạo riêng cho lần kiểm — 2 bác sĩ cùng Khoa + 1 Khoa khác): tiếp nhận theo Khoa (không chọn bác sĩ) → bác sĩ cùng Khoa thấy đúng ticket trong "Hàng chờ chung" → "Gọi khám — nhận ca này" → chuyển đúng `IN_CONSULTATION`, điều hướng sang màn khám, dải hàng chờ ẩn/hiện đúng → bác sĩ thứ hai không còn thấy ticket đã bị nhận. Không lỗi console ngoài dự kiến.

**"Hàng đợi khám" + "Chuyển vào hàng đợi" — chuỗi chỉnh sửa trực tiếp trên bản chạy thật (2026-08-21, ngoài kế hoạch, phản hồi liên tục của chủ dự án khi dùng thử #064), xem `docs/DECISIONS.md` #065.** Tóm tắt: (1) cột "Mã" hiện lại ở 4 danh mục nhân sự (bảng, không phải ô nhập) và cột "Người tiếp nhận" mới ở Danh sách tiếp nhận (`encounter.createdBy` resolve tên qua `DoctorDirectoryPort.getUserFullNames` mới); (2) khu vực "Điều phối" ở Tiếp nhận làm lại đúng mockup đã duyệt trước đó nhưng chưa từng code đúng — tách tab "Bác sĩ"/"Khoa" (ẩn khi 1 Khoa), lưới nhiều cột thay danh sách dọc, số "Đang chờ" thời gian thực theo bác sĩ/Khoa, "N bác sĩ đang trực" = đã chọn phòng làm việc hôm nay (fallback "thuộc Khoa" khi tenant 1 phòng); (3) "Chọn phòng làm việc hôm nay" cho "Bỏ qua" lúc đăng nhập (lưu `sessionStorage` theo ngày, F5 không hiện lại) nhưng bắt chọn lại bắt buộc khi vào "Hàng đợi khám"; (4) card 3 cột hàng đợi làm lại toàn bộ theo đúng mockup — sửa 1 bug thật (cảnh báo "chờ lâu" trước đây chỉ áp cho hàng chờ chung, bỏ sót ca "của tôi"), thêm badge số phút đang khám, nhóm "Của tôi"/"Hàng chờ chung" tách màu rõ, tìm kiếm riêng từng cột (client-side, không bỏ dấu — xem lý do kỹ thuật ở #065). **Đã xác minh thật**: 369/369 test `apps/api`, `pnpm -w typecheck/lint/build` sạch, Playwright qua Chrome thật với dữ liệu demo tạo cho BS. hungdt (ưu tiên/chờ lâu/hàng chờ chung/đang khám/đã khám).

**Màn hình khám — sửa 3 lỗi vận hành thật chủ dự án báo cáo (2026-08-21), xem `docs/DECISIONS.md` #066.** (1) "Xem lại" một lượt khám đã "Hoàn tất khám" trước đây vẫn hiện y hệt giao diện đang khám — nay chỉ hiện nút "Chỉnh sửa thông tin" (sửa được: đúng bác sĩ ca đó, hoặc tài khoản khác qua **break-glass** — lần đầu web thật sự gọi tới cơ chế này, thêm `BreakGlassDialog.tsx` dùng chung), backend mở khoá sửa tại chỗ sau hoàn tất kèm ghi `audit_log` action riêng có trước/sau nội dung (không dựng mô hình đính chính Thông tư 46 — để dành Sprint 5). (2) Ghi chú lâm sàng/dị ứng chưa bấm "Lưu" mà chuyển ca khám khác thì mất — nay **autosave debounce ~4 giây + flush khi rời trang**. Phát hiện thêm 1 bug nặng hơn lúc sửa: bấm từ dải "Hàng chờ" sang ca khác (không unmount trang) trước đây không nạp lại dữ liệu bệnh nhân mới, lẫn dữ liệu 2 bệnh nhân — đã sửa (`loadedForId` thay `initialized` boolean). (3) "Hoàn tất khám" nay **bắt buộc lưu xong ghi chú trước khi cho hoàn tất**, không còn đường nào đóng dấu "xong" mà bỏ sót nội dung chưa lưu. Nhân tiện: nút "Lưu nháp" → "Lưu"; thông báo lỗi footer làm nổi bật (khung viền đỏ + icon, vẫn giữ inline tại chỗ — không đổi sang Toast, đúng `.claude/docs/ui-guidelines.md` mục 4.3). **Đã xác minh thật**: `encounter-http.spec.ts` +5 test (374/374 tổng `apps/api`, không regression) — vá thêm 1 lỗ hổng `tenant-fixture.ts` chưa từng dọn `break_glass_session`. `pnpm -w typecheck/lint/build` sạch. Playwright qua Chrome thật: cả 3 kịch bản + luồng break-glass trọn vẹn (điều dưỡng bị chặn → dialog → sai mật khẩu báo lỗi tại chỗ → đúng mật khẩu → lưu thành công).

**Cùng phiên #066 — popup xác nhận, chuẩn Enter-to-submit toàn app, panel tiền sử thêm tên bác sĩ (`docs/DECISIONS.md` #067).** (1) "Hoàn tất khám" giờ hiện popup "Thành công" (cùng khuôn "Tiếp nhận thành công"), Enter cũng xác nhận được. (2) Phát hiện lỗ hổng **toàn app**: mọi form "Thêm mới" ở trang danh mục không bấm Enter lưu được (nút "Lưu" không nằm trong `<form>`) — đã sửa 8 modal/6 file, ghi thành **quy tắc bắt buộc mới** ở `.claude/docs/ui-guidelines.md` mục 4.4 (trừ modal Confirm xoá/nguy hiểm — không áp dụng). Còn 4 form lớn chưa áp dụng (cần audit riêng do có picker lồng nhau): `ReceptionIntakeForm.tsx`, `AppointmentQuickCreatePanel.tsx`, `AppointmentDetailPanel.tsx`, `UserAccountPane.tsx`. (3) Panel "Tiền sử & lịch sử khám" thêm tên bác sĩ (`EncounterHistoryItem.doctorName`, đổi contract), thẻ "Lần khám gần nhất"/"Lịch sử cũ hơn" làm nổi bật/đồng nhất hơn — **vẫn chỉ hiện chẩn đoán chính** (đã hỏi, không mở rộng ra toàn bộ chẩn đoán như ảnh mẫu chủ dự án gửi); "Đơn cũ"/"Xét nghiệm" trong ảnh mẫu KHÔNG làm được — chờ module Kê đơn (Sprint 4) + Cận lâm sàng (ngoài phạm vi v1). **Đã xác minh thật**: 374/374 test `apps/api`, `pnpm -w typecheck/lint/build` sạch, Playwright qua Chrome thật.

**Màn hình khám — sửa lỗ hổng "Tiền sử bản thân"/"Tiền sử gia đình" phải nhập lại mỗi lượt khám (chủ dự án phát hiện, `docs/DECISIONS.md` #068).** 2 trường này trước đây gắn `clinical_note.section` theo TỪNG lượt khám (như 4 mục "Thăm khám" khác) nên lượt khám mới luôn rỗng — bất tiện vì bản chất là dữ liệu chung, ít đổi giữa các lần khám, giống hệt "Tiền sử dị ứng" (đã tách khỏi `clinical_note` từ #060). Đã hỏi và chốt qua `AskUserQuestion`: chuyển hẳn thành `patient.personalHistory`/`familyHistory` (đúng khuôn `allergyNote`) thay vì chỉ tự mồi mà vẫn lưu riêng theo lượt khám. `clinical_note.section` còn lại 6 giá trị (bỏ `PERSONAL_HISTORY`/`FAMILY_HISTORY`). Migration `20260821150000_patient_history_fields` backfill nội dung không rỗng gần nhất của mỗi bệnh nhân trước khi xoá 2 section cũ. Web: `EncounterConsultationPage.tsx` gộp cơ chế autosave-vào-patient của "Tiền sử dị ứng" (`allergyDraft`) thành `patientNoteDraft` dùng chung cho cả 3 trường; `PatientFormFields.tsx` (dùng chung Thêm/Sửa bệnh nhân + Tiếp nhận bệnh nhân) thêm 2 trường này cạnh "Tiền sử dị ứng". **Đã xác minh thật**: `encounter-http.spec.ts` +1 test (375/375 tổng `apps/api`, không regression), `pnpm -w typecheck/lint/build` sạch toàn workspace, Playwright qua Chrome thật (tenant/tài khoản dev riêng): nhập tiền sử ở lượt khám 1 → hoàn tất → lượt khám mới cùng bệnh nhân hiện sẵn đúng nội dung cũ, sửa/lưu lại vẫn đúng.

**Lộ trình đa chuyên khoa — đã chốt xong hướng, chưa bắt đầu code (`docs/DECISIONS.md` #069/#070, 2026-08-22).** Chủ dự án hỏi kỹ về mô hình "Bệnh án/Đợt điều trị" 3 cấp — xác nhận không phải quy định pháp lý bắt buộc (chỉ thông lệ ngành), không áp dụng cho khám tổng quát v1 hiện tại. Đã có 2 khách hàng thật: **Nhi khoa** (chỉ cần thêm trường nhập liệu — cân nặng/vòng đầu lúc sinh, cách tính tuổi theo tháng/tuần — không cần tầng cha, ~15-20 dev-day) và **Sản phụ khoa** (cần tầng cha `pregnancy` thật sự đứng trên nhiều `encounter`, ~30-40 dev-day — đây chính là trường hợp cụ thể đầu tiên của khái niệm "tầng cha trên encounter" mà chủ dự án hỏi, chỉ đúng với sản khoa). Cả hai không bị chặn bởi Viện phí (chỉ nha khoa bị chặn). **Đã chốt hết mọi điểm treo**: thứ tự Nhi trước/Sản sau (không ràng buộc gì đổi ngược); Sprint 4 (Kê đơn) + pilot v1 nội khoa **vẫn đi trước**, chưa chuyển sang code chuyên khoa ngay; bác sĩ chuyên khoa đã thẩm định xong mẫu bệnh án + luồng dữ liệu cho cả 2 gói; cơ chế kích hoạt gói chốt dùng script/migration thuần (`tenant.enabled_specialties`, không UI, không license/DRM — khớp ràng buộc on-premise). **Việc kế tiếp thực tế vẫn là Sprint 4** — quay lại lập kế hoạch chi tiết (EnterPlanMode) cho gói Nhi khoa sau khi Sprint 4 + pilot xong, không phải bây giờ.

**Rà soát lại kế hoạch chuyên khoa — phát hiện lỗ hổng "Thu ngân cơ bản" thiếu ở CHÍNH v1, không riêng chuyên khoa (`docs/DECISIONS.md` #071, 2026-08-22).** Chủ dự án phản biện đúng: kết luận "chỉ nha khoa bị chặn bởi Viện phí" (ghi ở #070) chỉ đúng với **thanh toán phức tạp theo lộ trình** (mức 2 — báo giá/trả góp/công nợ, đặc thù nha khoa), **bỏ sót** rằng mọi phòng khám kể cả nội/nhi đều cần **thu ngân cơ bản** (mức 1 — phiếu thu mỗi lượt khám, in phiếu, đánh dấu đã thu, tổng kết cuối ngày). Đã xác minh schema: `encounter` ĐÃ lưu sẵn `exam_type_price`/`service_quantity`/`exam_type_unit` (snapshot từ #052) nhưng **không có bảng `invoice`/`payment` nào** — tức đang "thu thập giá nhưng không dùng được", lễ tân vẫn phải tính tiền/ghi sổ ngoài phần mềm. **Rủi ro với pilot v1 nội khoa** (không chờ tới chuyên khoa mới lộ): ảnh hưởng trực tiếp 2 chỉ số PRD mục 5 ("lượt khám nhập trong ngày >95%", "ca quay lại dùng giấy <2/tuần"). Rà soát cũng phát hiện thêm: **gating gói không hoãn được tới gói #2** (sửa #070 — phải làm cùng gói Nhi), **chưa có luồng nào SET `encounter.specialty`** dù #069 đã quyết chọn mẫu in theo field này, **PRE-06 (liều theo cân nặng) đang P2 nhưng là lõi nhi khoa mà Sprint 4 sắp tạo bảng `prescription_item`**, và chi phí retrofit `ReceptionIntakeForm.tsx` chưa tính vào ước lượng. **Cả 3 điểm đã chốt ngay trong phiên (`docs/DECISIONS.md` #072)**: (1) **"Thu ngân cơ bản" ĐƯA VÀO v1** — làm ở **Sprint 5/6** (sau pilot, trước GA), vì điều kiện GA "pilot ngừng dùng sổ giấy hoàn toàn" không thể đạt nếu thiếu thu ngân. Phạm vi giới hạn: 1 phiếu thu/lượt khám (tính từ giá đã snapshot sẵn), in phiếu, đã thu/chưa thu + phương thức, tổng kết cuối ngày — KHÔNG có bảng giá đa đối tượng/công nợ/trả góp/BHYT/báo cáo doanh thu. (2) **PRE-06 giữ P2 ở v1**, thành hạng mục bắt buộc của gói Nhi khoa — Sprint 4 không thêm gì, chấp nhận sau này thêm cột liều có cấu trúc (`dose` hiện là text tự do). (3) **Thêm phase "v1.5 — Gói chuyên khoa"** vào PRD Appendix A, giữa v1.1 và v2. **Đã cập nhật `CLAUDE.md` (dòng phạm vi v1), `docs/product/prd.md` (v1.2 — mục 4.7 BIL-01→04 + Appendix A + mốc tuần 9-10), `docs/product/plan.md` (S5-08 + gate GA), `docs/ERD.md` (mục 6, mục 7)** — chưa code/schema gì, chưa tới Sprint 5.

**CHỐT hướng đa chi nhánh: `tenant` = CÔNG TY, chi nhánh là `branch` bên trong (`docs/DECISIONS.md` #075, 2026-08-25).** Ngay sau #074, chủ dự án hỏi "1 tenant là 1 cơ sở, trong đó có chi nhánh A và B — sau này sửa được không?" rồi quyết định **chốt luôn hướng bây giờ** thay vì để treo như PRD Q6. Chốt qua `AskUserQuestion`: (1) **mô hình B** — `tenant` = công ty, `branch` bên trong (thay hướng ngầm định cũ "mỗi chi nhánh 1 tenant"; #074 vẫn đúng vì mô tả HIỆN TRẠNG v1, #075 chốt HƯỚNG MỞ RỘNG); (2) **bệnh nhân + mã dùng chung toàn công ty**, một `patient_code` duy nhất, khám chi nhánh nào cũng thấy đủ tiền sử; (3) **chốt hướng nay, CHƯA code** — chỉ làm khi có khách chuỗi thật. **Lý do chốt sớm**: chốt trước khi bán thì chỉ là thêm tầng vào schema; chốt sau khi khách đã chạy nhiều cài đặt riêng thì phải **merge nhiều database**, xử lý `patient_code` trùng (sequence theo tenant) + rủi ro nhầm bệnh nhân. **Ràng buộc bắt buộc cho code từ nay**: cấm giả định "1 tenant = 1 địa điểm vật lý"; `code_sequence` **giữ nguyên** theo `(tenant_id, prefix)` (đã đúng hướng, không sửa); `patient` **không** cần `branch_id`, `encounter`/`appointment`/`invoice` thì có; scope `branch` chèn giữa `department` và `global`. **Ràng buộc hạ tầng**: khách chuỗi **buộc phải dùng hạ tầng tập trung** (một database chung) — mỗi chi nhánh một máy riêng thì không thể gộp chung tenant. Trả lời một phần PRD Q6. Không đổi code/schema.

**Làm rõ 3 khái niệm triển khai, ghi thành `docs/Deploy.md` Phần 0 (`docs/DECISIONS.md` #074, 2026-08-25).** Chủ dự án hỏi và nêu thẳng điểm mơ hồ *"tenant được định nghĩa là chi nhánh hay 1 cơ sở/công ty"* — rà lại thì cả 3 câu chưa từng có ở tài liệu nào. Đã chốt và ghi: (1) **`tenant` = một CƠ SỞ/CHI NHÁNH, không phải công ty** — **không tồn tại tầng "công ty" trên `tenant`**, công ty 3 chi nhánh = 3 tenant tách biệt (bệnh nhân/tài khoản/danh mục riêng, không báo cáo tổng hợp) — giới hạn có chủ ý của v1, khớp PRD Q6 còn treo + scope `branch` chưa triển khai; đã để sẵn `global_patient_ref`/`PatientIdentityPort` để mở rộng không phải làm lại. (2) **Ranh giới quyết định cách viết code là single-tenant vs multi-tenant, KHÔNG phải on-prem vs cloud** — máy tại phòng khám/NAS/VPS đều single-tenant, **không cần đổi dòng code nào**, mọi quyết định on-prem (gồm #072 bật gói bằng script) vẫn đúng; chỉ SaaS multi-tenant mới đổi bản chất và là nơi tập trung rủi ro cache rò xuyên tenant. NAS/VPS khác ở **vận hành** (VPS bắt buộc TLS; NAS cần Docker + RAM ≥4 GB, **chưa từng kiểm chứng thật trên NAS**). (3) **Cấu hình trong DB bền vững qua restart** — script bật gói chạy MỘT lần là xong, chỉ phải chạy lại nếu khôi phục backup cũ hơn hoặc xoá volume (`down -v`). Cũng vá luôn chỗ gốc gây mơ hồ ở `.claude/docs/multi-tenancy.md`. Không đổi code/schema. **Lưu ý đọc cùng #075 (mục ngay trên)**: #074 mô tả HIỆN TRẠNG v1 (chưa có khái niệm chi nhánh), #075 chốt HƯỚNG MỞ RỘNG (`tenant` sẽ = công ty chứa nhiều `branch`) — khác thời điểm, không mâu thuẫn.

**Hiệu suất web — code-splitting theo route + cưỡng chế ranh giới `web ⇸ core` (`docs/DECISIONS.md` #073, 2026-08-22).** Trả lời câu hỏi "đa chuyên khoa có gây nặng không", đo lại thực tế phát hiện giả định "lazy load theo route" trong `multi-specialty-analysis.md` **chưa từng được hiện thực**: 0 lazy load, build ra **802 kB trong 1 chunk duy nhất**. Đã sửa: 11 trang nghiệp vụ chuyển sang `lazy()` + `<Suspense>` bọc riêng vùng nội dung trong `AppShell` (`shared/ui/PageFallback.tsx` mới) → chunk khởi động **802 → 440 kB** (gzip 213 → 132), 1 → 27 chunk, hết cảnh báo Vite. Thêm ESLint chặn `apps/web` import `@nexamed/core` (trước chỉ là quy ước ngầm, trong khi `core` nặng 9.6 MB — riêng `icd10/data.ts` 9.1 MB). **Ghi quy tắc hiệu suất bắt buộc vào `.claude/docs/coding-standards.md` mục "Hiệu suất"** (chunk ≤500 kB, trang mới bắt buộc lazy, cấm `web→core`, truy vấn danh sách phải có index, tránh N+1) — theo yêu cầu thường trực của chủ dự án "luôn đảm bảo hiệu suất". Đã xác minh: typecheck sạch, ESLint rule kiểm bằng file thử vi phạm, Playwright qua Chrome thật đủ 8 route lazy. Không đổi backend/schema.

**Rà soát toàn app: tiêu đề cột bảng danh sách không cố định khi cuộn — đã sửa xong 7 bảng (2026-08-21).** Chủ dự án yêu cầu quét toàn bộ danh sách dữ liệu. Chỉ `AllergenPane.tsx`/`AppointmentGridView.tsx` làm đúng từ trước; sửa `GeoPane.tsx`, `Icd10Pane.tsx`, `RoleMatrixTable.tsx`, `UserAccountPane.tsx`, `ReferenceCatalogPane.tsx`, `RoomPane.tsx`, `DepartmentPane.tsx` theo đúng mẫu `AllergenPane.tsx` (`min-h-0 flex-1 overflow-hidden` → `overflow-y-auto` → `<thead className="sticky top-0 z-10">`). Đã ghi quy tắc bắt buộc vào `.claude/docs/ui-guidelines.md` mục 9g để không phải nhắc lại. Không đổi backend/schema. Đã xác minh thật qua Playwright (Chrome thật, `getComputedStyle` xác nhận sticky + ảnh chụp lúc cuộn sâu), `pnpm -w typecheck` sạch.

**Sprint 4 (Kê đơn) — S4-01→04 đã xong, Sprint 4 mở khoá một phần (2026-08-25).** Trước khi bắt đầu, chủ dự án đề xuất tách "Đơn thuốc y khoa"/"Hoá đơn bán hàng" kiểu 2-Tab thuốc nội bộ/thuốc ngoài + luồng kho/thu ngân — đối chiếu `CLAUDE.md` thì vượt phạm vi v1, đã hỏi qua `AskUserQuestion` và chốt **giữ đúng phạm vi v1** (ghi nhận + in, không kho); toàn bộ đề xuất lưu vào `docs/product/future-modules-reference.md` §2.2.1 cho v2.1. Module `prescription` mới (bảng `drug` theo tenant/`prescription`/`prescription_item`, migration `20260825090000_prescription`) — `prescription` là `SignableEntity` ĐẦU TIÊN thật sự dùng logic ký, và **C8 (chặn sửa nội dung đã ký) lần đầu hiện thực bằng DB trigger thật** trong dự án (`clinical_note` có cột sẵn nhưng chưa từng có trigger). Đơn nháp sửa tự do (bulk-replace, đúng khuôn `diagnosis`); ký xong bất biến, sửa = đính chính (`POST .../prescription/amend`, tạo bản mới ĐÃ KÝ NGAY). PRE-02 (trùng hoạt chất)/PRE-03 (dị ứng) — hàm thuần trong `packages/core`, CHỈ cảnh báo mềm, không chặn ký (đã hỏi và chốt: không có dữ liệu chống chỉ định ở v1). **Phát hiện qua rà soát**: đã có sẵn danh mục "Dị nguyên" (#069) nhưng chưa từng liên kết bệnh nhân — thêm bảng nối `patient_allergen` (migration `20260825100000_patient_allergen`) thay vì chỉ so khớp `allergyNote` tự do; `allergyNote` giữ nguyên làm ghi chú bổ sung. Trang "Danh mục thuốc" mới (`/admin/catalog-pharmacy`, đổi nhãn từ "Danh mục Dược và Vật tư"). Hạ tầng in ấn dùng chung lần đầu (`@media print`, `apps/web/src/app/index.css`) — mẫu in **chưa chính thức** (plan.md T5 còn treo), dùng bố cục chuẩn tạm. **Đã xác minh thật**: `packages/core` `warnings.spec.ts` (10/10, tổng 99 test), `apps/api` `drug-http.spec.ts` (8/8) + `prescription-http.spec.ts` (8/8, gồm test trigger C8 chặn `UPDATE` thẳng qua SQL) + `patient-http.spec.ts` +4 test `allergenIds` — tổng **406/406 test `apps/api` pass**. Vá `tenant-fixture.ts` (cleanup 4 bảng mới đúng thứ tự FK — `prescription.supersedesId` không null-hoá-rồi-xoá được như `patient.mergedIntoId` vì trigger C8 chặn cả sửa cột này trên bản đã ký, phải xoá theo tầng). `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk khởi động web không đổi (440 kB). Playwright qua Chrome thật (dữ liệu tạo qua HTTP API riêng cho lần kiểm): CRUD Danh mục thuốc → gán dị nguyên cho bệnh nhân → kê đơn 2 thuốc trùng hoạt chất + 1 thuốc trùng dị nguyên → cả 2 cảnh báo hiện đúng → ký đơn (form khoá lại) → in đơn → "Sửa đơn" tạo bản đính chính đúng. **Còn treo**: S4-05→08 (đóng gói on-prem, dữ liệu pilot, cài đặt, đệm lỗi) — việc vận hành riêng, chưa làm. Chi tiết đầy đủ ở `docs/TASK.md`.

**Tiền sử bản thân/gia đình chuyển sang dữ liệu có cấu trúc + popup "Tìm kiếm khách hàng" ở Tiếp nhận (ngoài kế hoạch, 25/08/2026, chủ dự án yêu cầu trực tiếp).** Mockup Artifact (chip bệnh lý nền/thói quen, ma trận Tiền sử gia đình + cảnh báo nguy cơ chung chung không số %, dị ứng có lọc tìm + thêm nhanh) duyệt qua 2 vòng chỉnh sửa trước khi code — dùng `EnterPlanMode` + 5 lượt `AskUserQuestion` chốt cấu trúc (quyền tạo dị nguyên mới cho lễ tân/điều dưỡng/bác sĩ, bỏ cột "Trạng thái" ở popup tìm kiếm vì `patient` không có khái niệm này, áp UI mới cho CẢ 3 nơi dùng `PatientFormFields` thay vì chỉ Tiếp nhận, banner cảnh báo không đưa số liệu %, và phát hiện giữa chừng — màn Khám bệnh cũng phải đổi ô "Tiền sử gia đình" sang chỉ xem). Bảng mới `patient_condition`/`patient_family_history` (migration `20260825110000_patient_history_structured`) — `patient.family_history`/`allergy_note` giữ nguyên trong DB nhưng ngừng đọc/ghi từ UI mới. Permission mới `allergen_catalog.create` (tách khỏi `manage`). `PatientHistoryDialog.tsx`/`PatientSearchDialog.tsx` mới; `Icd10DiagnosisPicker.tsx` chuyển thành `shared/ui/Icd10SearchPicker.tsx` (dùng lần 2 thì tách). **Bug thật phát hiện lúc chủ dự án tự chạy `pnpm dev`**: circular import giữa `PatientFormFields.tsx`/`PatientHistoryDialog.tsx`/`patient-form.utils.ts` gây `ReferenceError` (chỉ lộ ở Vite dev/ESM gốc, không lộ ở `vite build`/Rollup nên typecheck+build sạch mà không bắt được) — đã sửa bằng tách hằng số dùng chung ra module lá riêng (`family-relation.ts`). **Đã xác minh thật**: 405/412 test `apps/api` (7 fail là flake `geo-http.spec.ts` đã biết, pass 100% khi chạy riêng), `pnpm -w typecheck/lint/build` sạch (lint 5 lỗi cấu hình có sẵn, không liên quan), Playwright qua Chrome thật: tạo bệnh nhân với chip bệnh lý + 2 dòng gia đình cùng bệnh (banner cảnh báo đúng) + thêm dị nguyên mới ngay trong dialog → Lưu → F5 lại vẫn còn nguyên; Tiếp nhận bấm icon tìm kiếm → tìm SĐT → Chọn → form tự điền đúng. Chi tiết đầy đủ ở `docs/CHANGELOG.md` 2026-08-25 (3), `docs/ERD.md` v1.26.

**Redesign popup "Tìm kiếm khách hàng" + chip dị nguyên theo nhóm + vá 2 lỗ hổng thật (ngoài kế hoạch, 25/08/2026, chủ dự án phát hiện lúc dùng thử demo).** `PatientSearchDialog.tsx` rộng hơn, hiện sẵn tiêu đề cột + 5 khách hàng mới tạo gần đây (thêm tham số `sort` cho `GET /patients`); `PatientHistoryDialog.tsx` chip dị nguyên nhóm theo Nhóm, giới hạn 5 chip/nhóm. **Bug #1**: `patient_condition` (Bệnh lý nền) chưa từng được `EncounterService.getConsultationDetail()` trả về dù đã lưu ở hồ sơ — đã export `PatientConditionRepository`, thêm `conditions` vào `consultationPatientSchema`, hiện chip "Bệnh lý nền" ở màn khám. **Bug #2**: `allergyNote` (ghi chú dị ứng tự do) có 2 nơi chỉnh không nhất quán — #065 đã bỏ ô nhập khỏi `PatientHistoryDialog` (chuyển hẳn sang chip `allergenIds`) nhưng bỏ sót ô nhập/autosave riêng vẫn còn ở màn khám (từ #060) — đã bỏ hẳn khỏi màn khám (cột DB giữ nguyên, đúng cách đã làm với `family_history`), banner "DỊ NGUYÊN" đổi tên "CẢNH BÁO DỊ ỨNG" + đổi sang chip riêng từng dị nguyên. **Đã xác minh thật**: 412/412 test `apps/api`, `pnpm -w typecheck/lint/build` sạch, xác nhận qua HTTP API thật (dữ liệu demo 20 bệnh nhân seed qua API). Chi tiết đầy đủ ở `docs/CHANGELOG.md` 2026-08-25 (4).

**Redesign màn hình khám — bỏ dải "Khách đang chờ", panel Tiền sử đổi thành 2 tab, badge "Loại tiếp nhận", giới hạn banner dị ứng (ngoài kế hoạch, 25/08/2026, chủ dự án yêu cầu trực tiếp qua nhiều lượt chỉnh trên bản chạy thật).** Panel trái đổi từ cột cuộn chung sang 2 tab "Tiền sử bệnh"/"Lịch sử khám" (mặc định: bệnh nhân mới → Tiền sử bệnh, bệnh nhân cũ → Lịch sử khám), rộng 280px→384px. Tab "Tiền sử bệnh" có 3 khung mới (Dị ứng/Bản thân/Gia đình) với nút "+ Thêm" dùng lại `PatientHistoryDialog` có sẵn, lưu PATCH ngay (không đợi nút Lưu màn khám) — không thêm trường "Phẫu thuật" riêng, dùng lại `personalHistory`. Khung "Thông tin khám lâm sàng" không còn lặp lại Tiền sử (chỉ còn "Thăm khám"); sửa lỗi chính tả "Chuẩn đoán"→"Chẩn đoán". Banner "CẢNH BÁO DỊ ỨNG" giới hạn 2 chip + nút "+N" mở dropdown xem đủ (chưa có "mức độ nghiêm trọng" — hệ thống chưa lưu trường này). Thêm badge "Loại tiếp nhận" (Khám mới/Tái khám) cạnh SĐT — `encounterSummarySchema` thêm 2 trường additive `examTypeName`/`receptionTypeCode` (export thêm cột DB có sẵn từ #052, không migration). `ReceptionDoctorQueuePage.tsx`: tên bệnh nhân trên thẻ hàng đợi bớt đậm (`font-extrabold`→`font-bold`). **Đã xác minh thật**: `pnpm -w typecheck` sạch, `pnpm --filter @nexamed/web run build` sạch, Playwright qua Chrome thật (dữ liệu demo qua HTTP API — 1 bệnh nhân 2 lượt khám). `apps/api` 396/412 test pass (16 skip chủ đích, 3 file fail là flake `geo-http.spec.ts`/`auth-login-http.spec.ts` đã biết trước, không liên quan). Chi tiết đầy đủ ở `docs/CHANGELOG.md` 2026-08-25 (5), `docs/DECISIONS.md` #077.

**Pill "Loại khám" chuyển từ "Danh mục hành chính" sang "Danh mục Chuyên môn", đổi nhãn "Dịch vụ khám" (2026-08-26).** Chỉ đổi vị trí + nhãn hiển thị, không đổi dữ liệu/category (`EXAM_TYPE`). Chi tiết `docs/CHANGELOG.md` 2026-08-26.

**"Danh mục hành chính" đổi tên "Danh mục dùng chung" + category `UNIT` "Đơn vị tính" mới (2026-08-26, ngoài kế hoạch).** `reference_catalog` thêm category `UNIT` + cột `description` (chỉ UNIT dùng). Form Thêm/Sửa của UNIT có thêm Mô tả + Trạng thái (Đang/Ngưng sử dụng) ngay trong form — category khác không đổi hành vi. Tiện vá lỗ hổng `eslint-plugin-react-hooks` chưa từng được cài (lint giảm từ 5 lỗi → 0). Chi tiết `docs/CHANGELOG.md` 2026-08-26 (2).

**"Đơn giá dịch vụ" cho Dịch vụ khám (EXAM_TYPE) — MỞ RỘNG phạm vi v1 có giới hạn (`docs/DECISIONS.md` #079, chủ dự án yêu cầu trực tiếp, chốt qua `AskUserQuestion` vì xung đột với "Price Book" từng loại khỏi v1).** Bảng mới `exam_type_price` TÁCH THEO TENANT (khác `reference_catalog` cha) — nhiều dòng đơn giá/dịch vụ theo Loại giá dịch vụ × Đơn vị tính × khoảng ngày hiệu lực, sửa/xoá tự do, C20 (`EXCLUDE USING gist`) chặn chồng lấn ngày hiệu lực ở tầng DB (cùng kỹ thuật C2). Modal riêng `ExamTypeFormModal.tsx` (tách khỏi `ItemFormModal` dùng chung), 2 component `shared/ui` mới dùng chung được (`MoneyInput`/`SearchableCombobox`). Mockup Artifact duyệt nhiều vòng trước khi code. Bug thật phát hiện + sửa cùng lúc: PATCH `reference-catalog` chỉ gửi field bảng con là no-op → nhầm 404 (sửa bằng cờ `hasCatalogFieldChanges`). **Đã xác minh thật**: `reference-catalog-http.spec.ts` +3 test (15/15), tổng 409/413 test `apps/api` pass (1 flake đã biết, không liên quan), `pnpm -w typecheck/lint/build` sạch. Chi tiết `docs/CHANGELOG.md` 2026-08-26 (3).

**"Chỉ định dịch vụ khám" ở Tiếp nhận — đổi từ 1 dịch vụ/lượt khám sang DANH SÁCH nhiều dịch vụ + cascade giá thật theo `exam_type_price` (`docs/DECISIONS.md` #080, ngoài kế hoạch, chủ dự án yêu cầu trực tiếp).** Đảo ngược #052 điểm 6 (khi đó cố ý làm phẳng "Loại giá dịch vụ" vì bảng đơn giá đa mức chưa tồn tại). Bảng mới `encounter_service_item` (sở hữu bởi module `reception`, đúng khuôn `vital_sign`) — 6 cột `exam_type_*`/`price_type_code`/`exam_type_unit`/`service_quantity` cũ trên `encounter` chuyển DEPRECATED (giữ nguyên DB, ngừng ghi). `ReceptionIntakeForm.tsx` viết lại: chọn Loại khám → "Loại giá dịch vụ" chỉ hiện mức còn hiệu lực hôm nay → Đơn vị/Đơn giá tự điền → bấm "+ Thêm" đẩy vào danh sách (dịch vụ chưa cấu hình giá vẫn thêm được, không chặn tiếp nhận). Tiện phát hiện + sửa 1 bug tiền nhiệm không liên quan: `scripts/check-mandatory-columns.mjs` thiếu exemption cho `AllergenGroup`/`Allergen` (danh mục toàn hệ thống #069). **Ghi nhận (chưa code)**: ý nghĩa thật của checkbox "Thanh toán sau" (gate hàng đợi khám) — để dành lúc code Thu ngân cơ bản Sprint 5/6, xem #080. **Ảnh hưởng cần nhớ cho Thu ngân (#072)**: tính tiền lượt khám giờ phải SUM nhiều dòng `encounter_service_item`, không còn đọc 1 giá trị đơn. **Đã xác minh thật**: `reception-http.spec.ts` +2 test, cập nhật toàn bộ payload cũ 3 file spec liên quan, tổng 411/418 test `apps/api` pass (7 skip do flake `geo-http.spec.ts` đã biết), `pnpm -w typecheck/lint/build` sạch, chunk web không đổi. Tiện sửa lúc kiểm bằng Playwright: bỏ chữ phụ 2 badge, bỏ prefix "BS." trùng lặp ở 3 nơi (bug thật có sẵn từ trước), thêm `Button variant="add"` gộp nút "+ Thêm" viết tay trùng lặp với `ExamTypeFormModal.tsx`. Playwright qua Chrome thật + Postgres thật (tenant dev có sẵn) xác nhận toàn bộ luồng cascade + thêm/xoá dịch vụ + Lưu tiếp nhận hoạt động đúng, query DB khớp tuyệt đối với UI.

**"Chức danh"/"Học vị-Học hàm" — thêm Mô tả + Trạng thái ngay trong form (`docs/DECISIONS.md` #081, 2026-08-27).** Chủ dự án yêu cầu xây 2 danh mục này đúng khuôn "Đơn vị tính" — rà soát phát hiện 2 category `STAFF_POSITION`/`ACADEMIC_TITLE` **đã có sẵn từ #063** (đúng vị trí `/admin/catalog-organization`, đã tự sinh mã), chỉ thiếu Mô tả/Trạng thái-trong-form. Cột đã dùng chung từ #078 (UNIT) nên **không migration** — chỉ mở rộng điều kiện hiển thị ở `ReferenceCatalogPane.tsx` (`DESCRIPTION_STATUS_CATEGORIES`). Phần chủ dự án hỏi thêm giữa chừng (2 trường Chức danh/Học vị-học hàm ở form Thêm tài khoản load đúng dữ liệu 2 danh mục) cũng đã làm xong từ #063 — chỉ xác minh lại, không sửa gì. **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch, chunk web không đổi, Playwright qua Chrome thật (CRUD 2 pill, đổi Trạng thái ẩn đúng khỏi danh sách/dropdown tài khoản, dữ liệu test đã xoá sạch sau khi kiểm).

**Redesign form "Thêm tài khoản" sang 3 Tab + nút "Vô hiệu hoá" nhanh ở danh sách (`docs/DECISIONS.md` #082, ngoài kế hoạch, 2026-08-27).** Chủ dự án yêu cầu trực tiếp, mockup Artifact tương tác duyệt nhiều vòng trước khi code (Thông tin chung / Chuyên môn và Pháp lý / Cấu hình và Vai trò). Migration `20260827100000_user_account_profile_tabs`: gộp `personal_email`/`company_email` cũ thành 1 cột `email`, thêm 7 cột mới (`dob`/`gender`/`license_issued_at`/`license_issued_place`/`display_name` bắt buộc lúc tạo mới/`signature_key` — ảnh chữ ký PNG, endpoint riêng chỉ nhận PNG/`default_room_id` — FK → `room`, thuần gợi ý hiển thị). `UserAccountFormDialog.tsx` viết lại toàn bộ theo 3 tab (Giới tính 2 checkbox tròn, Trạng thái segmented toggle teal, `UserAccountSignatureUpload.tsx` mới). Giữa lúc code, chủ dự án yêu cầu thêm nút "Vô hiệu hoá"/"Kích hoạt lại" nhanh ở danh sách tài khoản (không cần mở form Sửa) — chỉ `PATCH /users/:id`, không endpoint mới. **4 bug thật phát hiện lúc kiểm bằng Playwright, sửa cùng lúc**: 2 icon Thao tác chồng nhau (chốt quy tắc ≤3 nút xếp ngang/>3 nút gộp menu "..."), nút "Đặt lại mật khẩu" lệch khung, nút "Lưu thay đổi" bị khoá vĩnh viễn khi Sửa tài khoản cũ (SĐT/Tên hiển thị chỉ nên bắt buộc lúc Tạo mới, không phải Sửa), "Tên hiển thị" không tự gợi ý cho tài khoản cũ thiếu sẵn dữ liệu. **Đã xác minh thật**: 411/412 test `apps/api` pass (1 flake `icd10-http.spec.ts` đã biết), `pnpm -w typecheck/lint/build` sạch, chunk web không đổi (440.93 kB), Playwright qua Chrome thật 2 vòng (luồng Tạo tài khoản trọn vẹn + Vô hiệu hoá/Kích hoạt lại, rồi xác nhận lại 4 bug đã sửa đúng).

**`displayName` (#082) chưa từng được đọc ra ở nơi nào hiển thị tên tài khoản — vá xuyên suốt toàn app (`docs/DECISIONS.md` #083, 2026-08-27, chủ dự án phát hiện lúc dùng thử TopBar).** Nguyên nhân gốc: #082 chỉ thêm cột + form nhập, không có endpoint đọc nào (login/`/auth/me`, danh sách bác sĩ, resolve tên theo id) từng trả trường này — mọi nơi vẫn đọc `fullName` cũ. Đã vá đường đọc: `currentUserSchema`/`doctorOptionSchema` (`packages/shared`) thêm `displayName`; `findFullNamesByIds()` (phục vụ "Người tiếp nhận"/tên bác sĩ trong lịch sử khám) COALESCE `displayName ?? fullName` ngay tại repository; 8 nơi hiển thị tên bác sĩ/tài khoản ở `apps/web` (TopBar, "Chuyển vào hàng đợi", "Hàng đợi khám", in đơn thuốc, Đặt lịch nhanh, lưới/danh sách Lịch hẹn) áp `displayName ?? fullName` tại điểm tiêu thụ. Không đổi `UserAccountPane.tsx` (màn CRUD hiển thị đúng 2 cột riêng biệt). **Đã xác minh thật**: 422/422 test `apps/api` (1 flake `icd10-http.spec.ts` đã biết, pass riêng), `pnpm -w typecheck/lint/build` sạch, chunk web không đổi (440.96 kB), Playwright qua Chrome thật (tài khoản bác sĩ + bệnh nhân + lượt khám test tạo riêng, đã xoá cứng qua DB sau khi xác nhận).

**Thu ngân cơ bản (Sprint 5/6, BIL-01→04) — HOÀN TẤT, kể cả verify trình duyệt thật (2 phiên liên tiếp, phiên sau kết thúc 2026-08-27, `docs/DECISIONS.md` #084).** Backend + frontend code xong từ phiên trước (3 bảng mới `invoice`/`invoice_line`/`payment`, tự động tạo phiếu thu trong CÙNG transaction check-in/tiếp nhận trực tiếp; `encounter.allows_deferred_payment` + `tenant_setting.deferred_payment_enabled` gate "Bắt đầu khám"/"Nhận ca"/"Hàng đợi khám"; sidebar + trang "Cấu hình thanh toán"; 3 bug tự-phục vụ RBAC đã sửa — `GET /clinic-settings/deferred-payment-enabled`, `GET /clinic-profile/print-header`). Phiên sau hoàn tất nốt verify còn treo: chu trình gate đầy đủ qua cả API (`409 ENCOUNTER_PAYMENT_REQUIRED`) lẫn UI (`/billing` → "Chờ thu"/"Đã thu", thu tiền/"Đánh dấu chưa thu", tổng kết cuối ngày, bật/tắt "Thanh toán sau" ẩn/hiện đúng checkbox) — tất cả đúng như thiết kế.

**Cùng phiên hoàn tất, 3 yêu cầu chỉnh sửa trực tiếp + 1 tính năng mới (`docs/DECISIONS.md` #084)**: (1) menu "Thu ngân" đổi từ mục phẳng sang nhóm cha/con (icon `Wallet`, mục con "Danh sách cần thu"); (2) "Cấu hình thanh toán" bỏ `max-w-2xl` (full-width) + gộp 2 đoạn giải thích trùng lặp thành 1 đoạn ngắn gọn; (3) thanh pill "Danh mục dùng chung" thiếu class `.scroll-hover` (tiện ích ẩn thanh cuộn có sẵn từ 2026-08-14) khiến lộ thanh cuộn ngang mặc định trình duyệt khi tràn — thêm đúng 1 class; (4) **danh mục "Hình thức thanh toán" (`PAYMENT_METHOD`) mới**, nối thẳng vào Thu ngân thật (chốt qua `AskUserQuestion`) — `payment.method`/`invoice.pending_payment_method` đổi từ Postgres enum cố định (`CASH`/`BANK_TRANSFER`) sang TEXT tham chiếu `reference_catalog` (2 migration: đổi kiểu cột → seed 2 mã mặc định), pill mới trong "Danh mục dùng chung" (đúng khuôn "Đơn vị tính"), `InvoiceDetailPage.tsx`/`InvoicePrintView.tsx` render động theo danh mục thay 2 nút cứng.

**Đã xác minh thật**: `billing-http.spec.ts` 18/18 (chạy cô lập, xác nhận 2 migration mới tự áp đúng trên testcontainer sạch), 451/451 test `apps/api` khi chạy đủ không tranh chấp song song (chạy riêng có dính flake race `seedDefaultRolesForTenant`/`role_permission` đã biết từ nhiều phiên trước ở `geo-http.spec.ts`/`auth-login-http.spec.ts` — không liên quan billing). `pnpm -w typecheck/lint/build` sạch toàn workspace (chunk web khởi động 448.63 kB). Playwright qua Chrome thật (`dev.admin` + 3 tài khoản test tạm `billing-verify-*`): toàn bộ chu trình gate + thu tiền/revert + CRUD danh mục "Hình thức thanh toán" (2 mã mặc định + 1 mã tuỳ biến, cả 3 hiện đúng ở màn thu tiền) — **toàn bộ dữ liệu test (2 bệnh nhân, 3 lượt khám, phiếu thu, 3 tài khoản, 1 danh mục test) đã xoá cứng qua SQL trực tiếp, xác nhận `count(*)=0`**. `docs/ERD.md` (v1.31/v1.32), `docs/DECISIONS.md` (#084), `docs/TASK.md` (Sprint 5/6, S5-08), `docs/CHANGELOG.md` đã cập nhật xong trong phiên này.

**Huỷ lượt khám + hoàn tiền (28/08/2026, `docs/DECISIONS.md` #085) — CODE + TEST TỰ ĐỘNG XONG, CHƯA VERIFY PLAYWRIGHT.** Chủ dự án hỏi trực tiếp 3 tình huống chưa từng thiết kế: khách bỏ về chưa đóng tiền, khách đã đóng tiền rồi huỷ (cần hoàn), khách rút khỏi hàng đợi khi bác sĩ đã "Nhận ca". Khảo sát phát hiện 2 lỗ hổng thật: `POST /encounters/:id/cancel` có từ S3-03 nhưng 0 màn hình web nào gọi tới, và huỷ lượt khám không đụng gì tới phiếu thu (phiếu `UNPAID` treo vĩnh viễn, sai tổng kết BIL-04). Chốt qua 6 lượt `AskUserQuestion`: `invoice_status` mở 2→4 giá trị (`UNPAID/PAID/CANCELLED/REFUNDED`, huỷ khi đã `PAID` giữ nguyên `PAID` chờ hoàn riêng — không ép lễ tân chờ admin mới huỷ được ca); `payment.type` (`PAYMENT`/`REFUND`, dòng REFUND là dòng MỚI SỐNG đối ứng, không xoá vết cũ); hoàn tiền quyền riêng `invoice.refund` (chỉ `clinic_admin`), chỉ hoàn TOÀN PHẦN; `encounter-state-machine.ts` thêm 2 cạnh ở `IN_CONSULTATION` (`→CANCELLED` khách bỏ về giữa chừng, `→CHECKED_IN` "Trả về hàng chờ" — đường lùi đầu tiên của state machine, có chủ đích). Nút "Khách bỏ về/Huỷ lượt khám" thêm ở 4 màn hình (Danh sách tiếp nhận — đảo ngược một phần #044, Hàng đợi khám, Màn khám, Chi tiết phiếu thu) qua `shared/ui/CancelEncounterDialog.tsx` mới. `POST /encounters/:id/release` + `POST /billing/invoices/:encounterId/refund` mới. Tiện vá 1 bug wiring có sẵn phát hiện giữa chừng phiên: `queueView` (thiết kế lọc ẩn ca chưa thu khỏi Hàng đợi khám từ #084) chưa từng được frontend truyền lên. **Đã xác minh**: `packages/core` 118/118 test (+13 `invoice-lifecycle.spec.ts` mới, +2 cạnh state machine), `apps/api` 468/468 test (+10 `billing-http.spec.ts`, +8 `encounter-http.spec.ts`, chạy đủ 2 lần liên tiếp không flake), `pnpm -w typecheck` sạch, build web sạch (448.82 kB). Migration đã áp lên Postgres 18 dev DB thật. **Còn treo: chưa verify Playwright/trình duyệt thật** — việc kế tiếp khi tiếp tục phiên. `docs/ERD.md` (v1.33), `.claude/docs/clinical-workflow.md`, `docs/DECISIONS.md` (#085), `docs/CHANGELOG.md`, `docs/TASK.md` đã cập nhật.

**Đổi chữ "Khách bỏ về" + redesign popup + chuẩn hoá `Button` + checkbox chọn dòng hàng loạt (28/08/2026, `docs/DECISIONS.md` #086).** Phiên định quay lại verify Playwright cho #085 nhưng bị chuyển hướng bởi chuỗi phản hồi trực tiếp trên bản chạy thật (chủ dự án tự chạy `pnpm dev` riêng, gửi ảnh chụp theo thời gian thực): (1) đổi chữ "Khách bỏ về" theo trạng thái — CHECKED_IN → "Hủy", IN_CONSULTATION → "Hủy khám", popup dùng chung → "Hủy lượt khám"; (2) redesign `CancelEncounterDialog.tsx` bỏ icon tròn viền glow "nhìn AI look" về đúng mẫu popup xác nhận đơn giản đã có; (3) phát hiện + sửa 4 nút viết tay màu nhạt tuỳ tiện, chốt quy tắc bắt buộc dùng chung `Button` (`.claude/docs/ui-guidelines.md` mục 4.5 mới); (4) "Đã thu hôm nay" bỏ hiển thị ngày ở `InvoiceListPage.tsx`; (5) **checkbox chọn dòng + "chọn tất cả" cho 9 bảng danh sách** (scaffolding cho hành động hàng loạt sau này, chưa chốt hành động cụ thể) — 3 primitive mới `useRowSelection`/`SelectionCheckbox`/`SelectionToolbar` (mục 4.6 mới), bỏ qua ICD-10/Tỉnh-Thành-Phường-Xã (chỉ đọc). Bug thật phát hiện lúc kiểm: checkbox mặc định Chromium hiện đại tự vẽ bóng/glow xấu — sửa bằng `.selection-checkbox` (`appearance: none`, cùng kỹ thuật `.round-checkbox`). **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch (chunk 448.88 kB), Playwright qua Chrome thật (checkbox phẳng/chọn tất cả/indeterminate đúng trên bảng "Đơn vị tính"+"Danh sách bệnh nhân", "Đã thu hôm nay" đúng) — dùng lại dev server đang chạy sẵn của chủ dự án, không khởi động instance thứ hai. **Chưa xác minh được nút "Hủy khám"/dialog ở Hàng đợi khám qua ảnh chụp mới** (thiếu dữ liệu test ca đang khám) — chỉ xác nhận qua HMR lúc chủ dự án dùng thử.

**"Hàng chờ tại màn khám" — bác sĩ xem/chuyển ca ngay tại trang khám, không cần quay ra "Hàng đợi khám" + "Trả về hàng chờ" thêm ở 2 nơi mới (28/08/2026, `docs/DECISIONS.md` #088).** Chủ dự án phản hồi trực tiếp: sau khi bỏ dải "Khách đang chờ" ở #077, bác sĩ đang khám phải quay ra "Hàng đợi khám" mới xem/chuyển được khách khác. Mockup Artifact tương tác duyệt nhiều vòng (vị trí nút → cuối cùng chốt đặt trong `TopBar.tsx`, dùng chung toàn app nhưng CHỈ hiện ở `/encounters/:id` + vai trò "Bác sĩ") trước khi code. Panel trượt 3 nhóm "Đang khám dở"/"Bệnh nhân của tôi"/"Hàng chờ chung · Khoa", dùng lại đúng dữ liệu/mutation của "Hàng đợi khám" — chuyển ca chỉ điều hướng, không đóng/huỷ ca cũ. **Xác nhận qua code lúc thiết kế**: hệ thống hiện tại không giới hạn 1 bác sĩ chỉ được `IN_CONSULTATION` đúng 1 lượt khám cùng lúc — hành vi có chủ đích (hữu ích khi khám dở A chờ cận lâm sàng rồi chuyển khám B), không phải bug; dòng chữ sai ở `ReceptionDoctorQueuePage.tsx` chưa sửa. Trích xuất dùng chung: `queue-card.tsx` (mới, `WaitingCard`/`GroupLabel`/`ColumnEmpty`...) và `shared/ui/ReleaseEncounterDialog.tsx` (mới) — giải quyết luôn 1 lỗ hổng thao tác thật chủ dự án phát hiện giữa chừng: "Trả về hàng chờ" trước đây chỉ có ở trang Hàng đợi khám, nay dùng được cả ở footer màn khám và panel Hàng chờ. `TopBar.tsx` phải tự `lazy()` import `DoctorQueueButton` để không đẩy phụ thuộc module `reception` vào chunk khởi động (đo thật: 448.98→499.58 kB nếu import tĩnh, ~449.6 kB sau khi lazy). **2 bug tràn khung thật** (`truncate` đặt trên `<span>` rời không có tác dụng, tên bệnh nhân dài đẩy badge tràn ra ngoài thẻ) phát hiện qua ảnh chụp chủ dự án gửi lúc dùng thử — đã sửa cả `WaitingCard`/`InProgressCard`, ghi quy tắc mới vào `.claude/docs/ui-guidelines.md` mục 11. Dữ liệu test cho tài khoản `hoang.tran` tạo qua HTTP API thật bằng `dev.admin` (không SQL/role superuser, đúng bài học #087). **Đã xác minh**: `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk khởi động 449.66 kB (không tăng bất thường). **Chưa chạy Playwright tự động** — verify hoàn toàn thủ công qua chủ dự án dùng thử (đã phát hiện + xác nhận sửa đúng 2 bug nêu trên).

**S5-02/S5-03/S5-04 — Ký hồ sơ khám + đính chính (29/08/2026, quay lại đúng thứ tự Sprint 5/6 sau S5-08), xem `docs/DECISIONS.md` #089.** `diagnosis`/`clinical_note` chưa từng có khái niệm ký thật — "Hoàn tất khám" chỉ đổi trạng thái, nội dung khám luôn là bản nháp; "Chỉnh sửa thông tin" sau hoàn tất (#066) là sửa tự do tại chỗ, không lý do, không lưu lịch sử. `prescription` (Sprint 4, #076) dùng làm khuôn mẫu trực tiếp (Ký + Đính chính bằng DB trigger C8). **3 quyết định chốt qua `AskUserQuestion` trước khi code**: (1) ký TỰ ĐỘNG ngay khi bấm "Hoàn tất khám" (1 transaction, không tách nút riêng như `prescription`); (2) sau khi ký, "Chỉnh sửa thông tin" đổi hẳn sang đính chính bắt buộc lý do, thay #066 cho trường hợp đã ký; (3) đính chính `diagnosis` (danh sách nhiều dòng, không có "header" như `prescription`) ghép `supersedesId` theo `(icd10Code, type)` không đổi qua các lần đính chính (`pairDiagnosisAmendment`, `packages/core`, thuần, có unit test) — mã thực sự mới thì `supersedesId=null`. **Phát hiện lúc khảo sát code**: permission `clinical_note.sign` đã seed sẵn từ S1-04b (mô tả "Ký ghi chú khám") nhưng CHƯA TỪNG dùng tới — đúng dự đoán trước của dự án; `diagnosis` thiếu permission tương ứng, thêm mới `diagnosis.sign` cho đối xứng. Migration `20260829090000_diagnosis_clinical_note_signing`: `diagnosis` thêm 4 cột SignableEntity mới (khác `clinical_note` đã có sẵn từ Sprint 3, chỉ cần thêm trigger C8 + FK self-reference còn thiếu); backfill bắt buộc cho mọi encounter `COMPLETED` có sẵn từ trước (xấp xỉ `signed_at=completed_at`, `signed_by=updated_by`, không có actor chính xác hơn). 2 endpoint mới `POST .../diagnoses/amend`/`POST .../clinical-note/amend` (coi "chưa ký" là 404, đúng khuôn `amendPrescription()`). Web (`EncounterConsultationPage.tsx`): tách `canEditDraft` (chỉ `!isCompleted`, cho diagnosis/clinical-note) khỏi `canEditNow` cũ (vẫn giữ cho `PrescriptionPanel` — Kê đơn KHÔNG đổi, module riêng chưa ký tự động); "Chỉnh sửa thông tin" giờ chỉ hiện 2 nút "Đính chính" (dialog chẩn đoán tái dùng `Icd10SearchPicker` đúng UX "Sửa đơn"; dialog ghi chú khám gộp cả 6 mục + tự tính diff lúc submit) thay vì mở khoá input trực tiếp; footer bỏ nút "Lưu thay đổi" cho nhánh đã hoàn tất. **Phạm vi tự giới hạn**: KHÔNG dựng trang "Nhật ký hoạt động" đầy đủ (đó là S5-05/ADM-03, việc riêng) — chỉ badge "Đã ký lúc.../(bản đính chính)" inline, đúng khuôn `PrescriptionPanel.tsx`. **Đã xác minh thật**: `packages/core` `pair-diagnosis-amendment.spec.ts` 5/5 (tổng 123 test), `apps/api` viết lại toàn bộ describe test cũ của #066 thành 10 test mới cho ký/đính chính/trigger C8/break-glass/tenant isolation (61/61 `encounter-http.spec.ts`, tổng **476/476 test `apps/api` pass**, không regression). `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk web không đổi bất thường (449.66 kB). Migration đã áp thật lên Postgres dev qua `db:deploy` + `db:seed`. **Verify Playwright qua Chrome thật hoàn tất cùng ngày** — phát hiện + sửa 2 bug thật lúc kiểm: (1) `BreakGlassDialog` bị dialog "Đính chính" (cùng z-50) che khuất khi break-glass kích hoạt từ bên trong — đổi sang z-60 (áp dụng chung cho mọi dialog trong app); (2) state cục bộ (`diagnoses`/`clinical`) không đồng bộ sau khi đính chính thành công (dialog báo xong nhưng màn hình vẫn hiện nội dung cũ tới khi F5) — đã sửa, đồng thời bổ sung badge "(đã đính chính)" cho từng dòng chẩn đoán (bỏ sót lúc code lần đầu). Tiện phát hiện sự cố vận hành giống #087: `db:seed` chạy trước khi rebuild `packages/core` nên thiếu `diagnosis.sign` trong catalog — đã chạy lại. Sau 2 bugfix: `apps/api` 476/476, `pnpm -w typecheck/lint/build` sạch. Xem chi tiết `docs/DECISIONS.md` #089.

**"Xem chi tiết đợt khám cũ" + chuỗi chỉnh sửa trực tiếp trên 4 tab màn khám (29/08/2026), xem `docs/DECISIONS.md` #090.** Ngoài kế hoạch, cùng ngày với #089. (1) 4 tab neo cuộn ở màn khám sửa hover quá mờ rồi đổi tab đang chọn sang dạng pill xanh (đúng khuôn `ConfigScreenShell.tsx`); thêm scroll-spy thật (`activeTabId` theo vị trí cuộn), sửa 2 bug thật chủ dự án phát hiện lúc tự test (chọn nhầm tab liền kề — đổi từ `IntersectionObserver` sang tính trực tiếp theo vị trí cuộn; giật/nhảy tab khi bấm — khoá scroll-spy trong lúc cuộn mượt do bấm, mở lại khi `scrollend`). (2) **Tính năng mới**: bấm thẻ "Lịch sử khám" (trước đây chỉ tĩnh) mở dialog chỉ đọc `EncounterHistoryDetailDialog.tsx` — sinh hiệu/6 mục ghi chú khám/toàn bộ chẩn đoán (không chỉ chính)/đơn thuốc, tái dùng nguyên `GET /encounters/:id/consultation` có sẵn, không thêm API/schema. Mockup Artifact tương tác duyệt trước khi code, chốt qua `AskUserQuestion`. Trích `clinical-display.tsx` (mới) + export `PrescriptionItemsTable` từ `PrescriptionPanel.tsx` dùng chung với màn khám đang chỉnh sửa. (3) Gộp "Trả về hàng chờ"/"Hủy khám" thành nút "Xử lý" xổ menu — `shared/ui/ActionMenu.tsx` (mới, trích từ dropdown "Đăng xuất" ở `TopBar.tsx`), hiện thực thật đầu tiên cho quy tắc ">3 nút gộp menu" đã ghi ở `ui-guidelines.md` mục 4.5. (4) Sửa 1 lỗi độ đậm dòng ngày giờ tiêu đề dialog lịch sử khám (`font-extrabold`→`font-bold`). **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk web không đổi bất thường (449.71 kB), không đổi backend/schema. Playwright qua Chrome thật (dữ liệu có sẵn trên dev DB, không tạo mới): tab pill đúng mọi trạng thái, dialog mở đúng dữ liệu thật, chuyển tab không còn giật, menu "Xử lý" xổ đúng vị trí.

**S5-05 (ADM-03) — "Nhật ký hoạt động" (29/08/2026, quay lại đúng thứ tự Sprint 5/6), xem `docs/DECISIONS.md` #091.** Màn hình mới `/admin/activity-log` — lọc theo bệnh nhân (ĐẦY ĐỦ hồ sơ bệnh án, gồm cả `entityType='encounter'` của mọi lượt khám thuộc bệnh nhân đó, không chỉ `entityType='patient'`), theo người dùng, theo khoảng ngày, xem chi tiết before/after (đệ quy mảng/object lồng nhau, dịch cả field lẫn giá trị dạng mã sang tiếng Việt). 2 port mới `PatientReaderPort`/`EncounterReaderPort` (`packages/core/src/ports/`) — lần đầu `PatientReaderPort` có consumer thật.
**Phát hiện + xử lý thêm giữa phiên (chủ dự án yêu cầu/phản hồi trực tiếp)**: (1) **lỗ hổng compliance Thông tư 46/2018/TT-BYT thật** — `GET /encounters/:id/consultation` (xem hồ sơ khám) trước đây KHÔNG được ghi audit "xem" (chỉ `GET /patients/:id` có), đã vá bằng `@AuditView('encounter')`; (2) **chính sách lưu trữ audit_log 2 tầng** — "Log nghiệp vụ" (patient/encounter/appointment/invoice/vital_sign, mặc định AN TOÀN/giữ vĩnh viễn) vs "System Log" (tài khoản/vai trò/danh mục/cấu hình, chỉ giữ 90 ngày) — job nền `SystemLogPurgeJob` (lần đầu dùng `@nestjs/schedule`) xoá System Log quá hạn, cần **NGOẠI LỆ DUY NHẤT trong toàn schema** cho quy tắc "cấm DELETE" (`GRANT DELETE ON audit_log TO nexamed_app`, chỉ dùng qua đúng 1 chỗ code, không endpoint nào gọi tới); (3) **cảnh báo "phá kính"** — dòng break-glass tô nền đỏ nhạt + icon cảnh báo, đã hỏi và chốt giữ mô hình tự cấp ngay + soát sau (không thêm phê duyệt trước, đúng chuẩn ngành y tế khẩn cấp); (4) bỏ ghi `auth.refresh` vào audit_log (làm mới phiên quá thường xuyên, không phải một lượt truy cập mới có ý nghĩa) — `auth.refresh_reuse_detected` (sự cố bảo mật) vẫn giữ; (5) cột "Đối tượng"/before-after nhiều vòng chỉnh sửa từ hiện dạng kỹ thuật (`invoice · 01a048dd`, JSON thô) sang thuần tiếng Việt.
**Bug bundler thật, đúng lại lỗi đã biết #032**: hàm dịch field/giá trị ban đầu đặt ở `packages/shared` để web dùng, nhưng Vite dev không dò được named export hàm thuần qua `__exportStar` (cùng cơ chế #032, trước là hằng số giờ là hàm) — chuyển hẳn sang khai trực tiếp tại `apps/web/src/features/audit/field-labels.ts`.
**Đã xác minh thật**: `packages/core`/`packages/shared` (lần đầu có test)/`apps/web` (lần đầu có test) đều pass, `apps/api` 471/482 (11 skip, 1 flake `icd10-http.spec.ts` đã biết trước không liên quan) — gồm `system-log-purge.spec.ts` xác nhận role `nexamed_app` DELETE được thật qua migration mới. `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk web không đổi bất thường (452.70 kB). Playwright qua Chrome thật nhiều vòng, gồm 1 break-glass request thật để demo cảnh báo.

**S5-06 (Gộp hồ sơ trùng, PAT-04) + S5-07 (Tự đánh dấu Không đến, APP-05) — quay lại nốt 2 việc P1 cuối Sprint 5/6 (29/08/2026), xem `docs/DECISIONS.md` #092.** Không migration mới — cả hai đã có sẵn chỗ chốt từ trước (`patient.merged_into_id` + permission `patient.merge`, enum `NO_SHOW` có sẵn). Gộp hồ sơ: 2 điểm vào (chọn 2 dòng ở Danh sách bệnh nhân, nút ở Chi tiết bệnh nhân), chỉ chuyển `encounter` + 3 bảng tiền sử có cấu trúc sang hồ sơ đích, chặn tạo lượt khám mới cho hồ sơ đã gộp. Không đến: bật/tắt tự động (pill "Lịch hẹn" mới, trong "Cấu hình phòng khám") — tắt thì đánh dấu thủ công qua panel chi tiết lịch hẹn, bật thì job nền mỗi 5 phút tự chuyển. **Đã xác minh**: `apps/api` 495/502 pass (7 skip, 1 file flake race đã biết không liên quan), `pnpm -w typecheck/lint/build` sạch.

**Verify Playwright + cảnh báo đếm ngược/thông báo tự động (cùng ngày), xem `docs/DECISIONS.md` #093.** Playwright qua Chrome thật xác nhận cả gộp hồ sơ (2 điểm vào) lẫn cấu hình "Lịch hẹn" hoạt động đúng. Chuỗi chỉnh sửa UI trực tiếp: gộp 2 khung cấu hình Không đến thành 1, đổi nút "Sửa" chữ → icon (trích `shared/ui/EditIconButton.tsx` dùng chung, áp lại 4 nơi khác), đổi nút "Gộp hồ sơ" sang màu xanh nổi bật, đổi 2 câu mô tả theo đúng nội dung chủ dự án gửi. Câu hỏi trực tiếp của chủ dự án lộ ra lỗ hổng thật: chưa có cảnh báo trước khi tự động chuyển "Không đến" lẫn cách biết đã chuyển — đã bổ sung `noShowAutoMarked` (phân biệt tự động/thủ công, nhãn "(tự động)"), cảnh báo đếm ngược 3 mức ≤15/≤10/≤5 phút (Lưới + panel Chi tiết), và tự làm mới mỗi 30 giây (không dùng Toast, đúng quy tắc #066). Xác nhận "Sửa lịch"/"Dời lịch" không cần xử lý thêm gì — cả job lẫn cảnh báo đều tính trực tiếp từ `scheduledAt` hiện tại. **Đã xác minh**: `apps/api` 136/136 (5 file liên quan), `pnpm -w typecheck/lint/build` sạch, chunk web không đổi (452.75 kB).

**"Tạm nghỉ / Đóng ca" của bác sĩ (29/08/2026, ngoài kế hoạch, đặc tả UX gốc chủ dự án gửi), xem `docs/DECISIONS.md` #094.** Đã phân tích + hỏi cắt bỏ phần vượt hạ tầng v1 (SMS/Zalo, WebSocket, bảng điện tử gọi số, voice-to-text, Toast Undo — dành v2). Mô hình 3 trạng thái tách biệt hoàn toàn `encounter.status` (chỉ tác động routing): **ACTIVE** mặc định, **BREAK** ("Tạm nghỉ" — ca vẫn mở, không đụng lượt khám đang có), **ENDED** ("Đóng ca" — bulk trả toàn bộ `CHECKED_IN`/`IN_CONSULTATION` về hàng chờ chung Khoa, tái dùng nguyên cơ chế "Nhận ca"/pool #064). "Đóng ca" có **2 cách kích hoạt cùng 1 dialog/1 API**: đóng đột xuất (bác sĩ/lễ tân tự bấm bất kỳ lúc nào) và hết giờ làm việc (client tự poll 30s so với `businessHours` chung phòng khám — KHÔNG làm lịch riêng từng bác sĩ, đã hỏi và chốt — chỉ nhắc bằng chip treo, không tự đóng hộ). **2 công tắc mới ở pill "Cấu hình khám"** ("Cho phép bác sĩ đóng ca khẩn cấp" mặc định bật, "Cho phép lễ tân đóng ca hộ" mặc định tắt) — `tenant_setting` đơn giản, **không đi qua RBAC role-matrix** (đảo hướng so với bản nháp đầu, vì `req.user` không mang `roles` — mọi phân biệt trong dự án đi qua `data_scope`, không role-string). Bảng mới `doctor_availability` (không đụng `doctor_room_session`, giữ nguyên ý nghĩa "thuần vật lý phòng" đã chốt). Module mới `apps/api/src/modules/doctor-availability/`. `TopBar.tsx` dropdown avatar bác sĩ + chip nhắc quá giờ + chấm trạng thái; 2 dialog mới **lazy-load** (đo thật: main chunk 452.75→477.79 kB nếu tĩnh, sau lazy còn 468.76 kB — cao hơn baseline ~16 kB do hook đọc `businessHours` không lazy được ở TopBar, vẫn dưới ngưỡng 500 kB #073). `ReceptionIntakeForm.tsx` thêm badge trạng thái + nút thao tác hộ ở khu vực điều phối. Mockup Artifact duyệt nhiều vòng qua `AskUserQuestion` trước khi code (2 lần bổ sung yêu cầu giữa chừng). **Đã xác minh thật**: `doctor-availability-http.spec.ts` mới (12/12) + `clinic-http.spec.ts` +2, tổng 509 test `apps/api` pass thật (7 skip flake `geo-http.spec.ts` đã biết, không liên quan). `pnpm -w typecheck/lint/build` sạch toàn workspace. Migration đã áp thật lên Postgres dev + `db:seed` lại cho permission catalog mới.

**Verify Playwright qua Chrome thật hoàn tất (31/08/2026)** — tài khoản test tạo riêng qua HTTP API bằng `dev.admin` (`test.dongca.094` vai trò bác sĩ thuộc Khoa Nội, `test.letan.094` vai trò lễ tân — cả hai đã vô hiệu hoá lại sau khi kiểm xong), 1 lượt khám test (huỷ sạch sau khi kiểm). Xác nhận đúng toàn bộ theo thiết kế, **không phát hiện bug nào mới**: (1) dropdown avatar bác sĩ đúng 3 trạng thái ACTIVE/BREAK/ENDED (menu item, chip, chấm trạng thái đều đúng theo từng trạng thái); (2) "Đóng ca" khi có lượt khám đang `IN_CONSULTATION` — dialog cảnh báo đúng "Còn 1 lượt khám chưa xử lý", **xác nhận qua query DB trực tiếp** sau khi xác nhận: `doctor_id=NULL`, `started_at=NULL`, `status` quay về `CHECKED_IN`, vẫn giữ nguyên `department_id` — trả đúng về hàng chờ chung Khoa; `audit_log` ghi đủ cặp `doctor_availability.ended` (kèm `releasedEncounterCount`) + `encounter.released`; (3) test trực tiếp ở tầng API xác nhận đúng 2 nhánh gate: `allowEmergencyEndShift=false` chặn đóng ca thường (403 `DOCTOR_AVAILABILITY_EMERGENCY_DISABLED`) nhưng **không chặn** `trigger=SCHEDULED_END` (200, đúng thiết kế "hết giờ luôn được phép"); (4) lễ tân thao tác hộ (`allowReceptionistEndShift=true`) — "Cho tạm nghỉ hộ"/"Đóng ca hộ"/"Mở lại hộ" đều đúng, và khi `allowEmergencyEndShift=false` thì riêng mục "Đóng ca hộ" biến mất khỏi menu (chỉ còn "Cho tạm nghỉ hộ") — đúng gate kép; (5) Trường hợp 2 "Hết giờ làm việc" — hạ tạm giờ đóng cửa hôm nay về quá khứ rồi tải lại trang: chip đỏ + dialog tự bật đúng ngay, nội dung/khung cảnh báo đúng, "Để sau" giữ nguyên chip treo. Đã khôi phục `businessHours`/2 công tắc cấu hình về đúng giá trị mặc định sau khi kiểm xong. Xem `docs/DECISIONS.md` #094.

**TopBar — tách "Tạm nghỉ/Đóng ca" ra bánh răng riêng cạnh avatar, chỉ hiện ở Hàng đợi khám/Màn hình khám (01/09/2026), xem `docs/DECISIONS.md` #095.** Chủ dự án phản hồi trực tiếp trên ảnh chụp dropdown avatar thật: "Tạm nghỉ"/"Đóng ca hôm nay" đang nằm chung 1 menu với "Đăng xuất" — hai nhóm chức năng khác nhau. Mockup Artifact duyệt nhiều vòng trước khi code (so sánh Trước/Sau + thử tương tác trực tiếp). Nút bánh răng (`GearSix`) mới cạnh avatar — CHỈ hiện khi `isDoctor` VÀ đang ở `/reception/doctor-queue` hoặc `/encounters/:id` (đánh đổi có chủ đích: bác sĩ không còn Tạm nghỉ/Đóng ca được ở trang khác, đã nêu rõ với chủ dự án trước khi code); chứa đúng nội dung 3-trạng-thái ACTIVE/BREAK/ENDED đã có từ #094 (chỉ đổi nơi chứa), mang theo chấm trạng thái BREAK/ENDED (trước ở avatar). Avatar rút lại đúng 1 mục "Đăng xuất". 3 badge chip (quá giờ/tạm nghỉ/đóng ca) giữ nguyên hiện mọi trang. `getInitials()` tách ra `shared/format/initials.ts` (dùng chung lần 2). **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch, Playwright qua Chrome thật (`hoang.tran`) xác nhận đúng cả 2 điều kiện hiện/ẩn theo trang + nội dung menu.

**"Thông tin tài khoản" — popup tự xem/sửa hồ sơ cá nhân + đổi mật khẩu (01/09/2026, cùng phiên #095), xem `docs/DECISIONS.md` #096.** Mục mới trong menu avatar (trên "Đăng xuất"), mở popup (không phải trang riêng — đã hỏi và chốt qua mockup) cho tài khoản đang đăng nhập tự xem hồ sơ đầy đủ + sửa đúng **4 trường liên hệ** (SĐT/Email/Ngày sinh/Giới tính, Boxed Section Form Pattern mục 9b) + đổi mật khẩu (bắt mật khẩu hiện tại + xác nhận mật khẩu mới trùng khớp tại chỗ ở FE). Mọi trường hồ sơ nhân sự/pháp lý khác (Họ và tên, Tên đăng nhập, Mã NV, Học hàm/học vị, Chức danh, Khoa/Phòng, Vai trò, Trạng thái làm việc) khoá cứng có icon ổ khoá — dùng để ký HSBA/in đơn thuốc, vẫn do Quản trị kiểm soát qua `PATCH /users/:id` có sẵn, không mở rộng thêm ở đây (đã hỏi và chốt). Backend: `GET/PATCH /users/me` mới (`UserAccountController`, tự-phục vụ — không gắn `@RequirePermission`, `PermissionGuard` tự cho qua, mọi vai trò dùng được); `updateOwnProfileRequestSchema` CHỈ 4 trường, tách hẳn khỏi DTO admin để không có cách nào tự nới quyền. Đổi mật khẩu tái dùng nguyên `POST /auth/change-password` có sẵn từ trước, không sửa backend. Frontend: `MyAccountDialog.tsx` mới (lazy-load). **Đã xác minh thật**: `user-account-me-http.spec.ts` mới (5/5, tách file riêng tránh vượt rate-limit login), tổng 80/80 test module `iam` pass. `pnpm -w typecheck/lint/build` sạch, chunk web 473.29 kB (dưới 500 kB, dialog tách chunk riêng). Playwright qua Chrome thật (`hoang.tran`, dữ liệu/mật khẩu đã khôi phục về gốc sau khi kiểm): sửa SĐT → lưu → đóng/mở lại vẫn đúng; đổi mật khẩu thật (rồi đổi lại) → thành công; xác nhận sai/đúng → báo đỏ/xanh đúng.

**Cùng ngày, chuỗi chỉnh sửa nhỏ trực tiếp trên bản chạy thật** — đổi câu mô tả khung "Đổi mật khẩu" ở popup "Thông tin tài khoản"; đổi độ đậm tên tài khoản header cùng popup `font-extrabold`→`font-bold` (lặp lại đúng lỗi đã sửa ở #090); thêm icon (`Clock`/`Stethoscope`/`CheckCircle`) trước tiêu đề 3 cột "Hàng đợi khám" + tăng cỡ icon/chữ tiêu đề đó lên 1-2 bậc.

**S4-05 (đóng gói on-premise) + phần code S4-06 — ĐÃ XONG (01/09/2026), xem `docs/DECISIONS.md` #097.** Rà soát cho thấy toàn bộ tính năng nghiệp vụ Sprint 5/6 đã xong nhưng S4-05→08 (treo từ Sprint 4) chưa từng làm — pilot chưa từng chạy thật ngoài Playwright giả lập. Chủ dự án xác nhận máy chủ pilot đầu tiên là **PC thường tại phòng khám, CHƯA có server nội bộ** (khác giả định "máy chủ chuyên dụng" mặc định của `plan.md`) — ưu tiên trường hợp này, thiết kế dùng chung được cho NAS/máy chủ chuyên dụng sau này (2 script cài đặt riêng, cùng 1 `docker-compose.yml`, xem `docs/Deploy.md` Phần 0.2). Phạm vi chốt qua `AskUserQuestion`: chỉ S4-05 + phần CODE của S4-06 — S4-07 (cài đặt thật tại pilot)/S4-08 (đệm lỗi) cần phòng khám pilot cụ thể, chưa làm được.
  `deploy/on-prem/` mới: `docker-compose.yml` 5 service (`postgres`/`migrate`/`api`/`web`/`backup` — `migrate` chạy MỘT LẦN mỗi lúc `up`, idempotent: `prisma migrate deploy` → đổi mật khẩu role app → seed danh mục; `api`/`migrate` dùng CHUNG 1 Dockerfile, chỉ khác `command:`; `web` là nginx reverse-proxy `/api/*` sang `api` — cùng origin, không cần CORS/mở thêm cổng). Dockerfile CỐ Ý không dùng `pnpm deploy --prod` (thử thật lúc viết: lệnh đó không mang theo Prisma Client đã generate, thiếu thì API crash) — dùng bản đơn giản cài đủ devDependencies + build 1 stage, đổi lại ảnh nặng hơn (~1.5GB) nhưng đúng/dễ bảo trì hơn ở giai đoạn pilot. `GET /health` mới (loại khỏi prefix `api/v1`, dùng cho Docker healthcheck). Script sản xuất `seed-pilot-tenant.ts` (khác `seed-dev-tenant.ts` chỉ dev)/`rotate-app-role-password.ts` (đổi mật khẩu role `nexamed_app` khỏi giá trị hard-code #010). `install.ps1`(Windows PC)/`install.sh`(Linux/NAS) — tự sinh 4 bí mật ngẫu nhiên, build+up, chờ `api` healthy, hướng dẫn/tạo tenant pilot. `docs/pilot-onboarding.md` mới — hướng dẫn sử dụng + giáo án đào tạo 2 giờ theo vai trò (lễ tân/bác sĩ/quản trị).
  **1 bug thật phát hiện lúc verify container end-to-end** (không phải đọc code suông — soi header response HTTP thật qua nginx proxy): cookie `refresh_token` gắn `Secure` theo `NODE_ENV==='production'`, bị trình duyệt **âm thầm bỏ qua** cookie Secure trên origin `http://` (không báo lỗi) — chết đúng ca dùng chính: on-prem PC/NAS mặc định KHÔNG TLS (`docs/Deploy.md` Phần 0.2, "nên có" không "bắt buộc"). Hậu quả nếu không sửa: nhân viên bị đăng xuất mỗi 15 phút (refresh âm thầm hỏng). Sửa bằng biến `COOKIE_SECURE` mới (`env.schema.ts`, tách khỏi `NODE_ENV`) — không đặt thì giữ hành vi cũ, `deploy/on-prem/.env.example` đặt tường minh `false` làm mặc định on-prem.
  **Đã xác minh thật**: build + CHẠY cả 2 ảnh Docker thật (không dừng ở đọc Dockerfile) — dựng full stack trên network Docker cô lập đúng thứ tự phụ thuộc compose, `migrate` chạy đúng, `api` container báo `healthy` qua `docker inspect`, `GET /health` 200 qua HTTP thật, `web` phục vụ SPA + `config.json` mount-đè đúng không cần build lại, đăng nhập THẬT qua nginx proxy trả đúng token + cookie (chính lúc này phát hiện bug Secure). Sau khi sửa: build lại + verify lại xác nhận đúng, thêm test HTTP mới (`auth-login-http.spec.ts` +2). `apps/api` 518/525 pass + 7 skip (1 file flake `geo-http.spec.ts` đã biết từ nhiều phiên trước, pass 100% khi chạy riêng), `pnpm -w typecheck` sạch toàn workspace. Mạng sandbox lúc code rất chậm (`deb.debian.org`/`github.com` timeout hoàn toàn, `registry.npmjs.org` ~30KB/s) — dùng BuildKit mount pnpm store sẵn có trên host + mirror Debian nhanh hơn CHỈ để build-test cục bộ, KHÔNG đổi Dockerfile thật ship cho khách (vẫn dùng `deb.debian.org` mặc định, đúng cho mạng bình thường tại phòng khám).
  **Còn treo**: S4-07 (cài đặt thật tại pilot, đào tạo, trực hỗ trợ 2 ngày)/S4-08 (đệm lỗi) — cần phòng khám pilot cụ thể. S6-01→04/S6-07 (cảnh báo backup chủ động, diễn tập phục hồi thật, audit bảo mật đầy đủ, đo hiệu năng máy chủ pilot thật, tài liệu vận hành đầy đủ cho người không rành IT) — thuộc Sprint 6, chưa tới lượt, ghi rõ trong `docs/Deploy.md` mục "Còn treo".

**Sửa mô hình phân phối on-prem (01/09/2026, ngoài kế hoạch, chủ dự án phát hiện đúng vấn đề) — HOÀN TẤT, xem `docs/DECISIONS.md` #098.** Cách #097 làm (copy toàn bộ source sang máy khách rồi `docker compose build` tại chỗ) làm lộ `.ts`/`.git`/`.claude/docs/`/`docs/DECISIONS.md` (kể cả nội dung sự cố bảo mật thật đã xảy ra) cho bất kỳ ai truy cập máy khách — không chấp nhận được với phần mềm thương mại. Đã bàn 3 phương án (`docker save`/`load`, private registry, lọc bớt file) qua `AskUserQuestion`, chốt **`docker save`/`docker load`**: build 3 ảnh (`nexamed-api`/`nexamed-web`/`nexamed-backup`) ở máy dev/CI, xuất `.tar.gz`, máy khách chỉ nhận file đó + `deploy/on-prem/` đã lọc (không source). `docker-compose.yml` đổi `build:`→`image: nexamed-<tên>:${NEXAMED_VERSION:?...}`; `build-and-export.ps1` mới (chạy ở máy dev); `install.ps1`/`install.sh` đổi bước build→nạp ảnh (`docker load` + xác minh bằng `docker image inspect`).
  **Dung lượng thật** (chủ dự án hỏi trực tiếp, đo xong mới trả lời chính xác): `nexamed-api` 1.66GB raw nhưng nén gzip chỉ **358.6MB** (JS/JSON nén rất tốt) — tổng cả 3 ảnh ~532MB, đủ nhẹ để chuyển USB/LAN. Đã hỏi lại có muốn tối ưu bằng multi-stage build (ảnh có thể còn ~400-600MB) không — chủ dự án chọn **giữ nguyên**, chưa cần ở quy mô 1 pilot.
  **4 bug thật phát hiện lúc khảo sát/verify (không phải đọc code suông), TẤT CẢ chưa từng lộ ở #097 vì lần đó verify bằng Docker thủ công, chưa từng tự chạy `install.ps1` qua Windows PowerShell 5.1 thật**: (1) `context: ..` trong compose cũ resolve sai (thành `deploy/` thay vì gốc repo) — tự biến mất vì bỏ hẳn `build:`; (2) cả `install.ps1`/`build-and-export.ps1` fail parse trên PowerShell 5.1 do thiếu BOM UTF-8 — sửa bằng thêm BOM; (3) `New-RandomSecret()` dùng `[RandomNumberGenerator]::Fill()` — static method CHỈ có ở .NET Core/5+, không có ở .NET Framework (PowerShell 5.1 chạy trên đó) — sửa sang `.Create()`+`.GetBytes()`; (4) logic tự nhận diện version đọc nhầm "0.0.1-test" thành "0" do PowerShell tự bung mảng-1-phần-tử thành chuỗi rồi `$found[0]` lấy nhầm ký tự đầu — sửa bằng bọc `@(...)`. Case NAS Synology DS423 (ARM, RAM 2GB dưới ngưỡng tối thiểu 4GB) nêu ra lúc bàn bạc — quyết định chưa build sẵn `arm64`.
  **Đã xác minh thật toàn bộ chu trình 2 giai đoạn**: Giai đoạn A (`build-and-export.ps1` chạy thật, build+export cả 3 ảnh) → giả lập đúng máy khách (thư mục riêng NGOÀI repo, xác nhận không có file `.ts`/`.git`/`docs`, xoá sạch image cục bộ trước) → Giai đoạn B (`install.ps1` qua Windows PowerShell 5.1 thật — phát hiện+sửa 3/4 bug trên ngay lúc chạy) → `docker load`/`docker image inspect` đúng, `docker compose up -d` cả 5 container `healthy`, tạo tenant thật, **đăng nhập thật qua nginx proxy trả `200`+token đúng**. Đã dọn sạch container/volume/image/file test sau khi xong, không để lại rác.
  **Chuỗi cải thiện tiếp theo cùng ngày** — chủ dự án phản hồi trực tiếp lúc tự làm theo hướng dẫn thật: (1) `build-and-export.ps1` đổi sang **tự động gom sẵn 1 thư mục `deploy/on-prem/package/`** chứa đủ mọi file cần gửi khách (trước đó phải tự nhặt 5 file rải rác — nguồn gây nhầm lẫn); (2) viết lại toàn bộ `docs/Deploy.md` mục 2.3 theo đúng phong cách numbered step-by-step (không còn văn phong giải thích quyết định như DECISIONS.md) sau phản hồi "file deploy viết rất khó hiểu, không làm theo được" — kèm mục "Xử lý sự cố thường gặp" ghi lại đúng 2 lỗi thật chủ dự án gặp khi tự làm (giải nén zip lồng thư mục, cổng 80 bị chiếm trên máy khách + hệ quả lỡ chạy `docker compose up -d` trực tiếp bỏ lỡ bước tạo tài khoản).

**Diễn tập phục hồi thật (S6-02) — phát hiện + sửa bug thật chặn restore hoàn toàn — xem `docs/DECISIONS.md` #099.** Chủ dự án hỏi trực tiếp "làm sao restore" sau khi bàn cấu hình backup — lần đầu dự án thực sự thử khôi phục (trước đó chỉ có chiều TẠO backup, chưa từng kiểm chứng chiều KHÔI PHỤC). Test thật (dựng stack riêng, tạo tenant, `pg_dump`, mô phỏng "máy hỏng" bằng `docker compose down -v`, thử `pg_restore`) phát hiện **lỗi thật chặn restore hoàn toàn**: `nexamed_unaccent_lower()` (migration S2-02) gọi `unaccent(input)` không ghi rõ schema — chạy bình thường hằng ngày nhưng `pg_restore` cố ý đặt `search_path=''` (tính năng bảo mật Postgres chống CVE-2018-1058) khiến tên hàm không resolve được, restore lỗi ngay từ `CREATE TABLE patient`/`icd10_catalog`. Đã loại trừ nhiều hướng sai (không phải do `--clean`, không phải thiếu extension, không phải thứ tự TOC) trước khi tìm đúng nguyên nhân qua đọc script SQL thật (`pg_restore -f -`). Sửa bằng migration mới `20260901120000_fix_unaccent_search_path` (`public.unaccent`, forward-only, không sửa migration cũ). Rà soát toàn bộ migration khác — chỉ đúng 1 chỗ dính lỗi này. **Giới hạn đã biết**: fix chỉ áp dụng cho backup tạo ra SAU khi máy cập nhật — backup cũ hơn vẫn cần 1 lệnh sửa tay trước khi restore (đã ghi vào `docs/Deploy.md` 2.3c). **Đã xác minh thật**: áp fix → dump lại → xoá schema → restore 0 lỗi → dữ liệu đúng nguyên bản → đăng nhập thật qua nginx `200`; build lại ảnh `nexamed-api` có migration, test cả 2 kịch bản (cài mới, cập nhật từ bản cũ đang chạy) qua đúng cơ chế tự động `prisma migrate deploy` — không cần can thiệp tay. Cập nhật `docs/Deploy.md` (mục 2.3c restore + 2.3d bảng tra cứu lệnh Docker).

**S4-07 (cài đặt pilot) đã cài thành công (01/09/2026, chủ dự án xác nhận) — S4-08/S5-01 (vá lỗi từ pilot) bắt đầu, bug thật đầu tiên đã vá VÀ ĐÃ XÁC NHẬN LẠI THÀNH CÔNG trên máy pilot (`docs/DECISIONS.md` #100).** Cập nhật lên bản `2026.09.05` xong, đăng nhập báo lỗi chung chung — DevTools xác nhận **CORS error**, response OPTIONS đến từ `Microsoft-IIS/10.0` (request rơi vào IIS có sẵn trên Windows chiếm cổng 80, không tới container `web`). Nguyên nhân: `install.ps1`/`install.sh` hỏi lại "Địa chỉ IP LAN" và **ghi đè** `WEB_ORIGIN`/`config.json.apiBaseUrl` ở MỌI lần chạy (khác các bí mật khác chỉ sinh khi `.env` chưa có) — xoá mất workaround cổng `8080` máy đó từng chỉnh tay để né xung đột cổng 80. Sửa: 2 giá trị này giờ chỉ hỏi/ghi ở lần cài đầu tiên hoặc khi truyền rõ `-WebOrigin`/`--web-origin`, các lần cập nhật sau giữ nguyên. Cập nhật `docs/Deploy.md` 2.3b đính chính khẳng định sai trước đó. **Đã xác nhận lại trên máy pilot thật (02/09/2026)** — đăng nhập bình thường, hết treo.

**"Ca làm việc" Giai đoạn 2 — đăng ký ca theo ngày (MỌI nhân viên), "Lịch làm việc nhân viên", lọc lưới Lịch hẹn, chặn đặt lịch ngoài ca (02/09/2026, `docs/DECISIONS.md` #102).** Mockup Artifact 4 màn hình duyệt qua nhiều vòng `AskUserQuestion` trước khi code. Bảng mới `work_shift_assignment` (tenant-scoped, đúng khuôn `doctor_availability` nhưng nhận `work_date` từ client, nhiều dòng/ngày được). Permission mới `work_shift_assignment.create/read/delete` — điểm đáng chú ý: `create` của `clinic_admin` = `global` (khác `personal` của 4 vai trò còn lại) để tạo hộ được ở "Lịch làm việc nhân viên", ca đầu tiên trong dự án 1 permission có scope khác theo VAI TRÒ cho cùng action. Khoá "chỉ tự sửa/xoá trong đúng ngày đăng ký" (không phải ngày làm việc) kiểm ở Service. `ClinicSettings` thêm `blockBookingOutsideWorkShiftEnabled` (mặc định tắt, đặt ở pill "Cấu hình phòng khám" → "Lịch hẹn", cùng khối "Tự động đánh dấu Không đến" — sửa lại từ vị trí sai ban đầu trong mockup). Sidebar nhóm mới "Lịch làm việc" (2 mục: của tôi/nhân viên). Lưới Lịch hẹn (`AppointmentGridView.tsx`) hiện dải giờ ca theo cột bác sĩ + gạch chéo ngoài ca. Tiện vá 2 lỗ hổng thật không liên quan chủ dự án phát hiện giữa chừng: 4 module (`allergen_catalog`/`drug`/`invoice`/`doctor_availability`) thiếu nhãn tiếng Việt ở trang "Vai trò & Phân quyền", và Sidebar thiếu `scroll-hover` (lộ thanh cuộn mặc định xấu). **Đã xác minh thật**: `work-shift-assignment-http.spec.ts` (11/11) + `appointment-work-shift-http.spec.ts` (5/5) mới — tổng 551/551 test `apps/api` (2 lỗi lúc chạy chung song song là flake `role_permission` đã biết, pass 100% khi chạy riêng). `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk web 476.56 kB (dưới ngưỡng 500kB, 2 trang mới đã lazy). OpenAPI + web codegen đã sinh lại. **Còn treo lúc đó**: "Tháng" (month view) ở "Lịch làm việc của tôi" chưa xây (chỉ có Tuần, đầy đủ chức năng); **chưa verify Playwright/trình duyệt thật** — cả hai đã làm xong ngay sau đó, xem mục ngay dưới (#103).

**Verify #102 + làm nốt "Tháng" + sự cố seed thiếu permission + badge "Có ca hôm nay" ở Tiếp nhận (02/09/2026, cùng ngày, `docs/DECISIONS.md` #103).** Verify Playwright phát hiện 2 vấn đề thật: (1) nút "☑ Chọn nhiều ngày" của mockup không tồn tại — checkbox hiện luôn thay vì ẩn theo chế độ, đã sửa bằng state `bulkMode` mới; (2) permission `work_shift_assignment.*` chưa từng được `db:seed` vào Postgres dev (bug vận hành lặp lại đúng bài học #087/#089) — rebuild `packages/core` + `db:seed` + chạy tay `syncRolePermissionsForAllTenants`, xác nhận qua API. **"Tháng" làm xong, MỞ RỘNG hơn mockup gốc theo yêu cầu trực tiếp**: chọn nhiều ngày + đăng ký ca + "Sao chép tháng trước" ngay trong Tháng (không cần quay về Tuần) — `bulkMode` dùng chung cả 2 view. Bỏ dropdown `ActionMenu` cho "Sao chép..." (mỗi view giờ chỉ 1 hành động liên quan, hiện thẳng nút). **Redesign khung Tuần/Tháng qua 2 vòng phản hồi trực tiếp** ("khung xấu, nút không nổi bật"): bọc thẻ trắng bo góc + đổ bóng, "Hôm nay" tô nền xanh đậm đặc, cuối tuần tông xám riêng, nút thêm ca đổi sang `Button variant="add"` (trước viết tay). Bổ sung 2 chỗ mockup có nhưng code #102 bỏ sót hoàn toàn: bộ lọc Khoa + ô tìm bác sĩ ở "Lịch làm việc nhân viên"; dòng "Chưa đăng ký ca — theo giờ chung" ở lưới Lịch hẹn cho bác sĩ 0 ca. **Quyết định kiến trúc quan trọng** (chuỗi hỏi-đáp với chủ dự án): Tiếp nhận **KHÔNG lọc, KHÔNG chặn cứng** bác sĩ theo "có ca hôm nay" — lọc sẽ làm rỗng danh sách cho mọi phòng khám không dùng tính năng ca (đa số, mặc định tắt); chặn cứng sẽ mâu thuẫn với quy tắc đã chốt ở lưới Lịch hẹn (#102 điểm 5: chưa đăng ký ca thì không giới hạn gì thêm dù công tắc bật). Chốt: chỉ **đánh dấu** badge "Có ca hôm nay" (icon `CalendarCheck`, tông `brand-teal`) + **xác nhận nhẹ** (dialog "Vẫn chọn"/"Huỷ", không chặn) khi lễ tân chọn bác sĩ không có ca **và** công tắc `blockBookingOutsideWorkShiftEnabled` đang bật. Ngoài ra: đổi câu mô tả công tắc "Chặn đặt lịch ngoài ca đã đăng ký"; dời trường "Lý do tiếp nhận" lên ngay trên "Chuyển vào hàng đợi". **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk web không đổi bất thường (476.57 kB), Playwright qua Chrome thật (`dev.admin`) — Tuần/Tháng + chọn nhiều ngày, filter Khoa/tìm bác sĩ, khung thẻ mới, "Lý do tiếp nhận" đúng vị trí. Không có dữ liệu ca đăng ký thật trên dev DB nên chưa chụp được badge/dialog xác nhận thực tế hiện ra (đúng dự kiến, không phải lỗi) — để dành nếu cần bằng chứng trực quan cụ thể hơn. Không có migration/schema mới, `docs/ERD.md` không đổi.

**Danh mục "Ca làm việc" — Giai đoạn 1 xong (02/09/2026, `docs/DECISIONS.md` #101, chủ dự án yêu cầu trực tiếp).** Hỏi lại tính năng "Ca làm việc riêng từng bác sĩ" từng lùi ở #030 — chốt qua `AskUserQuestion`+mockup: chỉ làm **danh mục mẫu ca** (thêm/sửa/xoá) trước, đăng ký ca theo tuần/tháng cho bác sĩ + lọc lưới Lịch hẹn + chặn đặt lịch ngoài ca là **giai đoạn kế tiếp, CHƯA làm**. Bảng mới `work_shift` — **RIÊNG theo tenant** (đổi hướng giữa chừng từ đề xuất đầu dùng `reference_catalog` sau khi phát hiện đó là danh mục toàn hệ thống, sai bản chất cho dữ liệu riêng từng phòng khám). 9 trường: tên/mã tự sinh/giờ bắt đầu-kết thúc/màu (8 màu cố định)/giờ nghỉ (tuỳ chọn, độc lập)/tổng thời gian nghỉ (giờ hoặc phút)/số giờ công chuẩn (giờ)/thứ tự. Mục con mới "Ca làm việc" trong Cấu hình hệ thống → Cấu hình phòng khám. Modal riêng theo Boxed Section Form Pattern (không xếp 1 cột dọc, đúng phản hồi trực tiếp của chủ dự án khi duyệt mockup lần 1). **Đã xác minh thật**: `work-shift-http.spec.ts` 10/10 (tổng 535 test `apps/api` + 127 `packages/core`, không regression), `pnpm -w typecheck/lint/build` sạch, Playwright qua Chrome thật (thêm/sửa/validate giờ đều đúng, dữ liệu test đã xoá sạch).

**Chuỗi vá lỗi + redesign "Lịch làm việc nhân viên" + Nhập/Xuất Excel (02/09/2026, `docs/DECISIONS.md` #104).** Chủ dự án yêu cầu test tính năng khoá lịch (#101-#103) — lúc test lộ ra **2 bug RBAC thật trước giờ chưa ai chạm tới** (mọi verify trước đó dùng `dev.admin`/`clinic_admin`): (1) "Lịch làm việc của tôi" hiện nhầm ca của TOÀN BỘ nhân viên thay vì chỉ actor đăng nhập — do `MyWorkSchedulePage.tsx` không truyền `userId` vào query, actor scope `global` (clinic_admin) không tự lọc; sửa bằng truyền tường minh `userId=user.id`. (2) Trang tự nó **403 hoàn toàn** với 4/5 vai trò — `GET /work-shifts` (danh mục mẫu ca) đang đòi `clinic_config.read` (chỉ clinic_admin có), trong khi tính năng tự đăng ký ca dành cho MỌI nhân viên; sửa bằng permission riêng `work_shift.read` (global, cấp cho cả 5 vai trò), tách khỏi `clinic_config.read`. Cơ chế khoá (server-side, `WorkShiftAssignmentService.remove()`) xác minh đúng qua test trực tiếp: `canEdit=false` + xoá ngoài ngày đăng ký → 409 `WORK_SHIFT_ASSIGNMENT_LOCKED`, admin (scope global) không bị khoá.
Tiện đó, chủ dự án yêu cầu **redesign toàn diện "Lịch làm việc nhân viên"** qua nhiều vòng phản hồi trực tiếp trên bản chạy thật: khung ca cao hơn + màu phủ TOÀN thẻ (bỏ kiểu viền trái mỏng cũ, áp dụng luôn cho cả "Lịch làm việc của tôi" cho đồng nhất), tên nhân viên kèm mã NV dưới tên (bỏ avatar tròn), viền lưới rõ ràng (`slate-300`), tiêu đề cột đổi sang "Thứ hai ⓵" (tên đầy đủ + số ngày trong khung tròn, "Hôm nay" tô xanh). Đổi hẳn kiến trúc xem: từ **xem-theo-tuần** (nút tuần trước/sau) sang **xem nguyên 1 THÁNG liên mạch** — cột tên nhân viên `position: sticky left-0`, mỗi lần chỉ hiện đúng 7 cột ngày (đo bằng `ResizeObserver`, giữ đúng mật độ cũ), nút "Cuộn tuần ◀▶" cuộn đúng 7 cột/lần. **1 bug dựng-rồi-sửa thật**: thử dùng CSS `scroll-snap-type` kết hợp cột sticky trước — lộ lỗi hiển thị (nội dung bị lệch/cắt, cả tôi lẫn chủ dự án cùng phát hiện độc lập qua ảnh chụp) do xung đột đã biết của Chromium giữa 2 kỹ thuật này — bỏ hẳn snap tự động, thay bằng nút cuộn chủ động (đáng tin cậy hơn). Sau đó dời vị trí 2 nút cuộn 2 lần theo yêu cầu (cuối cùng: dưới nút Xuất/Nhập Excel, trên dòng tiêu đề bảng, cùng hàng với "Chú thích ca").
**Tính năng mới — Nhập/Xuất Excel** cho "Lịch làm việc nhân viên" (chỉ scope global/clinic_admin, đã hỏi và chốt qua `AskUserQuestion`): nút "Tải mẫu"/"Nhập Excel" (dialog xem trước 3 nhóm hợp lệ/trùng/lỗi rồi mới bấm "Xác nhận nhập" — đã hỏi và chốt KHÔNG tự động bỏ qua/ghi đè trùng ca âm thầm)/"Xuất Excel". **Đổi định dạng file giữa chừng theo yêu cầu trực tiếp**: bản đầu làm dạng hàng-dọc (1 dòng = 1 ca, cột Ngày kiểu `yyyy-mm-dd`) — sau khi thấy bản demo, chủ dự án yêu cầu đổi sang **dạng bảng ngang** (cột 1 = Mã nhân viên, các cột còn lại = từng ngày trong tháng, ô giao = Mã ca) khớp đúng cách nhìn của lưới mới. Câu hỏi "hệ thống biết nhập tháng nào" tự đề xuất và chốt: **tháng luôn là tham số UI chọn tường minh** (mặc định = tháng đang xem), KHÔNG dò/đoán từ nội dung file (an toàn hơn, không phụ thuộc người dùng gõ đúng ô ghi chú). Thư viện `exceljs` (mới, backend-only — không đẩy ra web, giữ đúng nguyên tắc hiệu năng đã chốt). `UserAccountRepository`/`WorkShiftService` export thêm từ `IamModule`/`ClinicModule` cho `WorkShiftAssignmentModule` dùng trong cùng transaction (đúng khuôn "reception/encounter/appointment chia sẻ Repository", #042).
Loạt chỉnh sửa UI nhỏ khác cùng phiên: đổi nhãn "Đăng ký ca làm việc"→"Quy tắc đặt lịch theo ca", bỏ 3 dòng mô tả thừa ở "Thông tin phòng khám"/"Cấu hình khám"/"Giờ làm việc", đổi câu mô tả "Danh mục Ca làm việc"/"Khoa-Phòng" cho đúng ý nghĩa thật (không còn ghi "chưa xây" dù tính năng đã xong).
**Đã xác minh thật**: `pnpm -w typecheck/lint` sạch toàn workspace, build web sạch (chunk khởi động 477.25 kB, dưới ngưỡng 500kB), 533/533 test `apps/api` không regression (11/11 riêng `work-shift-assignment-http.spec.ts`; 1 flake race `role_permission` tiền nhiệm đã biết khi chạy song song, pass 100% khi chạy riêng). Playwright qua Chrome thật + test API trực tiếp cho toàn bộ luồng: khoá lịch (canEdit/409), redesign lưới tháng (đúng ngày đầu tháng, cuộn tuần đúng 7 cột), Nhập Excel end-to-end qua UI thật (chọn tháng → xem trước → xác nhận → kết quả "Đã nhập X ca, bỏ qua Y trùng, Z lỗi" đúng số liệu). Không có migration/schema DB mới trong phiên này (chỉ code + permission catalog tự seed/sync lúc API khởi động).
**"Cấu hình chung" — công tắc bật/tắt tự đăng ký ca cho nhân viên — ĐÃ XONG (02/09/2026, làm nốt việc treo cuối #104).** Mục con mới "Cấu hình chung" trong pill "Cấu hình phòng khám", dưới "Ca làm việc". `tenant_setting.allow_staff_self_schedule_enabled` (mặc định `true`, đúng khuôn `allowEmergencyEndShift`/`blockBookingOutsideWorkShiftEnabled`), đọc/ghi qua `GET/PATCH /clinic-settings` có sẵn, không permission mới. **Phát hiện lúc code**: `MyWorkSchedulePage.tsx` không thể dùng `useClinicSettingsQuery()` (đòi `clinic_config.read`, chỉ `clinic_admin` có, trong khi trang này dành cho MỌI nhân viên) — đúng lớp lỗ hổng tự-phục vụ đã gặp nhiều lần (#030/#064/#104) — thêm chiếu tối thiểu `GET /clinic-settings/allow-staff-self-schedule-enabled` (đúng khuôn `deferred-payment-enabled`). Tắt: `WorkShiftAssignmentService.assertSelfScheduleAllowed()` chặn `create`/`bulkCreate`/`copy`/`remove` khi `dataScope==='personal'` (lỗi mới `WorkShiftAssignmentSelfScheduleDisabledError`, 403 `WORK_SHIFT_ASSIGNMENT_SELF_SCHEDULE_DISABLED`) — `list()` và scope `global` (clinic_admin ở "Lịch làm việc nhân viên") không bị ảnh hưởng; `MyWorkSchedulePage.tsx` ẩn hết nút tự đăng ký/xoá/"Chọn nhiều ngày"/"Sao chép tuần trước", hiện banner vàng, chỉ còn xem lịch đã phân công. **Đã xác minh thật**: `clinic-http.spec.ts`/`work-shift-assignment-http.spec.ts` +7 test (538/540 `apps/api`, 2 flake `geo-http.spec.ts`/`icd10-http.spec.ts` đã biết, pass riêng), `pnpm -w typecheck/lint/build` sạch (chunk web 477.58 kB). Playwright qua Chrome thật (dùng lại dev server đang chạy sẵn, `dev.admin`): tắt/bật công tắc phản ánh đúng ngay trên "Lịch làm việc của tôi", dữ liệu ca cũ giữ nguyên qua cả 2 lần đổi. Không migration/schema mới.

**Chuỗi #105-109 (03/09/2026) — vá lỗi + tính năng nhỏ, tiếp sau "Cấu hình chung".** Cờ `department.participatesInQueue` (Bộ phận Lễ Tân không lộ ra ở điều phối "Chuyển vào hàng đợi" nhưng vẫn hiện ở "Lịch làm việc nhân viên", có 1 lần sửa lại vì lọc cứng nhầm cả endpoint dùng chung); badge trạng thái toàn app đổi nền nhạt→đặc (`StatusBadge.tsx` mới, `.claude/docs/ui-guidelines.md` mục 2.1a); bulk-apply ca theo TỪNG DÒNG nhân viên ở "Lịch làm việc nhân viên" (tái dùng cơ chế đã có từ #101/#102, chỉ nối thêm UI); vá nhãn tiếng Việt còn thiếu ở "Nhật ký hoạt động" cho các module mới (`work_shift`/`work_shift_assignment`/`doctor_availability`/`tenant_setting`). Xem `docs/DECISIONS.md` #105-109, `docs/CHANGELOG.md` 2026-09-03. **Đã verify Playwright qua Chrome thật** (phiên này, trước đó chưa) — department filter đúng ở cả 2 nơi (queue vs quản lý nhân viên), badge nền đặc đúng, bulk-apply theo dòng hoạt động đúng end-to-end (chọn nhiều ngày → áp dụng ca → xoá lại), Nhật ký hoạt động hiện đúng tiếng Việt. Không phát hiện bug mới ngoài các bug đã ghi trong #105/#107.

**"Khoá bảng ca" theo tháng (work_shift_assignment) — chuẩn bị nền cho chấm công/tính lương v2 (03/09/2026, ngoài kế hoạch).** Lịch làm việc tháng cũ bị khoá TOÀN BỘ (Thêm/Sửa/Xoá/Nhập Excel) sau mốc "chốt bảng ca" cấu hình được theo tenant (`tenant_setting.work_shift_assignment_lock_grace_days`, mặc định 0 = khoá ngay khi sang tháng mới) — áp dụng MỌI actor kể cả `clinic_admin`, trừ ai có đặc quyền mới `work_shift_assignment.unlock` (mặc định `clinic_admin`/`system_admin`, cấu hình lại được qua "Vai trò & Phân quyền"). **Có quyền cũng KHÔNG tự động mở khoá giao diện** — phải bấm "Sửa" tường minh mới lộ nút thao tác (đảo hướng so với thiết kế đầu, chốt qua phản hồi trực tiếp chủ dự án khi test). Tiện phát hiện + sửa 2 bug thật không liên quan trực tiếp: "Lịch làm việc nhân viên" hiện sai theo thời gian tồn tại của tài khoản (nhân viên mới tạo lộ ở tháng trước ngày tạo; nhân viên nghỉ việc mất luôn khỏi tháng họ còn làm) — sửa bằng lọc `createdAt`/`updatedAt` (2 field mới additive trên `userAccountSummarySchema`, không migration). Xem `docs/DECISIONS.md` #110. **Không có migration** (chỉ thêm permission catalog + `tenant_setting` key). **Đã xác minh thật**: `packages/core` `month-lock.spec.ts` mới (11/11, tổng 138/138), `apps/api` 549/557 pass + 7 skip (2 suite flake đã biết — race/timing, pass 100% khi chạy riêng, không liên quan), `work-shift-assignment-http.spec.ts` riêng 12/12 không regression, `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk web không đổi bất thường. Đã `db:seed` + build lại + khởi động lại API, xác nhận qua `curl` thật (permission sync đúng 24 dòng, PATCH/GET grace-days round-trip đúng). **Chưa verify Playwright chính thức cho luồng khoá/mở khoá tháng** — chủ dự án đã tự test trực tiếp qua `pnpm dev` đang chạy và phản hồi từng vòng (kể cả phát hiện đúng 2 bug staff-filtering ở trên), coi như đã xác nhận qua thực tế nhưng chưa có bằng chứng ảnh chụp Playwright chính thức — để dành nếu cần.

**Redesign khung tổng kết Thu ngân + đồng bộ popup xác nhận có số tiền (03/09/2026, cùng phiên, chuỗi phản hồi trực tiếp).** `InvoiceListPage.tsx`: khung tổng kết đổi sang "thẻ số liệu" hiện đại (icon tròn, nhãn hoa nhỏ, số đậm, viền dọc ngăn cách), khối "Còn chờ thu" nền đặc amber; 2 vòng chỉnh theo phản hồi (giảm `font-extrabold`→`font-bold`, khối "Còn chờ thu" hết kéo giãn to bất thường); thêm cột "Đã hoàn" vào bảng. Rà soát phát hiện `RefundDialog` (`InvoiceDetailPage.tsx`) và `ReleaseEncounterDialog.tsx` lệch chuẩn `CancelEncounterDialog.tsx` (overlay/shadow/padding sai, `RefundDialog` thiếu `<form>` — vi phạm mục 4.4) — đã sửa khớp lại, đổi khung cảnh báo số tiền sang huy hiệu tròn đặc (đúng khuôn #095) thay khung chữ viền nhạt. Xem `docs/DECISIONS.md` #111. **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch, chunk web 477.58 kB, Playwright qua Chrome thật xác nhận khung Thu ngân + cột mới + popup "Hủy lượt khám" hiện đúng.

**"Chốt ca" (BIL-05, đối soát tiền mặt/két) — HOÀN TẤT (03/09/2026), xem `docs/DECISIONS.md` #112.** Tiếp nối phiên trước để dở dang ("CHƯA XONG, CHƯA TEST") — mockup 3 màn hình (Mở ca/Chốt ca 4 bước/Danh sách phiếu chốt ca) đã duyệt qua Artifact, kế hoạch kỹ thuật đầy đủ đã duyệt qua EnterPlanMode (`C:\Users\Administrator\.claude\plans\cuddly-napping-key.md`). v1 chỉ 1 két dùng chung toàn tenant, chỉ 1 ca `OPEN` tại một thời điểm. Bảng mới `cashier_shift` (migration `20260903180000_cashier_shift`), cột `reference_catalog.counts_as_cash`, `tenant_setting.cashier_shift_blind_close_enabled`, 3 permission `cashier_shift.create/read/manage`. Module `apps/api/src/modules/cashier-shift/` (10 endpoint), `apps/web/src/features/cashier-shift/` (Mở ca/Chốt ca 4 bước/Danh sách phiếu/Chi tiết+Mở khoá sửa/Phiếu in 3 khổ).
Hoàn tất phần treo theo đúng thứ tự đã ghi ở bản trước: typecheck `apps/web` lần đầu (phát hiện 2 lỗi thật — `queryKey()` sai kiểu tham số, thiếu `handoverNote` trong schema/service khiến "Sửa phiếu" không đọc lại được giá trị hiện tại), viết `cashier-shift-http.spec.ts` (37 test mới đủ theo kế hoạch: mở/chốt khớp+lệch, race mở ca đồng thời, quyền sở hữu, danh sách+filter, xử lý chênh lệch, duyệt phiếu, sửa sau chốt+audit before/after, cách ly tenant), vá `tenant-fixture.ts` thiếu dọn `cashier_shift` (vỡ FK). Tổng 594/594 test `apps/api` pass (2 flake `geo-http.spec.ts`/`icd10-http.spec.ts` đã biết, pass riêng).
**Verify Playwright qua Chrome thật** (chủ dự án đồng thời tự dùng thử dev server, báo trực tiếp từng lỗi) phát hiện + sửa **5 lỗi thật**: (1) dialog "Chốt ca" tự đóng ngay sau khi chốt xong, mất màn hình phiếu in (phụ thuộc dữ liệu sống `openShift` thay vì snapshot — đổi sang chụp lại `closingShift` lúc bấm nút); (2) mẫu in A4/A5 lệch hẳn kích thước giấy thật (dùng px màn hình 380/520 thay vì mm vật lý — đổi sang `190mm`/`128mm`, xác minh bằng `page.emulateMedia({media:'print'})`); (3) bản xem trước A4 bị flexbox co hẹp còn ~624px (thêm `shrink-0`); (4) không có nút thoát sau khi in (nút X header bị ẩn nhầm ở màn hình thành công); (5) theo yêu cầu bổ sung, "In phiếu" giờ tự thoát dialog ngay sau khi hộp thoại in trình duyệt đóng lại (`onAfterPrint`, không thể bỏ qua hộp thoại in bằng JS — giới hạn nền tảng web).
**Đảo hướng thiết kế qua `AskUserQuestion`**: chủ dự án phản biện đúng — chốt ca xong bị ép "Mở ca" ngay là vô lý ("kết ca nghĩa là hết ngày làm việc"). Chốt "cho xem trang, chỉ chặn thao tác tiền": bỏ hẳn `OpenShiftDialog` chặn toàn trang "Danh sách cần thu", thêm nút "Mở ca" chủ động, gate thật đặt ở `InvoiceDetailPage.handlePay()` (bấm "Thu tiền" mà chưa có ca mở → tự bật popup Mở ca, mở xong tự tiếp tục thu tiền).
**Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk web 480.68 kB (dưới ngưỡng 500kB). Playwright qua Chrome thật (dùng lại dev server đang chạy sẵn — chỉ chạm mtime `main.ts` một lần để buộc nạp lại permission catalog mới, đúng bài học lặp lại lần thứ 6 #087/#089/#104/#109/#110): mở ca → chốt ca (khớp) → khổ A4/A5 đúng tỉ lệ vật lý cả xem trước lẫn giả lập in → in tự thoát → danh sách phiếu chốt ca đúng → duyệt phiếu → không bị ép mở ca lại. "Xử lý chênh lệch"/"Mở khoá để sửa" xác nhận đầy đủ qua 37 test HTTP e2e, chưa lặp lại qua Playwright (không phát hiện thêm bug khi viết/chạy test). `docs/product/prd.md` (v1.3, BIL-05), `docs/ERD.md` (v1.39, mục 3.6, C24), `docs/DECISIONS.md` (#112), `docs/CHANGELOG.md`, `docs/TASK.md` đã cập nhật xong trong phiên này.
**Bổ sung bộ lọc "Thu ngân" ở "Danh sách phiếu chốt ca" — ĐÃ XONG (cùng ngày).** Chủ dự án xác nhận cần bộ lọc này (đã cố ý bỏ lúc code chính vì chưa có nguồn liệt kê thu ngân). Dùng lại `GET /users` có sẵn (không endpoint mới), lọc phía client theo `roleNames` chỉ giữ 2 vai trò có `cashier_shift.create` (`receptionist`/`clinic_admin`). Xem `docs/DECISIONS.md` #112 (đoạn "Việc còn treo — ĐÃ LÀM XONG"). **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch, Playwright qua Chrome thật xác nhận dropdown + lọc hoạt động đúng.

**Mã tự sinh NGẮN, TUẦN TỰ cho danh mục — ĐÃ XONG (03/09/2026), xem `docs/DECISIONS.md` #113.** Chủ dự án yêu cầu trực tiếp: mã tự sinh dạng `AC-3F9B2A1D` (ngẫu nhiên, 11-12 ký tự) quá dài, muốn khuôn `<2 ký tự><5 chữ số>` (vd `HV00001`), sinh theo đúng thứ tự đã tạo. Rà soát phát hiện 2 nhóm khác bản chất — chốt qua `AskUserQuestion` chỉ làm **Nhóm A "mã danh mục"** (6 category `reference_catalog` không nguồn dữ liệu chính thức — Học vị/Chức danh/Trạng thái-Hình thức làm việc/Đơn vị tính/Hình thức thanh toán + Nhóm dị nguyên/Dị nguyên + Ca làm việc), **không đụng Nhóm B "mã nghiệp vụ"** có tháng-năm (patient_code/encounter_no/booking_code/employee_code/department.code/invoice_no/cashier_shift.shift_no, khuôn đã chốt riêng). Bảng mới `global_code_sequence` (toàn hệ thống, khoá theo `prefix` — `reference_catalog`/`allergen_catalog` không có `tenant_id` nên không dùng lại được `code_sequence`); `work_shift` (tenant-scoped) dùng lại `code_sequence` có sẵn, tiền tố `CA`. Tiền tố 2 ký tự AI tự đề xuất, cố định tường minh (không cắt máy tên category như cũ — sửa luôn lỗi trùng "EM" giữa EMPLOYMENT_STATUS/EMPLOYMENT_TYPE): `HV`/`CD`/`TT`/`HL`/`DV`/`TM`/`ND`/`DN`/`CA`. **Đánh số lại TOÀN BỘ mã cũ đã tồn tại** (chủ dự án chọn, dù rủi ro hơn) — migration `20260903190000_short_sequential_catalog_codes` chỉ đổi dòng KHỚP ĐÚNG mẫu ngẫu nhiên cũ (regex, không đụng mã seed cố định như `CASH`/`FULL_TIME`/`ACTIVE`) + cascade UPDATE cùng lúc sang mọi cột đang lưu thẳng mã dạng chuỗi (không FK thật) đã rà soát đủ qua `schema.prisma`: `user_account.academic_title_code/position_code/employment_status_code/employment_type_code`, `exam_type_price.unit_code`, `encounter_service_item.unit_code`, `invoice_line.unit_code`, `payment.method`, `invoice.pending_payment_method`, `encounter.exam_type_unit` (deprecated). `allergen_group`/`allergen`/`work_shift` chỉ tham chiếu bởi `id` nên an toàn tuyệt đối, không cần cascade. Bỏ hẳn vòng lặp retry-khi-trùng-mã (không còn cần — cấp số atomic), xoá `WorkShiftDuplicateCodeError` không còn đường ném ra được. **Đã xác minh thật**: migration áp thật lên Postgres dev (không phải test container) — xác nhận qua query trực tiếp cascade đúng (`AC-DAE2DA4A`→`HV00001` cập nhật đúng cả 3 dòng `user_account` đang tham chiếu, mã seed/`TEST-*` giữ nguyên không đụng). `packages/core` 145/145 test, `apps/api` 576/594 pass + 18 skip (2 flake `geo-http.spec.ts`/`icd10-http.spec.ts` đã biết, pass 100% khi chạy riêng, không liên quan). Sửa 4 test assertion cũ còn kỳ vọng khuôn mã ngẫu nhiên. `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk web không đổi (480.68 kB, thay đổi thuần backend). **Chưa verify Playwright** — chỉ xác nhận qua HTTP e2e + query DB trực tiếp.

**"Cấu hình mẫu mã phát sinh" cho Nhóm B (mã nghiệp vụ có tháng-năm) — ĐÃ XONG (03/09/2026, cùng ngày), xem `docs/DECISIONS.md` #114.** Kế hoạch kỹ thuật duyệt qua `EnterPlanMode` trước khi code (rủi ro chính: `code_sequence` đang phục vụ 7 loại mã chạy thật kể cả tại pilot đã cài, phải tương thích ngược tuyệt đối). Chốt qua `AskUserQuestion`: đổi khuôn chỉ áp dụng mã tạo MỚI; bộ đếm tự reset theo chu kỳ mịn nhất trong khuôn (có `[Tháng]` → reset tháng, chỉ `[Năm]` → reset năm, không có → chạy liên tục như hiện tại); "Số bắt đầu đếm" chỉ đặt 1 lần rồi khoá vĩnh viễn; chèn token qua nút bấm; từng tenant tự cấu hình riêng. `code_sequence` thêm cột `period_key` (mặc định `''`, migration `20260903200000_code_sequence_period_key`, KHÔNG cascade/backfill — tenant chưa cấu hình chạy y hệt trước đó). `tenant_setting` thêm key `business_code_templates`. `BusinessCodeService` (module `clinic`) mới — điểm gọi DUY NHẤT thay 7 nơi gọi `formatDisplayCode`/`CodeSequenceRepository.next()` trực tiếp cũ (`patient`/`department`/`user-account`/`appointment` ×2/`reception` ×2/`invoice`/`cashier-shift`); tiền tố nội bộ (`BN`/`KP`/...) giữ nguyên làm khoá đếm, tách khỏi chữ hiển thị admin tự gõ. Cú pháp 5 token cố định `[Năm 2 số]`/`[Năm 4 số]`/`[Tháng]`/`[Ngày]`/`[Số đếm]`, hàm thuần đặt ở `packages/shared` (không phải `packages/core`, vì web cần gọi trực tiếp để xem trước không round-trip API — `apps/web` bị chặn import `@nexamed/core`). Pill mới "Cấu hình mẫu mã phát sinh" trong Cấu hình hệ thống, lazy. **Bug bundler thật lặp lại (#032/#091)**: Rollup không dò được export của file `business-code.ts` mới (cả hằng số lẫn hàm) qua `vite build` dù `tsc` sạch — khai lại 3 hàm xem trước THUẦN CLIENT tại `apps/web/src/features/clinic/business-code-preview.ts` (chỉ dùng xem trước, mã thật luôn do backend cấp). **Tiện phát hiện + sửa lỗi cấu trúc có sẵn từ trước** trong `docs/DECISIONS.md`: đoạn "Đã xác minh thật" của mục #111 bị lạc xuống cuối file từ một phiên trước — đã di chuyển lại đúng chỗ. **Đã xác minh thật**: `packages/shared` 20/20 test (+12, gồm bài test khuôn mặc định phải khớp đúng `formatDisplayCode` hiện tại), `apps/api` +3 test `code-sequence.repository.spec.ts` + `business-code-http.spec.ts` mới 11/11, toàn bộ 7 spec HTTP của module đã migrate KHÔNG một test nào phải sửa (đúng tiêu chí chấp nhận chính). `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk web 481.24 kB (dưới ngưỡng 500kB, pane mới tách chunk riêng 8.27 kB). Migration đã áp thật lên Postgres dev. **Chưa verify Playwright/trình duyệt thật cho màn hình mới** — chỉ xác nhận qua HTTP e2e + build/typecheck, để dành nếu cần.

**Bug thật "Mở ca" crash 500 (tái phát từ #114) — ĐÃ SỬA XONG + redesign nút Mở ca/Chốt ca (03/09/2026, cùng ngày), xem `docs/DECISIONS.md` #115.** Chủ dự án tự dùng thử "Chốt ca" bấm "Mở ca" gặp lỗi 500. Nguyên nhân: khuôn mã MẶC ĐỊNH của cả 7 loại mã nghiệp vụ (#114) chứa sẵn token `[Tháng]` (chỉ để hiển thị giống khuôn cũ) nhưng `BusinessCodeService.generate()` không phân biệt "khuôn mặc định" với "khuôn tenant chủ động lưu" — tính `periodKey` theo tháng ngay cả khi chưa ai cấu hình gì, phá vỡ đúng lời hứa tương thích ngược đã chốt ở #114. `cashier_shift` (module mới, mọi mã sinh trong tháng 09) lộ ra sớm nhất: bộ đếm chu kỳ mới bắt đầu lại từ 1, trùng mã cũ đã cấp ở bộ đếm liên tục → vi phạm UNIQUE `shift_no` → 500 — **lỗi tiềm ẩn cho cả 7 loại mã, không riêng cashier_shift**. Sửa: chỉ tính `periodKey` thật khi tenant THẬT SỰ đã lưu cấu hình riêng, khuôn mặc định luôn ép `periodKey=''`. Cùng phiên: redesign nút "Mở ca" (nút nhỏ ẩn trong thanh công cụ → banner xanh đậm full-width) và "Chốt ca" (viền nhạt → nền đặc `amberSolid` + icon, variant mới trong `Button.tsx`); vá 2 bug phụ (`GET /cashier-shifts/current` thiếu `handoverNote` của ca trước — người mở ca không đọc được ghi chú bàn giao; module `cashier_shift` thiếu nhãn tiếng Việt ở "Vai trò & Phân quyền"). **Verify Playwright #113/#114 hoàn tất cùng lúc** (giao song song đầu phiên) — cả hai đúng thiết kế, không bug nghiệp vụ; phát hiện thêm 1 sự cố hạ tầng không liên quan code (Vite dep pre-bundle kẹt, `@tanstack/react-virtual` 504 khi tải `/appointments`) — đã sửa bằng xoá cache `.vite` + restart web dev server. **Đã xác minh thật**: mở ca thật qua API thành công, số tiếp đúng mạch (không nhảy về 1, xác nhận qua query DB trực tiếp không còn `code_sequence` rác); 254 test liên quan pass (business-code/cashier-shift/patient/appointment/reception/billing/user-account), không regression; `pnpm -w typecheck` sạch toàn workspace.

**"Yêu cầu mở ca trước khi thu tiền" — công tắc tắt gate Chốt ca cho phòng khám nhỏ (04/09/2026), xem `docs/DECISIONS.md` #116.** Chủ dự án hỏi UX "Chốt ca" (#112) khi bác sĩ tự tiếp nhận-thu ngân-khám bệnh một mình — không có ai đối soát chéo thì gate "phải mở ca mới thu tiền" chỉ là ma sát. Thêm `tenant_setting.cashier_shift_required_enabled` (mặc định BẬT — giữ đúng hành vi hiện tại, không tự suy luận theo số tài khoản vì đây là dữ liệu tiền), đặt trong pill "Cấu hình thanh toán" cạnh "Chế độ đối soát Mù". Không migration/permission mới. Tắt: `InvoiceDetailPage.handlePay()` bỏ gate (backend vốn không đòi ca mở, gate thuần frontend); `InvoiceListPage.tsx` **ẩn hẳn** banner "Chưa mở ca hôm nay" + nút "Chốt ca" (chốt qua `AskUserQuestion` — ẩn hẳn, không giữ nút nhỏ tuỳ chọn). Trang "Phiếu chốt ca" riêng (`/billing/cashier-shifts`) giữ nguyên, vẫn đóng được ca lỡ còn mở từ trước khi tắt. **Đã xác minh thật**: `clinic-http.spec.ts` +3 test (32/32), tổng 612 test `apps/api` (16 flake `role_permission` race đã biết khi chạy song song, pass 100% khi chạy riêng). `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk web 481.62 kB.

**Chỉnh văn bản 2 công tắc + verify Playwright/test thu tiền thật hoàn tất (04/09/2026, cùng ngày)**: đổi mô tả "Yêu cầu mở ca trước khi thu tiền" ngắn gọn hơn theo yêu cầu chủ dự án; đổi tên "Chế độ đối soát Mù" → "Đối soát độc lập" (chỉ đổi câu chữ, không đổi hành vi/mặc định). Verify qua Chrome thật (`dev.admin`): text đúng, ẩn/hiện banner "Chưa mở ca hôm nay" + nút "Chốt ca" đúng cả 2 chiều bật/tắt. **Test thu tiền thật đầu-cuối**: tạo 1 bệnh nhân + 1 lượt khám + phiếu thu 250.000đ qua HTTP API (không SQL) → tắt công tắc → bấm "Thu tiền & In phiếu" trên phiếu thật → **thành công ngay, không bị chặn bởi bất kỳ màn "Mở ca" nào** — đúng thiết kế (gate thuần frontend). Không lỗi console bất thường. Dữ liệu test đã dọn sạch đúng quy trình nghiệp vụ (Đánh dấu chưa thu → Hủy lượt khám → phiếu tự đóng `CANCELLED`), khôi phục công tắc về BẬT. Xem `docs/DECISIONS.md` #116.

**"Đa thu ngân" — nhiều ca thu ngân chạy song song (04/09/2026, cùng ngày), xem `docs/DECISIONS.md` #117.** Chủ dự án hỏi tiếp: phòng khám có 3 thu ngân làm việc CÙNG LÚC (mỗi người 1 két riêng) — "Chốt ca" hiện tại (chỉ 1 két dùng chung) có đáp ứng được không. Lập kế hoạch qua `EnterPlanMode` trước khi code (chạm ràng buộc DB + cách gán tiền vào ca). Thêm `tenant_setting.cashier_shift_multi_cashier_enabled` (mặc định TẮT — giữ nguyên "1 két dùng chung"). Migration `20260904090000_cashier_shift_multi_cashier`: đổi partial unique index `cashier_shift` từ theo-tenant sang theo-từng-thu-ngân; `payment` thêm cột `cashier_shift_id` (nullable). **Giữ 2 luồng tính tổng kết ca song song, không hợp nhất** (TẮT: theo khoảng thời gian, code không đổi; BẬT: theo FK) — tránh vỡ "Tính toán lại" cho ca cũ trước migration. **An toàn đua tranh khi mở ca** chuyển từ ràng buộc DB sang `pg_advisory_xact_lock` ở tầng ứng dụng (lần đầu dùng kỹ thuật này trong dự án) — bắt buộc phải có vì test race có sẵn dựa chính vào constraint sắp bị nới lỏng. `billing` ⇄ `cashier-shift` phụ thuộc module 2 chiều thật (port mới `CASHIER_SHIFT_READER_PORT`) — dùng `forwardRef()` lần đầu trong dự án, đã xác nhận DI khởi động thật thành công qua `node dist/main.js`. Frontend gần như không đổi (`useCurrentCashierShiftQuery()` giữ nguyên contract, chỉ đổi ý nghĩa "của ai" ở backend).
**Đảo hướng 1 phần #116, cùng ngày**: hỏi phân tích tương tác 2 công tắc phát hiện lỗi — nút "Mở ca ngay" duy nhất bị ẩn theo "Yêu cầu mở ca", nên Bật "Đa thu ngân" + Tắt "Yêu cầu mở ca" thì không ai bấm mở ca được ở đâu cả, tính năng mới vô dụng. Sửa: "Yêu cầu mở ca" giờ CHỈ gate "Thu tiền", không còn quyết định hiện/ẩn banner+nút "Mở ca"/"Chốt ca" — chỉ đổi câu chữ theo bắt buộc/tuỳ chọn. Tiện sửa khối "Đã hoàn" ở thanh tổng kết `/billing` luôn hiện (kể cả 0đ) thay vì ẩn gây lệch layout xấu.
**Đã xác minh thật**: `cashier-shift-http.spec.ts` +6 test (43/43 — race 2 người khác nhau mở cùng lúc → cả 2 thành công khi BẬT, cùng 1 người mở 2 lần vẫn 409, thu tiền song song không lẫn ca, chốt ca người này không ảnh hưởng người kia), `clinic-http.spec.ts` +2 test (34/34). Tổng 620 test `apps/api` (602 pass trực tiếp + 18 flake `role_permission` đã biết, pass 100% khi chạy riêng). `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk web 481.62 kB (không đổi). OpenAPI + web codegen đã sinh lại. Playwright qua Chrome thật xác nhận đủ cả 4 tổ hợp trạng thái 2 công tắc + fix "Đã hoàn" (dọn 1 ca thu ngân treo có sẵn trên dev DB từ trước, không phải do phiên này tạo ra, để verify đúng nghĩa). Đã khôi phục cả 2 công tắc về mặc định sau khi verify.

**Bảo mật: sửa bug thu hồi quyền không có tác dụng (backend) + 19 file frontend hardcode tên vai trò (04/09/2026, ngoài kế hoạch, chủ dự án phát hiện + yêu cầu rà soát toàn hệ thống), xem `docs/DECISIONS.md` #119.** Bug gốc: `findScopesForUserPermission()` (nguồn tra quyền DUY NHẤT của `PermissionGuard`, mọi endpoint) thiếu `deletedAt: null` — quyền thu hồi/vai trò gỡ khỏi tài khoản qua "Vai trò & Phân quyền" vẫn hiệu lực vĩnh viễn. Sửa + thêm `findAllPermissionsForUser()` + `currentUserSchema.permissions` + hook `usePermission.ts` (web) + sửa 19 file hardcode vai trò + route guard bọc toàn bộ `router.tsx` (trước chỉ ẩn menu, gõ URL vẫn vào được) + tách nhóm "Quản trị" thành 7 mục con theo đúng quyền riêng. `.claude/docs/security-audit.md` mở rộng quy tắc chống hardcode sang frontend, bắt buộc test hồi quy cho hàm tra quyền mới. **Đã xác minh thật**: `apps/api` 611/611 (2 flake đã biết, pass riêng) gồm test hồi quy mới xác nhận bắt được bug, `encounter-http.spec.ts` 61/61 (break-glass HTTP không đổi), `pnpm -w typecheck/build` sạch, chunk web 486.53 kB raw/141.77 kB gzip.

**Popup "Tổng hợp ca khám hôm nay" khi Đóng ca (04/09/2026, cùng ngày, ngoài kế hoạch), xem `docs/DECISIONS.md` #118.** `DoctorEndShiftDialog.tsx` (#094) giờ mở đầu bằng khối tổng hợp 1 ngày làm việc trước khi bác sĩ xác nhận đóng ca — 5 chỉ số (Đã gọi khám/Đã hoàn thành/TB thời gian-ca/Huỷ khám/Đơn thuốc đã kê) tính theo CẢ NGÀY hôm nay, đặt trên panel nền `brand-teal` kèm lời chào cá nhân hoá "BS. {tên} đã hoàn thành ngày làm việc hôm nay với:". Mockup Artifact duyệt nhiều vòng (3 quyết định phạm vi chốt qua `AskUserQuestion` + nhiều vòng chỉnh màu/độ đậm chữ trực tiếp trên mockup tĩnh trước khi code, có tra `/ui-ux-pro-max` để đối chiếu nguyên tắc bố cục KPI-row) trước khi code. Backend: `GET /doctor-availability/{doctorId}/shift-summary` (cùng quyền/scope `setStatus()`), `EncounterRepository.getShiftCounts()` + `PrescriptionRepository.countSignedForDoctorToday()` (export thêm từ `EncounterModule`, dùng chung đúng tiền lệ #042), hàm thuần `computeShiftSummary()` (`packages/core`). **Đã xác minh thật**: `packages/core` +4 test (151/151), `apps/api` +7 test (619/626, 7 skip flake `geo-http.spec.ts` đã biết), `pnpm -w typecheck/lint/build` sạch, chunk `DoctorEndShiftDialog` tách riêng không ảnh hưởng chunk khởi động. Playwright qua Chrome thật (tài khoản test tạo/xoá qua HTTP API bằng `dev.admin`) xác nhận khớp gần như tuyệt đối với mockup đã duyệt, không lỗi console ngoài `401 /auth/refresh` đã biết.

**Redesign "Chốt ca" (Thu ngân) — ĐÃ CODE XONG (04-05/09/2026, `docs/DECISIONS.md` #120).** Mockup Artifact duyệt trước (chỉ phạm vi dialog 4 bước + màn xác nhận/in phiếu, giữ nguyên luồng/logic), rồi code thẳng vào `CloseShiftDialog.tsx`/`DenominationCounter.tsx`/`CashierShiftReceiptView.tsx`. 2 điểm đã lên: (1) cột "Số tờ" ở Bước 2 (và cả popup Mở ca, dùng chung component) đổi sang capsule trắng/xám nhẹ gộp trừ/số lượng/cộng; (2) trường tính toán mới **"Tổng doanh thu ca"** = Tiền mặt (đã trừ hoàn) + Phi tiền mặt (đã trừ hoàn, dữ liệu backend `computeCashierShiftTotals()` đã netted sẵn theo từng `payment.method` — không cần đổi API), KHÔNG cộng Vốn đầu ca — thành hero chính ở Bước 1 + dòng đầu phiếu in, "Tiền mặt dự kiến trong két" cũ lùi thành ô phụ.

**Chuỗi chỉnh sửa trực tiếp qua nhiều vòng phản hồi trên `pnpm dev` (cùng ngày)**: (a) **1 lỗ hổng thật** — "Đối soát mù" bật chỉ ẩn ô "dự kiến trong két" nhưng để lộ "Tổng thu tiền mặt"/"Chi tiền mặt", cộng trừ tay với "Vốn đầu ca" (luôn hiện) là suy ra được ngay, mất tác dụng ẩn — đã ẩn luôn 2 ô đó khi bật, chỉ giữ hiện số phiếu; (b) dòng "Tổng tiền mặt đếm được" đổi sang màu thương hiệu chính `bg-blue-600` (không dùng `brand-teal` — token đó chỉ dành lựa chọn 1-trong-nhiều theo `ui-guidelines.md`), khung popup dãn +20%; (c) **1 bug thật** — nút trừ/cộng ép `Button` dùng chung với `px-0` thua trận CSS specificity trước `px-4` mặc định khiến icon bị nuốt mất (nút hiện ô trắng rỗng) — sửa bằng `<button>` tay riêng, đúng ngoại lệ "control đổi giá trị, không phải hành động nghiệp vụ"; (d) thu nhỏ hero "Tổng doanh thu ca" ~10%; (e) **dựng lại stepper 4 bước** — phát hiện lỗi thật: hàng circle và hàng nhãn chữ tính theo 2 công thức `flex-1` khác nhau nên không bao giờ thẳng hàng chính xác, sửa bằng chuyển sang lưới `grid-cols-4` chung 1 hệ toạ độ (thẳng hàng tuyệt đối), thêm dấu tick cho bước đã qua, đổi màu `blue-600`→`emerald-600` (theo yêu cầu, đúng token "Thành công" có sẵn), tăng cỡ chữ nhãn.

Đã xác minh mọi vòng: `pnpm -w typecheck/lint` sạch, build web sạch, chunk khởi động không đổi (486.54 kB). Không migration/schema/API nào đổi. **Chưa verify Playwright** — toàn bộ xác nhận qua chủ dự án tự xem trực tiếp trên `pnpm dev`.

## Đã có

- `CLAUDE.md` — quy tắc và ràng buộc dự án (đã bổ sung quy định viết code hướng tái sử dụng).
- `.claude/docs/` — 7 tài liệu kỹ thuật đã chốt.
- `docs/product/prd.md`, `docs/product/plan.md` — đã có nội dung đầy đủ.
- `docs/ERD.md` — sơ đồ quan hệ dữ liệu đầy đủ.
- **S1-01 xong**: monorepo pnpm (`apps/api` NestJS, `apps/web` React+Vite, `packages/core`, `packages/shared`), cấu trúc thư mục theo `.claude/docs/project-structure.md`, ESLint boundary rule chặn `packages/core` import NestJS/Prisma/React/`process.env`, CI (`.github/workflows/ci.yml`).
- **S1-02 xong**: `apps/api/prisma/schema.prisma` với 7 bảng nền tảng (`tenant`, `tenant_setting`, `room`, `user_account`, `user_role`, `code_sequence`, `audit_log`), migration đầu tiên (`prisma/migrations/20260807090207_init`), script kiểm 8 cột bắt buộc (`scripts/check-mandatory-columns.mjs`, nối vào CI), Zod env validation ở `apps/api/src/config` (đã kiểm chứng: API crash đúng khi thiếu `DATABASE_URL`/`JWT_SECRET`/`ENCRYPTION_KEY`, chạy được khi đủ), `docker-compose.yml` gốc cho Postgres local dev, `apps/api/.env.example`. `packages/shared` có enum 5 vai trò (`roles.ts`).
- **Postgres nâng lên bản 18** (từ 16) để dùng `uuidv7()` thật cho mọi `id` — xem `docs/DECISIONS.md` #009.
- **S1-03 xong**: role Postgres riêng `nexamed_app` (không superuser/BYPASSRLS/DELETE) cho API runtime, tách khỏi role `nexamed` (chỉ dùng cho migration qua `MIGRATE_DATABASE_URL` + `apps/api/scripts/with-migrate-url.mjs`); RLS thật trên 6 bảng có `tenant_id`; `CHECK (version >= 1)`; `PrismaService` + `UnitOfWorkService` (`apps/api/src/infrastructure/persistence`, mở transaction + `SET LOCAL app.current_tenant_id`); `TenantContextMiddleware` (tạm đọc header, chờ JWT ở S1-04 — xem `docs/DECISIONS.md` #012) wire global vào `AppModule`. **Đã xác minh thật bằng integration test** (`tenant-isolation.spec.ts`, 4/4 pass trên Postgres thật): cách ly tenant đúng, không set context thì lỗi thay vì rò dữ liệu, không xoá được, không insert xuyên tenant được.
- **RBAC + Data Scope xong (S1-04b)** — thay hoàn toàn mô hình "5 vai trò cứng" theo yêu cầu chủ dự án (`docs/DECISIONS.md` #013-#016): bảng `department`, `role`, `permission` (danh mục toàn hệ thống, 23 permission), `role_permission` (ma trận `role × permission → data_scope`, 4 mức `none`/`personal`/`department`/`global`, không có `branch`), migration `*_rbac_data_scope` (RLS + CHECK + REVOKE cho bảng mới). Ma trận mặc định + logic seed đặt ở `packages/core/src/rbac/permissions.ts` (nguồn sự thật, dùng chung seed script/guard/web sau này). `seedDefaultRolesForTenant`/`seedPermissionCatalog` ở `apps/api/src/infrastructure/persistence/`. **Đã xác minh thật** (`rbac.spec.ts`, 4/4 pass): seed đúng 5 vai trò/tenant, RLS cách ly `role`/`role_permission` theo tenant, `permission` đọc được nhưng không ghi được qua role app, `doctor.encounter.read = global` đúng thiết kế (không break-glass cho tiền sử — khớp PRD ENC-01 P0).
- `docs/design/` — có `UI_GUIDELINE.md`, `AI_AVOID_RULES.md`.
- `docs/product/future-modules-reference.md` — tài liệu tham khảo (chưa chốt) cho Dược/kho, Viện phí, danh mục dùng chung, ý tưởng luồng khám mở rộng — dùng khi tới v2/v2.1/v3, đã ghi rõ mâu thuẫn với schema/state machine v1 đã chốt, không tự áp dụng.
- **Auth xong (S1-04)** — JWT access token 15 phút + refresh token httpOnly cookie xoay vòng mỗi lần refresh, Argon2id, khoá tài khoản tạm sau 5 lần sai/15 phút (`docs/DECISIONS.md` #019-#020). Bảng mới `user_session` (rotation chain + phát hiện reuse — dùng lại refresh token đã rotate thì thu hồi toàn bộ phiên của user), `user_account` thêm `failed_login_count`/`last_failed_login_at`/`locked_until`, migration `*_auth_sessions`. Module `apps/api/src/modules/iam/` (`AuthController`: `POST /auth/login|refresh|logout`, `AuthService`, `TokenService`, `SessionRepository`, `UserAccountAuthRepository`) + `packages/core/src/iam/lockout.ts` (nguồn sự thật ngưỡng khoá, unit test 9/9) + `packages/core/src/errors/` (khởi đầu lớp lỗi nghiệp vụ — `DomainError` + 5 lỗi auth cụ thể). `TenantContextMiddleware` đổi hẳn sang đọc JWT access token thật (không còn header tạm — hoàn thành cam kết ở `docs/DECISIONS.md` #012). Thêm `JwtAuthGuard` (xác thực tối thiểu, viết + test sẵn, **chưa áp vào controller nào** vì chưa có controller nghiệp vụ), `ResponseInterceptor`/`DomainExceptionFilter` (response envelope `{data,meta}`/`{error}` theo `architecture.md` — áp dụng lần đầu vì đây là controller thật đầu tiên của hệ thống), rate limit riêng cho `/auth/login` (`@nestjs/throttler`, 10 request/phút/IP). **Đã xác minh thật** (`auth.spec.ts`, 9/9 pass trên Postgres thật; `jwt-auth.guard.spec.ts`, 5/5 pass): login đúng/sai mật khẩu, khoá tài khoản đúng ngưỡng, tài khoản vô hiệu hoá, sai `tenantId` bị coi như sai thông tin đăng nhập (không lộ tồn tại xuyên tenant), refresh rotation, phát hiện reuse thu hồi toàn bộ phiên, logout, token hỏng/rác.

- **Break-glass xong (S1-04c)** — `POST /api/v1/break-glass` (yêu cầu access token — nơi dùng thật đầu tiên của `JwtAuthGuard`): xác thực lại mật khẩu (tái dùng `UserAccountAuthRepository.findById` + argon2, tái dùng `InvalidCredentialsError` — không tạo lỗi mới) + lý do bắt buộc → tạo `break_glass_session` (bảng đã có sẵn từ S1-04b, **không cần migration mới**) → ghi `audit_log` (`break_glass.request`) → gọi `NotificationPort.send` (no-op). Thời hạn phiên đọc `tenant_setting` key `break_glass_duration_minutes`, mặc định 120 phút nếu tenant chưa cấu hình. `packages/core/src/iam/break-glass.ts` (hàm thuần `computeExpiresAt`/`isSessionActive`, unit test 3/3) + `packages/core/src/ports/notification.port.ts` (`NotificationPort` — chỉ 1/6 port của S1-06 được kéo trước, giống cách S1-04 đã kéo trước một phần `errors/`) + adapter no-op (`apps/api/src/infrastructure/notification/noop.adapter.ts`). `BreakGlassService.tryConsume()` — primitive sẵn sàng cho guard `data_scope` thật của S2 (kiểm tra phiên còn hiệu lực + ghi `audit_log` `break_glass.access`), tự mở transaction riêng, chưa có guard nào gọi tới. **Đã xác minh thật** (`apps/api` `break-glass.spec.ts`, 6/6 pass trên Postgres thật + kiểm thủ công qua curl): đúng mật khẩu tạo phiên đúng hạn mặc định/tuỳ chỉnh, sai mật khẩu không tạo phiên không gọi notify, `tryConsume` đúng entity trong hạn mới `granted:true`, entity khác/hết hạn thì `false`, không có access token bị 401.

- **Audit log xong (S1-05)** — không có gì mới ở bảng/quyền DB (đã xong từ S1-03). Hai cơ chế ghi tách theo loại thao tác (`docs/DECISIONS.md` #021 — lý do: interceptor HTTP không thể tham gia transaction Prisma mà service tự mở/đóng kín bên trong): **ghi** → gọi tường minh `writeAuditLog(tx, ...)` trong transaction service, dời từ `modules/iam/` sang `apps/api/src/infrastructure/persistence/audit-log.helper.ts` (hạ tầng dùng chung, mọi module domain tương lai gọi được mà không phải import xuyên qua `iam`); **xem** → `AuditViewInterceptor` + decorator `@AuditView('entityType')` (`apps/api/src/common/`), tự mở transaction riêng sau khi handler trả về thành công, không ghi khi handler lỗi, lỗi ghi audit tự nổi lên thành response lỗi (không nuốt). `.claude/docs/security-audit.md` mục Audit log đã cập nhật quy ước này cho S2 theo. **Đã xác minh thật** (`audit-view.interceptor.spec.ts`, 3/3 pass trên Postgres thật) — chưa áp vào controller nào (chưa có controller nghiệp vụ, S2). Toàn bộ 28 test cũ (auth/break-glass/rbac/tenant-isolation/jwt-auth-guard) vẫn pass nguyên sau khi dời helper.

- **`packages/core` hoàn thiện xong (S1-06)** — khung entity thuần (`packages/core/src/entity/base-entity.ts`): `BaseEntity` (8 cột bắt buộc), `AppendOnlyEntity` (audit_log/break_glass_session), `SignableEntity` (dữ liệu lâm sàng ký được, `supersedesId`/`amendmentReason`) + hàm thuần `isSigned()` (nguồn sự thật duy nhất cho quy tắc "đã ký thì bất biến", unit test 2/2). Đủ 6 port ở `packages/core/src/ports/` — 5 port còn lại (`NotificationPort` đã có từ S1-04c): `StoragePort` (save/read/delete, adapter v1 `LocalDiskStorageAdapter` ghi thật ra disk dưới `STORAGE_DIR/<tenantId>/<key>`, có chặn path traversal), `EventBusPort` (publish/subscribe, adapter `InMemoryEventBusAdapter` — đồng bộ, handler lỗi ném lên để service rollback transaction, đăng ký handler là wiring lúc khởi động không phải state theo request), `SignaturePort` (adapter no-op trả `signaturePayload: null`), `InsuranceGatewayPort` (adapter no-op ném `NOT_IMPLEMENTED`, đúng như bảng port/adapter ở `project-structure.md` — chưa có service nào gọi, ngoài phạm vi v1), `PatientIdentityPort` (adapter `SameTenantPatientIdentityAdapter` trả chính `patient.id`, khớp `global_patient_ref` luôn null ở v1). Đăng ký DI qua `apps/api/src/infrastructure/ports.module.ts` (`@Global()`, tách khỏi `NOTIFICATION_PORT` vẫn đăng ký riêng trong `IamModule` như cũ — không đụng lại), wire vào `AppModule`. Thêm `STORAGE_DIR` vào `env.schema.ts`/`.env.example` (mặc định `./storage`, gitignore). **Đã xác minh thật**: `base-entity.spec.ts` (2/2), `local-disk.adapter.spec.ts` (4/4, ghi/đọc/xoá file thật + chặn key chứa `..`), `in-memory.adapter.spec.ts` (3/3); build thật rồi chạy `node dist/main.js` — log xác nhận `PortsModule dependencies initialized` và `Nest application successfully started` qua NestJS DI container thật (khác các test trước chỉ `new Service(...)` tay, không qua Nest). Không có folder `patient-identity/` sẵn trong cây thư mục minh hoạ ở `project-structure.md` (chỉ liệt kê storage/eventbus/signature/insurance/notification) — tạo thêm theo đúng pattern "một thư mục cho mỗi port" đã có, không phải đổi cấu trúc đã chốt.

- **Web app shell xong (S1-08)** — hỏi chốt luồng/menu với chủ dự án trước khi code (theo yêu cầu mới: luôn hỏi trước khi xây mỗi chức năng): menu sidebar kết hợp theo luồng nghiệp vụ **và** ẩn/hiện theo vai trò; có Dashboard chung dạng empty-state (không bịa số liệu, vì patient/appointment/encounter chưa tồn tại tới S2-S3); Quản trị là mục riêng trong sidebar (không gộp avatar), chỉ hiện với `clinic_admin`/`system_admin`; chỉ hiện menu module đã có backend thật (Đặt lịch/Tiếp nhận/Khám bệnh/Kê đơn chưa hiện, thêm dần đúng sprint); màu/token theo `.claude/docs/ui-guidelines.md` (không theo bảng hex/Geist ở `docs/design/UI_GUIDELINE.md` — tài liệu đó chỉ tham khảo layout/menu chung, không phải màu). `tenantId` nạp runtime qua `apps/web/public/config.json` (gitignore, có `config.example.json`) — sửa trực tiếp lúc deploy on-prem, không cần rebuild (chốt xong điểm treo ở `docs/DECISIONS.md` #020).
  Backend mở rộng nhỏ đi kèm (`docs/DECISIONS.md` #022): `loginResponseSchema.user` thêm `roles`; thêm `GET /auth/me` (`JwtAuthGuard`, lần dùng thật thứ hai sau break-glass) để web khôi phục danh tính/vai trò lúc reload trang (chỉ còn refresh cookie, chưa có `user` trong bộ nhớ).
  Frontend: Tailwind v4 qua `@tailwindcss/vite` (không cần `postcss.config.js`/`tailwind.config.js` riêng — khác giả định Tailwind v3 ban đầu), `react-router-dom` v7, `@phosphor-icons/react`; API client tối thiểu tự viết (`apps/web/src/shared/api/client.ts`, chỉ đủ cho auth flow, S1-09 thay hẳn bằng client sinh từ OpenAPI); Zustand auth store (session state, không phải server entity data); `LoginPage`/`RequireAuth`/`AppBootstrap`; `AppShell`/`Sidebar`/`UserMenu`; `DashboardPage`/`AdminPage` (empty state); `Button`/`EmptyState` dùng chung ở `shared/ui`.
  Phát hiện + sửa 1 race condition lúc kiểm bằng trình duyệt thật: React StrictMode (dev) gọi effect 2 lần khiến `AppBootstrap` bắn 2 request `/auth/refresh` gần đồng thời, có thể đụng cơ chế phát hiện reuse-token (S1-04) — chặn bằng `useRef` guard (chỉ ảnh hưởng dev, StrictMode không chạy ở production build, nhưng vá cho chắc).
  **Đã xác minh thật**: `pnpm -w lint/typecheck/build` sạch; 45 test `apps/api` pass; chạy `pnpm dev` thật + Playwright (headless Chromium — môi trường chưa có project skill `run`, tự cài để dùng) chụp ảnh xác nhận đúng luồng: chưa đăng nhập → `/login`; đăng nhập đúng → Dashboard đúng token màu (sidebar `bg-slate-900`), thấy Tổng quan + Quản trị (user test có vai trò `clinic_admin`); F5 giữ nguyên phiên (refresh+`/me` hoạt động); đăng xuất → `/login`.

- **S1-09 xong** — api client sinh từ OpenAPI thay cho bản `fetch` tối giản tự viết ở S1-08, TanStack Query cache-key infra (xem `docs/DECISIONS.md` #023). `apps/api/scripts/generate-openapi.ts`: dựng `OpenAPIRegistry` (`@asteasolutions/zod-to-openapi` v7, tương thích `zod@^3`) trực tiếp từ Zod schema đã có ở `@nexamed/shared` — không có contract thứ hai phải đồng bộ tay — xuất `apps/api/openapi/openapi.json` (5 endpoint: `/auth/login|refresh|logout|me`, `/break-glass`, đúng envelope `{data,meta}`/`{error}`). `packages/shared/src/auth.ts` thêm `refreshResponseSchema`/`logoutResponseSchema` làm nguồn cho hai endpoint trước đây trả object ad-hoc không có schema. `apps/web`: `openapi-typescript` sinh `src/shared/api/openapi-schema.d.ts` từ file JSON trên (không cần API đang chạy); `src/shared/api/client.ts` viết lại bằng `openapi-fetch` (giữ nguyên `ApiError`, việc bóc envelope, cách đính `Authorization` bearer qua middleware thay vì tham số mỗi lời gọi); `src/shared/api/query-keys.ts` — factory cache key `[tenantId, domain, ...]` đúng `.claude/docs/architecture.md`, chưa có nơi dùng thật (chờ S2 có server entity data qua `patient`/`appointment`) nhưng sẵn sàng. Cả `openapi.json` lẫn `openapi-schema.d.ts` đều **commit vào repo** (không gitignore) — `apps/web` phải build độc lập, không phụ thuộc `apps/api` đang chạy hay build trước. `auth.api.ts` đổi chữ ký `getMe()` (bỏ tham số `accessToken`). **Đã xác minh thật**: `pnpm -w lint/typecheck/build` sạch; 45 test `apps/api` + 14 test `packages/core` pass nguyên (không đổi hành vi backend); chạy `pnpm dev` thật + Playwright xác nhận qua ảnh chụp: đăng nhập → Dashboard; F5 giữ nguyên phiên qua client mới; đăng xuất → `/login`; không lỗi console ngoài dự kiến.

- **S2-01 xong (module `patient`)** — CRUD (tạo/xem/sửa; không có xoá, không có yêu cầu PRD) + mã hoá `national_id`. Cũng là lúc hiện thực **`PermissionGuard` thật** (việc treo từ Sprint 1) vì đây là controller nghiệp vụ đầu tiên — xem chi tiết cơ chế guard ở `.claude/docs/security-audit.md` mục Data Scope, chi tiết đầy đủ implementation ở `docs/TASK.md` S2-01. Trước khi viết migration, đã hỏi và xác nhận với chủ dự án vá lệch `docs/ERD.md` ↔ `.claude/docs/data-model.md` cho bảng `patient` (thêm `address_json`, `identity_verified_at` — xem `docs/DECISIONS.md` #024). `IamModule` đổi thành `@Global()` để `PermissionGuard` (ở `CommonModule`) resolve đúng `BreakGlassService` khi được dùng từ module khác (patient hôm nay, mọi module domain sau này). `AuditViewInterceptor` đã có nơi dùng thật đầu tiên (`GET /patients/:id`). **Đã xác minh thật**: `patient-http.spec.ts` (10/10, HTTP e2e — CRUD, trùng CCCD → 409, optimistic lock, permission guard chặn đúng + break-glass sẵn sàng, cách ly tenant qua guard+RLS → 404 không phải 403), `code-sequence.repository.spec.ts` (3/3), `pii-encryption.spec.ts` (5/5), `data-scope.spec.ts`/`format-display-code.spec.ts` (`packages/core`). Tổng 63 test `apps/api` + 20 test `packages/core` pass, không regress từ Sprint 1. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại cho 4 endpoint `/patients*` mới.

- **S2-02 xong (tìm kiếm bệnh nhân, PAT-02)** — `GET /api/v1/patients?q=...` tìm theo tên (không dấu, không phân biệt hoa/thường), số điện thoại, mã bệnh nhân, cùng một ô nhập. `packages/core/src/search/strip-vietnamese-diacritics.ts` chuẩn hoá `q` ở tầng ứng dụng, khớp đúng hàm SQL `nexamed_unaccent_lower()` (bọc `unaccent()` thành IMMUTABLE) sinh cột `patient.search_key` (Postgres `GENERATED ALWAYS AS (...) STORED`, migration `20260811055006_patient_search_s2_02`, bật thêm extension `pg_trgm`/`btree_gin`). GIN kết hợp `(tenant_id, search_key gin_trgm_ops)` đúng `docs/ERD.md` mục 5. Lúc scaffold migration, Prisma tự đề xuất xoá nhầm `patient_tenant_id_phone_idx` (index raw-SQL không khai báo trong schema.prisma bị coi là "drift") — đã phát hiện và bỏ dòng đó, bài học cho các migration sau trên bảng có index raw-SQL-only. **Đã đo hiệu năng thật với 50.000 bản ghi** (`apps/api/scripts/perf-patient-search.ts`, chạy tay, không vào CI): cả 3 kiểu tìm dưới 35ms — đạt mục tiêu "<1 giây" của PRD với biên độ lớn. Ở quy mô này Postgres planner chọn Seq Scan thay vì GIN theo mặc định (bảng đủ nhỏ để nằm gọn trong shared buffers — quyết định đúng của planner, không phải lỗi); đã xác nhận GIN index tồn tại/dùng được thật bằng cách ép `enable_seqscan=off`. **Đã xác minh thật**: `patient-http.spec.ts` thêm 6 test tìm kiếm (16/16 tổng), `strip-vietnamese-diacritics.spec.ts` (5/5). Tổng 69 test `apps/api` + 25 test `packages/core` pass. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại.

- **S2-03 xong (chống trùng mềm, PAT-03)** — `GET /api/v1/patients/check-duplicate?fullName=&dob=`: endpoint riêng gọi TRƯỚC khi tạo hồ sơ (đã hỏi và chốt cách thiết kế với chủ dự án — không suy diễn: chọn phương án endpoint check riêng thay vì gộp cảnh báo vào response tạo mới, để không đổi contract `POST /patients` đã test kỹ). Khớp CHÍNH XÁC tên đã chuẩn hoá (`searchKey`, tái dùng cột generated + `stripVietnameseDiacritics` của S2-02) + ngày sinh — khác `contains` của tìm kiếm PAT-02, vì mục đích là phát hiện trùng gần tuyệt đối chứ không gợi ý theo từ khoá. Không chặn, không tự tạo/sửa gì — chỉ trả danh sách hồ sơ nghi trùng để client tự quyết định (chặn CCCD đã có sẵn từ S2-01 qua DB constraint C3, không thuộc phạm vi việc này). Route `check-duplicate` phải khai báo trước `:id` trong controller (NestJS khớp theo thứ tự khai báo). **Đã xác minh thật**: `patient-http.spec.ts` thêm 6 test (22/22 tổng pass) — trùng tên không dấu/khác hoa thường + đúng dob, khác dob thì không trùng, gần giống nhưng không khớp tuyệt đối thì không trùng, gọi check không tạo bản ghi mới, cách ly tenant, thiếu tham số → 400. 75 test `apps/api` + 25 test `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại cho endpoint mới.
- **S2-05 xong (module `appointment`, APP-01/02/03)** — exclusion constraint `EXCLUDE USING gist` ở tầng DB (C2, `docs/ERD.md` mục 4) chặn thật hai lịch hẹn trùng khung giờ cùng bác sĩ, kể cả hai request đồng thời — **đã đạt gate cuối Sprint 2** ("một thành công, một `APPOINTMENT_SLOT_CONFLICT`", xác minh bằng `Promise.all`, không chỉ chạy tay). `appointment.room_id` nullable (chưa có module `tenant`/cấu hình phòng ở S2-07). Exclusion constraint cần hàm SQL riêng đánh dấu `IMMUTABLE` bọc `scheduled_at + interval` (Postgres từ chối toán tử `STABLE` trong index) — cùng kỹ thuật `nexamed_unaccent_lower()` đã dùng ở S2-02. Lỗi vi phạm exclusion constraint không có mã Prisma riêng (`PrismaClientUnknownRequestError`, khác `P2002`/`P2003` đã quen ở patient module) — phát hiện thật qua chạy test, không đoán từ tài liệu (chi tiết `docs/DECISIONS.md` #026).
  `AppointmentService` là nơi đầu tiên lọc `data_scope=personal` THẬT (`doctor.appointment.* = personal`) thay vì coi tương đương `global` như `patient` — bác sĩ chỉ đặt/xem lịch của chính mình, đặt hộ bác sĩ khác → 403, xem lịch bác sĩ khác → 404. Khuôn mẫu này sẽ tái dùng cho `encounter`/`vital_sign`/`clinical_note` ở S3. **Đã xác minh thật**: `appointment-http.spec.ts` (15/15 pass) — gate đồng thời, khác bác sĩ/giờ liền kề không xung đột, scope personal đúng, cách ly tenant, permission, tham chiếu không tồn tại (422), thiếu trường (400). Toàn bộ 90 test `apps/api` pass, không regress. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại.
- **S2-06 xong (huỷ lịch + walk-in, APP-04/06)** — hỏi và chốt với chủ dự án trước khi code: check-in thật (tạo `encounter`) KHÔNG làm ở S2-06, để dành cho module Tiếp nhận ở Sprint 3 (đúng quyết định "không đi đường tắt walk-in tối thiểu" đã ghi lúc S2-02). Walk-in (APP-06 phần "tạo lịch") không cần code mới — `appointment.source='walk_in'` đã có sẵn từ S2-05, dùng thẳng `POST /appointments` hiện có. `POST /api/v1/appointments/:id/cancel`: bắt buộc `cancelReason` + `version` (optimistic locking), chỉ huỷ được lịch còn `SCHEDULED` (`AppointmentNotCancellableError`, 409, cho lịch đã huỷ/không đến/đã chuyển đổi), tôn trọng `data_scope=personal` giống `getAppointment` (404 cho lịch ngoài phạm vi bác sĩ). Huỷ xong thì khung giờ đó hết bị exclusion constraint C2 chặn (đã xác minh: đặt lại được ngay). **Đã xác minh thật**: `appointment-http.spec.ts` thêm 10 test (25/25 tổng pass). Toàn bộ 100 test `apps/api` pass, không regress. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại.
- **S2-07 xong (tài khoản+vai trò ADM-01, cấu hình phòng khám ADM-02 trừ mẫu in)** — hỏi và chốt phạm vi trước khi code (bỏ "mẫu in", lùi tới S4-04/PRE-04). Theo đúng ranh giới module đã chốt ở `.claude/docs/architecture.md`: "tài khoản, vai trò" thuộc module `iam` (mở rộng, không tạo module `tenant` mới như tên gọi ở `plan.md`), "cấu hình phòng khám, phòng" thuộc module `clinic` (mới) — khớp ghi chú sẵn có trong code từ S1-04/S1-08 đã dự báo trước việc này.
  Sửa một lỗ hổng schema thật trước khi code: `user_role` UNIQUE đổi sang PARTIAL (`WHERE deleted_at IS NULL`) — unique thường sẽ chặn gán lại đúng vai trò đã từng bị gỡ. Tiện sửa luôn 1 bug đi kèm (`findRoleNamesForUser` thiếu filter `deletedAt`, chưa từng lộ vì trước đó không ai soft-delete `user_role`).
  `POST/GET/GET:id/PATCH /api/v1/users` + `POST /api/v1/users/:id/reset-password` — **lần đầu tiên `SessionRepository.revokeAllForUser()` (viết sẵn từ S1-04) được gọi thật**: đổi vai trò/vô hiệu hoá/đặt lại mật khẩu đều thu hồi toàn bộ phiên đang mở, đã xác minh bằng HTTP thật (đăng nhập lấy cookie thật → admin thao tác → cookie cũ bị từ chối). `POST/GET/PATCH /api/v1/rooms`, `GET/PATCH /api/v1/clinic-settings` (giờ làm việc theo ngày trong tuần + độ dài slot, đọc/ghi `tenant_setting`).
  **Đã xác minh thật**: `user-account-http.spec.ts` (14/14), `clinic-http.spec.ts` (13/13). Toàn bộ 127 test `apps/api` pass, không regress. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại.
- **Kế hoạch Tiếp nhận (Sprint 3) đã chốt sơ bộ với chủ dự án** (chưa code): xây đủ `appointment` (S2-05/06) + màn hình lịch (S2-09) trước Tiếp nhận, đúng thứ tự `plan.md` — không đi đường tắt "walk-in tối thiểu". Snapshot thẻ BHYT lúc check-in (`encounter.insurance_snapshot`) tạm thời luôn `self_pay=true` cho tới khi làm S2-04 (lùi lại sau Sprint 3, không chặn Tiếp nhận). Schema `appointment`/`encounter`/`vital_sign` dùng đúng những gì đã chốt trong `docs/ERD.md`, không có trường mới nào phát sinh thêm ngoài đó.

- **S2-08 xong (web bệnh nhân: danh sách, tạo mới, chi tiết/sửa, PAT-01→03)** — hỏi và chốt workflow trước khi code: chi tiết sửa TẠI CHỖ (không modal), luồng chống trùng kiểm tra LÚC bấm Lưu (gọi `check-duplicate` trước `POST /patients`, không debounce theo từng ký tự gõ), nghi trùng chặn submit lần đầu và hiện cảnh báo kèm "Vẫn tạo mới"/"Huỷ". Form dùng chung `PatientFormFields` cho cả tạo mới và sửa, lưới 2 cột theo `ui-guidelines.md`. `ErrorBanner`/`Skeleton`/`useDebouncedValue` đặt ở `shared/` với chủ đích tái dùng ngay.
  Phát hiện + sửa 2 bug lúc kiểm bằng trình duyệt thật: `Button.tsx` override `className` thô bị nuốt bởi class mặc định (nút "Huỷ" vô hình, chữ trắng nền trắng) → thêm `variant` (`primary`/`secondary`/`danger`); `QueryClient` mặc định retry cả lỗi `404 NOT_FOUND` (id không tồn tại vẫn thử lại 3 lần trước khi hiện empty state) → tắt retry riêng cho `NOT_FOUND`.
  **Đã xác minh thật qua trình duyệt** (Playwright headless Chromium, chụp ảnh từng bước): đăng nhập → danh sách + tìm kiếm đúng; tạo hồ sơ mới thành công; tạo lại đúng tên+ngày sinh → cảnh báo trùng đúng, "Huỷ" không tạo bản ghi; sửa hồ sơ tại chỗ lưu đúng giá trị mới; không lỗi console ngoài dự kiến. `pnpm -w lint/typecheck/build` sạch; 127 test `apps/api` pass, không regress.

- **Fix hạ tầng dùng chung sau khi dùng thử (docs/DECISIONS.md #028)** — để trang yên rồi thao tác lại báo "Không tải được dữ liệu" (access token 15 phút hết hạn giữa chừng, không có gì làm mới) và gõ tìm kiếm giao diện nháy liên tục (mỗi từ khoá mới xoá trắng bảng hiện có). Sửa ở `shared/api/client.ts` (tự phát hiện 401, làm mới token qua `/auth/refresh`, thử lại request cũ đúng 1 lần, dedupe nhiều 401 đồng thời) và `patient.queries.ts` (`placeholderData: keepPreviousData`, giữ kết quả cũ khi đổi từ khoá). Áp dụng cho MỌI module domain sau này qua cùng client/mẫu query, không riêng patient.

- **App Shell v2 — sidebar thu gọn, topbar/breadcrumb, List Screen Pattern (docs/DECISIONS.md #027, #029)** — chủ dự án yêu cầu làm lại giao diện tham khảo sản phẩm HIS khác; duyệt qua mockup Artifact trước khi code. Đổi tên "Bệnh nhân" → "Danh sách bệnh nhân", gộp vào nhóm sidebar mới **"Tiếp nhận và Đặt lịch"** (sẽ chứa thêm "Lịch hẹn"/S2-09, "Tiếp nhận"/S3 khi backend tương ứng ra đời). Bỏ nút "Thêm bệnh nhân" (tạo mới chuyển sang gắn với luồng Đặt lịch/Tiếp nhận, chưa xây — điểm vào cụ thể quyết định khi làm S2-09/S3). Sidebar thêm vùng logo + nút thu gọn (`w-60`⇄`w-16`). `TopBar.tsx` mới (breadcrumb dạng chip thay nút "← Quay lại" + lời chào/avatar mở dropdown đăng xuất) thay hẳn `UserMenu.tsx` (đã xoá). Danh sách bệnh nhân viết lại theo List Screen Pattern (`ui-guidelines.md` mục 9): rộng gần hết chiều ngang (bỏ `max-w-[1400px]` — chỉ áp dụng riêng màn hình danh sách, form vẫn giữ `max-w-4xl`), bảng CSS Grid (không phải `<table>`, để phối hợp với `@tanstack/react-virtual` định vị `position: absolute`) cố định chiều cao cuộn nội bộ, cuộn vô hạn giữ cursor pagination (`PATIENT_LIST_LIMIT` 20→50), mở hồ sơ bằng double-click mã bệnh nhân (không click cả hàng) + Tab/Enter tương đương bàn phím, nút "Sửa hồ sơ" chỉ hiện với vai trò có `patient.update` (`receptionist`/`clinic_admin`).
  **2 bug hạ tầng phát hiện qua chủ dự án dùng thử, đã sửa + ghi quy tắc bắt buộc ở `ui-guidelines.md` mục 8.3** (tránh lặp lại ở màn hình sau): breadcrumb tồn đọng (stale) ở `DashboardPage`/`AdminPage` vì quên gọi `useBreadcrumb` (context cố ý không tự xoá khi unmount, tránh nháy trắng — nên trang quên khai báo sẽ hiện nguyên breadcrumb trang trước); icon nhóm "Tiếp nhận và Đặt lịch" lúc sidebar thu gọn bấm không phản hồi gì (`onClick` có điều kiện chặn khi `collapsed`, quên tính trường hợp bấm chính icon đó để mở lại) — sửa để bấm icon nhóm lúc thu gọn sẽ mở lại sidebar và mở luôn nhóm.
  **Đã xác minh thật qua Playwright**: toàn bộ luồng điều hướng (mở/đóng nhóm sidebar, thu gọn/mở lại qua icon, double-click mở chi tiết, breadcrumb đổi đúng qua Tổng quan/Danh sách bệnh nhân/Quản trị). `pnpm -w lint/typecheck/build` sạch toàn workspace; 127 test `apps/api` pass, không regress (thuần web).

- **S2-09 xong (web Lịch hẹn — lưới + danh sách, APP-01)** — nhiều vòng hỏi/chốt lại theo mockup Artifact (chi tiết đầy đủ ở `docs/DECISIONS.md` #030, #031): kéo-chọn chỉ để đặt mới; tạo nhanh bệnh nhân ngay trong form; lưới cột=bác sĩ/hàng=khung giờ bước 30 phút chọn 1 ngày; bổ sung thêm sau khi xem mockup: đổi/dời lịch (`PATCH /appointments/:id/reschedule`, tái dùng khuôn `cancel()` + exclusion constraint C2), cảnh báo trễ hẹn (chỉ gợi ý thị giác, giữ ranh giới APP-05/Sprint 5), đường kẻ "bây giờ", gợi ý bác sĩ trống lịch trong form đặt nhanh (dùng dữ liệu ngày đã tải sẵn, không gọi API mới); xác nhận **không** làm "ca làm việc riêng từng bác sĩ" (cần bảng mới ngoài `docs/ERD.md` đã chốt).
  Backend mở rộng: `packages/core/src/date/vietnam-day-range.ts` (util VN UTC+7, cùng kỹ thuật `format-display-code.ts`); 2 port mới `DoctorDirectoryPort`/`ClinicConfigReaderPort` (module `appointment` đọc dữ liệu `iam`/`clinic` qua port, đúng mẫu `PatientReaderPort` ở `coding-standards.md`, **không** import chéo module) — giải quyết luôn lỗ hổng quyền thật phát hiện lúc thiết kế (lễ tân không có `user_account.read`/`clinic_config.read`, đã chốt KHÔNG mở rộng ma trận mà thêm 2 endpoint chiếu tối thiểu `GET /appointments/doctors|schedule-config` gắn quyền `appointment.read` sẵn có). `GET /appointments` thêm filter `date` + join `patientFullName`/`patientCode` (chỉ ở `list()`).
  Frontend: `apps/web/src/features/appointment/` (feature module đầy đủ theo khuôn `patient.*`), `apps/web/src/features/patient/PatientPicker.tsx` (mới, dùng chung — tìm + tạo nhanh bệnh nhân, tái dùng nguyên `PatientFormFields`). `Sidebar.tsx`/`router.tsx` thêm "Lịch hẹn".
  **Đã xác minh thật**: `appointment-http.spec.ts` thêm 13 test (38/38, tổng 140 test `apps/api` + 30 `packages/core` pass, không regress). `pnpm -w lint/typecheck/build` sạch. Playwright qua dữ liệu demo thật (5 bác sĩ, nhiều lịch hẹn tạo qua API): lưới/danh sách/đặt/huỷ/sửa lịch đều đúng. 2 bug thật phát hiện + sửa lúc kiểm bằng trình duyệt (không phải review code): giới hạn `limit` chế độ lưới (200) vượt trần schema đã có (`max(100)`) → 400; đường kẻ "bây giờ" ban đầu cài làm 1 phần tử CSS Grid chiếm trọn hàng thân bảng khiến toàn bộ cột giờ/bác sĩ (auto-placement) bị đẩy xuống hàng kế tiếp, lưới trông trống trơn dù DOM vẫn đúng — sửa bằng để mỗi cột tự vẽ đoạn kẻ riêng (bài học ghi ở `docs/DECISIONS.md` #031, áp dụng cho mọi overlay phủ nhiều cột CSS Grid sau này).

- **Đổi mô hình đặt lịch sang "lead capture" ngay sau khi dùng thử S2-09 (`docs/DECISIONS.md` #032, `docs/ERD.md` v1.4)** — chủ dự án yêu cầu: đặt lịch **không** còn tạo/gắn hồ sơ `patient` — chỉ ghi Họ tên/SĐT/Lý do khám trực tiếp lên `appointment` (`full_name`/`phone`/`reason` cột mới; `patient_id` đổi nullable, để sẵn cho Tiếp nhận thật gắn sau, Sprint 3 chưa xây). Mỗi lịch có `booking_code` riêng (mã đặt lịch khách trình lúc đến, sinh qua `code_sequence` đúng khuôn `patient_code`, prefix `LH`). `GET /appointments/lookup?phone=` tự điền tên theo lần đặt gần nhất khi lễ tân gõ SĐT + đếm số lần huỷ, cảnh báo spam khi ≥5 lần (chỉ cảnh báo, không chặn). `POST /appointments/:id/checkin` (tái dùng quyền `appointment.update`) chuyển thẳng `SCHEDULED → CONVERTED` (không sinh trạng thái mới, chưa tạo `encounter`/gắn `patient_id` — chỉ đổi trạng thái + thông báo, đúng đề nghị của chủ dự án cho tới khi Tiếp nhận thật ra đời).
  `AppointmentQuickCreatePanel.tsx` bỏ hẳn `PatientPicker` (giữ nguyên file, không xoá), thay bằng form SĐT/Họ tên/Lý do khám + banner spam + dialog xác nhận đặt lịch (nội dung đúng nguyên văn chủ dự án yêu cầu). `AppointmentDetailPanel.tsx` thêm nút Check-in + dialog thông báo. Migration `20260812012715_appointment_lead_capture` viết tay (không dùng `prisma migrate diff`, tránh drift giả với exclusion constraint C2).
  **Đã xác minh thật**: `appointment-http.spec.ts` viết lại toàn bộ (50/50 pass, 152 test `apps/api` tổng không regress); `pnpm -w lint/typecheck/build` sạch; Playwright qua dữ liệu thật xác nhận đúng dialog xác nhận, tự điền tên theo SĐT, banner spam, check-in chuyển trạng thái. 1 bug bundler thật (Rollup không dò được named export hằng số giá trị thuần từ `packages/shared` qua `vite build`, dù Node `require()` thấy đúng) — xử lý bằng chuyển hằng số ngưỡng spam sang khai báo tại `apps/web` (đúng vị trí kiến trúc thật, không chỉ né bug — xem `docs/DECISIONS.md` #032 để biết chi tiết + ghi chú cho lần sau).

- **Fix Lịch hẹn + S2-10 (test cách ly tenant, đạt gate cuối cùng của Sprint 2)** — chủ dự án báo bug thật: chế độ Danh sách không lọc theo ngày đã chọn (`AppointmentListView` gọi query thiếu tham số `date`, trong khi backend đã hỗ trợ kết hợp `date`+`cursor` từ S2-09) — đã sửa (`useAppointmentsListQuery(date)`); kèm 2 việc UX theo yêu cầu (badge "N lịch hẹn trong ngày" chuyển xuống ngang hàng nút "Hôm nay", đổi màu nổi bật, hiện cho cả 2 chế độ; phím tắt F2 + tooltip cho nút "Đặt lịch"). Sau đó làm S2-10: rà soát + bổ sung 7 test cách ly tenant còn thiếu ở `appointment-http.spec.ts` (+4: `/doctors`, `/schedule-config`, `/lookup?phone=`, danh sách) và `user-account-http.spec.ts` (+3: danh sách, `PATCH :id`, `reset-password` xuyên tenant) — **không tìm thấy bug thật**, code đã lọc `tenantId` đúng từ đầu, chỉ thiếu test xác nhận theo `.claude/docs/multi-tenancy.md`. `clinic-http.spec.ts` đã đủ từ S2-07, không cần thêm.
  **Đã xác minh thật**: 159/159 test `apps/api` pass (152 cũ + 7 mới, không regress), `pnpm -w typecheck` sạch toàn workspace, Playwright xác nhận đổi ngày ở Danh sách cập nhật đúng số dòng + badge.

- **Định hướng đa chuyên khoa (nhi/sản/nha) — chốt "specialty pack" + khung tối thiểu (`docs/DECISIONS.md` #033)** — chủ dự án hỏi khả năng thương mại hoá một source cho nhiều loại phòng khám. Phân tích khả thi đầy đủ ở `docs/product/multi-specialty-analysis.md` (mới, tài liệu chưa chốt triển khai). Đã hỏi và chốt phạm vi: **chỉ chuẩn bị khung tối thiểu**, giữ nguyên Sprint 3 theo kế hoạch (không xây hẳn 1 gói thật ngay, không đảo lộn roadmap). Đã làm phần không phụ thuộc `encounter` (bảng chưa được code, Sprint 3 chưa bắt đầu): thêm `encounter.specialty` (mặc định `'general'`) vào đặc tả thiết kế (`.claude/docs/data-model.md`, `docs/ERD.md` v1.5 — không migration/DB thật nào chạy); `packages/core/src/specialty/registry.ts` (mới) — `SpecialtyPack`/`createSpecialtyRegistry()`, hoàn toàn trơ (chỉ có gói `general`), chưa có specialty pack thật nào đăng ký.
  **Đã xác minh thật**: `registry.spec.ts` 5/5 pass, 35/35 test `packages/core` không regress, `pnpm -w typecheck` sạch toàn workspace.

- **Mở rộng form Thêm/Sửa hồ sơ bệnh nhân (PAT-01) — ảnh đại diện, CCCD ngày/nơi cấp, dân tộc/quốc tịch/nghề nghiệp, số bảo hiểm, Khu phố, Thông tin người thân (`docs/DECISIONS.md` #034)** — chủ dự án yêu cầu hiệu chỉnh lại giao diện, dừng Sprint 3 (đang bị chặn bởi thiếu file danh mục ICD-10) để làm việc này trước. Dùng Plan mode: khám phá backend/frontend hiện có rồi hỏi 4 điểm chốt (ảnh làm thật ngay qua `StoragePort`; dân tộc/quốc tịch/nghề nghiệp nhập tự do không danh mục DB; địa chỉ Tỉnh/Xã giữ text tự do; người thân 1 bộ trên `patient`, không tách bảng) trước khi viết migration.
  Migration `20260812150000_patient_profile_fields` (viết tay, 11 cột nullable — `photo_key`, `national_id_issued_at/place`, `ethnicity`, `nationality`, `occupation`, `insurance_number`, `relative_full_name/relationship/phone/address`); `address_json` thêm `neighborhood`, bỏ `district` khỏi UI (giữ trong schema/DB tương thích ngược, khớp cải cách hành chính 2 cấp Tỉnh→Xã 2025).
  **Lần đầu hệ thống có upload/serve file thật** — dựng signed-URL tối giản (`apps/api/src/infrastructure/storage/signed-url.ts`, HMAC tự chứa `{tenantId,key,exp}`) + endpoint công khai `GET /api/v1/files/:token` (không `JwtAuthGuard`, chữ ký là lớp xác thực), đúng yêu cầu "phục vụ qua signed URL có hạn" đã ghi từ S1-06 trong `security-audit.md` nhưng chưa từng dùng tới. `POST /patients/:id/photo` (multipart, kiểm magic byte qua `packages/core/src/storage/sniff-image-extension.ts`, giới hạn 3MB → 413).
  **Bố cục form đổi qua 3 vòng chỉnh trực tiếp trên bản dựng thật** (không phải chốt trước): mỗi nhóm trường bọc khung viền + badge xanh nổi trên viền (đồng nhất cho cả "Thông tin hành chính" lẫn "Thông tin người thân" — ban đầu người thân chỉ có divider mảnh, chủ dự án phản hồi "nhạt" nên đổi sang cùng khuôn box+badge); gap lưới thu hẹp 2 lần (`gap-6` → `gap-3`); trang Thêm mới/Chi tiết bỏ hẳn `max-w-4xl`, kéo full-width `p-6` theo đúng yêu cầu "giống bố cục Danh sách bệnh nhân" — **override** quy tắc "form giữ max-w" đã chốt ở `ui-guidelines.md` mục 9 cũ, đã cập nhật lại mục 9 (ghi ngoại lệ) + thêm mục 9b (Boxed Section Form Pattern) mô tả pattern mới để form khác sau này tái dùng đúng.
  **Đã xác minh thật**: `signed-url.spec.ts` (6/6), `sniff-image-extension.spec.ts` (5/5, `packages/core`), `patient-http.spec.ts` mở rộng (33/33, tổng 176 test `apps/api` không regress) — upload hợp lệ, sai magic byte → 400, quá 3MB → 413, version sai → 409, ảnh cũ bị xoá thật (URL cũ 404). `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật: tạo hồ sơ đủ field mới → lưu → vào chi tiết → upload ảnh → F5 vẫn thấy ảnh → sửa field mở rộng → lưu đúng, không console error ngoài dự kiến.

- **Danh mục dùng chung `reference_catalog` — Dân tộc/Quốc tịch, quản lý được qua UI (`docs/DECISIONS.md` #037)** — đảo ngược #034 cho riêng 2 field này (`occupation` không đổi). Chủ dự án tự cung cấp 2 file dữ liệu thật (`Danh_sach_54_dan_toc_Viet_Nam.md`, `Master_Data_Quoc_Tich_ISO3166.md`) thay vì để AI tự soạn — giải quyết đúng gốc rễ lý do #034 từng chặn. Đã dựng mockup Artifact trước khi code, chốt qua hỏi-đáp: lưu `code` (không lưu `name`) trên `patient.ethnicity`/`patient.nationality`; quyền quản lý `clinic_admin` (permission mới `reference_catalog.manage`, giới hạn lý thuyết đã ghi rõ — không cách ly đa-tenant); IA menu "Quản trị" đổi thành nhóm cha/con (giống "Tiếp nhận và Đặt lịch"), mục con "Danh mục dùng chung" → `/admin/reference-catalog` (trang riêng, tab theo category); "Xoá" là soft (`is_active=false`) — phát hiện role DB `nexamed_app` bị revoke `DELETE` toàn cục từ migration nền tảng, không thể xoá cứng dù muốn.
  Bảng mới `reference_catalog` (toàn hệ thống, không `tenant_id`, không `version` — khác `permission`: **không** revoke INSERT/UPDATE vì `clinic_admin` sửa qua chính API). Seed đầy đủ 54 dân tộc + 30 quốc tịch (không phải placeholder) tại `packages/core/src/reference-catalog/data.ts`. Module `apps/api/src/modules/reference-catalog/` (CRUD đầy đủ, `PermissionGuard` chuẩn — không lệch pattern như dự tính ban đầu, vì xác nhận được dự án chưa có tenant production nào nên thêm permission mới không cần backfill). `AdminPage.tsx` (thuần `EmptyState`, không còn dùng) đã xoá cùng route `/admin`.
  **Đã xác minh thật**: `reference-catalog-http.spec.ts` (10/10), `packages/core/src/reference-catalog/data.spec.ts` (2/2) — tổng 191 test `apps/api` + 42 test `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật (dữ liệu thật, không mock): sidebar nhóm Quản trị mở đúng → trang Danh mục dùng chung hiện đúng 54/30 dòng theo tab → thêm/sửa/xoá(ẩn)/khôi phục một mục qua UI đều phản ánh đúng ngay → form Thêm bệnh nhân có dropdown đúng dữ liệu, mặc định Quốc tịch = Việt Nam (`VNM`) → mở hồ sơ "cũ" có giá trị tên tự do trước #037 (tạo qua API để mô phỏng) → dropdown giữ nguyên giá trị cũ, không bị xoá trắng. Không console error ngoài dự kiến.
  **Phát hiện thật lúc kiểm bằng trình duyệt (không phải bug code, là đặc điểm cơ chế RBAC đã biết từ trước)**: tenant dev đã seed từ trước (trước khi thêm 2 permission mới) không tự có `role_permission` cho `reference_catalog.*` — `seedDefaultRolesForTenant()` chỉ chạy lúc **tạo tenant**, không có cơ chế backfill permission mới cho tenant đã tồn tại. Đã vá thủ công cho tenant dev bằng script một lần (không phải thay đổi code) — ghi lại ở mục "Đang chờ" vì đây sẽ là vấn đề thật khi phòng khám pilot đã cài đặt và dự án cần thêm permission mới sau này.

- **Danh mục hành chính `province`/`ward` (Tỉnh/Phường-Xã, sáp nhập hành chính 2025) — đã xong (`docs/DECISIONS.md` #038)** — chủ dự án gửi file dữ liệu chính thức Bộ Nội vụ (`docs/data/Danh-muc-Phuong-xa_moi.md`, 34 tỉnh + 3321 phường/xã), hỏi và chốt thiết kế 2 bảng (không gộp 1 bảng phẳng như file gốc) trước khi code, xác nhận thêm: **read-only** (khác `reference_catalog` — không endpoint quản lý qua UI, dữ liệu hành chính nhà nước) và tích hợp luôn vào `patient.address_json` (đảo ngược tiếp phần Tỉnh/Xã của #034).
  Bảng mới `province`/`ward` (toàn hệ thống, không `tenant_id`, không `version`/`is_active` — cùng bản chất `icd10_catalog`; REVOKE `INSERT`/`UPDATE` khỏi `nexamed_app`, chỉ seed script ghi được). `ward.code` (8 chữ số) duy nhất toàn quốc, dùng thẳng làm PK. Seed thật tại `packages/core/src/geo/data.ts`. Quyền dùng lại `patient.read` (không thêm permission mới, tránh lặp vấn đề backfill role_permission ghi ở trên). Module `apps/api/src/modules/geo/` (`GET /geo/provinces`, `GET /geo/wards?provinceCode=` — bỏ trống trả toàn bộ, dùng dựng bảng tra code→tên cho cột Địa chỉ ở Danh sách bệnh nhân).
  Web: `PatientFormFields.tsx` — 2 input text (Tỉnh/Phường-Xã) → Combobox cascading (đổi Tỉnh tự xoá Phường/Xã đã chọn). Tách `withLegacyValueOption()` từ `PatientFormFields.tsx` ra `shared/ui/Combobox.tsx` (dùng chung cho cả ethnicity/nationality lẫn province/ward — trùng lặp lần 2, tách theo `CLAUDE.md`).
  **Sửa dữ liệu theo yêu cầu chủ dự án lúc rà lại**: 4 tên tỉnh dùng viết tắt "Tp" trong file gốc (Hải Phòng, Đà Nẵng, Hồ Chí Minh, Cần Thơ) đổi thành "Thành phố" đầy đủ; `seed-geo.ts` phần `province` đổi sang upsert (thay vì bỏ qua trùng) để tên sửa đồng bộ được khi seed lại.
  **Đã xác minh thật**: `packages/core/src/geo/data.spec.ts` (4/4), `apps/api/src/modules/geo/geo-http.spec.ts` (7/7) — tổng 198 test `apps/api` + 46 test `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật (dữ liệu thật): Combobox cascading đúng (34 tỉnh, đúng số phường/xã lọc theo tỉnh, đổi tỉnh tự xoá phường/xã cũ); tạo hồ sơ đủ địa chỉ → Danh sách bệnh nhân hiện đúng tên đã resolve từ mã, không phải mã thô.

- **Trang "Danh mục" + "Cấu hình hệ thống" — mẫu Two-panel Config Screen (`docs/DECISIONS.md` #039, #040)** — chủ dự án gửi 2 ảnh tham khảo phần mềm phòng khám khác cho phong cách trang Cấu hình, dựng mockup Artifact 2 kiểu (Kiểu 1 danh sách phẳng / Kiểu 2 thanh nhóm + thẻ) trước khi code, chốt Kiểu 2 — nhưng chốt rõ mẫu này KHÔNG bắt buộc cho mọi trang Quản trị tương lai, phân bố menu do chủ dự án quyết định khi tạo từng menu mới (`.claude/docs/ui-guidelines.md` mục 10). Route `/admin/reference-catalog` đổi thành `/admin/catalog` (nhãn sidebar "Danh mục"), gộp `reference_catalog` (Dân tộc/Quốc tịch) và `geo` (Tỉnh/Thành, Phường/Xã — vẫn chỉ đọc đúng #038) chung 1 pill "Danh mục dùng chung" (4 mục). `ClinicHoursPane` (giờ làm việc + slot, dùng `GET/PATCH /clinic-settings` có sẵn từ S2-07, lần đầu có UI web) ban đầu đặt chung trang "Danh mục", sau đó tách hẳn sang trang riêng "Cấu hình hệ thống" (`/admin/system-config`) theo yêu cầu chủ dự án — nhóm sidebar "Quản trị" nay có 2 mục con.
  Nhiều vòng chỉnh trực tiếp trên bản chạy thật (không dựng mockup lại): bỏ pill "Danh mục hành chính" riêng (gộp hẳn vào "Danh mục dùng chung"); form "Giờ làm việc" đổi từ `max-w-2xl`/`max-w-3xl` (để trống nhiều bên phải) sang full-width theo Boxed Section Form Pattern (mục 9b, 2 khung viền+badge "Giờ làm việc theo tuần"/"Độ dài Slot"); **trích `ConfigScreenShell`** (`apps/web/src/shared/ui/ConfigScreenShell.tsx`) sau khi chủ dự án phát hiện trang "Cấu hình hệ thống" mới không đồng bộ bố cục với "Danh mục" — lần dùng thứ hai của khuôn pill+list+content nên tách theo `CLAUDE.md`, cả 2 trang cùng dùng để LUÔN đồng nhất (kể cả khi chỉ có 1 pill/1 mục — đã hỏi và chốt không tự ẩn cột trái/pill bar dù ít lựa chọn). `GeoPane` mode Phường/Xã mặc định chọn sẵn Thành phố Hồ Chí Minh (mã `29`) thay vì để trống chờ chọn.
  **Điều tra "lưới lịch hẹn không áp đúng giờ đã cấu hình" theo báo cáo chủ dự án**: xác minh bằng Playwright + curl trực tiếp trên dữ liệu thật — không có bug thật trong `ClinicHoursPane`/`AppointmentGridView`/`resolveDayHours()`, chỉ là dữ liệu `tenant_setting` bị lệch tạm thời do chính kịch bản test Playwright của tôi trong phiên này chọn nhầm ô giờ (`input[type=time] >> nth=N` khớp nhầm hàng khi layout đổi sang lưới 2 cột) — đã khôi phục đúng giá trị gốc và xác minh lại đúng cách (sửa giờ 1 ngày cụ thể qua UI → lưới lịch hẹn hiện đúng khung giờ đó ngay). Riêng việc lưới cần cuộn khá nhiều để thấy hết 1 ngày dài (9-10 tiếng, `ROW_HEIGHT_PX=92`/30 phút một hàng từ S2-09) là hành vi có sẵn từ trước, chưa phải bug — đang chờ chủ dự án xác nhận có muốn điều chỉnh mật độ hiển thị hay không.
  **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch toàn workspace (trừ 1 lỗi ESLint có sẵn từ trước, không liên quan). Không migration/schema/API contract nào đổi. Playwright qua trình duyệt thật + dữ liệu thật: cả 2 trang cấu hình đồng nhất bố cục, CRUD Dân tộc/Quốc tịch hoạt động, Phường/Xã mặc định TP.HCM có dữ liệu ngay, sửa giờ làm việc lưu đúng và phản ánh đúng trên lưới lịch hẹn.

**Fix nhỏ đi kèm — nhãn giờ đóng cửa ở lưới lịch hẹn**: chủ dự án xem trên màn hình phản hồi "chỉ hiển thị đến 16h30 trong khi giờ làm việc là 7h30-17h" — đúng là mỗi nhãn giờ trên lưới chỉ đánh dấu ĐẦU 1 khung 30 phút (`generateSlotLabels`, S2-09), nên hàng cuối "16:30" trông như lưới dừng ở đó dù khung đó thực chất kéo dài tới đúng giờ đóng cửa. Thêm 1 nhãn biên `range.close` (ví dụ "17:00") ngay dưới hàng cuối cùng trong `AppointmentGridView.tsx` — cuộn tới đáy giờ thấy rõ mốc giờ đóng cửa thật, xác nhận lưới đã hiển thị đủ. Không đổi `resolveDayHours()`/dữ liệu, chỉ thêm 1 nhãn hiển thị. Đã xác minh qua Playwright (cuộn tới đáy, thấy đúng "17:00").

- **Backfill `role_permission` cho tenant cũ — tự động lúc API khởi động**: đã hỏi và chốt cơ chế trước khi code (3 lựa chọn: script chạy tay / endpoint quản trị / tự động lúc khởi động) — chọn tự động. `apps/api/src/infrastructure/persistence/sync-role-permissions.ts` (`syncRolePermissionsForTenant`/`syncRolePermissionsForAllTenants`) — với mỗi tenant, chỉ THÊM dòng `role_permission` còn thiếu so với `DEFAULT_ROLE_PERMISSIONS` cho vai trò `is_system_default=true`, không sửa/xoá dòng đã có (an toàn nếu ADM-07 sau này cho `clinic_admin` tuỳ biến) và không đụng vai trò tuỳ biến. Gọi 1 lần trong `main.ts` (`bootstrap()`, sau khi tạo `app`, trước `app.listen()`) — không chạy trong test (test không qua `main.ts`). Không migration/schema nào đổi (`role_permission` đã ghi được bởi `nexamed_app` từ trước). Yêu cầu permission mới phải có sẵn trong bảng `permission` trước (vẫn qua `pnpm db:seed`, `nexamed_app` không có quyền ghi bảng đó) — nếu chưa, dòng đó bị bỏ qua thay vì crash cả API.
  **Đã xác minh thật**: `sync-role-permissions.spec.ts` (4/4, Postgres thật) — tenant seed đủ thì không thêm gì; xoá thử 1 dòng mô phỏng tenant cũ → vá đúng lại, đúng `data_scope` theo ma trận, gọi lại lần 2 không tạo trùng; không đụng vai trò tuỳ biến; `ForAllTenants` chỉ vá đúng tenant thiếu, cách ly tenant khác. Kiểm thật thêm trên DB dev thật (không phải test container): xoá `clinic_admin/audit_log.read` của tenant dev → chạy `node dist/main.js` → log `[startup] Đồng bộ role_permission: thêm 1 dòng còn thiếu (...)` → xác nhận DB đã phục hồi đúng `dataScope=global`. Tổng 202 test `apps/api` pass (198 cũ + 4 mới), không regress. `pnpm -w typecheck/lint/build` sạch (trừ 1 lỗi ESLint có sẵn từ trước, không liên quan).

- **Lưới lịch hẹn cuộn nhiều — đã hỏi và chốt**: giữ nguyên `ROW_HEIGHT_PX=92`/bước 30 phút (không đổi mật độ dữ liệu — quyết định thiết kế đã chốt lúc S2-09), chỉ tối ưu khoảng trống khác quanh lưới. `AppointmentSchedulePage.tsx`: gộp 2 hàng công cụ cũ (nút "Đặt lịch" riêng + hàng điều hướng ngày/legend/toggle) thành 1 hàng, `p-4`→`p-3`/`gap-3`→`gap-2.5`; `AppointmentGridView.tsx`: giảm padding dọc header cột bác sĩ (`py-2.5`→`py-2`). Nhường thêm vùng nhìn thấy cho thân lưới trước khi phải cuộn, không đụng dữ liệu/logic giờ.
  **Đã xác minh thật qua trình duyệt** (Playwright, dữ liệu dev thật): hàng công cụ gộp hiển thị đúng (điều hướng ngày + badge bên trái, legend + toggle Lưới/Danh sách + nút "Đặt lịch" bên phải), không vỡ layout; bấm "Đặt lịch" mở đúng panel đặt lịch nhanh; chuyển "Danh sách" đúng dữ liệu; thu hẹp xuống 1366x768 (ngưỡng tối thiểu theo CLAUDE.md) vẫn không tràn/vỡ hàng công cụ; không console error ngoài dự kiến. `pnpm -w typecheck/lint/build` sạch (trừ 1 lỗi ESLint có sẵn từ trước, không liên quan).

- **Trang "Thông tin phòng khám" — mở rộng `tenant`, upload logo, sửa bug ảnh vỡ có sẵn từ #034 (`docs/DECISIONS.md` #041)** — mục con mới trong pill "Cấu hình phòng khám" (`/admin/system-config`), đặt trước "Giờ làm việc & Slot" theo yêu cầu chủ dự án, mặc định mở đầu tiên. Migration `20260813120000_tenant_profile_fields` thêm 6 cột trên `tenant` (`phone`/`email`/`currency`/`taxCode` đã có/`timezone`/`logo_key`/`print_logo_key`) — không dùng `tenant_setting`, không permission mới (dùng lại `clinic_config.*`). `currency`/`timezone` chỉ lưu giá trị hiển thị, chưa nối vào logic tính toán/ngày giờ hệ thống. `apps/api/src/modules/clinic/clinic-profile.{controller,service,repository}.ts` (mới) — cùng khuôn upload ảnh đại diện bệnh nhân #034 (magic byte, signed URL, xoá file cũ). `apps/web/src/features/clinic/ClinicInfoPane.tsx` (mới).
  **2 bug phát hiện lúc dùng thử, đã sửa**: (1) ảnh vỡ (broken image icon) — `photoUrl`/`logoUrl` là đường dẫn tương đối, `<img>` phân giải theo origin trang web thay vì API → sửa bằng `resolveApiUrl()` dùng chung (`shared/api/client.ts`), áp dụng lại cho cả `PatientAvatarUpload.tsx` (bug có sẵn từ #034, không phải do tính năng mới); (2) upload ảnh hoạt động độc lập với chế độ Sửa gây khó hiểu — đổi sang ẩn nút "Chọn ảnh" cho tới khi bấm "Sửa", áp dụng cho cả 2 nơi (patient avatar + clinic logo), ghi thành quy tắc bắt buộc ở `.claude/docs/ui-guidelines.md` mục 9b.
  **Đã xác minh thật**: `clinic-profile-http.spec.ts` 18/18 (tổng 220 test `apps/api`), `pnpm -w typecheck/lint/build` sạch, Playwright qua trình duyệt thật (CRUD đầy đủ, upload logo hiển thị đúng ảnh thật, gating Sửa đúng ở cả 2 màn hình).

- **Tiếp nhận bệnh nhân (Sprint 3, phần 1: REC-01→03) — đã xong (`docs/DECISIONS.md` #042)** — check-in thật (tạo `encounter` + gắn `patient` + chuyển `appointment→CONVERTED`, atomic), trang "Tiếp nhận" mới (hàng đợi, `/reception`), nhập sinh hiệu có cảnh báo ngoài ngưỡng theo tuổi (không chặn lưu), nút "Bắt đầu khám"/"bỏ về". Không phụ thuộc ICD-10 nên làm được trước phần Khám bệnh (vẫn treo). Module mới `apps/api/src/modules/reception/` + `apps/api/src/modules/encounter/` (chia sẻ Repository qua Nest `exports` trong 1 transaction — quyết định kiến trúc mới, xem #042). Vá ma trận RBAC (3 permission `encounter.*` mới, nurse đổi personal→global). Endpoint `POST /appointments/:id/checkin` cũ đã **gỡ bỏ**, thay bằng `POST /reception/check-in`.
  **Bổ sung giữa phiên theo yêu cầu chủ dự án**: số điện thoại được phép trùng thật sự (VD: mẹ dùng chung SĐT cho nhiều con) — chỉ CCCD mới chặn. `GET /patients/by-phone` mới: cảnh báo mềm ở form Thêm/Sửa khách hàng (không chặn lưu), và ở trang Tiếp nhận dùng để lễ tân **chọn** đúng khách hàng khi tra theo SĐT lịch hẹn.
  **Đã xác minh thật**: `encounter-state-machine.spec.ts` (5/5), `vital-sign-thresholds.spec.ts` (10/10), `reception-http.spec.ts` (19/19), `encounter-http.spec.ts` (9/9), `patient-http.spec.ts` +4 test `by-phone` — tổng 246/246 test `apps/api` pass, chạy lặp lại 3 lần để xác nhận hết flaky sau khi sửa 1 race condition thật phát hiện lúc test (`syncRolePermissionsForTenant` đổi `create()` từng dòng sang `createMany({skipDuplicates:true})` — tránh Postgres abort cả transaction khi 2 tiến trình chèn cùng lúc). `pnpm -w lint/typecheck/build` sạch toàn workspace.

- **Thiết kế lại "Tiếp nhận" sau 3 vòng góp ý chủ dự án — đã xong (`docs/DECISIONS.md` #043)** — bản #042 (hàng đợi gộp appointment+encounter, "khách vãng lai" tạo appointment+encounter cùng lúc) bị bác bỏ hoàn toàn. Bản mới: **3 màn hình tách biệt** — "Danh sách tiếp nhận" (`/reception`, CHỈ encounter đã tiếp nhận, theo ngày, không gộp lịch hẹn chưa tới), "Tiếp nhận bệnh nhân" (`/reception/new`, form tạo `encounter` TRỰC TIẾP với `appointment_id=NULL` — khách không qua đặt lịch trước, `POST /reception/direct`), "Hàng đợi khám" (`/reception/doctor-queue`, trang riêng cho bác sĩ, lọc `doctorId` tường minh vì `encounter.read` vẫn `global`). Check-in từ lịch hẹn có sẵn chuyển hẳn thành popup trên panel chi tiết Lịch hẹn (`AppointmentDetailPanel.tsx`), không còn ở trang Tiếp nhận. 2 danh mục cấu hình mới **tái dùng `reference_catalog`** (thêm category `PATIENT_SOURCE`/`EXAM_TYPE` + cột `price`, không tạo bảng/module riêng) — "Loại khám" mang giá tham khảo, **snapshot** vào `encounter.exam_type_code/name/price` lúc tạo (không JOIN sống), chỉ lưu để hiển thị, KHÔNG tính viện phí (ngoài phạm vi CLAUDE.md). Migration mới `20260813160000_reception_direct_and_catalog_extension`. RBAC không đổi (tái dùng `encounter.create`).
  **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch (lint có 1 lỗi cấu hình ESLint plugin tiền-nhiệm không liên quan, ở file không đụng tới). `reception-http.spec.ts` viết lại (25/25 pass, thêm describe `POST /reception/direct` + `GET /reception/list` với `doctorId`). Toàn bộ 252 test `apps/api`+`packages/core` pass (`geo-http.spec.ts` có 1 race điều kiện tiền-nhiệm khi chạy song song nhiều file test — pass 100% khi chạy riêng, không liên quan thay đổi lần này, đã xác nhận qua nhiều lần chạy).

- **"Danh sách tiếp nhận" về đúng nghĩa THUẦN theo dõi trạng thái — đã xong (`docs/DECISIONS.md` #044)** — chủ dự án phát hiện trang này vẫn còn giữ nút Sinh hiệu/Bắt đầu khám/+Tiếp nhận copy từ thiết kế cũ, mâu thuẫn với quyết định #043. Đã sửa: "Bắt đầu khám" chuyển hẳn sang "Hàng đợi khám" (đúng đối tượng thao tác); "Sinh hiệu" bỏ hẳn khỏi trang này, chuyển sang nhập NGAY LÚC TIẾP NHẬN (thiếu thì bác sĩ tự bổ sung sau ở form phiếu khám); nút "+Tiếp nhận bệnh nhân" bỏ (đã có mục sidebar). **Hợp nhất 2 biểu mẫu tiếp nhận thành 1 component** `ReceptionIntakeForm.tsx` (bệnh nhân, lý do, nguồn khách, loại khám, bác sĩ, sinh hiệu tuỳ chọn) — dùng cho cả trang "Tiếp nhận bệnh nhân" (`mode='direct'`) lẫn popup check-in trên panel Lịch hẹn (`mode='checkin'`, thay hẳn dialog tối giản cũ, bác sĩ/giờ cố định theo lịch hẹn). Backend: `checkInRequestSchema` mở rộng khớp `registerReceptionRequestSchema` (bắt buộc kèm loại khám, tuỳ chọn kèm sinh hiệu — `intakeVitalSignFieldsSchema` dùng chung với `recordVitalSignRequestSchema`); `checkIn()`/`registerDirect()` đều tạo kèm `vital_sign` trong cùng transaction nếu có nhập. `POST /reception/encounters/:id/vital-signs` (REC-02/03) giữ nguyên phía backend làm hạ tầng bổ sung sau, chỉ gỡ trigger UI (`VitalSignForm.tsx` xoá — 0 caller còn lại).
  **Đã xác minh thật**: `reception-http.spec.ts` (25/25, thêm assertion tạo `vital_sign` kèm check-in), `encounter-http.spec.ts` (9/9, vá helper `checkInFreshEncounter()` riêng — payload cũ thiếu loại khám bắt buộc mới). Tổng 252/252 test `apps/api`+`packages/core` pass (không regress, `geo-http.spec.ts` cũng pass lần này). `pnpm -w typecheck/build` sạch toàn workspace, OpenAPI + web type đã sinh lại.

- **Sidebar — nhóm "Khám bệnh" mới, chuyển "Hàng đợi khám" vào đây** — dọn chỗ sidebar trước khi làm màn hình Khám bệnh thật (S3-05→07, vẫn chặn bởi ICD-10). "Tiếp nhận và Đặt lịch" còn 4 mục; "Khám bệnh" (mới, cùng điều kiện vai trò `DOCTOR_QUEUE_ROLES`) chỉ có "Hàng đợi khám", route/code giữ nguyên `/reception/doctor-queue` / `features/reception/` — thuần đổi vị trí sidebar, không đổi module. Sửa kèm `useBreadcrumb` của `ReceptionDoctorQueuePage.tsx` (lệch nhóm cũ, phát hiện lúc kiểm bằng trình duyệt). **Đã xác minh thật qua Playwright**: thứ tự nhóm đúng, vào thẳng `/reception/doctor-queue` chỉ nhóm "Khám bệnh" tự mở (không cả 2 nhóm cùng mở dù chung tiền tố `/reception`), breadcrumb đúng "Khám bệnh › Hàng đợi khám". `pnpm --filter @nexamed/web run typecheck` sạch, `pnpm -w lint` chỉ còn 1 lỗi ESLint có sẵn từ trước không liên quan.

- **Đặt lịch nhanh — thiết kế lại theo yêu cầu chủ dự án**: `AppointmentQuickCreatePanel.tsx` chia 2 nhóm Boxed Section (Thông tin hành chính/Thông tin đặt lịch), rộng 480px (~33% màn 1440px), thêm ngày hẹn editable + tìm SĐT khớp `patient` thật (khoá SĐT/Họ tên/Mã BN khi khớp, hiện kèm ngày sinh DD/MM/YYYY để đối chiếu, không tự điền), bỏ ô Thời lượng (dùng thẳng cấu hình slot), thêm "Người tạo lịch" (readonly, tên user đăng nhập). Không đổi API contract (`patientId` vẫn không gửi lên, đúng #032). Thêm `shared/format/date.ts` (`formatDobDisplay`, DD/MM/YYYY) áp dụng cho mọi nơi hiển thị `patient.dob` dạng chỉ-xem — KHÔNG áp dụng được cho `<input type="date">` (trình duyệt tự hiển thị theo locale OS, chưa có ô nhập ngày tự chế). Giá trị input toàn app đổi sang đậm/rõ (`font-semibold text-[15px]`, xem `.claude/docs/ui-guidelines.md` mục 4.1c) thay `text-sm` thường. Thêm màu nhấn phụ `brand-teal` (#099078, `apps/web/src/app/index.css` `@theme`) cho các chip lựa chọn (Nguồn đặt lịch/Bác sĩ), tách khỏi `blue-600`. Sửa 1 bug thật: "Nguồn đặt lịch" mặc định `'phone'` khiến tô màu ngay khi mở form dù chưa bấm — đổi mặc định `null`, bắt buộc bấm chọn mới submit được. Đã xác minh qua Playwright (search/khoá SĐT, đổi ngày, đặt lịch thành công, màu sắc đúng theo trạng thái). Hover chọn Bác sĩ đổi sang kết hợp 2 màu thương hiệu (viền `blue-400` + nền `brand-teal-tint`, xem ui-guidelines mục 2.1). Bỏ `shadow-sm` trang trí trên các thẻ đã tô nền đặc (đã chọn) theo phản hồi "nhìn AI tạo" — nguyên tắc mới ghi ở ui-guidelines mục 2.2 (không đổ bóng cho phần tử không thực sự nổi lớp). **Đã xây `shared/ui/TimeInput.tsx`** (mặt nạ `HH:mm`, chuẩn bắt buộc mới — ui-guidelines mục 4.1d) thay toàn bộ `<input type="time">` trong app (Đặt lịch nhanh, dời lịch, tiếp nhận, giờ làm việc phòng khám) — đảm bảo 24h tuyệt đối, không còn phụ thuộc locale trình duyệt/OS. Đã xác minh qua Playwright: gõ `0930` → `09:30`, gõ sai `9999` tự kẹp về `23:59` lúc rời ô.

- **Header danh sách đậm/nổi bật + "Danh sách tiếp nhận" đổi 8 cột + cuộn ngang/dọc ẩn thanh cuộn** — theo phản hồi trực tiếp: (1) header mọi bảng đổi `font-bold text-slate-800` (trước `font-semibold`/`font-medium` tuỳ file, không đồng nhất) — áp cho `PatientListPage`/`GeoPane`/`ReferenceCatalogPane`/`ReceptionListPage`/`AppointmentListView` (ui-guidelines mục 9 bổ sung). (2) `ReceptionListPage.tsx` đổi đúng 8 cột theo yêu cầu (Mã lượt khám/Họ tên/Năm sinh/SĐT/Địa chỉ/Ngày giờ tiếp nhận/Trạng thái/Người tiếp nhận, bỏ cột Bác sĩ) — 3 cột chưa có nguồn dữ liệu (Năm sinh/Địa chỉ/Người tiếp nhận, `ReceptionListItem` chưa có field) hiện `—` chờ khớp sau; cột cố định px + `min-width` tổng để `overflow-x-auto` cuộn ngang được khi không đủ chỗ (ui-guidelines mục 9c, kỹ thuật mới). (3) Bug thật: khung cuộn dọc thiếu `overflow-x-hidden` sinh thêm 1 thanh cuộn ngang RIÊNG (đặc tính CSS: khai `overflow-y` mà bỏ trống `overflow-x` thì trình duyệt tự suy ra `auto`) — đã sửa + ghi cảnh báo. (4) Thanh cuộn giờ ẨN mặc định, chỉ hiện khi rê chuột (class `scroll-hover` mới, `apps/web/src/app/index.css`) — áp cho mọi khung cuộn trong danh sách (`ReceptionListPage` cả 2 trục, `PatientListPage`/`AppointmentListView`/`GeoPane`). **Đã xác minh qua Playwright với 111 dòng dữ liệu test thật** (seed qua API, không phải mock): bảng không vỡ, header/thân bảng cuộn ngang khớp nhau tuyệt đối, không còn 2 thanh cuộn. `pnpm -w typecheck` sạch; lint chỉ còn lỗi cấu hình có sẵn không liên quan.

- **Trang "Danh mục hành chính" (đổi tên từ "Danh mục") — tách "Danh mục dùng chung" thành pill riêng từng danh mục, sửa bug breadcrumb tĩnh, thêm 4 sub-menu "Sắp ra mắt" (`docs/DECISIONS.md` #045, #046)** — chủ dự án yêu cầu trực tiếp (không mockup): 6 danh mục (Dân tộc/Phường-Xã/Tỉnh-Thành/Quốc tịch/Nguồn khách hàng/Loại khám) trước gộp chung 1 pill nay mỗi cái là 1 pill riêng ngang hàng; `ConfigScreenShell` thêm chế độ "pill phẳng" (không cột trái) khi pill không có `items` — nội dung sát menu chính. Sửa kèm bug thật (chủ dự án phát hiện lúc dùng thử): breadcrumb khai tĩnh một lần lúc mount, bấm đổi pill không cập nhật — sửa cho cả `CatalogAdminPage` lẫn `ClinicConfigPage` (đoạn cuối breadcrumb lấy theo pill/mục đang active). Thêm 4 mục sidebar "Sắp ra mắt" (Tổ chức và Nhân sự, Chuyên môn, Cận lâm sàng, Dược và Vật tư) — ngoại lệ có chủ đích với quy tắc "không dựng menu cho tính năng chưa xây" (đã hỏi qua `AskUserQuestion` trước khi làm), chỉ đặt chỗ UI (`ComingSoonPage` dùng chung), không mở rộng phạm vi v1 — Cận lâm sàng/Dược và Vật tư vẫn ngoài phạm vi v1 theo `CLAUDE.md`. Không migration/schema/API nào đổi.
  **Đã xác minh thật**: `pnpm -w typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật (tài khoản `dev.admin`): sidebar đúng thứ tự/nhãn, 6 pill Danh mục hành chính không còn "Danh mục dùng chung", không còn cột trái, breadcrumb đổi đúng theo pill/mục ở cả 2 trang, 4 trang "Sắp ra mắt" hiện đúng, không lỗi console ngoài dự kiến.

- **Tiêu đề tab trình duyệt động (`docs/DECISIONS.md` #047)** — tái dùng nguyên `BreadcrumbProvider` có sẵn (không thêm API riêng ở từng trang): tab hiện `"{menu cha} - {trang/tab đang chọn}"` (ví dụ `Tiếp nhận và Đặt lịch - Lịch hẹn`), về `"NEXAMed"` khi chưa đăng nhập/không có breadcrumb. Đã xác minh thật qua Playwright đọc `document.title` ở nhiều trang.

- **Trang 404 thương hiệu + redirect `/admin` → `/admin/catalog` (`docs/DECISIONS.md` #048)** — chủ dự án báo lỗi thật: gợi ý lịch sử trình duyệt dạng `/admin` (rút gọn từ `/admin/catalog`) ra trang trắng mặc định xấu của `react-router`. Thêm route `{path:'admin', element:<Navigate to="/admin/catalog"/>}` xử lý đúng ca cụ thể; thêm `NotFoundPage.tsx` gắn ở `path:'*'` (mục cuối `children` của route `/`, trong `AppShell`) bắt mọi URL không khớp khác — render kèm sidebar/breadcrumb như trang thường, không đứng riêng. Đã xác minh thật qua Playwright cả 4 trường hợp (redirect, 404 trong `/admin/*`, 404 ngẫu nhiên, chưa đăng nhập vẫn về đúng `/login`).

- **Cột mã định danh: bỏ gạch chân mã bấm được, đổi mã không bấm được sang màu đen (`docs/DECISIONS.md` #049)** — chủ dự án hỏi lý do `patientCode` gạch chân, chốt: mã bấm được (`patientCode`/`bookingCode`) chỉ còn màu xanh + con trỏ tay, bỏ `underline decoration-dotted`; mã không bấm được (`encounterNo`) đổi `text-blue-700` → `text-slate-800` (đen, tránh hiểu nhầm là link). Đã xác minh thật qua Playwright ở cả 3 danh sách (bệnh nhân, lịch hẹn, tiếp nhận).

- **Tăng độ đậm chữ dữ liệu trong bảng danh sách + gộp cột "Thời gian" 2 dòng ở Lịch hẹn (`docs/DECISIONS.md` #050)** — chủ dự án phản hồi chữ "mờ nhạt, không có điểm nhấn", yêu cầu chỉnh "tầm 400"; đo thật bằng `getComputedStyle` phát hiện chữ ĐÃ ở 400 sẵn (áp literal 400 sẽ vô tác dụng) — nâng lên `font-medium` (500) cho ô dữ liệu chính/phụ ở 5 bảng danh sách (bệnh nhân, lịch hẹn, tiếp nhận, danh mục hành chính × 2), giữ nguyên placeholder/badge/cột mã/metadata thứ yếu không đổi (giữ phân cấp thị giác). Đồng thời Lịch hẹn (chế độ Danh sách): bỏ cột "Thời lượng", đổi "Ngày giờ" → "Thời gian" (dời ra sau "Mã đặt lịch", trước "Họ tên"), hiển thị 2 dòng giờ đậm xanh/ngày nhỏ nhạt theo mẫu chủ dự án gửi. Đã xác minh thật qua Playwright.

- **Căn lề bảng danh sách, màu nhấn cột "Thời gian", dọn sạch `font-mono` (`docs/DECISIONS.md` #051)** — tiêu đề cột LUÔN canh giữa; dữ liệu canh trái cho cột Tên (Họ tên, Bác sĩ)/Địa chỉ, còn lại canh giữa, áp cho 5 bảng danh sách. Cột "Thời gian" ở Lịch hẹn đổi màu giờ sang `text-brand-teal` (#099078). Phát hiện + sửa dứt điểm nguyên nhân gốc slashed zero: `font-variant-numeric: lining-nums` (sửa lần đầu #CHANGELOG 2026-08-15(2)) KHÔNG tắt được số 0 gạch chéo của font monospace — phải bỏ hẳn `font-mono`; quét lại thấy sót 3 nơi (`AppointmentDetailPanel.tsx`, `ReferenceCatalogPane.tsx`, `GeoPane.tsx`), đã dọn sạch, xác nhận `grep -r "font-mono" apps/web/src` rỗng. Đã xác minh thật qua Playwright.

- **Thiết kế lại "Tiếp nhận bệnh nhân" — hiện đủ field bệnh nhân thay `PatientPicker`, tra trùng SĐT/CCCD qua popup, thêm Loại tiếp nhận/Hình thức khám/Ưu tiên khám/Chỉ định dịch vụ khám (`docs/DECISIONS.md` #052)** — mockup Artifact duyệt qua nhiều vòng chỉnh trên bản chạy thật. Bỏ `PatientPicker` khỏi màn Tiếp nhận, hiện thẳng `PatientFormFields` — gõ SĐT/CCCD tự tra trùng, bật popup xác nhận (`PatientMatchDialog.tsx`, mới, thay banner phẳng cũ — áp dụng nhất quán cho cả PAT-03) thay vì tự khoá âm thầm; chọn xong khoá riêng 4 field định danh (CCCD/Mã BN/Họ tên/Ngày sinh), còn lại sửa được, sửa xong tự PATCH hồ sơ gốc. 4 danh mục mới tái dùng `reference_catalog` (Loại tiếp nhận/Hình thức khám/Lý do ưu tiên/Loại giá dịch vụ, không seed sẵn — `clinic_admin` tự tạo). `encounter` thêm 7 cột (Ưu tiên khám bắt buộc kèm lý do; nhóm "Chỉ định dịch vụ khám" — Loại giá dịch vụ/Đơn vị/Số lượng/Thành tiền — chỉ lưu hiển thị, KHÔNG tính viện phí, "Thanh toán sau" chỉ là cờ UI không lưu DB). Endpoint mới `GET /patients/by-national-id`. Giới tính bệnh nhân không còn mặc định "Nữ" (áp dụng luôn cho Thêm/Sửa bệnh nhân, đúng quy tắc `ui-guidelines.md` 4.1c). **Đã xác minh thật**: 258/258 test `apps/api` (27 `reception-http.spec.ts` + 4 `by-national-id` mới), `pnpm -w typecheck/lint/build` sạch, Playwright qua trình duyệt thật (tạo mới/khớp SĐT/khớp CCCD/bảng dịch vụ tính đúng).

- **Panel "Chi tiết lịch hẹn" — tách "Sửa lịch" khỏi "Dời lịch", "Tiếp nhận" điều hướng trang, popup "Tiếp nhận thành công" (`docs/DECISIONS.md` #053)** — "Dời lịch" (`POST /appointments/:id/reschedule`, thay `PATCH .../reschedule` cũ S2-09) tạo `appointment` MỚI cho ngày/giờ/bác sĩ đã chọn (kế thừa fullName/phone/reason/roomId/durationMinutes/source từ lịch cũ), lịch cũ chuyển `status='RESCHEDULED'` (cột mới `rescheduled_from_id` trỏ về, migration `20260818150000_appointment_reschedule_status`). "Sửa lịch" khôi phục lại (`PATCH /appointments/:id`, tại chỗ, chỉ trong ngày — không có ô chọn ngày) tồn tại song song. `DoctorAvailabilityList.tsx` (mới, `apps/web/src/features/appointment/`) — danh sách bác sĩ trống/bận dùng chung cho cả Đặt lịch nhanh lẫn Dời lịch/Sửa lịch (trùng lặp lần 2, tách theo CLAUDE.md). Nút "Tiếp nhận" đổi từ mở popup (`ReceptionIntakeDialog.tsx`, đã xoá) sang điều hướng `/reception/new` kèm `location.state.checkin` — đảo ngược #043/#044. `ReceptionIntakeForm.tsx`: nộp "Lưu tiếp nhận" (`mode='direct'`) hiện popup "THÀNH CÔNG" thay vì điều hướng ngay, nút "Tiếp nhận mới" xoá form ở lại trang; footer đổi "Huỷ"→"Quay lại", thêm nút "Nhập lại" (`resetForm()`, giữ nguyên fullName/phone/doctorId từ `checkin` khi ở `mode='checkin'`). **Đã xác minh thật**: `appointment-http.spec.ts` viết lại 2 describe (`PATCH :id` 7 test + `POST :id/reschedule` 8 test, 56/56 pass), tổng 259 test `apps/api` không regress (trừ race điều kiện tiền-nhiệm đã biết ở `geo-http.spec.ts` khi chạy song song toàn suite). `pnpm -w typecheck/lint/build` sạch. Playwright qua trình duyệt thật: Dời lịch tạo đúng lịch mới + lịch cũ hiện "Đã dời lịch"; Sửa lịch lưu tại chỗ đúng cùng mã; Tiếp nhận điều hướng đúng kèm prefill; popup "THÀNH CÔNG" đúng mẫu tham khảo.

- **ADM-07 (Vai trò tuỳ biến + ma trận phân quyền) — đã xong (`docs/DECISIONS.md` #057)** — mockup Artifact duyệt qua 2 vòng (chốt bố cục CRUD Xem/Thêm/Sửa/Xoá/Đặc quyền mở rộng theo ảnh tham khảo chủ dự án gửi) trước khi code. Trang "Vai trò & Phân quyền" mới (`/admin/catalog-organization`, thay `ComingSoonPage`, pill trong `ConfigScreenShell`): cột trái danh sách 5 vai trò mặc định + vai trò tuỳ biến, cột phải bảng ma trận (hàng = tính năng nhóm theo 6 khu vực, cột = phạm vi dữ liệu Không/Cá nhân/Khoa-phòng/Toàn bộ). `clinic_admin` tạo/đổi tên/ẩn được vai trò tuỳ biến; 5 vai trò mặc định chỉ sửa ma trận. Backend mới `apps/api/src/modules/iam/role*.ts` (6 endpoint `/roles*`, dùng chung quyền `role_permission.manage`, không permission mới). **Ripple bắt buộc sang `user_account`**: `roleNames` (enum 5 tên cố định) → `roleIds` (uuid thật) trong `createUserAccountRequestSchema`/`updateUserAccountRequestSchema`, `currentUserSchema.roles` nới từ enum sang `string[]` — nếu không đổi, vai trò tuỳ biến tạo ra không gán được cho ai. Migration `20260820090000_role_management` (UNIQUE `role(tenant_id,name)` đổi thường→PARTIAL). **Giới hạn đã biết, chấp nhận ở v1**: Sidebar.tsx vẫn ẩn/hiện menu theo tên cứng 5 vai trò hệ thống — vai trò tuỳ biến không tự có đúng menu tương ứng dù quyền backend đúng.
  **Đã xác minh thật**: `role-http.spec.ts` (18/18), `user-account-http.spec.ts` cập nhật toàn bộ payload (18/18) — tổng 315 test `apps/api` + 73 `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua Chrome thật: toàn bộ luồng tạo/đổi tên/ẩn vai trò, sửa/lưu ma trận, tên vai trò hệ thống hiển thị tiếng Việt — không lỗi console.

## Đang chờ

- **Huỷ lượt khám + hoàn tiền (#085) — ĐÃ VERIFY XONG ở #087 (28/08/2026), không còn treo.** Code + test tự động (`packages/core`/`apps/api`) đã xong và pass đủ, nhưng chưa từng kiểm qua UI thật: 4 nút "Hủy"/"Hủy khám" (Danh sách tiếp nhận/Hàng đợi khám/Màn khám/Chi tiết phiếu thu, chữ đã đổi ở #086), "Trả về hàng chờ", nút "Hoàn tiền" (ẩn/hiện đúng theo quyền `invoice.refund`), badge 4 trạng thái + "Cần hoàn tiền" ở `/billing`. Phiên #086 định làm việc này nhưng bị chuyển hướng bởi phản hồi UI khác — vẫn là việc đầu tiên khi quay lại phiên sau, cần dựng dữ liệu test (ca đang khám + phiếu đã thu) trước vì DB dev hiện không có sẵn.
- **Checkbox chọn dòng hàng loạt (#086) — thuần scaffolding, CHƯA có hành động hàng loạt nào thật.** `SelectionToolbar` chỉ hiện số đã chọn + "Bỏ chọn". Khi chủ dự án chốt hành động cụ thể (ví dụ xoá/xuất/gán hàng loạt), thêm nút vào `children` của `SelectionToolbar`, không cần đổi gì ở 9 bảng đã áp dụng.
- ~~"Khoá bảng ca" theo tháng (#110) — chưa verify Playwright chính thức~~ **ĐÃ VERIFY XONG (03/09/2026), không còn treo.** Qua Chrome thật: `dev.admin` (có quyền unlock) — tháng hiện tại không khoá, tháng trước khoá đúng (banner amber + nút "Sửa"), bấm "Sửa" → banner "đang chỉnh sửa" + "Khoá lại" + ghi được 1 ca thật vào tháng đã khoá (xoá lại sau khi xác nhận) → "Khoá lại" trở về khoá; tăng `workShiftAssignmentLockGraceDays` lên 15 → tháng trước tự mở khoá không cần bấm "Sửa"; trả về 0 → khoá lại ngay. Tạo riêng 1 vai trò test (sao chép ma trận `clinic_admin` qua API, bỏ đúng quyền `work_shift_assignment.unlock`) + 1 tài khoản test qua HTTP API bằng `dev.admin` (không SQL/role superuser) — xác nhận đúng biến thể còn lại: banner khoá hiện, KHÔNG có nút "Sửa", đúng thông báo "liên hệ người có quyền...", không có nút "Thêm ca" nào lách được. Đã dọn sạch: xoá ca test, vô hiệu hoá tài khoản test, ẩn vai trò test, trả `workShiftAssignmentLockGraceDays` về 0.
- Đổi contract bất kỳ endpoint nào phải chạy lại `pnpm --filter @nexamed/api run openapi:generate` rồi `pnpm --filter @nexamed/web run api:codegen` — không sửa tay `openapi.json`/`openapi-schema.d.ts`.
- S2-04 (BHYT) lùi lại sau Sprint 3 theo quyết định đã chốt, không chặn.
- ~~"Ca làm việc riêng từng bác sĩ" — chưa làm~~ **ĐÃ LÀM XONG** qua "Ca làm việc" Giai đoạn 1+2 (`docs/DECISIONS.md` #101→#104, 02/09/2026): `work_shift` (mẫu ca) + `work_shift_assignment` (đăng ký ca theo ngày/nhân viên) + lưới Lịch hẹn hiện dải giờ ca theo cột bác sĩ + gạch chéo ngoài ca + công tắc "Chặn đặt lịch ngoài ca đã đăng ký" — đúng khái niệm #030 mục 5 từng lùi ở S2-09. Không còn treo, xoá dòng này khỏi mục "Đang chờ" (phát hiện lúc rà soát 2026-09-03, dòng cũ sót lại chưa dọn sau khi #101-104 hoàn tất).
- Đa chuyên khoa (nhi/sản/nha): chưa xây gói thật nào — chỉ mới có khung tối thiểu (`docs/DECISIONS.md` #033). Khi làm S3-06 (màn hình khám), component nhận cấu hình trường qua props, không hard-code riêng một chuyên khoa. Chọn gói đầu tiên khi có khách hàng thật trả tiền, không dựng khung trước — xem `docs/product/multi-specialty-analysis.md` mục 9.
- **Định nghĩa lại "ca khám" cho thông báo tự động hết giờ/Đóng ca — CHỐT: giữ nguyên hành vi hiện tại, không định nghĩa lại (02/09/2026).** Chủ dự án quyết định sau khi cân nhắc — `useClosingTimeReminder` vẫn dựa `isDoctor && !isEnded` + giờ đồng hồ như hiện có, chấp nhận hạn chế đã biết (bác sĩ đăng nhập chưa có bệnh nhân nào vẫn bị nhắc "Đóng ca" khi tới giờ). Không code gì thêm, không mở rộng `GET /doctor-availability`. Xem lịch sử phân tích các phương án đã cân nhắc (rồi bị bỏ) ở `docs/DECISIONS.md` #094 và ghi chú cũ.

## Lưu ý môi trường

- Máy dev hiện chạy Node v26.5.0, khác Node 20 LTS ghi trong `CLAUDE.md`/`package.json.engines`. Chưa gây lỗi (chỉ warning), nhưng cần dùng đúng Node 20 khi triển khai thật.
- Docker đã cài trên máy dev. Migration `20260807090207_init` **đã được áp thật lên Postgres 18.4** qua `prisma migrate deploy` và xác minh bằng insert thử — `id` sinh ra đúng UUID v7 (time-ordered, ví dụ `019fdc38-6d2f-7ff2-...`). Không còn là "chưa test".
- **Postgres container map ra cổng 5433, không phải 5432 mặc định** — máy dev này có sẵn một Postgres native (Windows service `postgres.exe`, không liên quan tới dự án) đang chiếm 5432. `DATABASE_URL` trong `.env.example`/`.env` đã trỏ sang `5433`. Nếu đổi máy dev khác không có xung đột này, có thể đổi lại 5432 trong `docker-compose.yml`.
- UUID v7 (`DECISIONS.md` #009) và cột `audit_log` (`DECISIONS.md` #007) đã chốt xong, không còn treo.
- Mật khẩu role `nexamed_app` đang hard-code trong migration (`'nexamed_app'`) — chỉ dùng được cho local dev/CI, **bắt buộc đổi** khi triển khai on-prem thật (S4-05), xem `docs/DECISIONS.md` #010.
