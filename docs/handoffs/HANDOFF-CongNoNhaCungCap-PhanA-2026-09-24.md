# Handoff — Công nợ nhà cung cấp, Phần A "Nền sổ công nợ" (CODE + TEST XONG, CHƯA VERIFY PLAYWRIGHT, CHƯA CẬP NHẬT DOCS) — chuyển phiên

**Ngày ghi**: 24/09/2026, chuyển phiên theo yêu cầu chủ dự án giữa chừng (không phải vì hết việc/bị chặn — đang đọc lại output `pnpm run build` thì bị ngắt). Tiếp nối trực tiếp `docs/handoffs/HANDOFF-CongNoNhaCungCap-2026-09-24.md` (handoff TRƯỚC, lúc đó "mockup đã duyệt, CHƯA CODE") — phiên này đã code xong TRỌN VẸN Phần A theo đúng 4 câu hỏi mở đã chốt (`docs/DECISIONS.md` #182). **CHƯA COMMIT gì** (`git status` sạch phần staged, toàn bộ thay đổi còn ở working tree).

## Việc đã xong trong phiên này (Phần A)

1. **Chốt 4 câu hỏi mở của #180** qua `AskUserQuestion` — ghi ở `docs/DECISIONS.md` #182: (1) tự duyệt điều chỉnh CHO PHÉP, gắn nhãn "Tự duyệt"; (2) huỷ phiếu nhập đã "Trả ngay" GIỮ NGUYÊN phiếu chi (NCC nợ lại); (3) **"Đối chiếu & chốt công nợ theo kỳ" (Phần E) — CHỐT LÀM LUÔN đợt này**, khác đề xuất "để sau" ban đầu — đã thêm vào mục 8 kế hoạch kỹ thuật, **CHƯA CODE, CHƯA CÓ MOCKUP**; (4) quyền mặc định CHỈ `clinic_admin` (5 quyền `read/pay/adjust/approve/unlock`).
2. Cập nhật `C:\Users\Administrator\.claude\plans\supplier-debt-cong-no-ncc.md`: mục 10 đánh dấu ĐÃ CHỐT, mục 8 thêm Phần E, mục 6 thêm quyền `unlock`. **File này vẫn còn dòng tiêu đề "(BẢN NHÁP, CHỜ DUYỆT MOCKUP — CHƯA CODE)" ở dòng 1 — CHƯA SỬA, việc kế tiếp phải sửa lại cho đúng ("Phần A đã xong").**
3. **Schema + migration** (`20260924100000_supplier_debt_phase_a`, **đã áp thật lên Postgres dev**): bảng mới `supplier_debt_account` (snapshot `balance`) + `supplier_debt_entry` (sổ append-only, enum `SupplierDebtEntryType` 8 giá trị — chỉ 3 giá trị PURCHASE/PAYMENT/OPENING_BALANCE có đường ghi thật ở Phần A); `stock_receipt` +4 cột "Trả ngay" (`prepaidAmount/prepaidPaymentMethodCode/prepaidCashAccountId/prepaidVoucherId`); `cash_voucher` +1 cột `supplierId`. Bảng `supplier_debt_adjustment` (Phần D) **CHƯA tạo** — cột `adjustment_id` trên `supplier_debt_entry` cũng chưa có, thêm bằng migration riêng khi code Phần D.
4. **`packages/core/src/supplier-debt/allocate-supplier-debt.ts`** — hàm thuần FIFO (Q5), 12 unit test PASS, đã đối chiếu khớp TUYỆT ĐỐI với số liệu mẫu "Minh Châu" trong mockup (28.340.000 + từng dòng PN12/PN19/PN27).
5. **Permissions**: `supplier_debt.read/pay/adjust/approve/unlock` — chỉ `clinic_admin=global`, vai trò khác chưa có gì (đúng chốt Q4).
6. **`packages/shared/src/supplier-debt.ts`** (schema API mới) + mở rộng `packages/shared/src/inventory.ts` (trường "Trả ngay" trên create/update/summary schema, filter `supplierId` cho `listStockReceiptsQuerySchema`).
7. **Backend `apps/api/src/modules/supplier-debt/`** — 2 repository, `SupplierDebtService`, `SupplierDebtController` (5 endpoint: `GET summaries`, `GET :id/summary`, `GET :id/ledger`, `GET :id/receipts`, `POST :id/opening-balance`).
8. **Hook xuyên module** (chạy TRONG CÙNG transaction, không qua port — xem comment `supplier-debt.module.ts`): `StockReceiptService.approve()` → `SupplierDebtService.recordPurchaseApproval()` (ghi PURCHASE + tạo voucher "Trả ngay" nếu có); `CashVoucherService.approve()`/`voidVoucher()` → `recordVoucherPosted()`/`reverseVoucherPayment()` khi voucher có `supplierId`.
9. **Sự cố module wiring THẬT đã gặp + sửa** (đáng đọc kỹ nếu đụng lại module này): thêm `SupplierDebtModule` vào chuỗi `inventory ⇄ drug`/`cashier-shift ⇄ cash-book/billing` sẵn có tạo ra vòng require() DÀI xuyên nhiều file (`EncounterModule → BillingModule → CashierShiftModule → CashBookModule → SupplierDebtModule → DrugModule → InventoryModule`), khiến `InventoryModule.imports` nhận `undefined` ở `BillingModule`/`EncounterModule`/`SupplierDebtModule` nếu không bọc `forwardRef()`. Đã sửa bằng cách bọc `forwardRef()` cho CẢ 3 trong `inventory.module.ts`, và cho `DrugModule`/`CashierShiftModule`/`CashBookModule` trong `supplier-debt.module.ts` — xem comment đầy đủ trong 2 file đó, đừng gỡ bỏ `forwardRef` tưởng là thừa.
10. **`tenant-fixture.ts`** — dọn 2 bảng mới đúng thứ tự FK, "xoá theo tầng" cho `reversalOfId` tự tham chiếu (đúng khuôn `prescription.supersedesId`) dù Phần A chưa tạo REVERSAL nào qua đường bình thường (chỉ qua `reverseVoucherPayment()`).
11. **OpenAPI + web codegen đã sinh lại** (`openapi.json`, `openapi-schema.d.ts`).
12. **Test HTTP `supplier-debt-http.spec.ts`** (20/20 PASS) — bao phủ: Duyệt phiếu ghi đúng netAmount (kể cả có chiết khấu), "Trả ngay" một phần/đủ/vượt (422), voucher PENDING_APPROVAL rồi Duyệt sau (FIFO fallback, có ghi rõ đơn giản hoá trong code), huỷ phiếu chi đã POSTED (REVERSAL đảo đúng) và huỷ khi còn PENDING_APPROVAL (no-op), Khai nợ đầu kỳ (1 lần, số âm được, 409 khi đã có bút toán — kể cả PURCHASE), `GET /summaries`, cách ly tenant, permission.
13. **Toàn bộ suite workspace PASS, không hồi quy**: `apps/api` 967/967, `packages/core` 224/224, `packages/shared` 25/25, `apps/web` 5/5.
14. **Frontend**: `apps/web/src/features/supplier-debt/` (`supplier-debt.api.ts`/`supplier-debt.queries.ts`/`SupplierDetailPage.tsx` — header + `StatCardRow` 3 số liệu + banner "chờ duyệt" + 2 tab "Phiếu nhập"/"Sổ công nợ" + dialog "Khai nợ đầu kỳ"; tab "Phiếu nhập" tự ghép `GET /inventory/receipts?supplierId=` với `GET /supplier-debt/:id/receipts` theo `stockReceiptId`, KHÔNG gọi `inventory` từ backend `supplier-debt` — xem lý do trong comment `supplier-debt.module.ts`). `SupplierPane.tsx` thêm cột "Tổng mua"/"Còn nợ" + nút "Xem" (chỉ hiện với `supplier_debt.read`). Route `/suppliers/:id` gác bằng `RequirePermissionRoute module="supplier_debt" action="read"` (KHÁC gate `DRUG_MANAGE_PERMISSIONS` của trang danh sách `/suppliers` — cố ý, vì đây là dữ liệu công nợ/tiền).
15. `apps/web` typecheck sạch. `pnpm run build` đã CHẠY XONG thành công ("✓ built in 41.38s", `SupplierDetailPage` tách chunk lazy riêng 20.46 kB/gzip 6.78 kB) — **NHƯNG bị ngắt phiên ngay khi đang đọc lại output đầy đủ, CHƯA xác nhận chunk khởi động (`index` + `vendor`) có vượt trần 500 kB hay có dòng cảnh báo "chunk size" nào không** (số liệu thấy được: `index-VDRMZ2nU.js` 184.45 kB, `vendor` 339.37 kB — cộng lại xấp xỉ ngưỡng, cần xem kỹ dòng cảnh báo Vite nếu có, và kiểm tra `SupplierPane.tsx` thêm import mới có vô tình kéo gì nặng vào chunk không-lazy hay không, vì trang danh sách NCC hiện chưa `lazy()` theo `SupplierManagementPage` sẵn có — cần xác nhận `SupplierPane` không nằm trong chunk khởi động).

## Việc kế tiếp — theo đúng thứ tự

1. **Đọc lại đầy đủ output `pnpm run build` ở `apps/web`** (chạy lại `pnpm --filter @nexamed/web run build`) — xác nhận không có cảnh báo "chunk size" nào, đặc biệt `index`/`vendor` có vượt 500 kB không (`.claude/docs/coding-standards.md` mục "Hiệu suất"). Nếu vượt, xử lý đúng tiền lệ #148/#154 (tách `manualChunks`, không lazy hoá tuỳ tiện làm vỡ tối ưu sẵn có).
2. **Verify Playwright/Chrome thật** cho toàn bộ luồng Phần A (chưa làm gì ở bước này):
   - Trang `/suppliers`: cột "Tổng mua"/"Còn nợ" hiện đúng, nút "Xem" điều hướng đúng `/suppliers/:id`.
   - Trang chi tiết NCC: header/StatCardRow đúng số liệu, banner "chờ duyệt" khi có phiếu PENDING_APPROVAL, 2 tab đúng dữ liệu (đặc biệt tab "Phiếu nhập" ghép đúng `receiptNo`/`occurredAt`/"(đã huỷ)" từ 2 nguồn API).
   - Dialog "Khai nợ đầu kỳ": Lưu thành công, gọi lần 2 báo lỗi đúng (409), số âm hoạt động đúng.
   - Luồng "Trả ngay" thật qua UI: tạo phiếu nhập PURCHASE có "Trả ngay" (form `StockReceiptFormPage.tsx` — **LƯU Ý: form CHƯA có UI cho 3 trường `prepaidAmount`/`prepaidPaymentMethodCode`/`prepaidCashAccountId` mới thêm vào schema** — chỉ backend đã sẵn sàng nhận, PHẢI thêm khối "Thanh toán" vào `StockReceiptFormPage.tsx` theo đúng mockup màn 5 trước khi verify được luồng này qua UI thật. Đây là việc CÒN THIẾU, không phải đã làm — kiểm tra lại `apps/web/src/features/inventory/StockReceiptFormPage.tsx` xem đã có khối này chưa, nếu chưa thì đây là việc phải làm TRƯỚC KHI verify Playwright, không thể test qua UI nếu không có chỗ nhập).
3. **Cập nhật tài liệu** (chưa làm gì ở bước này, đều đang treo):
   - `docs/DECISIONS.md` — thêm mục mới (≥#183) ghi lại "Phần A code + test xong" theo đúng khuôn các mục trước (tổng số test, chunk size xác nhận, kết quả verify Playwright).
   - `docs/CHANGELOG.md`/`docs/CURRENT.md`/`docs/TASK.md` — cập nhật ngắn gọn (feedback_concise_doc_updates: chỉ file liên quan, không viết dài).
   - `CLAUDE.md`/`docs/product/prd.md` mục phạm vi v1 — làm rõ "công nợ NCC" (đã mở rộng) khác "công nợ bệnh nhân" (vẫn ngoài v1) — việc này đã ghi là "CHƯA làm" từ handoff TRƯỚC, tới giờ VẪN CHƯA LÀM, cần làm ở bước này.
   - `docs/ERD.md` — thêm 2 bảng mới + 5 cột mở rộng vào sơ đồ.
   - `C:\Users\Administrator\.claude\plans\supplier-debt-cong-no-ncc.md` dòng 1 — sửa lại tiêu đề, không còn "CHƯA CODE".
4. **Sau khi verify + cập nhật docs xong Phần A** → bắt đầu **Phần B — Thanh toán** (Thanh toán công nợ trên TỔNG nợ + hook duyệt/từ chối/huỷ phiếu chi + trang "Công nợ nhà cung cấp" `/suppliers/debts` + trang "Phiếu thanh toán NCC" `/suppliers/payments`) — đọc lại mục 8 kế hoạch kỹ thuật trước khi bắt đầu, đúng nhịp độ "mỗi phần: code → test → verify → docs".

## Việc KHÔNG làm (đừng tự mở rộng)

- KHÔNG bắt đầu Phần C/D/E trước khi Phần A verify + docs xong và Phần B chưa tới lượt.
- KHÔNG tự chọn thiết kế mockup cho khối "Thanh toán" trên `StockReceiptFormPage.tsx` (việc kế tiếp #2 ở trên) khác với mockup màn 5 đã duyệt (`https://claude.ai/artifact/WvZtCgwbdEKAb9LcCzyCfh`) — đọc lại mockup trước khi code UI này.
- KHÔNG gỡ `forwardRef()` trong `inventory.module.ts`/`supplier-debt.module.ts`/`cash-book.module.ts` dù trông "thừa" — đã xác nhận bằng lỗi thật lúc test (mục 9 ở trên).

## Phát hiện phụ chưa xử lý (từ handoff trước, vẫn còn treo, không liên quan trực tiếp)

- `CashVoucherService.voidVoucher()` có thể không đảo số dư "Ví tạm ứng" khi huỷ phiếu nạp/tất toán (`PATIENT_ADVANCE`) — chưa kiểm chứng bằng test, chưa sửa, ngoài phạm vi Công nợ NCC.
- Q10 (#180): chưa có cơ chế badge "chờ duyệt" hiển thị được ở MỌI trang trong app (không riêng "Công nợ nhà cung cấp") — để dành bàn riêng.
