# Handoff — Kho Thuốc GĐ4, phần 3/4/5 (Xuất kho mở rộng / Nhập kho mở rộng / Báo cáo NXT) — CODE XONG, TEST DỞ DANG, CHƯA COMMIT

**Ngày ghi**: 23/09/2026 (giữa phiên, theo yêu cầu "ghi file để chuyển phiên"). **Trạng thái**: backend + frontend của cả 3 phần còn lại của GĐ4 đã viết xong, `pnpm -w typecheck/lint/build` từng chạy sạch ở một mốc trước đó trong phiên (chưa chạy lại sau khi thêm test mới — xem mục "Việc kế tiếp" #1). **Toàn bộ thay đổi CHƯA COMMIT** — `git status`/`git diff` trên working tree là nguồn sự thật, không phải file này. Không có mockup nào chưa duyệt: mockup đã được duyệt rõ ràng qua AskUserQuestion trong phiên (không phải suy diễn từ "tiếp tục công việc").

## Việc kế tiếp ưu tiên — theo đúng thứ tự

1. **Tiếp tục viết HTTP e2e test còn thiếu, rồi chạy lại toàn bộ suite `apps/api`:**
   - **Phần 3 (Phiếu xuất kho mở rộng)**: vừa thêm xong 1 describe block mới `'Phiếu xuất kho mở rộng — Nháp→Duyệt (docs/DECISIONS.md #170)'` ở cuối `apps/api/src/modules/inventory/stock-issue-http.spec.ts` (trước dấu đóng `});` cuối file) — **CHƯA CHẠY LẦN NÀO để xác nhận pass**. Bao phủ: thiếu departmentId (400)/WRITE_OFF kèm departmentId sai (400)/thiếu lý do (400)/403 tạo khi không có `stock_issue.create`/tạo Nháp OK không đụng tồn/sửa Nháp OK nhưng 403 khi Duyệt (bác sĩ không có `stock_issue.approve`)/Duyệt thiếu tồn → 422 không đổi trạng thái/Duyệt đủ tồn → POSTED, trừ tồn đúng, ledger reason đúng, **tiền = 0** (loại này không gắn giá bán)/Từ chối → REJECTED không đụng tồn/**Huỷ phiếu POSTED loại mới → hồi quy cho bug `listForSourceIssue()` đã sửa trong phiên** (test then đảo ngược đúng 80, KHÔNG dừng ở 50)/tenant B 404 khi get/duyệt phiếu tenant A.
   - **Phần 4 (RETURN_FROM_USE + Chiết khấu nhập kho)**: **CHƯA VIẾT TEST NÀO**. Cần thêm vào file test của `stock_receipt` (kiểm tra tên file đúng — có thể là `inventory-http.spec.ts` hoặc file riêng, dò lại bằng Grep `describe.*stock_receipt` hoặc `describe.*Phiếu nhập kho`). Nội dung cần phủ: tạo+duyệt+huỷ phiếu `RETURN_FROM_USE` (**đây cũng là 1 test hồi quy cho bug `listForSourceReceipt()` đã sửa trong phiên** — trước khi sửa, huỷ phiếu loại này không đảo tồn), chiết khấu mode `TOTAL` end-to-end (tổng tiền hàng → trừ chiết khấu → thành tiền đúng, `discountAmount`/`netAmount` trả về đúng ở cả list và detail), chiết khấu mode `PER_LINE` end-to-end, validate: đặt chiết khấu trên loại phiếu KHÁC `PURCHASE` → 400, đặt cả `discountType`+giá trị TOTAL và PER_LINE cùng lúc → 400 (2 mode loại trừ nhau — xem `checkStockReceiptDiscountRules()` trong `packages/shared/src/inventory.ts`).
   - **Phần 5 (Báo cáo Nhập-Xuất-Tồn)**: **CHƯA VIẾT TEST NÀO**. Cần file test mới hoặc thêm vào file có sẵn cho `stock-ledger-report`. Nội dung cần phủ: số liệu đầu kỳ/nhập/xuất/cuối kỳ đúng qua 1 kịch bản nhập rồi xuất nhiều lần trong và ngoài khoảng ngày lọc (đầu kỳ phải cộng dồn đúng các biến động TRƯỚC `from`, không chỉ đơn giản = tồn hiện tại), lọc theo `warehouseId`, 403 khi không có quyền `stock_receipt.report` (thử với vai trò không phải `clinic_admin`), endpoint export trả về buffer Excel hợp lệ (kiểm `Content-Type`, không cần parse nội dung — đúng khuôn các test export Excel khác trong repo, tham khảo `cash-flow-report` test nếu có) + có ghi audit log (`recordExportAudit()`).
   - Sau khi viết xong cả 3, chạy `pnpm --filter @nexamed/api run test` (toàn bộ, không chỉ file mới) và `pnpm --filter @nexamed/core run test` để bắt hồi quy — đối chiếu số lượng test pass với con số cuối cùng đã biết trước phiên này (xem `docs/CHANGELOG.md`/handoff GĐ4 phần 2 để biết baseline gần nhất, khoảng 906 test `apps/api`).
   - Chạy `pnpm -w run typecheck` và `pnpm -w run lint` lại từ đầu — đã sạch một lần trước khi thêm test mới, nhưng CHƯA xác nhận lại sau đó.

2. **Đồng bộ `role_permission` cho permission mới** (bài học lặp lại nhiều lần — #087/#089/#103/#104/#110/#158/#167): 2 permission mới `stock_receipt.report` và `stock_issue.approve` đã thêm vào `DEFAULT_ROLE_PERMISSIONS` trong `packages/core/src/rbac/permissions.ts` (chỉ `clinic_admin`, scope `global`) nhưng **CHƯA chắc đã chạy seed/migrate để áp vào DB dev thật**. Cần kiểm tra bảng `role_permission` trong Postgres dev trước khi verify Playwright — nếu thiếu, chạy lại đúng lệnh seed permission của dự án (tham khảo `apps/api/src/infrastructure/persistence/seed-permissions.ts` / `seed-tenant-roles.ts`, hoặc lệnh `pnpm db:migrate` nếu seed permission chạy kèm).

3. **Khởi động lại `pnpm dev`**: đã bị dừng giữa phiên trước (để gỡ khoá file Windows cho `prisma generate`), **CHƯA khởi động lại** — vẫn nợ từ phiên trước, chưa trả. API cổng 3001, Web cổng 5173.

4. **Verify Playwright/Chrome thật** cho cả 3 phần (chưa làm, cần làm SAU khi test HTTP pass hết):
   - `/inventory/issues/manual/new` (tạo Nháp 3 loại, chú ý field Khoa/Phòng chỉ hiện với `INTERNAL_ALLOCATION`), danh sách `/inventory/issues` (badge 4 trạng thái, nút Sửa/Từ chối/Duyệt theo đúng trạng thái+quyền), luồng Duyệt thiếu tồn báo lỗi rõ ràng trên UI.
   - `/inventory/receipts/new` chọn loại `RETURN_FROM_USE`, khối chiết khấu (toggle `TOTAL`/`PER_LINE` KHÔNG có giá trị mặc định tô sẵn — đúng mục 4.1c ui-guidelines.md), cột "Chiết khấu" trong bảng dòng chỉ hiện khi mode = PER_LINE, chân trang hiện đúng breakdown Tổng tiền hàng/Chiết khấu/Thành tiền.
   - `/inventory/reports/stock-ledger` — bộ lọc ngày+kho, 4 thẻ KPI, bảng, nút "Xuất Excel" tải file thật.
   - So khớp lại với mockup đã duyệt: `https://claude.ai/artifact/NVC5A4uZsmX9kFsAk5Td88` (đọc qua `Artifact({action:"read", url:...})`).

5. **Đồng bộ tài liệu** — theo đúng tiền lệ #072/#146...#173: **CHỈ làm bước này sau khi CẢ 5 phần của GĐ4 xong** (2 phần đầu — Kiểm kê, Điều chuyển kho — đã xong từ trước; 3 phần này xong test+Playwright là đủ 5/5). Khi đó cập nhật 1 lượt: `CLAUDE.md` (đoạn Kho Thuốc, thêm dòng chốt GĐ4 hoàn tất), `docs/ERD.md` (mục 3 đang nợ tài liệu cho `stock_issue`/`stock_count` từ trước — tiện thể bổ sung luôn phần mở rộng GĐ4 phần 3/4/5 + cột mới trên `stock_issue`/`stock_receipt`/`stock_receipt_line`), `docs/product/prd.md`, `docs/product/plan.md`, `docs/TASK.md`, `docs/CHANGELOG.md`, `docs/DECISIONS.md` (mục mới, số hiệu tiếp theo #174+ tuỳ số đã dùng — kiểm lại cuối `docs/DECISIONS.md` trước khi đặt số).

## Tóm tắt việc đã làm trong phiên này

### Migration + Schema
- `apps/api/prisma/migrations/20260923100000_stock_issue_status_draft_rejected/` — thêm `DRAFT`/`REJECTED` vào enum `stock_issue_status` (tách riêng transaction theo đúng tiền lệ #091/#114, không dùng chung migration với chỗ dùng giá trị mới).
- `apps/api/prisma/migrations/20260923110000_stock_issue_receipt_gd4_extend/` — thêm `approved_by`/`approved_at`/`rejection_reason`/`department_id` (+FK Department) vào `stock_issue`; thêm `discount_type`/`discount_value`/`discount_reason` vào `stock_receipt` và `discount_type`/`discount_value` vào `stock_receipt_line` (dùng lại enum `invoice_discount_type` có sẵn, không tạo enum mới).
- **Đã áp thật lên Postgres dev** trong phiên (đã chạy `prisma generate` sau khi gỡ khoá file Windows — xem mục môi trường bên dưới) — không cần chạy lại `db:deploy`, nhưng CHƯA xác nhận migrate ở máy khác/CI.

### Backend
- `stock-issue.{controller,service,repository}.ts`: thêm 3 loại xuất kho lập tay (`INTERNAL_ALLOCATION`/`RETURN_TO_SUPPLIER`/`WRITE_OFF`) luồng Nháp→Duyệt/Từ chối, 4 route mới `POST issues/manual`, `PATCH issues/manual/:id`, `POST issues/manual/:id/approve`, `POST issues/manual/:id/reject`. Tiền luôn = 0 cho 3 loại này (không gắn giá bán, khác `RETAIL_SALE`).
- `stock-receipt.{service,repository}.ts`: thêm loại phiếu `RETURN_FROM_USE`; thêm Chiết khấu (tái dùng `computeInvoiceDiscount()`/`computeDiscountAmount()` từ `packages/core`, có sẵn từ Thu ngân #137) — 2 mode `TOTAL`/`PER_LINE` loại trừ nhau, chỉ áp cho `receiptType='PURCHASE'`.
- **2 bug thật phát hiện + sửa trong `stock-ledger.repository.ts`** (không nằm trong phạm vi yêu cầu ban đầu, phát hiện khi rà lại logic huỷ phiếu): `listForSourceReceipt()` thiếu `RECEIPT_RETURN_FROM_USE` trong danh sách reason được đảo ngược khi huỷ phiếu; `listForSourceIssue()` chỉ lọc `ISSUE_RETAIL_SALE`, thiếu 3 reason mới. Nếu không sửa, huỷ phiếu loại mới sẽ ÂM THẦM không hoàn tồn kho. Đã có test hồi quy cho cả 2 (xem mục "Việc kế tiếp" #1).
- `stock-ledger-report.{controller,service}.ts` + `stock-ledger-report-export.service.ts` (file mới) — Báo cáo Nhập-Xuất-Tồn, dùng `StockLedgerRepository.sumQuantityGrouped()` (method mới, Prisma `groupBy`) gọi 3 lần (đầu kỳ/nhập/xuất) rồi merge theo `drugId:warehouseId`. Export dùng `exceljs`, endpoint export **không đăng ký OpenAPI** (đúng quy ước cho endpoint tải file nhị phân).
- Permission mới: `stock_receipt.report` (Báo cáo NXT) và `stock_issue.approve` (Duyệt/Từ chối phiếu xuất kho mở rộng) — cả 2 chỉ gán mặc định cho `clinic_admin`/`global`. **Quyết định trong phiên qua AskUserQuestion**: KHÔNG tái dùng `stock_receipt.read` cho báo cáo như kế hoạch gốc đề xuất — tách quyền riêng, giống mẫu `cash_voucher.report`.
- `packages/core/src/errors/inventory-errors.ts`: thêm `StockIssueNotDraftError` (`STOCK_ISSUE_NOT_DRAFT`, đã đăng ký trong `domain-exception.filter.ts` → 409 CONFLICT).
- `packages/shared/src/inventory.ts`: mở rộng lớn — `manualStockIssueTypeSchema`, `createManualStockIssueRequestSchema`/`updateManualStockIssueRequestSchema` (validate departmentId bắt buộc ↔ INTERNAL_ALLOCATION), `approveStockIssueRequestSchema`/`rejectStockIssueRequestSchema`, discount fields trên `stockReceiptLineInputSchema`/`stockReceiptHeaderFieldsSchema` (validate 2 mode loại trừ nhau), `getStockLedgerReportQuerySchema`/`stockLedgerReportItemSchema`/`getStockLedgerReportResponseSchema`.

### Frontend
- `StockIssueFormPage.tsx` (mới) — tạo/sửa/xem phiếu xuất kho lập tay, theo khuôn `StockTransferFormPage.tsx` (search-and-pick, tách dòng theo lô), field Khoa/Phòng chỉ hiện với `INTERNAL_ALLOCATION`, chân trang 3 nút "Lưu nháp"/"Lưu & chuyển duyệt"/"Lưu & Duyệt ngay" (nút cuối chỉ hiện nếu actor có quyền `stock_issue.approve`).
- `StockIssueListPage.tsx`/`StockIssueDetailDialog.tsx`/`StockIssuePrintView.tsx`: mở rộng 4 trạng thái (badge màu), nút thao tác theo trạng thái+quyền, hiện tên Khoa/Phòng khi có.
- `ReasonConfirmDialog.tsx`: **refactor từ hardcoded stock_receipt sang generic** (nhận `onConfirm: (reason) => Promise<unknown>` thay vì tự gọi mutation cố định) — đã cập nhật cả 2 chỗ gọi cũ trong `StockReceiptListPage.tsx`, dùng thêm ở `StockIssueListPage.tsx`/`StockIssueFormPage.tsx`.
- `StockReceiptFormPage.tsx`: thêm option `RETURN_FROM_USE`, khối chiết khấu (toggle `TwoOptionToggle` mặc định KHÔNG chọn sẵn — đúng mục 4.1c ui-guidelines.md, tự sửa sau khi vi phạm ban đầu), cột "Chiết khấu" điều kiện trong bảng dòng (grid columns tính động qua biến `lineGridCols` dùng chung 3 chỗ thay vì lặp ternary).
- `StockLedgerReportPage.tsx` (mới) — theo đúng khuôn `CashFlowReportPage.tsx`, 4 thẻ KPI + bảng 8 cột + xuất Excel.
- `router.tsx`: route `inventory/issues/manual/new`, `inventory/issues/manual/:id`, `inventory/reports/stock-ledger` (permission `stock_receipt.report`).
- `Sidebar.tsx`: mục mới "Báo cáo Nhập-Xuất-Tồn" (icon `ChartLine`). **2 bug thật phát hiện + sửa tiện thể** (không thuộc phạm vi yêu cầu, phát hiện khi sửa sidebar): `WAREHOUSE_GROUP_PATHS` thiếu `/inventory/transfers` (auto-expand nhóm sai), điều kiện hiện nhóm "Quản lý kho" thiếu `canSeeStockTransfer` (vai trò chỉ có quyền `stock_transfer` không thấy nhóm menu luôn).

### Test
- Đã thêm 1 describe block mới cho phần 3 vào `stock-issue-http.spec.ts` (xem chi tiết mục "Việc kế tiếp" #1) — **đây là hành động cuối cùng trước khi ghi file handoff này, chưa chạy để xác nhận pass**.
- Phần 4, phần 5: chưa có test nào.

## Lưu ý môi trường

- **`pnpm dev` KHÔNG chạy** — đã dừng từ phiên trước để gỡ khoá `query_engine.dll.node` cho `prisma generate` (đã xin phép qua AskUserQuestion, đã đồng ý), **chưa khởi động lại**. Khởi động từ gốc repo (`pnpm dev`) trước khi test tay/Playwright — API cổng 3001, Web cổng 5173.
- `prisma generate` đã chạy thành công sau khi gỡ khoá — Prisma Client hiện tại khớp schema mới.
- OpenAPI + web codegen: **ĐÃ chạy lại trong phiên** (`apps/api/openapi/openapi.json` và `apps/web/src/shared/api/openapi-schema.d.ts` đều nằm trong danh sách file đã sửa) — khớp code hiện tại, không cần chạy lại trừ khi đổi thêm contract.
- **Toàn bộ thay đổi của phiên này CHƯA COMMIT** — không có commit hash nào để tham chiếu, khác các handoff trước. Khi commit, nhớ tách hoặc gộp hợp lý theo quy ước dự án (xem `git log` gần đây để tham khảo văn phong message, ví dụ `feat(api,web): Kho Thuoc GD4 ...`).

## Việc KHÔNG làm (đã chốt hoặc cố ý hoãn, đừng tự mở rộng)

- Chưa đồng bộ tài liệu cấu trúc (`CLAUDE.md`/ERD/PRD/plan/TASK/CHANGELOG/DECISIONS) — cố ý đợi xong toàn bộ 5/5 phần GĐ4, đúng tiền lệ đã lặp lại nhiều lần trong dự án.
- Chưa verify Playwright — cố ý đợi sau khi test HTTP pass hết.
- Chưa retrofit phân quyền Khoa/Phòng cho báo cáo NXT hay cho 3 loại xuất kho mới theo kiểu đã làm cho `stock_transfer`/`stock_count` — phiên này CHƯA đặt câu hỏi này với chủ dự án, cần cân nhắc hỏi ở đầu phiên kế tiếp nếu thấy cần (mặc định permission `department`-scope vẫn hoạt động được vì `stock_issue.approve`/`create` vốn đã có sẵn cơ chế `warehouse-scope.helper.ts` dùng chung, nhưng CHƯA kiểm tra kỹ có áp dụng đúng cho 3 loại mới hay không — đáng ngờ, nên rà lại khi viết test phần 3 nếu có thời gian).
