# Handoff — Công nợ nhà cung cấp, Phần B "Thanh toán" (ĐÃ HOÀN TẤT — code + test + verify Playwright + cập nhật docs) — chuyển phiên

**Ngày ghi**: 24/09/2026, chuyển phiên theo yêu cầu chủ dự án (không phải vì hết việc/bị chặn — Phần B đã xong 100%, đang ở điểm dừng tự nhiên trước khi sang Phần C). Tiếp nối trực tiếp `docs/handoffs/HANDOFF-CongNoNhaCungCap-PhanA-2026-09-24.md` (Phần A) — phiên này làm nốt Phần B theo đúng thứ tự đã ghi ở việc kế tiếp #4 của handoff đó. **CHƯA COMMIT khi viết xong file này** — sẽ commit + push ngay sau (xem cuối file).

## Việc đã xong trong phiên này (Phần B)

1. **Không migration mới** — tái dùng nguyên schema Phần A (`supplier_debt_account`/`supplier_debt_entry`/`cash_voucher.supplier_id`).
2. **Backend**: `SupplierDebtService.recordPayment()` (mới) — "Thanh toán công nợ" trên TỔNG nợ, cùng khuôn tạo voucher như `recordPurchaseApproval()`'s "Trả ngay" nhưng là method top-level tự mở transaction riêng. Chặn trả vượt: `amount ≤ balance − pendingApprovalAmount` (422, tính CẢ phiếu chi khác đang Chờ duyệt cho cùng NCC để không cho cộng dồn vượt nợ thật). `listPayments()` (mới) — `CashVoucherRepository.listSupplierLinked()`, mặc định 90 ngày gần nhất khi bỏ trống `from`/`to` (S6-03 #142) qua hàm mới `resolveRecentDateRange()` (`packages/core/src/date/vietnam-day-range.ts` — **refactor** `CashVoucherService.resolveDateRange()` dùng chung, xoá trùng lặp lần 2 theo CLAUDE.md). 2 endpoint mới `POST /supplier-debt/:supplierId/payment` + `GET /supplier-debt/payments` (gate `supplier_debt.pay`/`.read` sẵn có, KHÔNG permission mới).
3. **Bug hook thật phát hiện + vá lúc code (không phải lúc verify)**: `CashVoucherService.approve()`/`voidVoucher()` đã hook `SupplierDebtService.recordVoucherPosted()`/`reverseVoucherPayment()` từ Phần A (cần cho "Trả ngay" khi bật duyệt), nhưng `create()` thì KHÔNG hook khi voucher `POSTED` NGAY (không cần duyệt) — thiếu đường ghi cho `recordPayment()` khi `cashVoucherApprovalEnabled=false`. Đã vá bằng cách gọi `applyVoucherEntry()` trực tiếp trong `recordPayment()` (không sửa `CashVoucherService.create()` — `recordPayment()` không đi qua nó, tự tạo voucher qua repository giống `recordPurchaseApproval()`).
4. **Frontend**: 2 trang mới `/suppliers/debts` ("Công nợ nhà cung cấp" — bảng tổng hợp mọi NCC + `StatCardRow` Tổng còn nợ/NCC đang nợ lại + banner chờ duyệt, mặc định ẩn NCC đã tất toán/chưa phát sinh, công tắc hiện lại) và `/suppliers/payments` ("Phiếu thanh toán NCC" — lịch sử mọi phiếu gắn NCC, lọc NCC/ngày/trạng thái, CHỈ ĐỌC — duyệt/từ chối vẫn ở trang "Phiếu thu/chi" chung, không nhân bản luồng duyệt). `SupplierDetailPage.tsx` thêm tab thứ 3 "Thanh toán" + nút "Thanh toán công nợ" (hiện khi `supplier_debt.pay` VÀ `balance>0`) mở dialog mới (số tiền + nút "Trả hết", phương thức, quỹ, ngày, ghi chú, khối xanh xem trước "Công nợ sau khi thanh toán", cảnh báo hổ phách khi `cashVoucherApprovalEnabled` bật). `shared/layout/Sidebar.tsx#NavItem` (dùng chung TOÀN APP) thêm prop `badge?: number` mới (pill hổ phách cuối dòng) — dùng cho "Công nợ nhà cung cấp" (đếm NCC có `pendingApprovalAmount>0`, tính từ `supplier-debt-summaries` đã tải sẵn, KHÔNG endpoint mới) — **khác** phương án "badge trên TÊN NHÓM sidebar" đã bị chủ dự án từ chối ở #180 (đây là badge trên đúng 1 mục lá cụ thể "Công nợ nhà cung cấp", không phải toàn nhóm "Quản lý nhà cung cấp").
5. **Route `/suppliers/debts`/`/suppliers/payments`** gác bằng `RequirePermissionRoute module="supplier_debt" action="read"` (đúng khuôn `/suppliers/:id`).
6. **Test HTTP `supplier-debt-http.spec.ts`** +9 test (29/29 file) — trả một phần, trả đủ, trả vượt 422, cộng dồn với phiếu chờ duyệt khác vượt nợ 422, luồng `cashVoucherApprovalEnabled` bật (PENDING_APPROVAL → Duyệt → balance giảm đúng), thiếu quyền 403, cách ly tenant 404, `GET /payments` lọc đúng theo supplierId + gồm cả "Trả ngay" lẫn "Thanh toán công nợ" đứng riêng.
7. **`packages/core` +8 test** (`resolveRecentDateRange`, `vietnam-day-range.spec.ts`).
8. **Toàn bộ suite workspace PASS, không hồi quy**: `apps/api` 976/976, `packages/core` (đã tính +8), `apps/web` 5/5, `pnpm -w typecheck/lint/build` sạch (2 trang mới lazy chunk ~5kB, không cảnh báo chunk size).
9. **OpenAPI + web codegen đã sinh lại** (`openapi.json`, `openapi-schema.d.ts`) — 2 route mới đã đăng ký thủ công trong `apps/api/scripts/generate-openapi.ts` (OpenAPI ở dự án này KHÔNG tự sinh từ NestJS controller, phải đăng ký tay từng route — dễ quên).
10. **Verify Playwright qua Chrome thật (`playwright-core` trỏ Chrome hệ thống)** trên **tenant test riêng** `01a0cc3c-8626-746b-9d2a-5ea0268ec19f` (đúng bài học #166, KHÔNG đụng tenant chủ dự án `01a070f4-...`) — cả 2 luồng đầu-cuối:
    - **Không cần duyệt**: "Trả hết" → chọn Phương thức/Quỹ → Xác nhận → voucher `POSTED` ngay, "Còn nợ" 500.000đ→0đ, hiện đúng ở tab "Thanh toán" lẫn trang "Phiếu thanh toán NCC".
    - **Cần duyệt** (`cashVoucherApprovalEnabled` bật tạm để test): dialog hiện cảnh báo hổ phách đúng, voucher `PENDING_APPROVAL`, "Còn nợ" GIỮ NGUYÊN + banner hổ phách (banner có sẵn từ Phần A, tự đúng cho nguồn Phần B) + **sidebar badge "1" cạnh "Công nợ nhà cung cấp" hiện đúng** → duyệt phiếu chi (qua API, trang "Phiếu thu/chi" chung) → "Còn nợ" giảm đúng.
    - 0 lỗi console suốt cả 2 luồng. Đã trả `cashVoucherApprovalEnabled` về `false` và `config.json` (không track git) về tenant thật sau khi xong. Dữ liệu test (2 NCC `NCC00001`/`NCC00002`, 2 phiếu `PCQ2609000001/2`) giữ nguyên trong DB tenant test — không xoá, không đụng tenant chủ dự án.
    - **Sự cố môi trường phát sinh + xử lý lúc verify**: cổng 5173 bị 1 process node.exe mồ côi từ trước chiếm (không phải của phiên này) → web dev server tự chuyển sang 5174 → CORS lỗi vì `WEB_ORIGIN` API cố định `http://localhost:5173` → đã `Stop-Process` process mồ côi + khởi động lại dev server đúng cổng 5173.
11. **Đã cập nhật tài liệu đầy đủ**: `CLAUDE.md` (dòng Phần B "đã xong"), `docs/product/prd.md` (SUP-03 "đã xây xong", version history v1.5), `docs/ERD.md` (v1.56, mục 3.10 mở rộng, mục 9 thêm dòng), `docs/CURRENT.md` (đoạn Phần B mới), `docs/CHANGELOG.md` (mục 2026-09-24 mới), `docs/TASK.md` (Phần B đánh dấu `[x]`), `docs/DECISIONS.md` (mục #184 mới, chi tiết đầy đủ), tiêu đề `C:\Users\Administrator\.claude\plans\supplier-debt-cong-no-ncc.md` (sửa "Phần A + Phần B ĐÃ XONG").

## Việc kế tiếp — theo đúng thứ tự

1. **Commit + push** — xem mục cuối file này (đang thực hiện ngay sau khi ghi handoff).
2. **Phần C — Trả hàng NCC** (mục 8 kế hoạch kỹ thuật `supplier-debt-cong-no-ncc.md`): `supplier_id`/giá trả trên phiếu xuất trả (dùng lại "Phiếu xuất kho mở rộng" GĐ4, loại `RETURN_TO_SUPPLIER` đã có enum nhưng CHƯA gắn NCC/tiền — xem mục 3 "Luồng nghiệp vụ" của kế hoạch) + ghi `RETURN` (giảm nợ) + "Thu tiền NCC hoàn lại" (chỉ khi `balance<0`, tạo `cash_voucher` INCOME → `REFUND_RECEIVED`, dùng lại hook `applyVoucherEntry()` đã có sẵn cho cả 2 chiều direction). Đọc lại mục 3/7/8 kế hoạch kỹ thuật trước khi bắt đầu — mockup đã có sẵn (màn 6 "Phiếu xuất trả NCC" trong Artifact `https://claude.ai/artifact/WvZtCgwbdEKAb9LcCzyCfh`), KHÔNG cần dựng mockup mới, chỉ cần đọc lại/xác nhận với chủ dự án nếu có điểm chưa rõ.
3. Sau Phần C → Phần D (Luồng xử lý sai sót) → Phần E (Đối chiếu & chốt công nợ theo kỳ, CHƯA có mockup — phải dựng Artifact riêng trước khi code, đúng nhịp độ dự án).

## Việc KHÔNG làm (đừng tự mở rộng)

- KHÔNG bắt đầu Phần D/E trước khi Phần C xong (đúng thứ tự đã chốt).
- KHÔNG tự thiết kế lại UI "Phiếu xuất trả NCC" khác mockup màn 6 đã duyệt — đọc lại Artifact trước khi code.
- KHÔNG gỡ `forwardRef()` trong `inventory.module.ts`/`supplier-debt.module.ts`/`cash-book.module.ts` (từ Phần A, vẫn cần nguyên).
- KHÔNG quên đăng ký route mới vào `apps/api/scripts/generate-openapi.ts` khi thêm endpoint Phần C — OpenAPI ở dự án này không tự sinh, quên là `apps/web` typecheck lỗi ngay (bài học vừa gặp ở mục 9 trên).

## Phát hiện phụ chưa xử lý (từ handoff Phần A, vẫn còn treo, không liên quan trực tiếp)

- `CashVoucherService.voidVoucher()` có thể không đảo số dư "Ví tạm ứng" khi huỷ phiếu nạp/tất toán (`PATIENT_ADVANCE`) — chưa kiểm chứng bằng test, chưa sửa, ngoài phạm vi Công nợ NCC.
- Q10 (#180): chưa có cơ chế badge "chờ duyệt" hiển thị được ở MỌI trang trong app (không riêng "Công nợ nhà cung cấp") — để dành bàn riêng. Badge mới thêm ở mục 4 trên (Sidebar) chỉ giải quyết đúng 1 chỗ cụ thể, KHÔNG phải hệ thống thông báo chung.

## Trạng thái Git lúc ghi file này

Nhánh `master`, đã đồng bộ `origin/master` trước phiên. Toàn bộ thay đổi Phần B (backend/frontend/docs) đang ở working tree, CHƯA add/commit. File handoff này cũng chưa commit. Kế hoạch: 1 commit `feat(api,web)` cho code+docs Phần B, 1 commit `docs` riêng cho file handoff này (đúng tiền lệ commit `d4bec4f`), rồi `git push`.
