# Changelog — NEXAMed

Định dạng dựa theo [Keep a Changelog](https://keepachangelog.com/). Ghi theo ngày, mới nhất ở trên.

## 2026-08-25 (4)

Redesign popup "Tìm kiếm khách hàng" + chip dị nguyên giới hạn theo nhóm + banner cảnh báo dị ứng ở màn khám (theo yêu cầu chủ dự án) — kèm phát hiện + vá 2 lỗ hổng thật lúc dùng thử:

- **`PatientSearchDialog.tsx`** (popup "Tìm kiếm khách hàng" ở Tiếp nhận): khung rộng hơn (`max-w-3xl`→`max-w-4xl`), tiêu đề cột luôn hiện sẵn (trước chỉ hiện sau khi có kết quả tìm), hiện sẵn 5 khách hàng **mới đăng ký gần đây nhất** khi chưa gõ tiêu chí (thay khung trống ban đầu). Thêm tham số `sort` (`created_asc`/`created_desc`, mặc định `created_asc` — không đổi hành vi Danh sách bệnh nhân) cho `GET /patients` + `useRecentPatientsQuery` phục vụ riêng nhu cầu này. Loading đổi từ khung xương chung chung sang skeleton dạng hàng bảng đúng hình dáng dữ liệu thật (`.claude/docs/ui-guidelines.md` mục 3).
- **`PatientHistoryDialog.tsx`** ("Tiền sử dị ứng"): chip dị nguyên khi CHƯA gõ tìm nay nhóm theo Nhóm dị nguyên, mỗi nhóm chỉ hiện tối đa 5 chip "chọn nhanh" (chip đã chọn luôn ưu tiên hiện trước, không bao giờ bị ẩn) kèm gợi ý "+N khác — gõ tên để tìm" — trước hiện phẳng toàn bộ danh mục (hàng trăm chip) cùng lúc. Đang gõ tìm vẫn trả phẳng không giới hạn như cũ. Nhãn "Ghi chú bổ sung (tự do)" bỏ "(tự do)".
- **Bug thật #1 phát hiện lúc seed dữ liệu demo kiểm tra hiển thị màn khám**: `patient_condition` (Bệnh lý nền + thói quen, phiên trước) có dữ liệu ở hồ sơ bệnh nhân nhưng `GET /encounters/:id/consultation` chưa từng trả field này (khác `allergens`/`familyHistoryRows` đã có), `PatientConditionRepository` cũng chưa export khỏi `PatientModule`. Đã vá: export repository, wire vào `EncounterService.getConsultationDetail()`, thêm `conditions` vào `consultationPatientSchema`. Web: `EncounterConsultationPage.tsx` thêm khung "Bệnh lý nền" (chip màu `brand-teal`, chỉ xem) vào panel Tiền sử.
- **Bug thật #2 phát hiện lúc chủ dự án xem demo — 2 nơi chỉnh cùng khái niệm "dị ứng" không nhất quán**: đợt redesign #065 (phiên trước) đã cố tình bỏ ô nhập `allergyNote` (ghi chú dị ứng tự do) khỏi `PatientHistoryDialog`, chuyển hẳn sang chip `allergenIds` có cấu trúc — nhưng bỏ sót ô nhập/autosave `allergyNote` riêng vẫn còn nguyên trong màn khám (từ #060), khiến 1 trường xuất hiện ở nơi không có đường sửa "chính thức". Đã bỏ hẳn `allergyNote` khỏi màn khám: xoá ô "Tiền sử dị ứng" (textarea) + banner "GHI CHÚ DỊ ỨNG" khỏi `EncounterConsultationPage.tsx`, bỏ khỏi `consultationPatientSchema`/`getConsultationDetail()` — cột DB `allergy_note` GIỮ NGUYÊN (đúng cách đã làm với `family_history` cũ), chỉ ngừng đọc/ghi từ UI. Banner "DỊ NGUYÊN" đổi tên "CẢNH BÁO DỊ ỨNG" + đổi từ 1 dòng text nối dấu phẩy sang từng chip riêng (tên + nhóm dị nguyên trong ngoặc), rõ nét hơn theo yêu cầu.
- **Đã xác minh thật**: 412/412 test `apps/api` pass, `pnpm -w typecheck/lint/build` sạch toàn workspace (lint chỉ còn 5 lỗi cấu hình có sẵn, không liên quan), OpenAPI + web codegen đã sinh lại. Gọi API thật (dữ liệu demo 20 bệnh nhân seed qua HTTP API, không migration/mock) xác nhận `conditions` trả đúng.

## 2026-08-25 (3)

Tiền sử bản thân/gia đình chuyển sang dữ liệu có cấu trúc (Sprint 5, ngoài kế hoạch, chủ dự án yêu cầu trực tiếp) + popup "Tìm kiếm khách hàng" ở Tiếp nhận:

- **Bối cảnh**: mockup Artifact (chip bệnh lý nền, ma trận Tiền sử gia đình, cảnh báo nguy cơ) duyệt qua 2 vòng chỉnh sửa (bỏ "Ghi chú dị ứng khác", thêm lọc tìm + "+" thêm dị nguyên mới trực tiếp). Trong lúc chờ, chủ dự án gửi ảnh tham khảo yêu cầu thêm popup "Tìm kiếm khách hàng" ở Tiếp nhận — gộp làm cùng đợt vì đụng chung file. 4 điểm cấu trúc hỏi và chốt qua `AskUserQuestion` trước khi code (mở quyền tạo dị nguyên cho lễ tân/điều dưỡng/bác sĩ; bỏ cột "Trạng thái" trong popup tìm kiếm — `patient` không có khái niệm này; áp UI Tiền sử mới cho CẢ 3 nơi thay vì chỉ Tiếp nhận, tránh 2 phiên bản dữ liệu lệch nhau; banner cảnh báo chỉ liệt kê người thân+bệnh, không số %). Phát hiện thêm giữa chừng: màn Khám bệnh cũng đọc/ghi `patient.familyHistory` qua textarea autosave riêng — đổi sang CHỈ XEM (tóm tắt), bỏ hẳn autosave family history khỏi màn khám.
- **Schema mới**: `patient_condition` (bệnh lý nền + thói quen/lối sống dùng CHUNG 1 bảng, thói quen mã hoá ICD-10 Chương XXI Z72.x — không tách cột riêng) và `patient_family_history` (ma trận Quan hệ×Bệnh lý(ICD-10)+tuổi phát hiện, KHÔNG unique — nhiều người thân cùng quan hệ có thể cùng mắc 1 bệnh), migration `20260825110000_patient_history_structured`. `patient.family_history`/`allergy_note` GIỮ NGUYÊN trong DB, ngừng đọc/ghi từ UI mới; `patient.personal_history` giữ nguyên ý nghĩa "ghi chú bổ sung tự do" cạnh chip mới.
- **Quyền mới**: `allergen_catalog.create` (tách khỏi `manage`) — lễ tân/điều dưỡng/bác sĩ/clinic_admin tạo được dị nguyên mới ngay lúc nhập Tiền sử; sửa/ẩn vẫn chỉ `clinic_admin` qua `manage`.
- **Backend**: `patient-condition.repository.ts`/`patient-family-history.repository.ts` (mới, cùng khuôn `PatientAllergenRepository.replaceForPatient()` — bulk-replace toàn bộ khi Lưu). `patient.service.ts` mở rộng create/update; `EncounterService.getConsultationDetail()` đổi `familyHistory` (string) → `familyHistoryRows` (mảng có cấu trúc, chỉ đọc). `PatientModule` export thêm `PatientFamilyHistoryRepository` cho `EncounterModule` dùng chung (cùng tinh thần `PatientAllergenRepository`).
- **Frontend**: `PatientHistoryDialog.tsx` (mới, `apps/web/src/features/patient/`) — modal 3 khối Boxed Section (chip bệnh lý nền cứng 6 mã + 3 chip thói quen + tìm ICD-10 tự do; bảng động Tiền sử gia đình + banner cảnh báo trùng bệnh lý gia đình, generic không số %; dị ứng dùng chip tự viết thay `MultiSelectCombobox` — có ô lọc bỏ dấu + nút "+" thêm nhanh gọi `useCreateAllergenMutation`). `PatientFormFields.tsx` thay 4 field rời rạc bằng 1 dòng tóm tắt (pill trạng thái) + nút "Thêm/Sửa" mở dialog, thêm icon tìm kiếm cạnh "Mã bệnh nhân" (chỉ hiện khi truyền `onSearchPatient`, tức chỉ ở Tiếp nhận). `PatientSearchDialog.tsx` (mới, `apps/web/src/features/reception/`) — tìm theo Họ tên/Điện thoại/CCCD (CCCD khớp tuyệt đối qua hash, không tìm được theo phần; gộp SĐT/tên vào `q` sẵn có), cột CCCD hiện dạng che, không có cột "Trạng thái", chọn xong tái dùng đúng pipeline `pendingMatchId` có sẵn (không viết lại logic điền form). `Icd10DiagnosisPicker.tsx` chuyển thành `shared/ui/Icd10SearchPicker.tsx` (dùng lần 2 thì tách, theo CLAUDE.md). Gộp `patientDetailToFormValuesLocal` trùng lặp ở `ReceptionIntakeForm.tsx` vào `patient-form.utils.ts`.
- **Bug thật phát hiện + sửa lúc kiểm bằng trình duyệt**: circular import (`PatientFormFields.tsx` → `PatientHistoryDialog.tsx` → `patient-form.utils.ts` → `PatientFormFields.tsx`) khiến đọc `FAMILY_RELATION_LABELS` ở top-level bị `ReferenceError` lúc `pnpm dev` (Vite ESM gốc thực thi đúng chuẩn TDZ) — `vite build` (Rollup, tối ưu lại thứ tự module) không lộ lỗi này nên typecheck/build sạch mà không phát hiện được, chỉ lộ khi chạy dev thật. Sửa bằng tách `FAMILY_RELATION_LABELS` ra module lá riêng `family-relation.ts` (không nằm trong vòng lặp import). Cũng gặp lại lỗi Rollup quen thuộc (#032) — hằng số `FAMILY_RELATION_LABELS` từ `@nexamed/shared` không dò được named export qua barrel lúc `vite build`, xử lý bằng trùng bản ghi nhỏ ở web (đã có tiền lệ `calculateAgeYears`).
- **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch toàn workspace (lint chỉ còn 5 lỗi cấu hình ESLint plugin có sẵn từ trước, không liên quan — tăng từ 3 vì 2 file mới dùng cùng mẫu `eslint-disable-next-line` đã có). `apps/api`: 405/412 test pass (7 fail chỉ do flake `geo-http.spec.ts` đã biết từ trước, xác nhận pass 100% khi chạy riêng) — thêm test `conditionCodes`/`familyHistoryRows` (CRUD, full-replace, cách ly tenant) ở `patient-http.spec.ts`, viết lại test quyền dị nguyên ở `allergen-http.spec.ts` (receptionist POST được nhưng PATCH/DELETE vẫn 403). Playwright qua Chrome thật (dữ liệu tạo qua UI, tenant dev riêng): Thêm bệnh nhân → chọn chip bệnh lý nền + thói quen → thêm 2 dòng Tiền sử gia đình cùng 1 bệnh → banner cảnh báo hiện đúng (không số %) → thêm dị nguyên mới ngay trong dialog → Lưu → tạo hồ sơ thật → F5 lại → dữ liệu Tiền sử còn nguyên (xác nhận round-trip backend). Tiếp nhận bệnh nhân → bấm icon tìm kiếm → tìm theo SĐT → Chọn → form tự điền đúng.
- Cập nhật `docs/ERD.md` (v1.26), `docs/CURRENT.md`.

## 2026-08-25 (2)

Sprint 4 (Kê đơn) — S4-01→04 xong, đơn thuốc là `SignableEntity` đầu tiên thật sự dùng logic ký + C8 lần đầu là DB trigger thật:

- **Bối cảnh**: chủ dự án đề xuất tách "Đơn thuốc y khoa"/"Hoá đơn bán hàng" (2-Tab thuốc nội bộ có kho / thuốc ngoài, luồng thu ngân/xuất kho) — đối chiếu `CLAUDE.md` (dược/kho ngoài v1) thì vượt phạm vi, hỏi qua `AskUserQuestion` và chốt **giữ đúng phạm vi v1 đã chốt** ("Trường hợp A" — ghi nhận + in, không kho). Đề xuất đầy đủ lưu vào `docs/product/future-modules-reference.md` §2.2.1 làm đặc tả cho v2.1.
- **Schema mới** (2 migration): `drug` (theo tenant, phòng khám tự nhập, không tồn kho/giá — PRD mục 8), `prescription`/`prescription_item` (`20260825090000_prescription`); `patient_allergen` (`20260825100000_patient_allergen`, bảng nối mới liên kết danh mục "Dị nguyên" #069 có sẵn — trước đây chưa từng liên kết bệnh nhân, phát hiện lúc rà soát PRE-03).
- **C8 (`docs/ERD.md` mục 4) lần đầu là DB trigger thật**: `clinical_note` có sẵn 4 cột `SignableEntity` nhưng chưa từng có trigger chặn UPDATE — `prescription` là bảng đầu tiên cưỡng chế "đã ký thì bất biến" ở tầng DB (trigger chỉ chặn sửa nội dung đã ký: `signedAt/signedBy/signaturePayload/encounterId/supersedesId/amendmentReason`, CHO PHÉP `printedAt`/soft-delete/version tiếp tục đổi).
- **Luồng kê đơn**: đơn NHÁP sửa tự do (`PUT .../prescription-items`, bulk-replace đúng khuôn `diagnosis`); ký (`POST .../prescription/sign`, `SignaturePort` — consumer thật đầu tiên) xong bất biến; sửa = đính chính (`POST .../prescription/amend`, tạo bản MỚI đã ký ngay, `supersedesId` trỏ về bản cũ, bản cũ soft-delete); in (`POST .../prescription/print`, ghi `printedAt` idempotent).
- **PRE-02/03 — cảnh báo mềm, KHÔNG chặn ký**: hàm thuần `findDuplicateActiveIngredients`/`findAllergyMatches` (`packages/core/src/prescription/warnings.ts`) — đã hỏi và chốt không làm "chặn ký cứng" vì v1 không có dữ liệu chống chỉ định/liều theo tuổi (PRE-06 hoãn P2, #072).
- **Web**: `apps/web/src/features/drug/DrugCatalogPane.tsx` (mới, thay `ComingSoonPage` tại `/admin/catalog-pharmacy`, đổi nhãn sidebar "Danh mục Dược và Vật tư"→"Danh mục thuốc"); `apps/web/src/features/encounter/PrescriptionPanel.tsx`/`DrugPicker.tsx`/`PrescriptionPrintView.tsx` (gắn vào tab 3 "Kê đơn thuốc" của `EncounterConsultationPage.tsx`, trước là "Sắp ra mắt"); `PatientFormFields.tsx` thêm `MultiSelectCombobox` "Dị nguyên đã biết". Hạ tầng in ấn dùng chung lần đầu (`@media print` + kỹ thuật `visibility:hidden`, `apps/web/src/app/index.css`) — chủ đích tái dùng cho BIL-02 (Sprint 5/6, #072); mẫu in **chưa chính thức** (plan.md T5 còn treo), dùng bố cục chuẩn tạm.
- **Đã xác minh thật**: `packages/core` `warnings.spec.ts` 10/10 (tổng 99 test); `apps/api` `drug-http.spec.ts` 8/8, `prescription-http.spec.ts` 8/8 (gồm test trigger C8 chặn `UPDATE` thẳng qua SQL, không qua service), `patient-http.spec.ts` +4 test `allergenIds` — tổng **406/406 test `apps/api` pass**. Vá `tenant-fixture.ts`: `prescription.supersedesId` KHÔNG null-hoá-rồi-xoá được như `patient.mergedIntoId` (trigger C8 chặn cả sửa cột này trên bản đã ký) — đổi sang xoá theo tầng (loop xoá dòng không còn bị dòng nào khác trỏ tới). `pnpm -w typecheck/lint/build` sạch toàn workspace, chunk khởi động web không đổi (440 kB, `DrugCatalogPane`/`EncounterConsultationPage` đều lazy). Playwright qua Chrome thật (dữ liệu tạo qua HTTP API riêng): CRUD Danh mục thuốc → gán dị nguyên cho bệnh nhân → kê 2 thuốc trùng hoạt chất + 1 thuốc trùng dị nguyên → cả 2 cảnh báo hiện đúng → ký đơn (form khoá lại) → in đơn → "Sửa đơn" tạo bản đính chính đúng (badge, `supersedesId`, dữ liệu giữ nguyên) — không lỗi console ngoài dự kiến.
- **Còn treo**: S4-05→08 (đóng gói on-prem, dữ liệu pilot, cài đặt, đệm lỗi) — việc vận hành riêng biệt, chưa làm. PRE-05 (sao chép đơn từ lần khám trước, P1) chưa làm.
- Cập nhật `docs/TASK.md` (Sprint 4 mới), `docs/CURRENT.md`, `docs/product/future-modules-reference.md` (§2.2.1 mới), `docs/ERD.md` (v1.25).

## 2026-08-25

Làm rõ 3 khái niệm triển khai chưa từng ghi ở đâu, ghi thành `docs/Deploy.md` Phần 0 — xem `docs/DECISIONS.md` #074:

- **Bối cảnh**: chủ dự án hỏi 3 câu khi bàn triển khai thật (script bật gói chạy 1 lần có phải chạy lại sau restart? VPS khách thuê có tính là on-prem? cài trên NAS nội bộ được không?) và nêu thẳng điểm mơ hồ: *"tôi thật sự không hiểu tenant được định nghĩa là chi nhánh hay 1 cơ sở/công ty"*. Rà lại: cả 3 câu **chưa từng có ở tài liệu nào** — `Deploy.md` chỉ nói on-prem chung chung, `multi-tenancy.md` định nghĩa "một tenant = một phòng khám" nhưng không nói rõ "phòng khám" là chi nhánh hay pháp nhân.
- **0.1 — `tenant` = một CƠ SỞ/CHI NHÁNH, không phải công ty**: **không tồn tại tầng "công ty" nào trên `tenant`**. Công ty 3 chi nhánh = 3 tenant tách biệt hoàn toàn (bệnh nhân khám 2 chi nhánh = 2 hồ sơ độc lập, nhân viên = 2 tài khoản, danh mục/giá nhập riêng, không có báo cáo tổng hợp). Là **giới hạn có chủ ý của v1** — khớp PRD Q6 còn treo + scope `branch` chưa triển khai + "hồ sơ dùng chung liên chi nhánh" ở phase v3+. Đã để sẵn `patient.global_patient_ref` + `PatientIdentityPort` để mở rộng mà không đụng schema.
- **0.2 — ranh giới quyết định cách viết code là single-tenant vs multi-tenant, KHÔNG phải on-prem vs cloud**: máy tại phòng khám / NAS nội bộ / VPS khách thuê đều single-tenant, giống nhau ở mọi mặt ảnh hưởng tới code → **không cần đổi một dòng code nào**, mọi quyết định on-prem vẫn đúng (gồm #072 bật gói bằng script, không license/DRM). Chỉ SaaS multi-tenant mới đổi bản chất và là nơi tập trung rủi ro cache/state rò xuyên tenant. Khác biệt của NAS/VPS nằm ở **vận hành**: VPS bắt buộc TLS + chi phí vận hành tăng tuyến tính theo số khách; NAS cần Docker + RAM ≥ 4 GB + RAID không thay thế backup. Ghi rõ điều **chưa kiểm chứng**: `postgres:18`/Node 20 có bản arm64 nên NAS ARM về nguyên tắc chạy được, nhưng dự án **chưa từng thử thật trên NAS**.
- **0.3 — cấu hình nào bền vững qua restart**: dữ liệu trong DB (gồm `tenant.enabled_specialties`) bền vững nhờ named volume `postgres_data` → **chạy script một lần là xong**; chỉ phải chạy lại khi khôi phục backup cũ hơn hoặc xoá volume (`docker compose down -v`). Phân biệt với `syncRolePermissionsForAllTenants()` (cố ý chạy mỗi lần bootstrap vì idempotent) — script bật gói **không** đưa vào bootstrap vì "khách mua gói nào" là quyết định thương mại theo hợp đồng.
- Vá luôn chỗ gốc gây mơ hồ: `.claude/docs/multi-tenancy.md` thêm ghi chú ngay dưới định nghĩa tenant (nơi agent đọc khi viết code chạm tenant). Sửa 2 lỗi tồn đọng ở đầu `Deploy.md`: typo "on-premide" và trạng thái lỗi thời "dự án đang ở Sprint 1/12" (thực tế đã xong Sprint 1-3).
- **CHỐT hướng đa chi nhánh (#075, cùng ngày)**: ngay sau khi làm rõ #074, chủ dự án quyết định chốt luôn thay vì để treo. Chọn **mô hình B** — `tenant` = **CÔNG TY**, chi nhánh là bảng `branch` **bên trong** (thay hướng ngầm định cũ "mỗi chi nhánh một tenant"); **bệnh nhân + mã dùng chung toàn công ty** (một `patient_code` duy nhất); **chốt hướng nay, chỉ code khi có khách chuỗi thật**.
  - *Lý do chốt sớm dù chưa code*: chốt trước khi bán = thêm tầng vào schema; chốt sau khi khách đã chạy nhiều cài đặt riêng = phải **merge nhiều database**, xử lý `patient_code` trùng nhau (sequence unique theo `(tenant_id, prefix)`) + rủi ro nhầm bệnh nhân khi gộp dữ liệu y tế.
  - *Ràng buộc bắt buộc cho mọi code viết từ nay*: **cấm giả định "1 tenant = 1 địa điểm vật lý"**; `code_sequence` **giữ nguyên** theo tenant (đã đúng hướng — không sửa); `patient` **không** cần `branch_id` còn `encounter`/`appointment`/`invoice` thì có; `room`/`floor`/`department` sau này gắn thêm `branch_id`; scope `branch` chèn giữa `department` và `global`.
  - *Ràng buộc hạ tầng (quan trọng nhất)*: mô hình B đòi hỏi cả công ty dùng chung **một database** → **khách chuỗi buộc phải dùng hạ tầng tập trung**; mỗi chi nhánh một máy riêng thì không thể gộp chung tenant. Phải nói rõ với khách chuỗi trước khi ký hợp đồng.
  - Trả lời một phần **PRD Q6** — không còn là câu hỏi kiến trúc bỏ ngỏ, chỉ còn là câu hỏi thời điểm thương mại.
- **Không đổi code/schema** — làm rõ khái niệm đã có, không phải đổi thiết kế.
- Cập nhật `docs/Deploy.md` (Phần 0 mới + hướng mở rộng ở 0.1), `docs/DECISIONS.md` (#074, #075), `.claude/docs/multi-tenancy.md`, `.claude/docs/security-audit.md`, `docs/product/prd.md` (Q6), `docs/CURRENT.md`.

## 2026-08-22

Hiệu suất: code-splitting theo route + cưỡng chế ranh giới `web ⇸ core` bằng ESLint — xem `docs/DECISIONS.md` #073:

- **Vấn đề đo được (không phải suy đoán)**: `apps/web` có **0 lazy load / 0 dynamic import**, router import tĩnh 21 module → build ra **802.68 kB trong MỘT chunk** (gzip 213 kB), Vite cảnh báo vượt 500 kB. Trong khi `docs/product/multi-specialty-analysis.md` mục 4 đã kết luận sẵn "bundle web không vấn đề — lazy load theo route/chuyên khoa" — tức giả định của tài liệu **chưa từng được hiện thực**.
- **Rủi ro thứ hai**: `packages/core` nặng 9.6 MB (riêng `icd10/data.ts` 9.1 MB). Web không dính khối này chỉ nhờ quy ước ngầm (`package.json` không khai dependency) — ESLint không hề chặn chiều `web → core`.
- **Đã sửa**: 11 trang nghiệp vụ chuyển sang `lazy(() => import(...))` (`app/router.tsx`), `<Suspense>` bọc riêng vùng nội dung trong `AppShell` (Sidebar/TopBar không nháy), `shared/ui/PageFallback.tsx` mới; giữ eager `LoginPage`/`RequireAuth`/`AppShell`/`DashboardPage`/`ChangePasswordPage`/`NotFoundPage`. Thêm ESLint `no-restricted-imports` chặn `apps/web` import `@nexamed/core`.
- **Kết quả**: chunk khởi động **802.68 → 440.65 kB** (gzip 213.47 → 132.14), giảm 45%; 1 → 27 chunk; hết cảnh báo Vite. Lễ tân nay tải 440 kB + đúng chunk trang mình dùng (5.66 kB) thay vì cả màn khám + nhóm Quản trị.
- **Quy tắc mới bắt buộc** (`.claude/docs/coding-standards.md` mục "Hiệu suất"): chunk khởi động ≤ 500 kB; mọi trang nghiệp vụ mới BẮT BUỘC lazy; cấm `web → core` (cần hàm thuần dùng chung thì chuyển sang `packages/shared`); truy vấn danh sách phải có index; endpoint gộp phải tránh N+1.
- **Đã xác minh thật**: `pnpm -w typecheck` sạch; ESLint rule mới kiểm bằng file thử vi phạm → chặn đúng rồi xoá; Playwright qua Chrome thật điều hướng đủ 8 route lazy — render đúng, không route nào kẹt fallback, 0 request lỗi. Không đổi backend/schema/API contract.
- Cập nhật `docs/DECISIONS.md` (#073), `.claude/docs/coding-standards.md`, `docs/product/multi-specialty-analysis.md` (đính chính mục 4), `docs/CURRENT.md`.

## 2026-08-21 (6)

Rà soát toàn app: tiêu đề cột không cố định khi cuộn — sửa 7 bảng, ghi quy tắc bắt buộc vào `.claude/docs/ui-guidelines.md` mục 9g:

- Phát hiện chỉ `AllergenPane.tsx`/`AppointmentGridView.tsx` làm đúng (tiêu đề dính khi cuộn); 7 bảng còn lại thiếu: `GeoPane.tsx`, `Icd10Pane.tsx`, `RoleMatrixTable.tsx` (thiếu `sticky` trên `thead`), `UserAccountPane.tsx` (có `sticky` nhưng thiếu `z-10`), `ReferenceCatalogPane.tsx`/`RoomPane.tsx`/`DepartmentPane.tsx` (không có vùng cuộn nội bộ giới hạn chiều cao — bảng tự giãn kéo cả trang cuộn theo, tiêu đề trôi mất cùng).
- Sửa theo đúng mẫu đã có ở `AllergenPane.tsx`: bọc `min-h-0 flex-1 overflow-hidden` (khung ngoài) → `overflow-y-auto` (khung trong, cuộn thật) → `<thead className="sticky top-0 z-10">`. `RoomPane.tsx`/`DepartmentPane.tsx`/`Icd10Pane.tsx` thêm `min-h-0` còn thiếu ở cột danh sách bên phải (nguyên nhân gốc khiến vùng cuộn không giới hạn được chiều cao).
- Bảng dựng bằng CSS Grid (`PatientListPage.tsx`/`AppointmentListView.tsx`/`ReceptionListPage.tsx`) đã đúng sẵn từ trước (tiêu đề là hàng tách riêng ngoài khung cuộn) — không đổi.
- **Đã xác minh thật**: Playwright qua Chrome thật (`dev.admin`) — kiểm `getComputedStyle` xác nhận cả 7 bảng đều `position: sticky; top: 0; z-index: 10`; cuộn sâu bảng Phường/Xã (TP.HCM, ~500 dòng) và ICD-10 (tìm "viêm", hàng trăm dòng) bằng ảnh chụp — tiêu đề cột đứng yên đúng khi thân bảng cuộn qua. `pnpm -w typecheck` sạch toàn workspace, không đổi backend/schema.
- Cập nhật `.claude/docs/ui-guidelines.md` mục 9g (mới) — quy tắc bắt buộc cho mọi bảng danh sách phát sinh sau này, không phải nhắc lại.

## 2026-08-21 (5)

"Tiền sử bản thân"/"Tiền sử gia đình" chuyển sang thuộc bệnh nhân thay vì gắn theo lượt khám — lỗ hổng chủ dự án phát hiện, xem `docs/DECISIONS.md` #068:

- **Vấn đề**: 2 trường này trước đây là section của `clinical_note` (gắn `encounter_id`, giống 4 mục "Thăm khám" khác) nên mỗi lượt khám mới luôn rỗng, bác sĩ phải gõ lại từ đầu dù nội dung hầu như không đổi — khác bản chất dữ liệu (thuộc về bệnh nhân, không thuộc một lần khám cụ thể). Hỏi và chốt hướng qua `AskUserQuestion` trước khi sửa: chuyển hẳn thành trường `patient.*` đúng khuôn `allergyNote` (đã tách khỏi `clinical_note` từ #060), thay vì chỉ tự mồi mà vẫn lưu riêng theo lượt khám.
- **Schema**: thêm `patient.personal_history`/`patient.family_history` (nullable). `clinical_note.section` còn lại 6 giá trị (bỏ `PERSONAL_HISTORY`/`FAMILY_HISTORY`). Migration `20260821150000_patient_history_fields` — backfill nội dung KHÔNG RỖNG gần nhất của mỗi bệnh nhân (theo `encounter.checked_in_at DESC`) từ `clinical_note` cũ trước khi xoá 2 section này và dựng lại enum.
- **Backend**: đọc/ghi qua `PATCH /patients/:id`, tái dùng permission `doctor.patient.update=global` có sẵn từ #060 (không thêm permission mới). `EncounterService.getConsultationDetail()` trả `personalHistory`/`familyHistory` cùng `allergyNote` trong `patient`; `saveClinicalNote()` bớt 2 field khỏi payload.
- **Web**: `EncounterConsultationPage.tsx` gộp cơ chế autosave-vào-patient (trước chỉ có `allergyDraft`/`allergyBaseline`) thành `patientNoteDraft`/`patientNoteBaseline` dùng chung cho cả 3 trường (`allergyNote`/`personalHistory`/`familyHistory`) — chỉ gửi field nào thực sự đổi so với baseline. `PatientFormFields.tsx` (dùng chung Thêm/Sửa bệnh nhân + Tiếp nhận bệnh nhân) thêm 2 trường cạnh "Tiền sử dị ứng" để xem/sửa được cả ở hồ sơ bệnh nhân.
- **Đã xác minh thật**: `encounter-http.spec.ts` +1 test ("Tiền sử bản thân/gia đình lưu ở patient... lượt khám sau vẫn thấy lại") — tổng 375/375 test `apps/api` (28 file), không regression. `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright qua Chrome thật (tenant/tài khoản dev riêng tạo qua API): nhập tiền sử ở lượt khám 1 → autosave → Hoàn tất khám → tạo lượt khám MỚI cho CÙNG bệnh nhân → 2 trường đã hiện sẵn đúng nội dung cũ, sửa/lưu lại vẫn đúng. Không lỗi console ngoài dự kiến.
- Cập nhật `docs/ERD.md` (v1.24), `.claude/docs/data-model.md`, `.claude/docs/clinical-workflow.md`, `docs/product/prd.md` (ENC-02), `docs/DECISIONS.md` (#068), `docs/CURRENT.md`.

## 2026-08-21 (4)

Popup xác nhận "Hoàn tất khám", chuẩn Enter-to-submit bắt buộc toàn app, panel tiền sử thêm tên bác sĩ — xem `docs/DECISIONS.md` #067:

- **Popup "Hoàn tất khám thành công"** — cùng khuôn "Tiếp nhận thành công", Enter cũng xác nhận được (nghe `keydown` ở `window`, popup không phải `<form>`).
- **Chuẩn "Enter để lưu" — bắt buộc cho mọi form Thêm/Sửa từ nay** (`.claude/docs/ui-guidelines.md` mục 4.4, mới) — phát hiện lỗ hổng toàn app: form "Thêm mới" ở trang danh mục chỉ bấm được bằng chuột. Đã sửa 8 modal/form: `ReferenceCatalogPane.tsx`, `RoomPane.tsx` (Tầng + Phòng), `DepartmentPane.tsx` (Loại Khoa/Phòng + Khoa/Phòng), `ExamStationDialog.tsx`, `UserAccountFormDialog.tsx`, `RolePermissionPane.tsx`, `VitalSignsDialog.tsx` — bọc `<form onSubmit>`, nút chính `type="submit"`. `PatientPicker.tsx` thêm chặn Enter nổi bọt (ô tìm bệnh nhân không phải submit). Ngoại lệ: modal Confirm xoá/nguy hiểm KHÔNG áp dụng. Chưa áp dụng (cần audit riêng): `ReceptionIntakeForm.tsx`, `AppointmentQuickCreatePanel.tsx`, `AppointmentDetailPanel.tsx`, `UserAccountPane.tsx`.
- **Panel "Tiền sử & lịch sử khám"** thêm tên bác sĩ (`EncounterHistoryItem.doctorName` — đổi contract, đã sinh lại OpenAPI + web type), thẻ "Lần khám gần nhất"/"Lịch sử cũ hơn" làm nổi bật/đồng nhất hơn theo ảnh mẫu chủ dự án gửi — vẫn chỉ hiện **chẩn đoán chính** (đã hỏi, không mở rộng toàn bộ chẩn đoán). "Đơn cũ"/"Xét nghiệm" trong ảnh mẫu chưa làm được (module Kê đơn Sprint 4 + Cận lâm sàng ngoài phạm vi v1 chưa có dữ liệu).
- **Đã xác minh thật**: `encounter-http.spec.ts` thêm assertion `doctorName` (374/374 tổng `apps/api`, không regression). `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright qua Chrome thật: Enter lưu ở danh mục "Nghề nghiệp" thành công, Enter xác nhận popup hoàn tất khám, panel tiền sử hiện đúng "BS. {tên}" cả 2 loại thẻ không vỡ dòng.
- Cập nhật `docs/CURRENT.md`, `docs/DECISIONS.md` (#067), `.claude/docs/ui-guidelines.md` (mục 4.4 mới).

## 2026-08-21 (3)

Màn hình khám — sửa 3 lỗi vận hành thật chủ dự án báo cáo, xem `docs/DECISIONS.md` #066:

- **"Xem lại" lượt khám đã "Hoàn tất khám"** không còn hiện "Lưu nháp"/"Hoàn tất khám" — chỉ 1 nút "Chỉnh sửa thông tin". Sửa được: đúng bác sĩ ca đó, hoặc tài khoản khác qua **break-glass** (`apps/web/src/shared/api/break-glass.api.ts`, `shared/ui/BreakGlassDialog.tsx` — lần đầu web gọi tới cơ chế này). Backend (`encounter.service.ts`) mở khoá `saveDiagnoses()`/`saveClinicalNote()` cho cả trạng thái `COMPLETED` (trước chỉ `IN_CONSULTATION`), ghi `audit_log` action riêng (`*_amended_after_completion`) kèm trước/sau đầy đủ khi sửa sau hoàn tất.
- **Autosave** (debounce ~4 giây + flush khi rời trang) cho ghi chú lâm sàng + "Tiền sử dị ứng" — chống mất nội dung gõ dở khi chuyển sang khám bệnh nhân khác mà chưa bấm "Lưu". Phát hiện thêm bug nặng hơn: bấm từ dải "Hàng chờ" sang ca khác (không unmount trang, route `/encounters/:id` không đổi `key`) trước đây không nạp lại dữ liệu — lẫn dữ liệu 2 bệnh nhân. Sửa bằng `loadedForId` (so `encounterId`) thay `initialized` boolean.
- **"Hoàn tất khám"** nay bắt buộc lưu xong ghi chú (flush đồng bộ) trước khi cho chuyển trạng thái — không còn đường nào hoàn tất mà bỏ sót nội dung chưa lưu.
- Nhân tiện: nút "Lưu nháp" → "Lưu"; lỗi ở footer làm nổi bật (khung viền đỏ + icon) — vẫn **giữ inline tại chỗ**, không đổi sang Toast (đúng `.claude/docs/ui-guidelines.md` mục 4.3, đã hỏi trước khi quyết định giữ nguyên).
- **Đã xác minh thật**: `encounter-http.spec.ts` +5 test (374/374 tổng `apps/api`, 28 file, không regression) — vá thêm 1 lỗ hổng hạ tầng test (`tenant-fixture.ts` chưa từng dọn `break_glass_session`). `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright qua Chrome thật (dữ liệu tạo riêng qua API — BS. `hungdt` + tài khoản điều dưỡng test): cả 3 kịch bản pass + luồng break-glass trọn vẹn (điều dưỡng bị chặn `403` → dialog → sai mật khẩu báo lỗi tại chỗ → đúng mật khẩu → lưu thành công, audit log đủ `break_glass.request/access` + action sửa sau hoàn tất).
- Cập nhật `docs/CURRENT.md`, `docs/DECISIONS.md` (#066).

## 2026-08-21 (2)

"Hàng đợi khám" + "Chuyển vào hàng đợi" — chuỗi phản hồi trực tiếp trên bản chạy thật, làm đúng lại mockup #064 (`docs/DECISIONS.md` #065):

- Cột "Mã" hiện lại ở bảng danh sách 4 danh mục nhân sự (Học vị, Chức danh, Trạng thái làm việc, Hình thức làm việc) — ô nhập trong modal vẫn ẩn.
- Cột "Người tiếp nhận" mới ở Danh sách tiếp nhận (`encounter.createdBy` resolve tên qua `DoctorDirectoryPort.getUserFullNames()` mới).
- "Chuyển vào hàng đợi" (Tiếp nhận) làm lại đúng mockup: tách tab "Bác sĩ"/"Khoa" + lọc theo Khoa, lưới nhiều cột, số "Đang chờ" thời gian thực, "N bác sĩ đang trực" = đã chọn phòng hôm nay (fallback "thuộc Khoa" khi 1 phòng).
- "Chọn phòng làm việc hôm nay" cho "Bỏ qua" lúc đăng nhập (nhớ qua `sessionStorage` theo ngày), bắt chọn lại bắt buộc khi vào "Hàng đợi khám".
- Card 3 cột "Hàng đợi khám" viết lại theo đúng mockup — sửa 1 bug thật (cảnh báo "chờ lâu" trước đây bỏ sót ca "của tôi", chỉ áp cho hàng chờ chung), thêm badge phút đang khám, nhóm "Của tôi"/"Hàng chờ chung" tách màu rõ, tìm kiếm riêng từng cột.
- **Phát hiện kỹ thuật**: `apps/web` import `@nexamed/core` qua barrel làm vỡ `vite build` (Rollup, khác `vite dev` chạy được) — đã lùi lại, không dùng `stripVietnameseDiacritics` cho tìm kiếm cột (so khớp thường, không bỏ dấu). Chi tiết + lý do ở `docs/DECISIONS.md` #065.
- **Đã xác minh thật**: `reception-http.spec.ts` thêm 1 test, tổng 369/369 test `apps/api` pass. `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright qua Chrome thật với dữ liệu demo cho BS. hungdt (ưu tiên/chờ lâu/hàng chờ chung/đang khám/đã khám).
- Cập nhật `docs/CURRENT.md`, `docs/DECISIONS.md` (#065).

## 2026-08-21 (1)

"Hàng đợi khám" — triển khai "Hàng đợi ảo" (Virtual Queue theo Bác sĩ/Khoa) — kiến trúc đã chốt qua mockup ở phiên trước (`docs/DECISIONS.md` #064), phiên này code + kiểm chứng thật:

- **Schema**: migration `20260820210000_encounter_virtual_queue` — `encounter.doctor_id` → nullable (`NULL` = lượt khám còn trong hàng chờ chung Khoa, chưa được bác sĩ nào nhận), thêm `encounter.department_id` bắt buộc; `department.is_default` (partial unique `(tenant_id) WHERE is_default` — C16, đúng 1 "Khoa chung"/tenant). Backfill dữ liệu cũ (Khoa của bác sĩ đã gán, hoặc Khoa mặc định) trước khi ép NOT NULL — giữ nguyên dữ liệu dev, không xoá. `seedDefaultRolesForTenant()` (gọi cho mọi tenant mới, kể cả trong test) nay cũng seed "Khoa chung".
- **Backend**: `DoctorDirectoryPort` (`packages/core`) mở rộng `getDoctorDepartmentId()`/`getDefaultDepartmentId()` — luôn resolve TRƯỚC khi mở transaction chính (port tự mở transaction riêng, tránh `$transaction` lồng nhau). `ReceptionService.resolveRouting()` — chọn "đích danh bác sĩ" thì server tự suy `departmentId` (không tin client gửi kèm); chọn "theo Khoa" thì `doctorId=null`. `checkInRequestSchema`/`registerReceptionRequestSchema` đổi `doctorId` thành tuỳ chọn, thêm `departmentId` tuỳ chọn, bắt buộc có ít nhất một. `EncounterService.startConsultation()` — nhánh mới "Nhận ca" khi `existing.doctorId===null`: chỉ bác sĩ cùng Khoa, set `doctorId=actor` atomic (`EncounterRepository.claimFromPool()`, ghi có điều kiện `WHERE doctor_id IS NULL`) — lỗi mới `EncounterAlreadyClaimedError` (409 `ENCOUNTER_ALREADY_CLAIMED`) khi thua cuộc đua. `GET /reception/list` thêm `includeDepartmentPool` (cờ tường minh, mặc định `false`); `ReceptionListItem` bổ sung `departmentId`/`isPriority`/`chiefComplaint`/`patientCode`.
- **2 bug thật phát hiện + sửa cùng lúc** (ngoài phạm vi #064 nhưng chạm đúng chỗ): (1) `createEncounterAndConvert()` (check-in từ lịch hẹn có sẵn) trước đây quên forward 7 trường "Thông tin tiếp nhận"/"Chỉ định dịch vụ khám" vào `encounterRepository.create()` dù `registerDirect()` đã lưu đúng từ #052; (2) form/trang web ban đầu dùng `GET /departments` (yêu cầu `user_account.read`, chỉ admin có) để tra Khoa — lễ tân/bác sĩ thật bị 403 (chỉ lộ ra khi kiểm Playwright đúng vai trò, không lộ khi test bằng tài khoản admin) — thêm endpoint tự-phục vụ `GET /departments/options` (gắn `reference_catalog.read`, đúng khuôn `GET /appointments/doctors` #030).
- **Frontend**: `ReceptionDoctorQueuePage.tsx` viết lại thành board 3 cột (Đang chờ — 2 nhóm "Bệnh nhân của tôi"/"Hàng chờ chung · Khoa" / Đang khám / Đã khám hôm nay), toast tự ẩn cho `ENCOUNTER_ALREADY_CLAIMED`. `ReceptionIntakeForm.tsx` (dùng chung cho cả `mode='direct'` và `mode='checkin'`) thêm khu vực "Điều phối" — toggle "Theo bác sĩ cụ thể"/"Theo Khoa (chưa rõ bác sĩ)", tự ẩn khi tenant chỉ có 1 Khoa. `EncounterConsultationPage.tsx` thêm dải hàng chờ thu gọn bên trái (ẩn/hiện được), bấm vào gọi thẳng "Nhận ca" rồi điều hướng SPA.
- **Đã xác minh thật**: `encounter-http.spec.ts` thêm 6 test "Nhận ca" (thành công, race 2 bác sĩ cùng Khoa — chấp nhận 404 hoặc 409 tuỳ thời điểm đọc/ghi chồng lấn giống precedent `APPOINTMENT_SLOT_CONFLICT`, khác Khoa → 404, chưa gán Khoa → 404, thiếu quyền → 403, cách ly tenant → 404); `reception-http.spec.ts` thêm 10 test (routing theo bác sĩ/theo Khoa cho cả check-in lẫn direct, server tự suy departmentId bỏ qua giá trị giả mạo, fallback Khoa mặc định, `includeDepartmentPool` đúng phạm vi). Tổng 368/368 test `apps/api` (28 file) pass, không regression. `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright qua Chrome thật (tenant/tài khoản dev tạo riêng): luồng đầy đủ tiếp nhận theo Khoa → bác sĩ cùng Khoa thấy ticket trong hàng chờ chung → nhận ca → chuyển màn khám đúng → bác sĩ thứ hai không còn thấy ticket đã bị nhận; dải hàng chờ trong màn khám ẩn/hiện đúng. Không lỗi console ngoài dự kiến.
- Cập nhật `docs/ERD.md` (v1.23), `.claude/docs/data-model.md`, `.claude/docs/clinical-workflow.md`, `.claude/docs/security-audit.md`, `docs/DECISIONS.md` (#064), `docs/CURRENT.md`, `docs/TASK.md`.

## 2026-08-20 (8)

Mở rộng ADM-01 — "Danh mục quản lý tài khoản" đầy đủ hồ sơ nhân sự, chủ dự án yêu cầu trực tiếp (`docs/DECISIONS.md` #063):

- **Schema**: `user_account` thêm 10 cột (`employee_code` tự sinh prefix `NV`, `phone`, `personal_email`, `company_email`, `academic_title_code`/`position_code`/`employment_status_code`/`employment_type_code` — 4 mã tham chiếu `reference_catalog`, `can_sign_medical_record` chỉ metadata, `must_change_password` enforce thật). `reference_catalog` thêm 4 category (`ACADEMIC_TITLE`/`STAFF_POSITION`/`EMPLOYMENT_STATUS`/`EMPLOYMENT_TYPE`, seed sẵn 2 category sau) + cột `deactivates_account` (chỉ ý nghĩa với `EMPLOYMENT_STATUS`, tách khỏi `code` vì `code` sửa được qua UI). Bảng mới `department_type`; `department` (có từ S1-04b, chưa từng có UI) thêm `code` (tự sinh prefix `KP`), `department_type_id`, `is_active`. 4 migration: `20260820170000_user_account_hr_profile`, `20260820180000_department_management`, `20260820190000_department_code_and_catalog_autocode`, `20260820200000_department_type`.
- **Backend**: `resolveAccountActiveState()` (`packages/core/src/iam/employment-status.ts`) — chọn trạng thái làm việc có `deactivatesAccount=true` (vd "Nghỉ việc") tự động `isActive=false`; cố ép kích hoạt lại trong lúc vẫn ở trạng thái đó → `409 ACCOUNT_CANNOT_REACTIVATE_WHILE_RESIGNED`. `POST /auth/change-password` (chỉ `JwtAuthGuard`) — verify mật khẩu hiện tại, thu hồi toàn bộ phiên khác, xoá cờ `mustChangePassword`. Module `department`/`department-type` mới (`apps/api/src/modules/iam/`), quyền `user_account.read`/`user_account.manage` (không thêm permission mới). `generateReferenceCatalogCode()` — 4 category nhân sự + `department` tự sinh mã, không nhận `code` client gửi.
  - **Bug thật phát hiện qua test HTTP e2e**: gọi `ReferenceCatalogReaderPort` (tự mở transaction riêng) từ TRONG transaction ghi của `user-account.service.ts` gây deadlock — sửa bằng cách resolve trạng thái làm việc TRƯỚC khi mở transaction ghi.
- **Frontend**: `UserAccountPane`/`UserAccountFormDialog` (mới, Boxed Section 3 khối, `MultiSelectCombobox` cho Vai trò — theo phản hồi "chọn vai trò dạng combobox chọn nhiều", checkbox gộp thành hàng riêng thay vì cạnh ô nhập, `PasswordInput` ẩn/hiện mật khẩu). `DepartmentPane` (mới, master-detail 2 cột "Loại Khoa/Phòng" | "Khoa/Phòng", tham khảo `RoomPane.tsx`). `ChangePasswordPage` (mới) + `RequireAuth.tsx` chặn điều hướng khi `mustChangePassword=true`. 4 pill danh mục mới trong "Vai trò & Phân quyền" (tái dùng `ReferenceCatalogPane`).
  - **Bug thật phát hiện qua Playwright**: `UserAccountPane.handleSubmit` luôn gửi `isActive` cụ thể kể cả khi user chỉ đổi Trạng thái làm việc — backend hiểu nhầm thành "cố ép kích hoạt lại" → lỗi sai. Sửa: gửi `isActive: undefined` khi trạng thái mới đang deactivating.
- **Đã kiểm chứng RBAC thật bằng Playwright** (không chỉ suy luận từ code cũ): tạo tài khoản chỉ gán vai trò "Điều dưỡng" → đăng nhập bằng chính tài khoản đó → sidebar không hiện "Quản trị" → gọi thẳng URL trang quản trị bằng phiên thật → API trả `403` thật từ backend (không phải do frontend ẩn giúp) → vào trang được phép ("Hàng đợi khám") vẫn bình thường.
- Rà soát mã hoá mật khẩu toàn hệ thống theo yêu cầu chủ dự án: Argon2id đã dùng từ S1-04, đạt chuẩn OWASP — không cần sửa.
- **Đã xác minh thật**: `employment-status.spec.ts` (4/4), `generate-code.spec.ts` (2/2, `packages/core`); `auth-change-password-http.spec.ts` (4/4), `user-account-hr-profile-http.spec.ts` (9/9) mới ở `apps/api`. Tổng 352 test `apps/api` (351 pass ổn định qua nhiều lần chạy, 1 flake tiền-nhiệm không liên quan ở `appointment-http.spec.ts`). `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright qua Chrome thật (dữ liệu tạo qua UI): luồng đầy đủ tạo tài khoản → bắt đổi mật khẩu lần đầu → tự động vô hiệu hoá khi "Nghỉ việc" → CRUD danh mục/Khoa-Phòng → RBAC thật (xem trên).
- Cập nhật `docs/ERD.md` (v1.22), `.claude/docs/data-model.md`, `docs/DECISIONS.md` (#063), `docs/CURRENT.md`, `docs/TASK.md`.

## 2026-08-20 (7)

Màn hình khám: dải sinh hiệu có đường kẻ phân cách + chấm trạng thái, panel trái hẹp lại; nhãn trường toàn app to/đậm hơn — chuỗi phản hồi trực tiếp trên bản chạy thật (`docs/DECISIONS.md` #062):

- Dải sinh hiệu (`VitalChip`, `EncounterConsultationPage.tsx`): đường kẻ dọc phân cách rõ giữa 6 chỉ số (theo ảnh tham khảo), khôi phục chấm trạng thái (emerald/amber/rose) đã bỏ nhầm ở #060, nền đậm hơn (`bg-slate-100`).
- Panel "Tiền sử & lịch sử khám" hẹp lại `360px`→`280px`; 2 đường viền header (panel trái + thanh tab) sửa lệch bằng chiều cao cố định `h-11`.
- "Ghi chú dị ứng" đổi tên hiển thị thành "Tiền sử dị ứng" ở form Thêm/Sửa bệnh nhân + Tiếp nhận bệnh nhân (dùng chung `PatientFormFields.tsx`).
- **Nhãn trường (`label`) toàn app**: đảo ngược quy tắc "nhạt/nhỏ để tương phản" (chốt 2026-08-14) — đổi `text-xs font-medium text-slate-500` sang `text-sm font-semibold text-slate-800` cho MỌI nhãn trường, không còn ngoại lệ giảm cỡ chữ cho biến thể "dense" (chỉ ô nhập giảm cỡ, nhãn giữ nguyên `text-sm`). Tiêu đề phụ chia nhóm trong khung (khác nhãn trường) cũng đổi `text-slate-400`/`text-slate-500` → `text-slate-700`. Áp dụng cho toàn bộ form nghiệp vụ hiện có: `PatientFormFields.tsx`, `ReceptionIntakeForm.tsx`, `VitalSignsDialog.tsx`, `Textarea.tsx`, `RoomPane.tsx`, `RolePermissionPane.tsx`, `ReferenceCatalogPane.tsx`, `AppointmentQuickCreatePanel.tsx`, `AppointmentDetailPanel.tsx`, `ClinicInfoPane.tsx`, `LoginPage.tsx`, `EncounterConsultationPage.tsx`. Quy tắc mới đã ghi vào `.claude/docs/ui-guidelines.md` mục 4.1c — bắt buộc cho form phát sinh sau này.
- **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch toàn workspace (lint chỉ còn 3 lỗi cấu hình có sẵn). Không đổi backend/schema — chạy lại 338 test `apps/api` để chắc chắn không ảnh hưởng gì ngoài ý muốn, pass toàn bộ trừ 1 flake môi trường không liên quan (`appointment-http.spec.ts` walk-in test, `new Date()` thật va chạm fixture cố định đúng ngày chạy test — không phải regression). Playwright qua Chrome thật (dữ liệu tạo qua API rồi xoá sau khi kiểm xong).
- Cập nhật `.claude/docs/ui-guidelines.md`, `docs/DECISIONS.md` (#062).

## 2026-08-20 (6)

Danh mục "Nghề nghiệp" — chủ dự án yêu cầu trực tiếp, đúng khuôn `PATIENT_SOURCE`/`EXAM_TYPE` (`docs/DECISIONS.md` #061):

- Thêm category `OCCUPATION` vào enum `reference_catalog_category` (migration `20260820160000_reference_catalog_occupation`, chỉ `ALTER TYPE ... ADD VALUE`) — backend `apps/api/src/modules/reference-catalog/` đã hoàn toàn generic từ #037, không cần sửa gì. Không seed cứng (khác Dân tộc/Quốc tịch — không có nguồn dữ liệu chính thức) — `clinic_admin` tự thêm/sửa/ẩn qua UI.
- Pill "Nghề nghiệp" mới trong trang "Danh mục hành chính" (`CatalogAdminPage.tsx`, giữa "Quốc tịch" và "Nguồn khách hàng").
- `patient.occupation` (cột có sẵn từ #034, không đổi kiểu) chuyển từ input text tự do sang `Combobox` load từ danh mục này trong `PatientFormFields.tsx` — đúng pattern `ethnicity`/`nationality` (`withLegacyValueOption` giữ nguyên giá trị cũ). Sửa đúng 1 chỗ vì `ReceptionIntakeForm.tsx` dùng chung component này — cả 2 nơi (Thêm/Sửa bệnh nhân, Tiếp nhận bệnh nhân) cùng có field mới.
- **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch toàn workspace (lint chỉ còn 3 lỗi cấu hình có sẵn từ trước), 338 test `apps/api` không regress (không có test mới — thay đổi hoàn toàn generic). Playwright qua Chrome thật (dev server thật): thêm pill "Nghề nghiệp" → CRUD 2 mục → hiện đúng trong bảng; Combobox ở cả 2 form load đúng danh mục, chọn được, hiển thị đúng label sau khi chọn; không lỗi console ngoài dự kiến.
- Cập nhật `docs/ERD.md` (v1.21), `.claude/docs/data-model.md`, `docs/DECISIONS.md` (#061), `docs/CURRENT.md`, `docs/TASK.md`.

## 2026-08-20 (5)

S3-09 (test + đo hiệu năng màn hình khám) — Sprint 3 hoàn tất toàn bộ:

- Bổ sung 3 test cách ly/scope còn thiếu ở `encounter-http.spec.ts` theo `.claude/docs/multi-tenancy.md` ("mỗi endpoint chạm dữ liệu phải có test cách ly tenant") — rà soát 4 endpoint S3-05 phát hiện `PUT .../clinical-note` thiếu cả "bác sĩ khác (scope personal) → 404" lẫn "tenant B → 404" (`consultation`/`diagnoses`/`complete` đã có sẵn); `complete` thiếu riêng test scope personal. Không tìm thấy bug thật — code đã lọc đúng `tenantId`/`doctorId` từ S3-05, chỉ thiếu xác nhận (cùng kết luận như S2-10).
- `apps/api/scripts/perf-consultation.ts` (mới, thủ công không chạy CI, cùng khuôn `perf-patient-search.ts` S2-02) — seed 1 bệnh nhân có 20 lượt khám cũ (COMPLETED, kèm chẩn đoán + 8 mục ghi chú lâm sàng) + 1 lượt khám đang `IN_CONSULTATION`, đo đúng 4 truy vấn tuần tự mà `EncounterService.getConsultationDetail()` thực hiện (gọi thẳng 3 repository liên quan, qua role app `DATABASE_URL` + `SET LOCAL app.current_tenant_id`, không qua role đặc quyền).
- **Đã xác minh thật**: 338/338 test `apps/api` pass (30/30 `encounter-http.spec.ts`, thêm 3 — 1 lần chạy đầu deadlock ở `afterAll` cleanup khi chạy song song nhiều file test, cùng loại flake tiền-nhiệm đã ghi nhận ở `geo-http.spec.ts`, xác nhận qua chạy lại pass 100%). `pnpm -w typecheck/build` sạch toàn workspace (lint chỉ còn 3 lỗi cấu hình ESLint plugin có sẵn từ trước, không liên quan). Chạy thật `pnpm --filter @nexamed/api run perf:consultation` trên dev DB: tổng 4 truy vấn 33ms — ĐẠT xa ngưỡng "dưới 2 giây" (PRD mục 5, gate cuối Sprint 3 ở `docs/product/plan.md` mục 6).
- Cập nhật `docs/TASK.md` (đánh dấu S3-09 xong), `docs/CURRENT.md`.

## 2026-08-20 (4)

Chủ dự án dùng thử màn hình khám (2026-08-20 (3)) và yêu cầu một loạt điều chỉnh trực tiếp — lớn nhất là đổi `clinical_note.section` từ 4 mục SOAP sang 8 mục nhóm "Tiền sử"/"Thăm khám" (`docs/DECISIONS.md` #060, thay thế quyết định nội dung tab 1 ở #059):

- **Schema**: migration `20260820150000_clinical_note_sections_v2` — `clinical_note.section` đổi `SUBJECTIVE/OBJECTIVE/ASSESSMENT/PLAN` → `PERSONAL_HISTORY/FAMILY_HISTORY/REASON_FOR_VISIT/ILLNESS_PROGRESS/PRELIMINARY_DIAGNOSIS/GENERAL_EXAM/REGIONAL_EXAM/PLAN` (xoá dữ liệu demo cũ trước khi đổi enum, chưa có tenant production). `packages/shared/src/encounter.ts` đổi theo 8 khoá; `reasonForVisit`/`preliminaryDiagnosis` bắt buộc. "Tiền sử dị ứng" đọc/ghi thẳng `patient.allergyNote` (không phải section riêng) — ripple RBAC: thêm `doctor.patient.update = global` (`packages/core/src/rbac/permissions.ts`).
- **`ReceptionService.recordVitalSigns()`** (REC-02/03) nới điều kiện trạng thái từ chỉ `CHECKED_IN` sang cả `IN_CONSULTATION` — bug thật phát hiện lúc test (bác sĩ bấm "Bổ sung sinh hiệu" giữa màn khám → 409). `VitalSignsDialog.tsx` (mới) gọi endpoint này.
- **Ngưỡng nhiệt độ** (`packages/core/src/vital-sign/vital-sign-thresholds.ts`) đổi trần 39.0°C→38.5°C. Thêm phân loại BMI theo WHO Châu Á - TBD (5 mức, THUẦN hiển thị web, không đổi backend).
- **Web**: gộp 3 khối lại thành 1 khung "THÔNG TIN KHÁM LÂM SÀNG" (bỏ 3 khung Boxed Section riêng), ô nhập rút gọn (`Textarea` thêm prop `dense`), lưới 3 cột. Tab 1 đổi tên "Khám & Chẩn đoán", nhãn "Chẩn đoán sơ bộ" → "Chuẩn đoán". `VitalChip` đổi bố cục 1 dòng (nhãn/giá trị+đơn vị+phân loại), bỏ chấm màu trạng thái.
- **Đã xác minh thật**: `encounter-http.spec.ts` (27/27, viết lại block clinical-note), `reception-http.spec.ts` (28/28), `patient-http.spec.ts` (47/47, cập nhật 1 test theo quyền mới), `vital-sign-thresholds.spec.ts` — tổng 335/335 test `apps/api` pass. `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright qua Chrome hệ thống xác nhận toàn bộ luồng + màu cảnh báo đúng ngưỡng mới.
- Cập nhật `docs/product/prd.md` (ENC-02), `.claude/docs/data-model.md`, `docs/ERD.md` (v1.20), `.claude/docs/security-audit.md`, `docs/DECISIONS.md` (#060), `docs/CURRENT.md`, `docs/TASK.md`.

## 2026-08-20 (3)

S3-06/S3-07 (web màn hình khám bệnh) hoàn thành — code theo mockup/plan đã duyệt từ phiên trước, cùng vài vòng chỉnh trực tiếp trên bản chạy thật theo phản hồi chủ dự án:

- **`apps/web/src/features/encounter/`** (mới): `EncounterConsultationPage.tsx` (route `/encounters/:id`), `encounter.api.ts`/`encounter.queries.ts`, `Icd10DiagnosisPicker.tsx` (tái dùng `useIcd10SearchQuery` từ `catalog-clinical`). `shared/ui/Textarea.tsx` (mới, trích từ mẫu textarea đã lặp 2 lần). Vào từ "Hàng đợi khám" — `ReceptionDoctorQueuePage.tsx` nay hiện cả `IN_CONSULTATION` (nút "Tiếp tục khám") ngoài `CHECKED_IN` ("Bắt đầu khám"), điều hướng thẳng `/encounters/:id` sau khi bắt đầu.
- **Chẩn đoán ICD-10 tự lưu mỗi lần thêm/xoá/đổi bệnh chính** (giữ bất biến đúng 1 PRIMARY, không cần nút Lưu riêng); **SOAP lưu thủ công qua "Lưu nháp"**; "Hoàn tất khám" chặn phía client nếu chưa có chẩn đoán chính trước khi gọi API.
- **Chỉnh theo phản hồi chủ dự án ngay trong phiên** (không dựng mockup lại — sửa trực tiếp trên bản chạy thật):
  - Banner bệnh nhân tách 2 tầng (định danh chính + mã lượt khám + cảnh báo dị ứng ở hàng 1; tuổi/giới/SĐT ở hàng 2 nhạt hơn) và khối sinh hiệu neo cố định bên phải (`flex-shrink-0`) — sửa lỗi vỡ dòng ở 1366px do dồn quá nhiều phần tử vào 1 hàng `flex-wrap`. Dùng skill `ui-ux-pro-max` tham khảo nguyên tắc phân cấp thị giác; giữ nguyên bảng màu đã chốt (`ui-guidelines.md`), không theo màu cyan/xanh lá gợi ý chung chung của skill. Mỗi ô sinh hiệu thêm chấm trạng thái (emerald=trong ngưỡng/rose=ngoài ngưỡng, đúng "Tín hiệu Y tế" mục 2.1) — không chỉ dựa màu chữ (quy tắc "Color Only" trong bộ UX checklist).
  - **Sidebar tự thu gọn khi vào màn hình khám, tự khôi phục khi rời đi** — nâng trạng thái `collapsed` từ `useState` cục bộ trong `Sidebar.tsx` lên `shared/layout/sidebar.context.tsx` (context dùng chung, cùng khuôn `breadcrumb.context.tsx`), thêm `useAutoCollapseSidebar()` cho trang cần nhiều không gian ngang.
  - Panel "Tiền sử & lịch sử khám" đổi bố cục thẻ theo đúng mockup gốc (chủ dự án gửi lại ảnh tham khảo): nhóm "Lần khám gần nhất" (thẻ nổi bật viền/nền xanh) tách khỏi "Lịch sử cũ hơn" (thẻ phẳng), có nhãn thời gian tương đối ("N tháng trước"). Chỉ 3 trường đã chốt phạm vi trước đó (ngày, lý do, chẩn đoán chính) — không thêm đơn thuốc cũ/kết quả xét nghiệm (chưa có trong `EncounterHistoryItem`, xem `docs/DECISIONS.md` #059).
  - **Đổi tên nhãn 4 ô SOAP** sang thuật ngữ tự nhiên ("Lý do khám / Triệu chứng", "Diễn biến bệnh & Khám lâm sàng", "Đánh giá / Chẩn đoán sơ bộ", "Kế hoạch điều trị & Lời dặn") thay ký hiệu S/O/A/P — đã hỏi và xác nhận **vẫn giữ đúng 4 ô lưu 4 cột SOAP trong DB** (PRD ENC-02), chỉ đổi cách hiển thị, không gộp thành 1 ô như ảnh tham khảo gốc (ảnh đó chỉ có 1 ô, không đủ đáp ứng ENC-02).
  - "Lý do khám / Triệu chứng" tự mồi từ `encounter.chiefComplaint` (lý do tiếp nhận) khi mục SOAP `subjective` chưa từng được lưu — bác sĩ gõ thêm/sửa trên đó; đã lưu rồi thì tôn trọng đúng giá trị đã lưu (kể cả rỗng có chủ đích), không mồi lại đè lên.
- **Đã xác minh thật qua Playwright** (Chrome hệ thống, dữ liệu tạo qua API — tài khoản bác sĩ + bệnh nhân demo có tiền sử 3 lần khám cũ): luồng đầy đủ Hàng đợi khám → Bắt đầu khám → điền SOAP → thêm chẩn đoán E11 (tự "Bệnh chính") → Lưu nháp → Hoàn tất khám → quay lại hàng đợi, dòng biến mất; xác nhận qua API `encounter.status=COMPLETED` đúng 4 section SOAP + 1 diagnosis PRIMARY. Sidebar co/giãn đúng lúc vào/rời màn hình khám, không tràn ngang ở 1366px. `pnpm -w typecheck/build` sạch toàn workspace (lint chỉ còn 3 lỗi cấu hình ESLint plugin có sẵn từ trước — thêm 1 lỗi cùng loại ở `sidebar.context.tsx` do dùng lại đúng mẫu code đã có, không phải lỗi mới). Không đổi backend/API/schema — 333 test `apps/api` không regress.
- Cập nhật `docs/TASK.md` (đánh dấu S3-06/07 xong).

## 2026-08-20 (2)

S3-05 (API màn hình khám) xong — module `encounter` mở rộng đủ 4 endpoint; mockup web S3-06/07 chốt xong bố cục sau nhiều vòng tham khảo HIS khác, code web tạm dừng theo yêu cầu chủ dự án — `docs/DECISIONS.md` #059:

- **Bối cảnh**: EnterPlanMode + 3 agent Explore khảo sát trước khi code (kế hoạch lưu `C:\Users\Administrator\.claude\plans\lexical-watching-wall.md`) — xác nhận bảng `diagnosis`/`clinical_note` hoàn toàn chưa tồn tại dù đã có sẵn trong đặc tả `docs/ERD.md`/`.claude/docs/data-model.md` từ trước. Hỏi và chốt 3 điểm qua AskUserQuestion trước khi lập kế hoạch chi tiết: "Hoàn tất khám" không đợi Kê đơn Sprint 4; panel tiền sử chỉ cần danh sách tóm tắt; dựng mockup Artifact duyệt trước khi code web.
- **Backend (S3-05, đã xong)**: migration `20260820120000_clinical_diagnosis_note` — bảng `diagnosis`/`clinical_note` đúng khuôn đã đặc tả sẵn (không đổi cột). `apps/api/src/modules/encounter/` thêm `diagnosis.repository.ts`/`clinical-note.repository.ts`, mở rộng `encounter.service.ts`/`.controller.ts` đủ 4 route: `GET /encounters/:id/consultation` (gộp encounter + bệnh nhân/dị ứng + sinh hiệu mới nhất + tiền sử tóm tắt + chẩn đoán + SOAP trong MỘT request), `PUT .../diagnoses` (thay toàn bộ danh sách, ép Zod + DB partial unique đúng 1 `PRIMARY`), `PUT .../clinical-note` (lưu cả 4 mục SOAP một lần, upsert tìm-hoặc-tạo theo section, bản nháp — ký/đính chính để dành Sprint 5), `POST .../complete` (`IN_CONSULTATION→COMPLETED`, chỉ cần đúng 1 chẩn đoán chính, tái dùng permission `encounter.update`, không thêm permission mới). 2 lỗi mới `DiagnosisPrimaryRequiredError`/`EncounterNotInConsultationError` (`packages/core`). `apps/api/src/testing/tenant-fixture.ts` vá thêm dọn `diagnosis`/`clinical_note` trước `encounter` (FK RESTRICT).
- **Mockup web (S3-06/07) — đã chốt bố cục, chưa code**: chủ dự án gửi 2 ảnh tham khảo HIS khác ("MedUX Clinic ERP") qua nhiều vòng góp ý. Bố cục cuối: trang cuộn dọc MỘT khung (không ẩn/hiện tab), 4 tab đánh số là neo bấm-để-cuộn-tới theo quy trình khám (1. Chẩn đoán và điều trị — nội dung thật; 2. Chỉ định cận lâm sàng, 3. Kê đơn thuốc, 4. Lời dặn & hẹn tái khám — CHỈ giữ chỗ "Sắp ra mắt", ngoài phạm vi v1/Sprint 4). Panel trái cố định (bệnh nhân + dị ứng + tiền sử tóm tắt), dải sinh hiệu (có BMI) luôn hiện dưới topbar, thanh hành động cố định dưới cùng. Màu/font khi code thật phải theo `.claude/docs/ui-guidelines.md`, không copy nguyên bảng màu sky-blue/font Inter của ảnh tham khảo (mockup so sánh bố cục dùng tạm màu gốc, không phải quyết định cuối). Mockup lưu `docs/design/encounter-consultation-mockup.html` + Artifact.
- **Đã xác minh thật**: `encounter-http.spec.ts` thêm 17 test (27/27 tổng file) — tổng 333/333 test `apps/api` pass (1 flake tiền-nhiệm không liên quan ở `clinic-profile-http.spec.ts`, xác nhận qua chạy lại riêng, không phải regression từ thay đổi này). `pnpm -w typecheck` sạch toàn workspace. OpenAPI (`openapi:generate`) + web codegen (`api:codegen`) đã sinh lại cho 4 endpoint mới.
- Cập nhật `.claude/docs/data-model.md`, `docs/ERD.md` (v1.19), `docs/DECISIONS.md` (#059), `docs/CURRENT.md`, `docs/TASK.md`.

## 2026-08-20 (1)

ADM-07 (Vai trò tuỳ biến + ma trận phân quyền) — mockup Artifact 2 vòng trước khi code, ripple sang `user_account` (`roleIds` thay `roleNames`) — `docs/DECISIONS.md` #057:

- **Bối cảnh**: ADM-07 (P1) treo từ Sprint 1. Chủ dự án yêu cầu làm trước trong lúc chờ mở khoá phần "Khám bệnh". Hỏi chốt phạm vi qua AskUserQuestion (CRUD đầy đủ vai trò tuỳ biến, chấp nhận giới hạn Sidebar chưa theo permission thật, gộp vào "Danh mục Tổ chức và Nhân sự") trước khi dựng mockup. Mockup đầu tiên (2 phương án bố cục) rồi chủ dự án gửi ảnh tham khảo HIS khác (bảng CRUD Xem/Thêm/Sửa/Xoá/Đặc quyền mở rộng) — dựng lại mockup thứ 2 ánh xạ đúng danh mục quyền thật của NEXAMed, được duyệt.
- **Backend**: `apps/api/src/modules/iam/role.controller|service|repository.ts`, `role-permission.repository.ts` (mới) — 6 endpoint `/api/v1/roles*` (list/create/rename/hide/xem+sửa ma trận), dùng chung quyền `role_permission.manage` (không permission mới). Vai trò mặc định (`is_system_default=true`) chỉ sửa ma trận, không đổi tên/ẩn (`RoleImmutableError`). Ẩn vai trò tuỳ biến còn tài khoản gán → `RoleInUseError` (409). `role.name` UNIQUE đổi từ thường sang PARTIAL (`WHERE deleted_at IS NULL`, migration `20260820090000_role_management`) để tạo lại đúng tên đã ẩn không lỗi.
- **Ripple bắt buộc sang `user_account`**: `createUserAccountRequestSchema`/`updateUserAccountRequestSchema` đổi `roleNames: UserRole[]` (enum 5 tên cố định) → `roleIds: string[]` (uuid thật) — không đổi thì vai trò tuỳ biến tạo ra không gán được cho ai. `currentUserSchema.roles` (`/auth/login`, `/auth/me`) nới từ enum sang `z.array(z.string())` để không crash khi user mang vai trò tuỳ biến.
- **Frontend**: `apps/web/src/features/role/` (mới) — trang "Vai trò & Phân quyền" (`/admin/catalog-organization`, thay `ComingSoonPage`, pill trong `ConfigScreenShell`). Cột trái danh sách vai trò, cột phải bảng ma trận CRUD (`RoleMatrixTable.tsx`) nhóm theo 6 khu vực tính năng (`permission-grouping.ts`), tên 5 vai trò hệ thống dịch tiếng Việt chỉ ở tầng hiển thị (`role-labels.ts`).
- **3 vấn đề UI thật phát hiện sau khi chủ dự án dùng thử bản chạy thật (khác mockup tĩnh)**: (1) bảng HTML mặc định tự nới cột theo nội dung, cột "Đặc quyền mở rộng" tràn khung nhìn không dấu hiệu cuộn — khoá `table-fixed` + `<colgroup>` độ rộng tường minh; (2) bỏ nhầm luôn cả phần gộp nhiều tính năng dưới 1 tiêu đề chung khi dọn hàng tiêu đề trùng lặp — thêm lại có điều kiện (chỉ hiện khi nhóm ≥2 module); (3) tên vai trò hiện nguyên tiếng Anh kỹ thuật — dịch sang tiếng Việt.
- **Đã xác minh thật**: `role-http.spec.ts` (18/18), `user-account-http.spec.ts` cập nhật toàn bộ payload `roleNames`→`roleIds` (18/18) — tổng 315 test `apps/api` + 73 `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua Chrome thật (`executablePath`): toàn bộ luồng tạo/đổi tên/ẩn vai trò, sửa/lưu ma trận — không lỗi console.
- Cập nhật `.claude/docs/data-model.md`, `docs/ERD.md` (v1.18, thêm C14), `docs/DECISIONS.md` (#057), `docs/CURRENT.md`.

## 2026-08-19 (2)

Module `icd10_catalog` (S3-01, mở khoá một phần) — schema mở rộng đầy đủ, trang tra cứu 3 cột cascade, seed ĐỦ Chương I-XXII (15.844 mã) — `docs/DECISIONS.md` #056:

- **Bối cảnh**: S3-01 treo từ đầu Sprint 3 vì thiếu file danh mục chính thức. Phiên trước bị dừng giữa chừng lúc chuẩn bị dữ liệu nguồn (Chương I dán qua chat bị lỗi mojibake). Resume phiên này: chủ dự án cung cấp lại đường dẫn file gốc trên đĩa — hoá ra file gốc đã đúng UTF-8 sẵn, KHÔNG hề mojibake (lỗi chỉ sinh ra lúc dán vào khung chat) — copy byte-for-byte (`cp`, xác minh `diff`) vào `docs/data/icd10-chuong-<số>.md`, không cần bước sửa mojibake đã dự trù trong plan đã duyệt (EnterPlanMode + 2 agent Explore từ phiên trước). Chủ dự án gửi tiếp toàn bộ Chương II-XXII trong cùng phiên (thư mục nguồn có sẵn đủ 22 chương) — nạp bằng đúng script/quy trình đã dựng cho Chương I, không sửa code nghiệp vụ.
- **Schema mở rộng đầy đủ** (thay field `chapter` đơn lẻ dự kiến ban đầu): `code`(PK)/`name_vi`/`name_en`/`search_key`(generated, tái dùng `nexamed_unaccent_lower()` của `patient`)/`chapter_code`/`chapter_name`/`block_code`/`block_name`/`group_code`/`group_name`/`is_billable`/`gender_restriction`/`usage_restriction`/`who_note` — đúng khuôn `province`/`ward`. Migration `20260819110000_icd10_catalog` (2 enum mới, REVOKE INSERT/UPDATE khỏi `nexamed_app`, chỉ chạy một lần dù nạp thêm chương).
- **Script parse dùng-một-lần** đọc TOÀN BỘ file khớp mẫu `docs/data/icd10-chuong-*.md` (22 file) → `packages/core/src/icd10/data.ts` — tách ký hiệu chéo `†` (dagger, mã nguyên nhân)/`*` (asterisk, mã biểu hiện, hệ thống WHO) khỏi `code`/`group_code`, giữ nguyên trong `name_vi`/`name_en`.
- **2 vấn đề kỹ thuật thật phát sinh khi dữ liệu tăng lên 15.844 dòng, đã sửa**: (1) mảng object literal làm TypeScript lỗi `TS2590` (vượt ngưỡng độ phức tạp) — chuyển `data.ts` sang nhúng chuỗi JSON + `JSON.parse` lúc chạy; (2) `chapterCode` là số La Mã ("I".."XXII"), sắp theo thứ tự chuỗi ở DB sai thứ tự thật (`"IX" < "V"`) — thêm `packages/core/src/icd10/roman-numeral.ts` (`romanToInt`, unit test), `Icd10Service.listChapters` tự sắp lại ở tầng ứng dụng.
- **Backend**: `apps/api/src/modules/icd10/` (`chapters`/`groups`/`codes`/tìm kiếm theo tên không dấu, 4 endpoint, dùng lại quyền `patient.read`).
- **Frontend**: `apps/web/src/features/catalog-clinical/` — trang "Danh mục Chuyên môn" (`/admin/catalog-clinical`, thay `ComingSoonPage`), `ConfigScreenShell` pill phẳng "ICD-10", 3 cột cascade (Chương | Nhóm gộp theo Khối | Bảng mã chi tiết) + ô tìm nhanh bỏ qua cascade, badge cảnh báo mềm giới tính/hạn chế bệnh chính (không chặn — logic chặn thật thuộc S3-06/07, chưa xây).
- **Hạn chế đã biết, chủ động để nguyên theo yêu cầu chủ dự án**: ô tìm nhanh (`contains` trên `search_key`) với từ khoá ngắn có thể khớp chuỗi con không liên quan ngữ nghĩa (ví dụ "tả"→"ta" khớp cả "lao ở tai") — không phải vấn đề tốc độ (đã có GIN trigram), chỉ là độ liên quan kết quả; để dành cân nhắc lúc làm bộ chọn chẩn đoán thật.
- **Đã xác minh thật**: `packages/core` `data.spec.ts` (9/9 — đúng số dòng từng Chương I-XXII, code duy nhất, không còn ký hiệu `†`/`*`) + `roman-numeral.spec.ts` (3/3), `apps/api` `icd10-http.spec.ts` (11/11, HTTP e2e trên Postgres thật, seed đủ 22 chương, thứ tự Chương trả về đúng số La Mã) — tổng 296 test `apps/api` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. Reseed dev DB thật (`pnpm db:seed`, idempotent) — xác nhận đủ 15.844 dòng đúng số lượng từng chương. Playwright qua Chrome cài sẵn trên máy (`executablePath` — Playwright không tự tải được Chromium bundled trong môi trường này sau 3 lần thử, tải treo ở bước giải nén 100%, nghi antivirus): đăng nhập → Danh mục Chuyên môn → Chương I → Nhóm A00 → đúng 4 mã, tìm nhanh hoạt động, không lỗi console.
- Cập nhật `.claude/docs/data-model.md`, `docs/ERD.md` (v1.17), `docs/DECISIONS.md` (#056), `docs/CURRENT.md`, `docs/TASK.md`.

## 2026-08-19 (1)

"Phòng làm việc hôm nay" (định tuyến theo phòng, tham khảo tài liệu chủ dự án gửi) + "Tầng phòng" (Tầng/Phòng/Bàn khám-Ghế, tích hợp chung 1 màn hình Quản trị) — `docs/DECISIONS.md` #054, #055:

- **Bảng mới `doctor_room_session`**: bác sĩ tự chọn phòng lúc đăng nhập mỗi ngày (popup `RoomSessionDialog.tsx`, gate bởi `RoomSessionGate.tsx` trong `AppShell.tsx`). Endpoint tự-phục vụ `GET /rooms/options`, `GET/PUT /rooms/my-session` (chỉ `JwtAuthGuard`, không permission mới — cùng nguyên tắc `/auth/me`). **Không đổi `data_scope`/cách lọc "Hàng đợi khám"** (vẫn `doctor_id` như #042) — chỉ điều phối/hiển thị: `GET /appointments/doctors` thêm `currentRoomName`, badge "Đang ở: {phòng}" ở Hàng đợi khám, nhãn phòng cạnh tên bác sĩ ở `DoctorAvailabilityList.tsx` (Đặt lịch/Dời lịch) và dropdown bác sĩ ở `ReceptionIntakeForm.tsx` (Tiếp nhận bệnh nhân).
- **Bảng mới `floor` (Tầng, tùy chọn — `room.floor_id` nullable) và `exam_station` (Bàn khám/Ghế, bắt buộc thuộc 1 phòng)** — thuần mô tả, đơn vị điều phối thật vẫn ở cấp `room`, không đổi. `RoomController` (S2-07) lần đầu có UI web quản lý — pane "Tầng phòng" mới (`RoomPane.tsx`, `/admin/system-config`) bố cục master-detail (thẻ "Tầng" lọc bảng "Phòng", theo ảnh tham khảo chủ dự án gửi) + `ExamStationDialog.tsx` (modal quản lý bàn khám/ghế theo phòng).
- **Tự động ẩn hoàn toàn ở quy mô 1-3 bác sĩ**: 0-1 phòng active thì không có UI phòng nào xuất hiện. Riêng thẻ "Tầng" (Quản trị) LUÔN hiện với nút "+" nổi bật (nền xanh đặc) dù 0 tầng — phát hiện lúc tự kiểm bằng Playwright: nếu ẩn hết thì không có cách nào tạo tầng đầu tiên. Thêm ô tìm kiếm tầng theo tên phía trên danh sách (giữ nguyên "Tất cả").
- **Đã xác minh thật**: `doctor-room-session-http.spec.ts` (8/8), `floor-exam-station-http.spec.ts` (11/11) — tổng 285 test `apps/api` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật (dữ liệu thật, không mock): toàn bộ luồng 0→2 phòng/2 tầng/2 bàn khám, dialog chọn phòng, badge, nhãn phòng ở cả 2 nơi chọn bác sĩ.
- Cập nhật `.claude/docs/data-model.md`, `docs/ERD.md` (v1.16), `docs/DECISIONS.md` (#054, #055), `docs/CURRENT.md`.

## 2026-08-18 (2)

Panel "Chi tiết lịch hẹn" — tách "Sửa lịch" (tại chỗ, trong ngày) khỏi "Dời lịch" (tạo lịch mới, đổi ngày), 2 thao tác tồn tại song song; "Tiếp nhận" điều hướng trang thay popup; popup "Tiếp nhận thành công" + nút "Nhập lại" (`docs/DECISIONS.md` #053):

- **"Dời lịch"** (`POST /appointments/:id/reschedule`, thay hẳn `PATCH .../reschedule` cũ S2-09): chọn ngày mới + giờ + bác sĩ (danh sách trống/bận theo ngày đã chọn) → lịch CŨ chuyển `status='RESCHEDULED'` (giá trị enum mới), lịch MỚI được TẠO RIÊNG (kế thừa fullName/phone/reason/roomId/durationMinutes/source), cột mới `appointment.rescheduled_from_id` trên lịch mới trỏ về lịch cũ. Cả 2 bước cùng 1 transaction.
- **"Sửa lịch"** (khôi phục, endpoint mới `PATCH /appointments/:id`): đổi giờ/bác sĩ/thời lượng TẠI CHỖ, cùng id, không đổi status, không có ô chọn ngày (chỉ trong ngày hiện có).
- `DoctorAvailabilityList.tsx` (mới) — danh sách bác sĩ trống/bận dùng chung cho Đặt lịch nhanh lẫn Dời lịch/Sửa lịch (trùng lặp lần 2, tách theo CLAUDE.md).
- **"Tiếp nhận"** đổi từ mở popup (`ReceptionIntakeDialog.tsx`, đã xoá) sang điều hướng `/reception/new` kèm `location.state.checkin` — đảo ngược #043/#044.
- `ReceptionIntakeForm.tsx`: "Lưu tiếp nhận" (`mode='direct'`) hiện popup "THÀNH CÔNG / Tiếp nhận thành công" thay vì điều hướng ngay, nút "Tiếp nhận mới" xoá form và ở lại trang. Footer đổi "Huỷ" → "Quay lại", thêm nút "Nhập lại" (`resetForm()` — giữ nguyên fullName/phone/doctorId khi `mode='checkin'`, tránh xoá mất danh tính khách đang check-in).
- Migration `20260818150000_appointment_reschedule_status`: thêm `RESCHEDULED` vào enum `appointment_status`, cột `appointment.rescheduled_from_id`.
- **Đã xác minh thật**: `appointment-http.spec.ts` viết lại 2 describe (`PATCH :id` 7 test + `POST :id/reschedule` 8 test, 56/56 pass) — tổng 259 test `apps/api` không regress. `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright qua trình duyệt thật (dữ liệu thật, không mock): Dời lịch → lịch mới đúng giờ/mã, lịch cũ hiện "Đã dời lịch"; Sửa lịch → lưu tại chỗ đúng cùng mã, không có ô ngày; Tiếp nhận → điều hướng đúng kèm prefill Họ tên/SĐT; popup "THÀNH CÔNG" đúng mẫu tham khảo chủ dự án gửi.
- Cập nhật `.claude/docs/data-model.md`, `.claude/docs/clinical-workflow.md`, `docs/ERD.md` (v1.14), `docs/DECISIONS.md` (#053), `docs/CURRENT.md`.

## 2026-08-18 (1)

Thiết kế lại "Tiếp nhận bệnh nhân" — mockup Artifact duyệt qua nhiều vòng, hiện đủ field bệnh nhân thay `PatientPicker`, tra trùng SĐT/CCCD qua popup (`docs/DECISIONS.md` #052):

- **Thông tin hành chính**: bỏ `PatientPicker`, hiện thẳng `PatientFormFields` — gõ SĐT/CCCD tự tra trùng, bật `PatientMatchDialog.tsx` (popup mới, thay banner phẳng cũ) để chọn đúng hồ sơ, khoá riêng 4 field định danh sau khi khớp (`identityLocked`), field còn lại vẫn sửa được và tự PATCH khi có thay đổi. Popup dùng chung cho cả PAT-03 (nghi trùng tên+ngày sinh), thêm khả năng bấm chọn thẳng hồ sơ nghi trùng thay vì chỉ "Huỷ"/"Vẫn tạo mới". `PhoneDuplicateWarning` inline ẩn riêng trong màn này (đã có popup, tránh trùng cảnh báo).
- **Thông tin tiếp nhận**: thêm Loại tiếp nhận/Hình thức khám (bắt buộc), Ưu tiên khám + Lý do ưu tiên (bắt buộc kèm khi bật cờ) — 3 danh mục mới tái dùng `reference_catalog`. Ngày/Giờ tiếp nhận sửa được ở cả 2 chế độ (trực tiếp lẫn check-in).
- **Chỉ định dịch vụ khám**: thêm Loại giá dịch vụ (danh mục phẳng, chỉ ghi chú — không phải bảng giá đa mức thật) + Đơn vị/Số lượng (snapshot trên `encounter`), bảng hiển thị Mã/Tên/SL/Đơn giá/Thành tiền/Trạng thái — chỉ lưu để hiển thị, KHÔNG tính viện phí. "Thanh toán sau" chỉ là cờ UI, không lưu DB.
- Migration `20260818090000_reception_intake_redesign`: `reference_catalog.unit` + 4 category mới; `encounter` thêm 7 cột. Endpoint mới `GET /patients/by-national-id`.
- **Bug fix đi kèm**: giới tính bệnh nhân không còn mặc định "Nữ" (đúng `ui-guidelines.md` 4.1c, áp dụng luôn Thêm/Sửa bệnh nhân); Mã bệnh nhân trống không còn hiện placeholder "Sẽ cấp tự động..." (chỉ làm mờ ô); BMI bỏ dòng chú thích công thức + đơn vị "kg/m²"; sửa nhãn sai "Ưu tiên khám" dán nhầm cho checkbox "Thanh toán sau"; khoảng cách giữa các khối form đồng nhất `gap-8`; trang `/reception/new` bỏ tiêu đề hiển thị (chỉ `sr-only`, đúng quy tắc breadcrumb-là-định-danh-trang); popup check-in nới `max-w-lg` → `max-w-4xl`.
- **Đã xác minh thật**: `reception-http.spec.ts` thêm 2 test (27/27), `patient-http.spec.ts` thêm 4 test `by-national-id` — tổng 258/258 test `apps/api` pass. `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright qua trình duyệt thật (dữ liệu thật, không mock): tạo mới + tiếp nhận đủ field mới thành công; tra lại đúng SĐT/CCCD vừa tạo → popup hiện đúng, chọn đúng khoá 4 field; không lỗi console ngoài dự kiến.
- Cập nhật `.claude/docs/data-model.md`, `docs/ERD.md` (v1.13), `docs/DECISIONS.md` (#052), `docs/CURRENT.md`, `docs/TASK.md`.

## 2026-08-15 (8)

Căn lề dữ liệu bảng danh sách, màu nhấn phụ cho cột "Thời gian", dọn sạch `font-mono` — nguyên nhân gốc slashed zero (docs/DECISIONS.md #051):

- **Căn lề** (5 bảng danh sách): tiêu đề cột LUÔN `text-center` (kể cả Họ tên/Địa chỉ). Dữ liệu: `text-center` mặc định, ngoại lệ `text-left` cho cột Tên người (Họ tên, Bác sĩ) và Địa chỉ. Áp dụng `PatientListPage.tsx`, `AppointmentListView.tsx`, `ReceptionListPage.tsx`, `ReferenceCatalogPane.tsx`, `GeoPane.tsx`.
- **`AppointmentListView.tsx`**: dòng giờ ở cột "Thời gian" đổi `text-blue-600` → `text-brand-teal` (#099078) theo yêu cầu chủ dự án.
- **Sửa dứt điểm slashed zero** — bản sửa lần đầu (2026-08-15 (2)) chẩn đoán chưa đủ: `font-variant-numeric: lining-nums` không tắt được số 0 gạch chéo của font monospace (đó là thiết kế glyph, không phải kiểu số lining/old-style). Quét `grep -r "font-mono" apps/web/src` phát hiện 3 chỗ còn sót (`AppointmentDetailPanel.tsx` — đúng nơi chủ dự án báo lại, `ReferenceCatalogPane.tsx`, `GeoPane.tsx`), đổi hết sang `font-semibold`/`font-bold` (font chữ thường của app). Xác nhận `grep` trả về rỗng sau khi sửa.
- Không migration/schema/API nào đổi — thuần CSS.
- **Đã xác minh thật**: `pnpm -w typecheck` sạch toàn workspace. Playwright qua trình duyệt thật (tài khoản `dev.admin`): 5 bảng đúng căn lề; cột "Thời gian" đúng màu teal; panel "Chi tiết lịch hẹn" hết slashed zero. Không lỗi console ngoài dự kiến.
- Cập nhật `.claude/docs/ui-guidelines.md` (mục 9 nhóm 3 + cảnh báo `font-mono`, mục 9e, 9f mới), `docs/DECISIONS.md` (#051), `docs/CURRENT.md`.

## 2026-08-15 (7)

Tăng độ đậm chữ dữ liệu trong bảng danh sách + gộp cột "Thời gian" 2 dòng ở Lịch hẹn (docs/DECISIONS.md #050):

- **Bối cảnh**: chủ dự án phản hồi chữ trong bảng danh sách "mờ nhạt, không có điểm nhấn", yêu cầu chỉnh "tầm 400". Đo thật bằng `getComputedStyle` qua Playwright: `font-weight` các ô dữ liệu ĐÃ LÀ `400` từ trước — áp literal "400" sẽ không có tác dụng gì. Quyết định nâng lên `font-medium` (500) để thực sự giải quyết cảm giác mờ nhạt.
- **5 màn hình danh sách** (`PatientListPage.tsx`, `AppointmentListView.tsx`, `ReceptionListPage.tsx`, `ReferenceCatalogPane.tsx`, `GeoPane.tsx`): thêm `font-medium` vào ô dữ liệu chính/phụ, giữ nguyên màu. Không đổi placeholder `—`, badge trạng thái, cột mã định danh, cột metadata thứ yếu (`sortOrder`) — giữ phân cấp thị giác.
- **`AppointmentListView.tsx`** (Lịch hẹn, chế độ Danh sách): bỏ cột "Thời lượng"; đổi "Ngày giờ" → "Thời gian", dời ra ngay sau "Mã đặt lịch", trước "Họ tên"; ô hiển thị 2 dòng — giờ bắt đầu-kết thúc `HH:mm - HH:mm` (tính từ `scheduledAt+durationMinutes`) đậm xanh, ngày `DD-MM-YYYY` nhỏ nhạt dưới, theo mẫu chủ dự án gửi. `GRID_COLUMNS` 7→6 cột, `ROW_HEIGHT_PX` 44→52px.
- Không migration/schema/API nào đổi — thuần CSS + 1 phép tính hiển thị.
- **Đã xác minh thật**: `pnpm -w typecheck` sạch toàn workspace, lint chỉ còn lỗi cấu hình có sẵn không liên quan. Playwright qua trình duyệt thật (tài khoản `dev.admin`): 4 màn hình danh sách chữ đậm hơn rõ rệt; cột "Thời gian" đúng vị trí/2 dòng/giờ kết thúc tính đúng; không còn cột "Thời lượng". Không lỗi console ngoài dự kiến.
- Cập nhật `.claude/docs/ui-guidelines.md` (mục 9d, 9e mới), `docs/DECISIONS.md` (#050), `docs/CURRENT.md`.

## 2026-08-15 (6)

Cột mã định danh: bỏ gạch chân mã bấm được, đổi mã không bấm được sang màu đen (docs/DECISIONS.md #049):

- **`PatientListPage.tsx`/`AppointmentListView.tsx`** (mã bấm được — `patientCode`/`bookingCode`): bỏ `underline decoration-dotted underline-offset-2`, chỉ còn `text-blue-600 hover:text-blue-700` + `cursor-pointer`.
- **`ReceptionListPage.tsx`** (mã không bấm được — `encounterNo`): đổi `text-blue-700` → `text-slate-800` (đen, giữ `font-semibold`), tránh hiểu nhầm là link vì trước cùng tông xanh với mã bấm được.
- `GeoPane`/`ReferenceCatalogPane` (mã danh mục ngắn) không đổi — đã đúng "không bấm được → màu đen" từ trước.
- Không migration/schema/API nào đổi — thuần CSS.
- **Đã xác minh thật**: `pnpm -w typecheck` sạch toàn workspace. Playwright qua trình duyệt thật (tài khoản `dev.admin`): cả 3 danh sách (bệnh nhân, lịch hẹn, tiếp nhận) hiển thị đúng màu/không gạch chân theo quy tắc mới. Không lỗi console ngoài dự kiến.
- Cập nhật `.claude/docs/ui-guidelines.md` (mục 9, 3 nhóm mã định danh), `docs/DECISIONS.md` (#049), `docs/CURRENT.md`.

## 2026-08-15 (5)

Trang 404 thương hiệu + redirect `/admin` → `/admin/catalog` (docs/DECISIONS.md #048):

- **Bối cảnh**: chủ dự án báo lỗi thật — bấm gợi ý lịch sử trình duyệt dạng `http://localhost:5173/admin` (rút gọn từ `/admin/catalog` đã ghé trước) ra trang trắng mặc định xấu của `react-router` ("Unexpected Application Error! 404 Not Found"). `/admin` chưa từng là route thật, và router chưa có route bắt-mọi nào.
- **`router.tsx`**: thêm `{ path: 'admin', element: <Navigate to="/admin/catalog" replace /> }` xử lý đúng ca cụ thể đã báo. Thêm `{ path: '*', element: <NotFoundPage /> }` là mục CUỐI trong `children` của route `/` (bắt mọi URL con không khớp khác).
- **`apps/web/src/app/NotFoundPage.tsx` (mới)**: dùng đúng khuôn `DashboardPage`/`ComingSoonPage` (breadcrumb + `EmptyState` + nút "Về trang chủ") — render kèm sidebar/topbar như trang thường vì `RequireAuth` luôn chặn user chưa đăng nhập chuyển `/login` TRƯỚC khi tới được route `*` này, không cần route `*` riêng ở ngoài `AppShell`.
- **Đã xác minh thật**: `pnpm -w typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật (tài khoản `dev.admin`): `/admin` → redirect đúng; `/admin/khong-ton-tai` và `/duong-dan-linh-tinh` (ngẫu nhiên) → đều hiện đúng trang 404 thương hiệu kèm sidebar/breadcrumb; chưa đăng nhập gọi đường dẫn linh tinh → về đúng `/login`. Không lỗi console ngoài dự kiến.
- Cập nhật `docs/DECISIONS.md` (#048), `docs/CURRENT.md`.

## 2026-08-15 (4)

Tiêu đề tab trình duyệt động — "menu cha - trang đang chọn" (docs/DECISIONS.md #047):

- **`breadcrumb.context.tsx`**: `BreadcrumbProvider` thêm `useEffect` đồng bộ `document.title` theo `items` hiện có — không thêm API/prop riêng ở từng trang. `computeDocumentTitle`: 0 đoạn → `"NEXAMed"`; 1 đoạn → `"{đoạn} - NEXAMed"`; ≥2 đoạn → `"{đoạn đầu} - {đoạn cuối}"`. Thêm `useEffect` cleanup trả tiêu đề về `"NEXAMed"` khi rời `AppShell` (đăng xuất về `/login`).
- Không sửa trang nào khác — tự động đúng cho mọi trang đã gọi `useBreadcrumb` từ trước, kể cả trang có pill/tab (nhờ breadcrumb đã phản ánh đúng pill đang chọn từ #045).
- **Đã xác minh thật**: `pnpm -w typecheck` sạch toàn workspace. Playwright đọc `document.title` thật qua nhiều trang (Tổng quan, Lịch hẹn, Danh sách bệnh nhân, Danh mục hành chính đổi pill, trang "Sắp ra mắt", về `/login`) — đúng cả 6 trường hợp, không lỗi console ngoài dự kiến.
- Cập nhật `docs/DECISIONS.md` (#047), `docs/CURRENT.md`.

## 2026-08-15 (3)

Trang "Danh mục hành chính" — tách "Danh mục dùng chung" thành pill riêng từng danh mục, sửa bug breadcrumb tĩnh, thêm 4 sub-menu "Sắp ra mắt" (docs/DECISIONS.md #045, #046):

- **`ConfigScreenShell.tsx` thêm chế độ "pill phẳng"**: `ConfigScreenPill.items` đổi optional — khi pill không khai `items`, khung không render cột trái nữa, nội dung cột phải sát ngay thanh pill. `ClinicConfigPage` (1 pill × 2 mục) giữ nguyên chế độ cũ, không đổi hành vi.
- **`CatalogAdminPage.tsx`**: đổi tên trang "Danh mục" → "Danh mục hành chính" (sidebar + breadcrumb + `pageLabel`, route giữ nguyên `/admin/catalog`). 6 danh mục (Dân tộc, Phường/Xã, Tỉnh/Thành, Quốc tịch, Nguồn khách hàng, Loại khám) trước gộp chung 1 pill "Danh mục dùng chung" nay mỗi cái là 1 pill riêng — bỏ hẳn pill bọc ngoài.
- **Sửa bug thật phát hiện lúc chủ dự án dùng thử**: breadcrumb khai tĩnh một lần lúc mount, bấm đổi pill (ví dụ "Nguồn khách hàng") breadcrumb không cập nhật, vẫn đứng ở "Danh mục hành chính". Sửa cho cả `CatalogAdminPage` (theo `activePillKey`) lẫn `ClinicConfigPage` (theo `activeItemKey`, cùng lỗi tiềm ẩn, sửa luôn cho nhất quán).
- **4 sub-menu "Sắp ra mắt" mới** trong nhóm Quản trị, giữa "Danh mục hành chính" và "Cấu hình hệ thống": Danh mục Tổ chức và Nhân sự, Danh mục Chuyên môn, Danh mục cận lâm sàng, Danh mục Dược và Vật tư. Ngoại lệ có chủ đích với quy tắc "không dựng menu cho tính năng chưa xây" — đã hỏi qua `AskUserQuestion` trước khi làm, chủ dự án chọn trang rỗng thật (`ComingSoonPage.tsx` mới, dùng chung) và xác nhận **không** mở rộng phạm vi v1 (Cận lâm sàng/Dược và Vật tư vẫn ngoài phạm vi theo `CLAUDE.md`). 4 route mới trong `router.tsx` (`admin/catalog-organization|clinical|paraclinical|pharmacy`), không gọi API nào.
- Không migration/schema/API contract nào đổi.
- **Đã xác minh thật**: `pnpm -w typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật (tài khoản `dev.admin`): sidebar đúng nhãn/thứ tự; 6 pill Danh mục hành chính đúng, không còn "Danh mục dùng chung"; không còn cột trái; chuyển pill đổi đúng nội dung; breadcrumb đổi đúng theo pill/mục ở cả 2 trang cấu hình; 4 trang "Sắp ra mắt" hiện đúng; không lỗi console ngoài dự kiến (401 pre-login đã biết).
- Cập nhật `.claude/docs/ui-guidelines.md` (mục 10, điểm 8-9), `docs/DECISIONS.md` (#045, #046), `docs/CURRENT.md`.

## 2026-08-15 (2)

Sidebar — sắp xếp lại thứ tự sub-menu nhóm "Tiếp nhận và Đặt lịch" + loại bỏ slashed zero từ toàn bộ dự án:

- **Sidebar order**: Nhóm "Tiếp nhận và Đặt lịch" sắp xếp lại theo yêu cầu chủ dự án — thứ tự mới: **Lịch hẹn** → **Tiếp nhận bệnh nhân** → **Danh sách tiếp nhận** → **Danh sách bệnh nhân** (thay thế thứ tự cũ: Danh sách bệnh nhân → Lịch hẹn → Danh sách tiếp nhận → Tiếp nhận bệnh nhân). Sửa `Sidebar.tsx` dòng 154-161, loại bỏ `font-mono` khỏi hiển thị mã lịch hẹn `AppointmentQuickCreatePanel.tsx` (dòng 406).
- **Loại bỏ slashed zero**: Thêm CSS rule `font-variant-numeric: lining-nums;` vào `apps/web/src/app/index.css` (áp dụng toàn bộ `html`, tắt slashed zero trên mọi số). Áp dụng theo yêu cầu chủ dự án không muốn dùng kiểu chữ số 0 có gạch chéo — tuyệt đối không sử dụng kiểu đó trong toàn bộ dự án.
- **Đã xác minh thật**: `pnpm -w typecheck` sạch toàn workspace. Không test Playwright lúc này do dev server khác dùng, nhưng typecheck và sửa CSS/Sidebar không có risk — UI thuần.

## 2026-08-14 (1)

Sidebar — thêm nhóm menu "Khám bệnh", chuyển "Hàng đợi khám" sang nhóm này:

- **Bối cảnh**: chủ dự án yêu cầu tách "Hàng đợi khám" ra khỏi nhóm "Tiếp nhận và Đặt lịch" sang một nhóm sidebar riêng "Khám bệnh" (icon ống nghe), đặt ngay dưới "Tiếp nhận và Đặt lịch" — chuẩn bị chỗ cho các màn hình Khám bệnh thật (S3-05→07) khi ICD-10 hết chặn. Đã hỏi và chốt: giữ nguyên route `/reception/doctor-queue` và code ở `apps/web/src/features/reception/` (không đổi module/thư mục), chỉ đổi vị trí hiển thị trong sidebar — thuần UI.
- **`Sidebar.tsx`**: nhóm "Tiếp nhận và Đặt lịch" nay còn 4 mục con (Danh sách bệnh nhân, Lịch hẹn, Danh sách tiếp nhận, Tiếp nhận bệnh nhân). Nhóm "Khám bệnh" mới (điều kiện hiện `DOCTOR_QUEUE_ROLES` — doctor/clinic_admin, không đổi) chỉ chứa "Hàng đợi khám", cùng khuôn accordion/thu gọn với 2 nhóm còn lại. Logic tự mở nhóm theo route (`RECEPTION_GROUP_PATHS`) phải loại trừ `EXAMINATION_GROUP_PATH` (`/reception/doctor-queue`) vì cùng tiền tố `/reception` — nếu không cả 2 nhóm sẽ cùng tự mở khi vào "Hàng đợi khám".
- **Bug phát hiện lúc kiểm bằng trình duyệt**: `ReceptionDoctorQueuePage.tsx` vẫn khai `useBreadcrumb([{ label: 'Tiếp nhận và Đặt lịch' }, ...])` — breadcrumb lệch với vị trí sidebar mới. Sửa thành `{ label: 'Khám bệnh' }`.
- **Đã xác minh thật qua Playwright** (dữ liệu dev thật, đăng nhập `dev.admin`/clinic_admin): thứ tự nhóm đúng Tổng quan → Tiếp nhận và Đặt lịch → Khám bệnh → Quản trị; nhóm "Tiếp nhận và Đặt lịch" không còn "Hàng đợi khám"; nhóm "Khám bệnh" đúng 1 mục "Hàng đợi khám"; vào thẳng `/reception/doctor-queue` (fresh load) chỉ nhóm "Khám bệnh" tự mở, "Tiếp nhận và Đặt lịch" đóng — xác nhận logic loại trừ đúng; breadcrumb đúng "Khám bệnh › Hàng đợi khám" sau khi sửa. `pnpm --filter @nexamed/web run typecheck` sạch; `pnpm -w lint` chỉ còn đúng 1 lỗi ESLint cấu hình plugin có sẵn từ trước (không liên quan, đã ghi nhận nhiều lần trong các entry trước). Không console error ngoài dự kiến (chỉ 401 refresh trước đăng nhập, hành vi đã biết).
- Không migration/schema/API contract nào đổi — thuần điều hướng web.

## 2026-08-13 (30)

"Danh sách tiếp nhận" về đúng nghĩa THUẦN theo dõi trạng thái — dồn Sinh hiệu/Bắt đầu khám ra khỏi trang, hợp nhất 2 biểu mẫu tiếp nhận thành 1 component dùng chung (docs/DECISIONS.md #044):

- **Bối cảnh**: sau khi hiện thực #043 (3 màn hình tách biệt), chủ dự án phát hiện "Danh sách tiếp nhận" vẫn còn giữ nút Sinh hiệu/Bắt đầu khám/+Tiếp nhận copy nguyên từ thiết kế cũ — mâu thuẫn trực tiếp với quyết định "chỉ để theo dõi trạng thái". Đã hỏi lại từng điểm (AskUserQuestion nhiều vòng) rồi sửa ngay trong phiên, không quay lại Plan mode vì phạm vi nhỏ và đã có đủ hạ tầng từ #043.
- **Quyết định**: "Bắt đầu khám" chuyển hẳn sang "Hàng đợi khám" (đúng đối tượng thao tác — bác sĩ). "Sinh hiệu" bỏ hẳn khỏi "Danh sách tiếp nhận", chuyển sang nhập NGAY LÚC TIẾP NHẬN trên biểu mẫu tạo `encounter` — thiếu thì bác sĩ tự bổ sung sau ở form phiếu khám (module Khám bệnh, chưa xây). Nút "+Tiếp nhận bệnh nhân" bỏ (đã có mục sidebar riêng). Dialog check-in tối giản cũ bị bỏ hẳn — bấm "Tiếp nhận" trên panel Lịch hẹn giờ mở popup chứa ĐÚNG biểu mẫu tiếp nhận đầy đủ (khác `mode='direct'` chỉ ở chỗ bác sĩ/giờ cố định theo lịch hẹn, đọc-only).
- **`packages/shared/src/encounter.ts`**: tách `intakeVitalSignFieldsSchema` (8 chỉ số, đều tuỳ chọn) dùng chung cho `checkInRequestSchema` (`.merge()`, giờ CŨNG bắt buộc kèm loại khám như `registerReceptionRequestSchema`) và chính `registerReceptionRequestSchema`. `recordVitalSignRequestSchema` (endpoint bổ sung sau, REC-02/03) refine thêm ràng buộc "ít nhất 1 chỉ số" trên cùng shape.
- **Backend**: `ReceptionService.checkIn()`/`registerDirect()` dùng chung helper `createIntakeVitalSign()` — có sinh hiệu thì tạo kèm 1 dòng `vital_sign` trong CÙNG transaction với `encounter` (không tính `warnings` — REC-03 chỉ áp cho endpoint bổ sung riêng). `createEncounterAndConvert()` (helper dùng chung của `checkIn()`) mở rộng nhận `patientSourceCode`/`examType*` — trước chỉ `registerDirect()` có. `POST /reception/encounters/:id/vital-signs` (REC-02/03) **giữ nguyên** làm hạ tầng PRD yêu cầu cho lúc bổ sung sau — module Khám bệnh tương lai sẽ gọi lại đúng endpoint này.
- **Web**: `ReceptionIntakeForm.tsx` (mới) — biểu mẫu dùng chung 2 chế độ (`mode='direct'`/`'checkin'`). `ReceptionRegisterPage.tsx` giờ chỉ còn khung chrome, gọi form dùng chung. `ReceptionIntakeDialog.tsx` (mới, popup) thay `ReceptionCheckInDialog.tsx` (xoá). `ReceptionListPage.tsx` bỏ hẳn cột "Hành động"/mọi nút, chỉ còn 5 cột thuần hiển thị. `ReceptionDoctorQueuePage.tsx` thêm nút "Bắt đầu khám" thật (gọi `useStartConsultationMutation`, dòng tự biến mất khỏi hàng đợi sau khi chuyển `IN_CONSULTATION`). `VitalSignForm.tsx` xoá hẳn (0 caller còn lại sau khi gỡ khỏi `ReceptionListPage.tsx`) — cùng `useRecordVitalSignsMutation`/`recordVitalSigns()` web wrapper (backend endpoint không đổi).
- **Bug test phát hiện lúc sửa**: `encounter-http.spec.ts` có helper `checkInFreshEncounter()` RIÊNG (không dùng chung `reception-http.spec.ts`), gọi check-in với payload cũ thiếu loại khám bắt buộc mới — 3 test transition ăn theo helper đó lỗi `Cannot read properties of undefined`. Vá thêm 3 trường vào payload của helper.
- **Đã xác minh thật**: `reception-http.spec.ts` (25/25 pass, thêm assertion tạo `vital_sign` kèm check-in), `encounter-http.spec.ts` (9/9 pass sau khi vá helper). Tổng 252/252 test `apps/api`+`packages/core` pass, không regress. `pnpm -w typecheck/build` sạch toàn workspace. OpenAPI + web type sinh lại.
- Cập nhật `.claude/docs/data-model.md`, `docs/ERD.md` (v1.12), `docs/DECISIONS.md` (#044), `docs/CURRENT.md`, `docs/TASK.md`.

## 2026-08-13 (29)

Thiết kế lại "Tiếp nhận" sau 3 vòng góp ý chủ dự án — "Tiếp nhận bệnh nhân" tạo thẳng `encounter` không qua `appointment`, 2 danh mục mới tái dùng `reference_catalog`, tách "Hàng đợi khám" thành trang riêng cho bác sĩ (docs/DECISIONS.md #043):

- **Bối cảnh**: bản #042 (entry trước) — trang "Tiếp nhận" gộp lịch hẹn `SCHEDULED` + `encounter`, nút "Tiếp nhận khách vãng lai" định tạo `appointment(source='walk_in')`+`encounter` cùng lúc — bị chủ dự án bác bỏ qua 3 vòng góp ý liên tiếp trong cùng phiên. Đã dùng AskUserQuestion nhiều vòng để hỏi lại và chốt từng điểm, rồi quay lại Plan mode thiết kế lại toàn bộ trước khi sửa code.
- **Quyết định cuối**: (1) "Danh sách tiếp nhận" chỉ còn hiển thị `encounter` đã có mặt, theo ngày, KHÔNG gộp lịch hẹn chưa tới; (2) check-in từ lịch hẹn có sẵn chuyển hẳn thành popup trên panel chi tiết Lịch hẹn; (3) "Tiếp nhận bệnh nhân" — menu con MỚI, form tạo THẲNG `encounter` với `appointment_id=NULL`, không qua `appointment`; (4) 2 danh mục cấu hình mới "Nguồn khách hàng"/"Loại khám" tái dùng `reference_catalog` (thêm category + cột `price`) thay vì bảng riêng; (5) giá trên "Loại khám" chỉ lưu để hiển thị, không tính viện phí; (6) bản ghi từ "Tiếp nhận bệnh nhân" xuất hiện cả ở "Danh sách tiếp nhận" (lễ tân) và "Hàng đợi khám" (trang riêng, bác sĩ).
- **Schema**: migration `20260813160000_reception_direct_and_catalog_extension` (nối tiếp #042, không sửa migration đã deploy) — `ALTER TYPE reference_catalog_category ADD VALUE 'PATIENT_SOURCE'/'EXAM_TYPE'`, `reference_catalog.price` (bigint, nullable), `encounter.patient_source_code`/`exam_type_code`/`exam_type_name`/`exam_type_price` (snapshot, cùng tinh thần `insurance_snapshot`).
- **Backend**: `reception.service.ts` bỏ hẳn `walkIn()` (hướng đã bác bỏ), thêm `registerDirect()` (đơn giản hơn — không có `appointment` để cập nhật kèm theo). `GET /reception/queue` đổi tên `GET /reception/list` (thêm `doctorId?` filter — bác sĩ có `encounter.read=global` nên phải lọc tường minh ở query cho "Hàng đợi khám", không dựa permission scope). Thêm `POST /reception/direct` (tái dùng permission `encounter.create` có sẵn, không cần permission mới). `reference-catalog` module: chỉ mở rộng `packages/shared` (category + `price` optional) và repository/service pass-through — controller không đổi (đã generic theo category).
- **Web**: `ReceptionQueuePage.tsx` → `ReceptionListPage.tsx` (bỏ hẳn phần gộp appointment). Mới: `ReceptionRegisterPage.tsx` (form "Tiếp nhận bệnh nhân" — Boxed Section Form Pattern, `PatientPicker` + Combobox nguồn khách/loại khám/bác sĩ, chọn loại khám hiện giá read-only), `ReceptionDoctorQueuePage.tsx` (Hàng đợi khám, nút "Mở khám bệnh" khoá tạm — màn hình khám bệnh chưa xây, không dựng giao diện giả). `AppointmentDetailPanel.tsx` gắn lại nút "Tiếp nhận" mở `ReceptionCheckInDialog` dạng popup (đơn giản hoá props, không còn phụ thuộc shape hàng đợi cũ). `CatalogAdminPage.tsx`/`ReferenceCatalogPane.tsx` thêm 2 tab + input giá (hiện có điều kiện khi category `EXAM_TYPE`). Sidebar thêm 2 mục menu mới ("Tiếp nhận bệnh nhân", "Hàng đợi khám" — riêng cho `doctor`/`clinic_admin`).
- **Bug test phát hiện lúc viết lại `reception-http.spec.ts`**: test lọc `GET /reception/list` theo `date` tương lai tuỳ ý (khuôn cũ dùng cho `appointment.scheduledAt`) nhưng `checkIn()` luôn set `checked_in_at = new Date()` (giờ hệ thống thật) — sửa bằng cách không truyền `date` cho các case xuất phát từ check-in (mặc định "hôm nay"); `POST /reception/direct` (nhận `checkedInAt` tuỳ ý) không bị ảnh hưởng.
- **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch toàn workspace (1 lỗi ESLint cấu hình plugin có sẵn từ trước, không liên quan, ở file không đụng tới). `reception-http.spec.ts` viết lại hoàn toàn theo API mới (25/25 pass — thêm describe `POST /reception/direct` 5 test, `GET /reception/list` 5 test gồm `doctorId` filter). Tổng 252 test `apps/api`+`packages/core` pass (`geo-http.spec.ts` có 1 race điều kiện tiền-nhiệm khi nhiều file test chạy song song cùng testcontainer — pass 100% khi chạy riêng lẻ nhiều lần, không liên quan thay đổi lần này, thuộc `seedDefaultRolesForTenant()` chưa dùng `createMany skipDuplicates` như `sync-role-permissions.ts` đã sửa ở #042).
- Cập nhật `.claude/docs/data-model.md`, `docs/ERD.md` (v1.11), `docs/DECISIONS.md` (#043), `docs/CURRENT.md`, `docs/TASK.md`.

## 2026-08-13 (28)

Tiếp nhận bệnh nhân (Sprint 3, phần 1: REC-01→03) — check-in thật (tạo `encounter`, gắn `patient`, chuyển `appointment→CONVERTED` atomic), trang "Tiếp nhận" mới, sinh hiệu có cảnh báo theo tuổi; số điện thoại được phép trùng (docs/DECISIONS.md #042):

- **Bối cảnh**: Sprint 3 (Tiếp nhận + Khám bệnh) tạm hoãn vì S3-01 (ICD-10) thiếu file danh mục chính thức. Xác định phần "Tiếp nhận" không phụ thuộc ICD-10 — chỉ "Khám bệnh" (chẩn đoán) mới cần — nên làm được ngay. Dùng Plan mode: hỏi và chốt 4 điểm trước khi code (phạm vi trọn REC-01→03 + state machine đầy đủ; trang "Tiếp nhận" riêng thay nút check-in cũ; vá ma trận RBAC; ngưỡng sinh hiệu theo nhóm tuổi). Giữa phiên, chủ dự án bổ sung yêu cầu: số điện thoại được phép trùng (VD: mẹ dùng chung SĐT cho nhiều con) — chỉ CCCD mới chặn trùng.
- **Kiến trúc mới — chia sẻ Repository giữa module trong 1 transaction**: check-in phải atomic (tạo `encounter` + cập nhật `appointment` + gắn `patient_id`) — port hiện có không dùng được (tự mở transaction riêng, phá atomic). `AppointmentModule`/`EncounterModule` thêm `exports: [AppointmentRepository]`/`[EncounterRepository]`, `ReceptionModule` (mới, `apps/api/src/modules/reception/`) import cả hai, điều phối trong 1 `unitOfWork.runInTenantScope`. Lần đầu 2 module NestJS chia sẻ Repository qua `exports` — tiền lệ mới cho module sau (`prescription` chạm `encounter`). `reception` không sở hữu bảng nào của riêng nó trừ `vital_sign`; "Bắt đầu khám"/"bỏ về" thuộc `apps/api/src/modules/encounter/` riêng (đúng ranh giới `architecture.md`).
- **Schema**: migration `20260813150000_encounter_vital_sign_reception` (viết tay) — bảng `encounter` (đủ 8 cột bắt buộc + `patient_id`/`doctor_id`/`appointment_id?`/`encounter_no`/`status`/`specialty`/`checked_in_at`/`started_at?`/`completed_at?`/`chief_complaint?`/`insurance_snapshot`/`cancel_reason?` — cột cuối bổ sung ngoài ERD gốc, luồng "bỏ về" cần lưu lý do), `vital_sign` (đủ 8 cột bắt buộc + các chỉ số đo, đều nullable). Partial unique `(tenant_id, appointment_id)` chặn double check-in (C11). `vital_sign` không có `measured_by` riêng (mô tả tóm tắt cũ của data-model.md lệch ERD chi tiết — dùng `created_by`, cùng cách #007/#024).
- **RBAC**: vá 2 lỗ hổng ma trận seed từ S1-04b — thêm `encounter.create`/`.update`/`.cancel`; đổi `nurse.encounter.read`/`.vital_sign.create` personal→global (scope "personal" theo `doctor_id`, điều dưỡng không phải bác sĩ nên chưa từng thật sự hoạt động); thêm `receptionist.encounter.read=global`. Permission mới tự sync cho tenant cũ lúc API khởi động; 2 dòng scope đã đổi của nurse thì vá thủ công 1 lần (sync cố ý chỉ-thêm, không sửa).
- **Race condition thật phát hiện lúc chạy test**: `syncRolePermissionsForTenant()` dùng `create()` từng dòng — khi 2 tiến trình chèn cùng dòng gần như đồng thời, `create()` ném P2002 và làm ABORT CẢ TRANSACTION Postgres (mọi câu lệnh sau đó lỗi `25P02` dù đã try/catch ở tầng JS). Sửa bằng `createMany({skipDuplicates:true})` — xử lý xung đột ở tầng SQL, không bao giờ ném lỗi.
- **Ngưỡng cảnh báo sinh hiệu** (`packages/core/src/vital-sign/vital-sign-thresholds.ts`): 3 nhóm tuổi cho mạch/nhịp thở, ngưỡng chung cho nhiệt độ/SpO2, 2 nhóm cho huyết áp — tham khảo phổ biến kiểu APLS/PALS, không thay thế đánh giá lâm sàng, hằng số cố định (không đọc `tenant_setting`). Cân nặng/chiều cao không làm percentile tăng trưởng, chỉ chặn giá trị phi lý. Luôn chỉ cảnh báo, không chặn lưu.
- **Số điện thoại trùng**: `GET /patients/by-phone?phone=&excludePatientId=` (module `patient`, khớp chính xác) — `PhoneDuplicateWarning.tsx` (mới) cảnh báo mềm trong `PatientFormFields.tsx` (áp dụng tự động cho cả Thêm mới/Sửa/tạo nhanh trong `PatientPicker`); `PatientPicker.tsx` thêm prop `initialQuery` để trang Tiếp nhận tự tìm theo SĐT lịch hẹn, lễ tân chọn đúng người.
- **Web**: `apps/web/src/features/reception/` (mới) — `ReceptionQueuePage.tsx` (route `/reception`, sidebar "Tiếp nhận và Đặt lịch"), `ReceptionCheckInDialog.tsx`, `VitalSignForm.tsx` (Boxed Section Form Pattern). **Gỡ bỏ hoàn toàn** nút Check-in cũ + dialog trong `AppointmentDetailPanel.tsx` và endpoint `POST /appointments/:id/checkin` (thay bằng `POST /reception/check-in`).
- **Đã xác minh thật**: `encounter-state-machine.spec.ts` (5/5, ma trận 6×6), `vital-sign-thresholds.spec.ts` (10/10), `reception-http.spec.ts` (19/19 — check-in atomic, double check-in race, hàng đợi, sinh hiệu, cách ly tenant), `encounter-http.spec.ts` (9/9), `patient-http.spec.ts` +4 test `by-phone`. Tổng **246/246 test `apps/api` pass**, chạy lặp lại 3 lần liên tiếp xác nhận hết flaky. `pnpm -w lint/typecheck/build` sạch toàn workspace (trừ 1 lỗi ESLint có sẵn từ trước, không liên quan).
- Cập nhật `.claude/docs/data-model.md`, `.claude/docs/security-audit.md`, `docs/ERD.md` (v1.10), `docs/DECISIONS.md` (#042), `docs/CURRENT.md`, `docs/TASK.md`.

## 2026-08-13 (27)

Trang "Thông tin phòng khám" (mở rộng `tenant`, 2 logo upload) + sửa bug ảnh vỡ có sẵn từ #034 + gate upload ảnh theo chế độ Sửa (docs/DECISIONS.md #041):

- **Bối cảnh**: chủ dự án yêu cầu thêm trang cấu hình "Thông tin phòng khám" (tên, điện thoại, địa chỉ, email, đơn vị tiền tệ, mã số thuế, múi giờ, logo chính 220×110px, logo mẫu in 110×110px) vào pill "Cấu hình phòng khám" (`/admin/system-config`), đặt trước "Giờ làm việc & Slot". Dùng EnterPlanMode: đã hỏi và chốt 4 điểm trước khi code vì đụng schema `tenant` — nơi lưu (mở rộng cột `tenant`, không dùng `tenant_setting`), múi giờ/tiền tệ chỉ lưu chưa nối logic, kích thước logo chỉ khuyến nghị.
- **Schema**: migration `20260813120000_tenant_profile_fields` (viết tay) — thêm `phone`/`email`/`currency` (default `'VND'`)/`timezone` (default `'Asia/Ho_Chi_Minh'`)/`logo_key`/`print_logo_key` lên `tenant` (`name`/`address`/`taxCode` đã có sẵn). Không RLS mới (`tenant` không có `tenant_id`), không permission mới (dùng lại `clinic_config.read`/`.update`).
- **`packages/shared/src/currency.ts`/`timezone.ts` (mới)**: danh sách mã hợp lệ cho Zod enum (10 tiền tệ, 10 múi giờ) — chỉ dùng validate server-side, KHÔNG import vào `apps/web` (né lại đúng giới hạn Rollup đã ghi ở #032/#036 — danh sách hiển thị cho dropdown đặt lặp lại có chủ đích trong `ClinicInfoPane.tsx`). `packages/core/src/errors/clinic-errors.ts` (mới): `InvalidLogoError` (`CLINIC_INVALID_LOGO`) — phải thêm dòng vào `DOMAIN_ERROR_STATUS` (`domain-exception.filter.ts`), quên bước này khiến lỗi rơi vào 422 mặc định thay vì 400 (bắt được lúc chạy test, sửa ngay).
- **Backend**: `apps/api/src/modules/clinic/clinic-profile.{controller,service,repository}.ts` (mới, mở rộng module `clinic` có sẵn) — `GET/PATCH /clinic-profile`, `POST /clinic-profile/logo`, `POST /clinic-profile/print-logo` — cùng khuôn upload ảnh đại diện bệnh nhân (#034): magic-byte sniff (`sniffImageExtension`, tái dùng nguyên), `StoragePort`, signed URL 15 phút, xoá file cũ SAU khi lưu file mới thành công, tối đa 2MB (nhỏ hơn 3MB của ảnh bệnh nhân vì logo luôn nhỏ).
- **Frontend**: `apps/web/src/features/clinic/ClinicInfoPane.tsx` (mới) — Boxed Section Form Pattern (mục 9b): khối "Thông tin cơ bản" (2 khung logo cạnh lưới trường) + khối "Hiển thị" (Combobox Đơn vị tiền tệ có cờ quốc gia trước mã, Combobox Múi giờ). `clinic.api.ts`/`clinic.queries.ts` mở rộng thêm 4 hàm/hook. `ClinicConfigPage.tsx` thêm mục "Thông tin phòng khám" (icon `Buildings`) TRƯỚC "Giờ làm việc & Slot" trong cùng pill — tự động thành tab mặc định.
- **2 bug phát hiện lúc chủ dự án dùng thử qua Playwright, đã sửa trong cùng phiên**:
  1. **Ảnh vỡ (broken image icon)** ở cả logo phòng khám lẫn ảnh đại diện bệnh nhân — `photoUrl`/`logoUrl` server trả về là đường dẫn TƯƠNG ĐỐI (`/api/v1/files/<token>`), `<img src>` bị trình duyệt phân giải theo origin TRANG WEB (5173) thay vì API (3000); Vite dev server tự trả `index.html` (SPA fallback) cho path lạ thay vì 404 nên lỗi âm thầm. Đây là **bug có sẵn từ #034**, chỉ lộ rõ khi làm logo. Sửa bằng `resolveApiUrl()` dùng chung (`apps/web/src/shared/api/client.ts`, ghép `apiBaseUrl` vào path tương đối) — áp dụng cho cả `PatientAvatarUpload.tsx` (sửa bug cũ) và `ClinicInfoPane.tsx`.
  2. **Upload ảnh hoạt động độc lập với chế độ Sửa** (bấm "Chọn ảnh" lưu ngay, không cần vào "Sửa" trước) — thiết kế copy nguyên từ `PatientAvatarUpload.tsx` (#034), chủ dự án phản hồi gây khó hiểu ở CẢ 2 nơi. Đã hỏi và chốt: ẩn hẳn nút "Chọn ảnh" tới khi bấm "Sửa" — sửa `PatientAvatarUpload.tsx` (thêm prop `disabled`, truyền từ `PatientFormFields`/`PatientDetailPage`) và `LogoUploadBox` trong `ClinicInfoPane.tsx`. Kèm thêm guard bỏ qua đồng bộ `useEffect` khi đang `editing` trong `ClinicInfoPane` (upload logo đổi `version` giữa lúc sửa trường văn bản trước đó sẽ âm thầm ghi đè mất phần đang gõ dở). Ghi thành quy tắc bắt buộc ở `.claude/docs/ui-guidelines.md` mục 9b cho mọi upload ảnh sau này.
- **Đã xác minh thật**: `clinic-profile-http.spec.ts` (18/18 pass, tổng 220 test `apps/api`) — GET mặc định đúng `currency=VND`/`timezone=Asia/Ho_Chi_Minh`, PATCH đủ trường + optimistic lock + permission, upload logo/print-logo độc lập nhau (magic byte, size limit, version conflict, xoá file cũ), cách ly tenant. `pnpm -w typecheck/lint/build` sạch toàn workspace (trừ 1 lỗi ESLint có sẵn từ trước, không liên quan). Playwright qua trình duyệt thật (dữ liệu dev thật, không mock): tab "Thông tin phòng khám" mặc định mở đầu tiên; sửa đủ field + Combobox tiền tệ/múi giờ → Lưu → đúng, F5 giữ nguyên; upload 2 logo → hiển thị đúng ảnh thật (xác minh bằng đọc `naturalWidth`/byte thật từ response, không chỉ nhìn ảnh chụp); nút "Chọn ảnh" đúng ẩn/hiện theo chế độ Sửa ở cả 2 màn hình (đếm số nút trước/sau: 0→2→0 ở Thông tin phòng khám, 0→1 ở Chi tiết bệnh nhân).
- Cập nhật `.claude/docs/data-model.md`, `.claude/docs/ui-guidelines.md` (mục 9b), `docs/ERD.md` (v1.9), `docs/DECISIONS.md` (#041), `docs/CURRENT.md`, `docs/TASK.md`.

## 2026-08-13 (26)

Backfill `role_permission` tự động cho tenant cũ lúc API khởi động + tối ưu khoảng trống lưới lịch hẹn (giải quyết 2 mục "Đang chờ" trong `docs/CURRENT.md`):

- **Bối cảnh**: 2 việc treo lại từ trước — (1) tenant cũ không tự có `role_permission` khi thêm permission mới (phát hiện lúc làm `reference_catalog` #037, vá thủ công một lần); (2) lưới lịch hẹn phải cuộn nhiều để thấy hết 1 ngày làm việc dài (ghi nhận lúc làm #039/#040). Đã hỏi chủ dự án chốt hướng cho cả hai trước khi code: cơ chế backfill tự động lúc khởi động (thay vì script chạy tay / endpoint quản trị); giữ nguyên mật độ lưới (`ROW_HEIGHT_PX`/bước 30 phút), chỉ tối ưu khoảng trống khác quanh lưới.
- **`apps/api/src/infrastructure/persistence/sync-role-permissions.ts` (mới)**: `syncRolePermissionsForTenant`/`syncRolePermissionsForAllTenants` — so từng tenant với `DEFAULT_ROLE_PERMISSIONS` (`packages/core`), chỉ THÊM dòng `role_permission` còn thiếu cho vai trò `is_system_default=true`, không sửa/xoá dòng đã có (an toàn với tuỳ biến ADM-07 sau này) và không đụng vai trò tuỳ biến do `clinic_admin` tự tạo. Gọi 1 lần trong `main.ts` (`bootstrap()`, sau khi tạo `app`, trước `app.listen()`) — không chạy trong test vì test không qua `main.ts`. Không migration/schema nào đổi (`role_permission` đã ghi được bởi `nexamed_app` từ trước, chỉ bảng `permission` mới bị revoke ghi). Permission mới vẫn phải seed vào bảng `permission` qua `pnpm db:seed` trước (`nexamed_app` không có quyền ghi bảng đó) — thiếu thì dòng đó bị bỏ qua thay vì crash cả API.
- **`AppointmentSchedulePage.tsx`/`AppointmentGridView.tsx`**: gộp 2 hàng công cụ cũ (nút "Đặt lịch" tách riêng + hàng điều hướng ngày/legend/toggle) thành 1 hàng duy nhất, `p-4`→`p-3`/`gap-3`→`gap-2.5`, giảm padding dọc header cột bác sĩ (`py-2.5`→`py-2`) — nhường thêm vùng nhìn thấy cho thân lưới trước khi phải cuộn, không đụng `ROW_HEIGHT_PX`/`GRID_STEP_MINUTES`/dữ liệu giờ làm việc.
- **Đã xác minh thật**: `sync-role-permissions.spec.ts` (4/4, Postgres thật qua testcontainers) — tenant seed đủ thì không thêm gì (idempotent); mô phỏng tenant cũ thiếu 1 permission → vá đúng lại theo `data_scope` trong ma trận, gọi lại lần 2 không tạo trùng; không đụng vai trò tuỳ biến; `ForAllTenants` chỉ vá đúng tenant thiếu, cách ly tenant khác. Kiểm thêm trên DB dev thật (ngoài test container): xoá thủ công dòng `clinic_admin/audit_log.read` của tenant dev → build + chạy `node dist/main.js` → thấy log `[startup] Đồng bộ role_permission: thêm 1 dòng còn thiếu (...)` → xác nhận DB đã phục hồi đúng `dataScope=global`. Tổng 202 test `apps/api` pass (198 cũ + 4 mới), không regress. `pnpm -w typecheck/lint/build` sạch toàn workspace (trừ 1 lỗi ESLint có sẵn từ trước, không liên quan). Playwright qua trình duyệt thật (dữ liệu dev thật, không mock): hàng công cụ gộp hiển thị đúng, không vỡ layout; nút "Đặt lịch" mở đúng panel; chuyển chế độ Danh sách đúng dữ liệu; thu hẹp xuống 1366×768 (ngưỡng tối thiểu theo `CLAUDE.md`) vẫn không tràn/vỡ hàng công cụ; không console error ngoài dự kiến.
- Cập nhật `docs/CURRENT.md`, `docs/TASK.md`.

## 2026-08-13 (25)

Trang "Danh mục" + "Cấu hình hệ thống" — mẫu Two-panel Config Screen (Kiểu 2), `ConfigScreenShell` dùng chung, giờ làm việc phòng khám có UI web lần đầu (docs/DECISIONS.md #039, #040):

- **Bối cảnh**: chủ dự án gửi 2 ảnh tham khảo phần mềm phòng khám khác cho phong cách trang Cấu hình (danh sách bên trái + nội dung bên phải, có nơi chia thêm khung "Nhóm"). Dựng mockup Artifact 2 kiểu bố cục trước khi code — chốt **Kiểu 2 (thanh nhóm + thẻ)**, đồng thời chốt rõ mẫu này KHÔNG mặc định cho mọi trang Quản trị tương lai, phân bố menu do chủ dự án quyết định khi tạo từng menu mới.
- **Route đổi**: `/admin/reference-catalog` → `/admin/catalog` (nhãn sidebar "Danh mục"), gộp `reference_catalog` (Dân tộc/Quốc tịch, quản lý được) và `geo` (Tỉnh/Thành, Phường/Xã — vẫn chỉ đọc đúng #038) chung 1 pill "Danh mục dùng chung" (4 mục). Xoá hẳn `ReferenceCatalogAdminPage.tsx` cũ, tách phần bảng/modal CRUD ra `ReferenceCatalogPane.tsx` (nhận `category` qua prop thay vì tự quản lý tab).
- **`apps/web/src/features/geo/GeoPane.tsx` (mới)**: 2 chế độ chỉ đọc — Tỉnh/Thành (bảng phẳng) và Phường/Xã (Combobox chọn tỉnh trước, mặc định Thành phố Hồ Chí Minh mã `29` để có dữ liệu ngay thay vì trang trắng chờ chọn).
- **`apps/web/src/features/clinic/` (mới)**: `clinic.api.ts`/`.queries.ts` + `ClinicHoursPane.tsx` — form giờ làm việc 7 ngày + độ dài slot, dùng `GET/PATCH /clinic-settings` đã có backend từ S2-07 nhưng chưa từng có UI web. `useUpdateClinicSettingsMutation` invalidate luôn cache `appointment/schedule-config` để lưới lịch hẹn (S2-09) phản ánh ngay không cần F5. Chỉ `clinic_admin` (có `clinic_config.update`) thấy nút Sửa.
- **Nhiều vòng chỉnh trực tiếp trên bản chạy thật** (chủ dự án xem qua `pnpm dev`, không dựng lại mockup):
  - Bỏ pill "Danh mục hành chính" riêng, gộp hẳn Tỉnh/Thành + Phường/Xã vào pill "Danh mục dùng chung".
  - Form "Giờ làm việc" đổi từ giới hạn `max-w-2xl`/`max-w-3xl` (để trống nhiều bên phải cột nội dung, phản hồi "dãn ra hết bên phải và gần sát bên trái") sang full-width theo Boxed Section Form Pattern (`.claude/docs/ui-guidelines.md` mục 9b) — 2 khung viền + badge riêng ("Giờ làm việc theo tuần", "Độ dài Slot") thay vì 1 khối gộp chung.
  - Đổi tên pill "Phòng khám" → "Cấu hình phòng khám", tách hẳn khỏi trang "Danh mục" sang mục sidebar mới **"Cấu hình hệ thống"** (`/admin/system-config`, `ClinicConfigPage.tsx`) trong nhóm "Quản trị" (nay có 2 mục con).
  - **Trích `apps/web/src/shared/ui/ConfigScreenShell.tsx`** sau khi chủ dự án phát hiện trang "Cấu hình hệ thống" mới không đồng bộ bố cục với "Danh mục" (lúc đó nghĩ "chỉ 1 pill/1 mục thì không cần pill bar/cột trái" — sai, đã bị bác bỏ trước đó khi thử ẩn cột trái cho "Giờ làm việc & Slot" và chủ dự án yêu cầu giữ nguyên). Khung dùng chung (pill bar + danh sách trái + `children`) — lần dùng thứ hai của cùng 1 khuôn UI nên tách theo `CLAUDE.md`, cả `CatalogAdminPage` và `ClinicConfigPage` cùng dùng, đảm bảo LUÔN đồng nhất dù số lượng pill/mục khác nhau.
- **Điều tra "lưới lịch hẹn không áp đúng giờ đã cấu hình" theo báo cáo chủ dự án** (3 lần liên tiếp): xác minh bằng Playwright + curl trực tiếp trên dữ liệu thật — sửa giờ 1 ngày cụ thể (Thứ Ba 09:00–16:00) qua UI, `PATCH /clinic-settings` trả đúng, điều hướng sang Lịch hẹn đúng ngày Thứ Ba → lưới hiện đúng khung 09:00–15:30 (khớp `resolveDayHours()` đã viết đúng từ S2-09). **Kết luận: không có bug thật** trong `ClinicHoursPane`/`AppointmentGridView` — nguyên nhân ban đầu là dữ liệu `tenant_setting.business_hours` bị lệch tạm thời do chính kịch bản Playwright của tôi trong phiên này định vị nhầm ô giờ (`input[type="time"] >> nth=N` và `locator('div',{hasText}).first()` đều khớp nhầm phần tử cha khi layout đổi sang lưới 2 cột) — đã khôi phục đúng giá trị gốc qua API. Riêng việc lưới cần cuộn khá nhiều để thấy hết 1 ngày dài (`ROW_HEIGHT_PX=92`/30 phút một hàng, quyết định từ S2-09) là hành vi có sẵn, chưa phải bug — ghi vào "Đang chờ" để hỏi chủ dự án có muốn đổi mật độ hiển thị không.
- **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch toàn workspace (trừ 1 lỗi ESLint có sẵn từ trước, không liên quan, xác nhận qua trạng thái trước khi sửa). Không migration/schema/API contract nào đổi — thuần tổ chức lại UI web trên các endpoint đã có sẵn (`reference-catalog`, `geo`, `clinic-settings`). Playwright qua trình duyệt thật + dữ liệu thật: sidebar đúng 2 mục con "Danh mục"/"Cấu hình hệ thống"; "Danh mục" hiện đúng 4 mục (Dân tộc/Phường-Xã/Tỉnh-Thành/Quốc tịch) dùng chung 1 pill; "Cấu hình hệ thống" bố cục đồng nhất; sửa giờ làm việc lưu đúng + phản ánh đúng ngay trên lưới lịch hẹn khi điều hướng qua; Phường/Xã mặc định TP.HCM có dữ liệu ngay.
- Cập nhật `.claude/docs/ui-guidelines.md` (mục 10, 7 điểm), `docs/DECISIONS.md` (#039, #040), `docs/CURRENT.md`, `docs/TASK.md`.

## 2026-08-12 (24)

Danh mục hành chính `province`/`ward` (Tỉnh/Phường-Xã, sáp nhập hành chính 2025) — 2 bảng read-only + tích hợp Combobox cascading vào địa chỉ bệnh nhân (docs/DECISIONS.md #038):

- **Bối cảnh**: chủ dự án gửi file dữ liệu hành chính chính thức (`docs/data/Danh-muc-Phuong-xa_moi.md`, Bộ Nội vụ — 34 tỉnh/thành, 3321 phường/xã), hỏi nên tách 2 bảng hay gộp 1 bảng phẳng. Đã khuyến nghị 2 bảng chuẩn hoá (tránh lặp tên tỉnh hàng nghìn lần, đúng quan hệ cha-con) — chủ dự án đồng ý, xác nhận thêm: bảng read-only (không cần quản lý qua UI như Dân tộc/Quốc tịch), và làm luôn tích hợp vào `patient.address_json` (đảo ngược tiếp phần Tỉnh/Xã của #034).
- **Schema**: migration `20260812210000_geo_province_ward` (viết tay) — bảng `province` (`code` PK "1".."34"), `ward` (`code` PK 8 chữ số, **duy nhất toàn quốc** — đã xác nhận 0 trùng trên 3321 dòng, nên PK đơn không cần composite), FK `ward.province_code → province.code`, index `(province_code)`. Cùng bản chất `icd10_catalog` (không `tenant_id`, không `version`/`is_active`), **khác `reference_catalog`**: REVOKE `INSERT`/`UPDATE` khỏi `nexamed_app` — read-only thật, chỉ seed script ghi được.
- **Sửa 1 lỗ hổng có sẵn của `scripts/check-mandatory-columns.mjs`** phát hiện khi thêm exemption cho bảng mới: `ReferenceCatalog` (từ #037) thiếu dòng exemption trong `EXEMPTIONS` (chưa từng chạy lại script này từ lúc thêm bảng đó) và bản thân script có bug — `missing.unshift('id')` không kiểm tra model có được miễn trừ cột `id` hay không trước khi thêm, chặn cả trường hợp cố ý miễn trừ (PK là `code`, không phải `id`, như `province`/`ward`). Đã sửa cả hai.
- **Permission**: dùng lại `patient.read`, không thêm permission mới — tránh lặp lại vấn đề "chưa có cơ chế backfill `role_permission` cho tenant cũ" đã ghi ở `docs/CURRENT.md` (phát hiện lúc làm #037), cùng cách `appointment.read` được dùng lại cho `GET /appointments/doctors|schedule-config` (S2-09).
- **Seed**: `packages/core/src/geo/data.ts` (34 + 3321 dòng, sinh từ file gốc bằng script chạy một lần, không giữ script sinh trong repo — cùng cách `reference-catalog/data.ts`). `apps/api/src/infrastructure/persistence/seed-geo.ts` dùng `createMany({ skipDuplicates: true })` thay vì upsert từng dòng (nhanh hơn nhiều cho ~3355 dòng, phù hợp vì bảng không có đường ghi nào khác ngoài seed).
- **Backend**: `apps/api/src/modules/geo/` (module mới, read-only) — `GET /geo/provinces`, `GET /geo/wards?provinceCode=` (cascading trong form) và `GET /geo/wards` không kèm `provinceCode` (toàn bộ 3321 dòng — dùng dựng bảng tra code→tên ở màn hình danh sách, xem điểm dưới).
- **Frontend**: `apps/web/src/features/geo/` (`geo.api.ts`/`.queries.ts`: `useProvincesQuery`, `useWardsQuery(provinceCode)` cascading, `useAllWardsQuery()` không lọc). `PatientFormFields.tsx`: 2 input text (Tỉnh/Phường-Xã) → Combobox cascading — đổi Tỉnh tự xoá Phường/Xã đã chọn (tránh lưu cặp không khớp). `patient.address_json.province`/`.ward` nay lưu **mã**, không lưu tên — cùng cách `ethnicity`/`nationality` ở #037, giữ nguyên giá trị cũ qua `withLegacyValueOption()` khi mở hồ sơ trước #038.
- **Tách `withLegacyValueOption()` từ `PatientFormFields.tsx` ra `shared/ui/Combobox.tsx`** — trùng lặp lần thứ hai đúng khuôn "hồ sơ cũ giá trị không khớp danh mục hiện tại" (lần đầu ethnicity/nationality #037), tách theo `CLAUDE.md`. Đổi chữ ký nhận `ComboboxOption[]` (tổng quát hơn `ReferenceCatalogItem[]`) — refactor luôn 2 nơi gọi cũ dùng chung, hành vi giữ nguyên.
- **1 vấn đề UX thật phát hiện lúc code (không phải bug, đặc điểm thiết kế cần xử lý)**: `patientSummarySchema.address` (cột Địa chỉ ở Danh sách bệnh nhân) vốn hiển thị nguyên giá trị `province`/`ward` — đổi sang lưu mã sẽ hiện mã thô ("1", "10105001") thay vì tên nếu không xử lý. Giải quyết ở client (tải danh mục 1 lần qua `useProvincesQuery`/`useAllWardsQuery`, cache `staleTime: Infinity`, `useMemo` dựng `Record<code,name>` tra O(1) mỗi hàng) thay vì denormalize tên vào response — giữ `patientSummarySchema` không đổi, không cần sửa backend patient module.
- **Sửa dữ liệu theo yêu cầu chủ dự án lúc rà lại**: 4 tên tỉnh dùng viết tắt "Tp" trong file gốc (Hải Phòng, Đà Nẵng, Hồ Chí Minh, Cần Thơ) đổi thành "Thành phố" đầy đủ. `seed-geo.ts` phần `province` đổi từ `createMany({skipDuplicates})` sang upsert từng dòng (34 dòng, rẻ) để tên sửa lại đồng bộ được khi seed lại, không kẹt ở bản đã insert trước đó.
- **Đã xác minh thật**: `packages/core/src/geo/data.spec.ts` (4/4) — đúng 34 tỉnh + 3321 phường/xã, không trùng `code`, mọi `ward.provinceCode` hợp lệ, Hà Nội đúng 126 phường/xã. `apps/api/src/modules/geo/geo-http.spec.ts` (7/7) — danh sách đúng thứ tự, lọc theo tỉnh đúng, không lọc trả toàn bộ, tỉnh không tồn tại → rỗng không lỗi, `system_admin` (không có `patient.read`) → 403, không cách ly tenant (có chủ đích). Tổng 198 test `apps/api` (191 cũ + 7 mới) + 46 test `packages/core` (42 cũ + 4 mới) pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace (trừ 1 lỗi ESLint có sẵn từ trước phiên này, không liên quan — xác nhận bằng `git stash`). Playwright qua trình duyệt thật (dữ liệu thật): Combobox Tỉnh đúng 34 mục ("Thành phố Hà Nội" không còn viết tắt), chọn Tỉnh → Combobox Phường/Xã bật đúng số mục lọc theo tỉnh (Hà Nội: 126), đổi lại Tỉnh khác → Phường/Xã đã chọn tự xoá; tạo hồ sơ đủ địa chỉ → lưu → Danh sách bệnh nhân hiện đúng tên đã resolve từ mã ("Phường Bến Thành, Thành phố Hồ Chí Minh", không phải mã thô). Không console error ngoài dự kiến.
- Cập nhật `.claude/docs/data-model.md`, `docs/ERD.md` (v1.8), `docs/DECISIONS.md` (#038), `docs/CURRENT.md`, `docs/TASK.md` trong cùng lúc.

## 2026-08-12 (23)

Bỏ tiêu đề trang (`<h1>` hiển thị) khỏi mọi trang — breadcrumb là định danh trang duy nhất:

- **Bối cảnh**: chủ dự án nhận thấy tiêu đề to (ví dụ "Lịch hẹn") lặp lại đúng chữ đã có ở đoạn cuối breadcrumb ngay phía trên — thừa thông tin, tốn khoảng trống dọc. Yêu cầu bỏ tiêu đề lẫn dòng mô tả/chú thích phụ đi kèm ở mọi trang, dùng `/ui-ux-pro-max` để cân chỉnh lại layout sau khi bỏ.
- **`DashboardPage.tsx`, `PatientListPage.tsx`, `PatientNewPage.tsx`, `AppointmentSchedulePage.tsx`, `ReferenceCatalogAdminPage.tsx`**: `<h1>` hiển thị đổi thành `<h1 className="sr-only">` (ẩn thị giác, giữ đúng cấu trúc heading cho screen reader/document outline — không bỏ hẳn thẻ `h1`). Bỏ luôn dòng mô tả/chú thích phụ từng đặt dưới tiêu đề (`PatientListPage`: "Nhấp đúp vào mã bệnh nhân để mở hồ sơ"; `ReferenceCatalogAdminPage`: mô tả "Master data toàn hệ thống..."). `AppointmentSchedulePage`: hàng chứa tiêu đề+nút "Đặt lịch" đổi `justify-between` → `justify-end` (chỉ còn nút, đẩy sang phải, khớp quy ước nút hành động chính góc phải đã dùng ở màn Danh mục dùng chung).
- **Ngoại lệ có chủ đích**: `PatientDetailPage.tsx` (tên bệnh nhân) và `LoginPage.tsx` (không có breadcrumb, màn hình trước đăng nhập) **giữ nguyên** — tên bệnh nhân là dữ liệu định danh bản ghi phục vụ an toàn người bệnh (xác nhận đúng hồ sơ đang xem), không phải tên trang điều hướng.
- **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright chụp ảnh 4 trang (Danh sách bệnh nhân, Lịch hẹn, Danh mục dùng chung, Tổng quan) xác nhận không còn tiêu đề/chú thích thừa, layout cân đối, breadcrumb vẫn đúng; `h1.sr-only` vẫn tồn tại trong DOM (xác nhận qua `document.querySelector`). Không console error ngoài dự kiến.
- Cập nhật `.claude/docs/ui-guidelines.md` mục 8.2 — ghi thành quy tắc bắt buộc cho mọi trang mới sau này.

## 2026-08-12 (22)

Áp dụng `Combobox` (thay `<select>` thuần) cho toàn bộ dropdown còn lại — chốt thành tiêu chuẩn thiết kế bắt buộc:

- **Bối cảnh**: chủ dự án yêu cầu áp dụng `Combobox` mới (đã dựng ở #21 cho Dân tộc/Quốc tịch) cho mọi dropdown khác trong app, và ghi thành chuẩn bắt buộc cho mọi combobox sau này — không riêng danh mục dài, kể cả danh sách ngắn 3-4 mục (đồng nhất thị giác quan trọng hơn việc "danh sách ngắn không cần tìm kiếm").
- Rà toàn bộ `apps/web/src` — còn 4 `<select>` HTML thuần, đổi hết sang `Combobox`: Giới tính (`PatientFormFields.tsx`, 3 lựa chọn tĩnh), chọn Bác sĩ + Thời lượng khi sửa lịch hẹn (`AppointmentDetailPanel.tsx`), Thời lượng khi đặt lịch nhanh (`AppointmentQuickCreatePanel.tsx`). Giá trị số (thời lượng phút) chuyển đổi qua `String()`/`Number()` ở nơi gọi — `Combobox` chỉ nhận `value: string`.
- **Đã xác minh thật**: `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright qua trình duyệt thật (dữ liệu thật): panel Đặt lịch nhanh — combobox Thời lượng đúng 4 lựa chọn, chọn "30 phút" cập nhật đúng. Không console error ngoài dự kiến.
- Cập nhật `.claude/docs/ui-guidelines.md` mục 4.1b (mới) — ghi `Combobox` thành chuẩn BẮT BUỘC cho mọi dropdown chọn 1 giá trị, cấm dùng `<select>` thuần trong code mới.

## 2026-08-12 (21)

Combobox tìm kiếm được cho Dân tộc/Quốc tịch (thay `<select>` thuần) + mặc định Dân tộc = Kinh:

- **Bối cảnh**: chủ dự án dùng thử `<select>` thuần vừa đổi ở #037 — mũi tên xổ xuống mặc định của trình duyệt xấu, danh sách 54 dân tộc đôi khi xổ LÊN (trình duyệt tự chọn hướng theo khoảng trống còn lại), không gõ tìm được, không giới hạn số dòng hiển thị.
- **`apps/web/src/shared/ui/Combobox.tsx` (mới)** — dropdown tìm kiếm được dùng chung, đặt ở `shared/ui` với chủ đích tái dùng ngay (cần cùng lúc ở cả Dân tộc lẫn Quốc tịch, đúng ngưỡng "cần dùng chung ngay từ đầu" của `CLAUDE.md`): input gõ để lọc (không phân biệt hoa/thường trên `label`/`value`), nút mũi tên `CaretDown` riêng (xoay + đổi màu xanh khi mở — thay mũi tên mặc định của trình duyệt), panel luôn mở CỐ ĐỊNH bên dưới (`top-full`, không phụ thuộc khoảng trống màn hình), cao đúng 5 dòng (`188px`) rồi cuộn, điều hướng bàn phím (mũi tên lên/xuống, Enter chọn, Escape/Tab đóng), click ra ngoài tự đóng.
- `PatientFormFields.tsx`: 2 field Dân tộc/Quốc tịch đổi từ `<select>` sang `<Combobox>`. `EMPTY_PATIENT_FORM.ethnicity` đổi mặc định từ `''` sang `'1'` (Kinh) — khớp yêu cầu "Dân tộc mặc định là Kinh"; Quốc tịch giữ nguyên mặc định `'VNM'` (Việt Nam) đã đúng từ #037.
- **Đã xác minh thật qua trình duyệt** (Playwright, dữ liệu thật): mặc định đúng Kinh/Việt Nam; mở combobox → panel luôn xổ xuống, cao 188px, đủ 54 option trong DOM (không phân trang, chỉ giới hạn hiển thị qua `overflow-y-auto`); gõ "Ê Đê" → lọc còn đúng 1 kết quả; Enter chọn đúng; click ra ngoài đóng và giữ đúng giá trị đã chọn. `pnpm -w typecheck/build` sạch toàn workspace, không console error ngoài dự kiến.

## 2026-08-12 (20)

Danh mục dùng chung `reference_catalog` (Dân tộc, Quốc tịch) — quản lý được qua UI bởi `clinic_admin`, đảo ngược `docs/DECISIONS.md` #034 phần `ethnicity`/`nationality` (docs/DECISIONS.md #037):

- **Bối cảnh**: việc từng treo "chờ chỉ thị" ở `docs/CURRENT.md` — chủ dự án xác nhận tiếp tục, tự cung cấp 2 file dữ liệu thật (`Danh_sach_54_dan_toc_Viet_Nam.md`, `Master_Data_Quoc_Tich_ISO3166.md`) thay vì để AI tự soạn (giải quyết đúng gốc rễ lý do #034 từng chặn — "danh mục cần nguồn dữ liệu thật"), và muốn xây thành **master data quản lý được** (không chỉ xem như phác thảo ban đầu). Đã dựng mockup Artifact (App Shell + sidebar + tab + bảng CRUD + modal) trước khi code, hỏi và chốt IA menu Quản trị qua mockup.
- **Schema**: bảng `reference_catalog` mới (toàn hệ thống, không `tenant_id`, không đủ 8 cột bắt buộc — ngoại lệ như `permission`/`icd10_catalog` — và **không có `version`**, khác `room`/`user_account`). Cột `category` (enum `ETHNICITY`/`NATIONALITY`), `code` (mã chính thức: dân tộc "1".."54", quốc tịch ISO 3166-1 alpha-3), `name`, `sort_order`, `is_active`. Migration `20260812200000_reference_catalog` viết tay — **khác `permission`**: không REVOKE `INSERT`/`UPDATE` (bảng này sửa được qua chính API bởi `clinic_admin`, không chỉ seed script đặc quyền).
- **Phát hiện lúc rà migration nền tảng**: role DB `nexamed_app` bị `REVOKE DELETE` toàn cục (migration `*_tenant_context`, áp cho mọi bảng kể cả bảng tạo sau) — API không thể xoá cứng dù muốn. "Xoá" trong UI quản lý là `is_active=false` (soft), có nút "Khôi phục".
- **`patient.ethnicity`/`patient.nationality` lưu `code`** (ví dụ `"VNM"`, `"1"`), không lưu `name` — đã hỏi và chốt (chuẩn hơn cho báo cáo/BHYT sau này). Cột DB không đổi kiểu, Zod không ép khớp danh mục — hồ sơ cũ có giá trị dạng tên tự do (`"Việt Nam"`) hoặc mục đã bị ẩn sau khi hồ sơ đã lưu vẫn hiển thị đúng qua helper `withLegacyValueOption()` mới (`PatientFormFields.tsx`), không mất dữ liệu.
- **Quyền**: permission mới `reference_catalog.read` (`global`, mọi vai trò lâm sàng) / `reference_catalog.manage` (`global`, chỉ `clinic_admin`) — thêm đúng chuẩn `PermissionGuard`/`@RequirePermission`, không lệch pattern như dự tính ban đầu (đã xác nhận `seedDefaultRolesForTenant()` chỉ chạy lúc tạo tenant và dự án chưa có tenant production nào, nên rủi ro backfill chưa xảy ra ở giai đoạn này). **Giới hạn đã biết**: bảng không cách ly theo tenant — `clinic_admin` một tenant sửa/ẩn ảnh hưởng mọi tenant khác (chấp nhận ở v1 on-premise một tenant/instance).
- **Backend**: `apps/api/src/modules/reference-catalog/` (CRUD đầy đủ: `GET /:category?includeInactive=`, `POST`, `PATCH /:id`, `DELETE /:id` soft, `POST /:id/reactivate`). `packages/core/src/reference-catalog/data.ts` (seed THẬT, 54 dân tộc + 30 quốc tịch, nguyên văn từ file gốc — không placeholder). `packages/core/src/errors/reference-catalog-errors.ts` (`ReferenceCatalogDuplicateCodeError`, 409).
- **Web**: `apps/web/src/features/reference-catalog/` (`reference-catalog.api.ts`/`.queries.ts`, `ReferenceCatalogAdminPage.tsx` — tab Dân tộc/Quốc tịch, tìm kiếm, bảng CRUD, modal thêm/sửa, dialog xác nhận ẩn, công tắc "Hiện cả mục đã ẩn"). `Sidebar.tsx`: "Quản trị" đổi từ mục phẳng thành nhóm cha/con (giống "Tiếp nhận và Đặt lịch"), mục con "Danh mục dùng chung" → `/admin/reference-catalog`. `AdminPage.tsx` (thuần `EmptyState`, không còn mục sidebar nào trỏ tới) và route `/admin` cũ **đã xoá**.
- **1 vấn đề hạ tầng RBAC thật phát hiện lúc kiểm bằng trình duyệt** (không phải bug code — đặc điểm cơ chế đã biết từ trước): tenant dev seed từ trước không tự có `role_permission` cho 2 permission mới (`seedDefaultRolesForTenant()` chỉ chạy lúc tạo tenant, không có cơ chế backfill cho tenant đã tồn tại) — vá thủ công một lần cho tenant dev, ghi lại thành việc cần làm ở `docs/CURRENT.md` mục "Đang chờ" (sẽ là vấn đề thật ở pilot nếu cần thêm permission sau khi đã cài đặt).
- **Đã xác minh thật**: `reference-catalog-http.spec.ts` (10/10 pass) — đúng 54/30 mục theo `sortOrder`, receptionist đọc được nhưng bị chặn ghi (403), trùng `(category, code)` → 409, soft-delete/khôi phục đúng luồng, 2 tenant cùng thấy/cùng sửa được (xác nhận có chủ đích không cách ly, giống `permission`). `packages/core/src/reference-catalog/data.spec.ts` (2/2). Tổng 191 test `apps/api` (181 cũ + 10 mới) + 42 test `packages/core` (40 cũ + 2 mới) pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật (dữ liệu thật, không mock): sidebar → trang Quản lý danh mục đúng 2 tab đúng dữ liệu → thêm/sửa/xoá/khôi phục qua UI phản ánh đúng ngay → form Thêm bệnh nhân có dropdown đúng, mặc định Quốc tịch = Việt Nam → hồ sơ "cũ" (tạo qua API mô phỏng dữ liệu trước #037) mở lên vẫn giữ đúng giá trị cũ, không bị xoá trắng. Không console error ngoài dự kiến.
- Cập nhật `docs/DECISIONS.md` (#037), `.claude/docs/data-model.md`, `.claude/docs/security-audit.md`, `.claude/docs/architecture.md`, `docs/ERD.md` (v1.7), `docs/CURRENT.md`, `docs/TASK.md` trong cùng lúc.

## 2026-08-12 (19)

Danh sách bệnh nhân — thêm cột CCCD (che 4 số cuối) + Địa chỉ, đổi thứ tự cột + khoảng cách hợp lý (docs/DECISIONS.md #035); ràng buộc CCCD bắt buộc từ 18 tuổi lúc tạo mới (docs/DECISIONS.md #036):

- **Cột danh sách**: đổi thứ tự thành Mã bệnh nhân, Họ tên, CCCD, Giới tính, Năm sinh, Điện thoại, Địa chỉ. Sửa lỗi khoảng cách quá rộng — bản trước cột "Họ tên" dùng `1fr` một mình nuốt hết chiều rộng còn lại sau khi trang đổi full-width (#034); nay 2 cột dài (Họ tên + Địa chỉ) cùng chia phần dư (`minmax(0,1.3fr)`/`minmax(0,1.6fr)`) cộng `gap-x-4` giữa các cột.
- **CCCD trong danh sách**: đã hỏi trước vì đụng quyết định bảo mật cố ý từ S2-01 (`patientSummarySchema` chỉ trả `hasNationalId`, không giải mã hàng loạt). Chốt: che bớt, chỉ hiện 4 số cuối (`nationalIdMasked`). `PatientService.toSummary()` nay nhận `encryptionKey`, giải mã rồi cắt lấy 4 ký tự cuối, không trả nguyên giá trị. Thêm `address` vào `patientSummarySchema` cho cột Địa chỉ (không nhạy cảm, không thoả hiệp).
- **CCCD bắt buộc từ 18 tuổi (chỉ lúc tạo mới)**: `calculateAgeYears()` + `.superRefine()` mới trong `createPatientRequestSchema` — dưới 18 vẫn tuỳ chọn (khớp edge case trẻ em đã có). Cố ý **không** áp lại cho `updatePatientRequestSchema` (tránh chặn sửa hồ sơ người lớn cũ chưa có CCCD). Web (`PatientFormFields.tsx`) bật `required` động trên CCCD chỉ ở trang Thêm mới (`patientId === undefined`), khớp đúng phạm vi.
- **Gặp lại đúng lỗi Rollup đã ghi ở #032** (không phải bug mới): `calculateAgeYears` không import được từ `@nexamed/shared` vào `apps/web` qua `vite build` (barrel `export *` → `__exportStar` runtime, Rollup không dò tĩnh được) — lần này với hàm chứ không phải hằng số, xác nhận nguyên nhân gốc là kiểu re-export chứ không riêng gì "hằng số giá trị thuần". Xử lý bằng khai báo trùng lặp có chủ đích ở `patient-form.utils.ts`, ghi rõ comment tham chiếu ngược để không ai "dọn dẹp" trùng lặp rồi lặp lại lỗi build.
- **Bài học viết test**: dựng ngày sinh test bằng `new Date(y,m,d)` (giờ địa phương) rồi `.toISOString()` lệch 1 ngày so với cách server parse `"YYYY-MM-DD"` (coi là UTC) tuỳ múi giờ máy chạy — đủ lật kết quả test biên sát ranh giới tuổi. Sửa bằng `Date.UTC()` tường minh (`utcDateString()` helper mới trong `patient-http.spec.ts`).
- **Đã xác minh thật**: `patient-http.spec.ts` (38/38, tổng 181 test `apps/api` không regress — cộng 1 test mask CCCD từ #035 + 4 test ràng buộc tuổi từ #036, cùng sửa nhiều test cũ dùng `nationalId: undefined` tuỳ tiện để né trùng CCCD sang `randomNationalId()`). 40/40 test `packages/core`. `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật: cột đúng thứ tự + khoảng cách hợp lý, CCCD che đúng định dạng; dob 30 tuổi → CCCD bắt buộc (dấu `*` + browser chặn submit khi rỗng); dob ~5 tuổi → CCCD tuỳ chọn, tạo được.
- Cập nhật `.claude/docs/data-model.md`, `docs/DECISIONS.md` (#035, #036).

## 2026-08-12 (18)

Mở rộng form Thêm/Sửa hồ sơ bệnh nhân (PAT-01) — ảnh đại diện, CCCD ngày/nơi cấp, dân tộc/quốc tịch/nghề nghiệp, số bảo hiểm, Khu phố, Thông tin người thân, bố cục khung-viền+badge full-width (docs/DECISIONS.md #034):

- **Sprint 3 tạm hoãn theo yêu cầu chủ dự án** — S3-01 (danh mục ICD-10) chặn ngay từ đầu vì thiếu file danh mục chính thức, chủ dự án chọn quay lại hiệu chỉnh giao diện bệnh nhân trước. Dùng Plan mode: 2 agent khám phá song song (backend patient module + schema, frontend patient module + UI patterns) rồi hỏi 4 điểm chốt trước khi viết migration — ảnh làm thật ngay qua `StoragePort`; dân tộc/quốc tịch/nghề nghiệp nhập tự do không danh mục DB (né lặp vướng mắc ICD-10 vừa gặp); địa chỉ Tỉnh/Xã giữ text tự do (đúng ERD đã ghi "để dành v3+"); người thân 1 bộ trên `patient`, không tách bảng.
- **Schema**: migration `20260812150000_patient_profile_fields` (viết tay, 11 cột nullable — `photo_key`, `national_id_issued_at/place`, `ethnicity`, `nationality`, `occupation`, `insurance_number`, `relative_full_name/relationship/phone/address`); `address_json` thêm `neighborhood` (Khu phố), bỏ `district` khỏi UI (giữ schema tương thích ngược, khớp cải cách hành chính 2 cấp Tỉnh→Xã 2025).
- **Lần đầu hệ thống upload/serve file thật**: `apps/api/src/infrastructure/storage/signed-url.ts` (mới — HMAC tự chứa `{tenantId,key,exp}`, verify không cần tra DB, khoá dẫn xuất riêng từ `ENCRYPTION_KEY` khác `pii-encryption.ts`) + `PublicFileController` (`GET /api/v1/files/:token`, không `JwtAuthGuard` — chữ ký là lớp xác thực) + `StorageHttpModule`. Đúng yêu cầu "phục vụ qua signed URL có hạn" đã ghi từ S1-06 trong `.claude/docs/security-audit.md` nhưng chưa từng hiện thực. `POST /patients/:id/photo` (multipart, `FileInterceptor`, `@HttpCode(200)`) — chỉ JPG/PNG, kiểm **magic byte** qua `packages/core/src/storage/sniff-image-extension.ts` (không tin `Content-Type`), giới hạn 3MB (multer `MulterError` → 413). `PatientService.uploadPhoto()` xoá file cũ SAU khi lưu file mới thành công (tránh mất cả hai nếu lỗi giữa chừng). `InvalidPhotoError` (`PATIENT_INVALID_PHOTO`, 400) mới trong `packages/core/src/errors/patient-errors.ts`.
- **Frontend**: `apps/web/src/shared/api/client.ts` thêm `uploadFile()` (raw `fetch`, không qua `openapi-fetch` — xác nhận qua đọc source, thư viện không tự serialize `FormData` cho `multipart/form-data`). `features/patient/PatientAvatarUpload.tsx` (mới). `PatientFormFields.tsx` viết lại hoàn toàn.
  **Bố cục đổi qua 3 vòng chỉnh trực tiếp trên bản dựng thật** (chủ dự án phản hồi trên ảnh chụp, không đặc tả trước — đúng thói quen dự án): (1) mỗi nhóm trường bọc khung viền `rounded-lg border` + badge tiêu đề nổi trên viền (`bg-blue-600` chữ trắng) — ban đầu "Thông tin người thân" chỉ có divider mảnh + chữ xanh nhạt, phản hồi "nhạt" nên đổi sang cùng khuôn box+badge với "Thông tin hành chính"; (2) gap lưới thu hẹp 2 lần `gap-6` → `gap-4` → `gap-3`; (3) trang Thêm mới/Chi tiết bỏ hẳn `mx-auto max-w-4xl`, đổi sang full-width `p-6` theo đúng yêu cầu "giống bố cục Danh sách bệnh nhân" — **override** quy tắc "form giữ max-w" đã chốt ở `.claude/docs/ui-guidelines.md` mục 9, cập nhật lại mục 9 (ghi ngoại lệ) + thêm mục 9b (Boxed Section Form Pattern) để form khác sau này tái dùng đúng.
  Ảnh trên trang Thêm mới vô hiệu (chưa có `patientId`) kèm ghi chú "Lưu hồ sơ trước, sau đó có thể thêm ảnh"; trang Chi tiết cho phép đổi ảnh không cần vào chế độ "Sửa hồ sơ" trước.
- **Riêng, theo yêu cầu chủ dự án dùng `/ui-ux-pro-max`**: thanh tiêu đề bảng Danh sách bệnh nhân (`PatientListPage.tsx`) đổi từ `bg-slate-50 text-slate-500 font-medium` (quá nhạt) sang `bg-slate-100 text-slate-700 font-semibold` + viền dưới `border-b-2 border-blue-600` — tách biệt rõ khỏi thân bảng, dùng lại đúng token màu đã chốt (không phát minh sắc độ mới).
- **Đã xác minh thật**: `signed-url.spec.ts` (6/6), `sniff-image-extension.spec.ts` (5/5, `packages/core`), `patient-http.spec.ts` mở rộng (33/33, tổng 176 test `apps/api` không regress) — upload JPG/PNG hợp lệ, sai magic byte → 400, quá 3MB → 413, version không khớp → 409, ảnh cũ bị xoá thật (URL cũ 404 sau khi thay ảnh mới), thiếu quyền → 403. 40/40 test `packages/core`. `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua trình duyệt thật (Chromium, dữ liệu thật qua UI không mock): tạo hồ sơ đủ field mới → lưu đúng → vào chi tiết → upload ảnh → F5 vẫn thấy ảnh (xác nhận signed URL + DB đúng) → sửa hồ sơ đổi field mở rộng → lưu đúng; không console error ngoài dự kiến (chỉ 401 lần `/auth/refresh` đầu tiên, hành vi đã biết từ S1-08/S1-09).
- Cập nhật `.claude/docs/data-model.md`, `docs/ERD.md` (v1.6), `.claude/docs/ui-guidelines.md` (mục 9 + 9b mới), `docs/DECISIONS.md` (#034), `docs/TASK.md`, `docs/CURRENT.md` trong cùng lúc.

## 2026-08-12 (17)

Định hướng đa chuyên khoa (nhi/sản/nha) — chốt kiến trúc "specialty pack" + khung tối thiểu (docs/DECISIONS.md #033):

- **Phân tích khả thi theo yêu cầu chủ dự án** (thương mại hoá một source cho nhiều loại phòng khám, mỗi loại có form/quy trình/biểu mẫu bệnh án khác nhau + câu hỏi độ nặng source + ảnh hưởng khi chuyển cloud sau này): viết đầy đủ ở `docs/product/multi-specialty-analysis.md` (file mới). Kết luận chính: 3 tầng biến thiên chi phí chênh nhau rất xa (form nhập/biểu mẫu in rẻ, quy trình nghiệp vụ dài hạn như thai kỳ/lộ trình niềng răng đắt vì cần mô hình dữ liệu mới, không giải bằng form động); hướng đúng là "specialty pack" làm sẵn, **không** phải form builder cho khách tự dựng (rủi ro pháp lý — mẫu bệnh án do Bộ Y tế quy định); `encounter` không cần đổi state machine, chỉ thêm tầng cha dài hạn sau; chuyển cloud không đội chi phí theo số chuyên khoa (nằm ở tầng hạ tầng đã tách qua port), thứ đổi bản chất là cơ chế "gói" (on-prem = cấu hình, cloud = module `subscription` thật).
- **Đã hỏi và chốt phạm vi cụ thể** (chủ dự án chọn giữa 3 hướng: chỉ chuẩn bị khung tối thiểu / xây hẳn 1 gói thật ngay / đảo lộn roadmap sang multi-specialty) — chọn **khung tối thiểu**, giữ nguyên Sprint 3 theo kế hoạch, không lùi pilot tuần 8.
- **Thực hiện phần khung tối thiểu không phụ thuộc `encounter`** (bảng này chưa được code — Sprint 3 chưa bắt đầu, không đụng DB thật):
  - `.claude/docs/data-model.md`, `docs/ERD.md` (v1.5): thêm `encounter.specialty` (text, mặc định `'general'`) vào đặc tả thiết kế — sửa đặc tả trước khi Sprint 3 code, tránh retrofit sau.
  - `packages/core/src/specialty/registry.ts` (mới): `SpecialtyPack`/`createSpecialtyRegistry()` — registry hoàn toàn trơ (chỉ có gói `general` mặc định), factory không phải singleton toàn tiến trình (tránh rò state ngoài ý muốn). Chưa có specialty pack thật nào, chỉ chuẩn bị nơi tra cứu hợp lệ cho `encounter.specialty`.
- **Đã xác minh thật**: `registry.spec.ts` 5/5 pass, 35/35 test `packages/core` tổng không regress. `pnpm -w typecheck` sạch toàn workspace. Không migration/OpenAPI nào đổi (chưa có bảng/endpoint thật nào chạm tới).

## 2026-08-12 (16)

S2-10 — Test cách ly tenant cho toàn bộ endpoint mới + vá lỗi (đạt gate cuối cùng còn treo của Sprint 2):

- Rà soát `appointment-http.spec.ts` (S2-05→S2-09b), `user-account-http.spec.ts` (S2-07), `clinic-http.spec.ts` (S2-07) theo yêu cầu `.claude/docs/multi-tenancy.md` — "mỗi endpoint chạm dữ liệu... phải có test cách ly tenant: tenant A gọi ID/dữ liệu tenant B → 404, không phải 403". `clinic-http.spec.ts` đã đủ từ trước (room list/PATCH, clinic-settings độc lập theo tenant), không thêm gì.
- **Bổ sung 7 test còn thiếu — không tìm thấy bug thật**, chỉ thiếu xác nhận (đã đọc lại `AppointmentRepository`/`UserAccountRepository`: mọi query đều ghép `tenantId` của actor vào `WHERE`, `findById`/`updateIfVersionMatches` không có đường nào bỏ sót):
  - `appointment` (+4): `GET /appointments/doctors` — tenant B không thấy bác sĩ tenant A; `GET /appointments/schedule-config` — tenant B độc lập, không lẫn cấu hình tenant A; `GET /appointments/lookup?phone=` — tenant B tra đúng SĐT đã đặt ở tenant A vẫn không lộ tên/lịch sử (trả như chưa từng đặt) — điểm đáng chú ý nhất vì endpoint này tra theo SĐT chứ không theo ID cụ thể, rủi ro lộ PII cao hơn nếu quên lọc tenant; `GET /appointments` (danh sách, scope global) — tenant B không thấy lịch hẹn tenant A.
  - `user-account` (+3): `GET /users` (danh sách) — tenant B không thấy tài khoản tenant A; `PATCH /users/:id` — tenant B sửa tài khoản tenant A → 404 (trước đó chỉ có test cho `GET /users/:id`, chưa có cho `PATCH`); `POST /users/:id/reset-password` — tenant B đặt lại mật khẩu tài khoản tenant A → 404, xác nhận thêm mật khẩu cũ vẫn dùng được (không có tác dụng phụ nào lọt qua).
- **Đã xác minh thật**: 159/159 test `apps/api` pass (152 cũ không regress + 7 mới). `pnpm -w typecheck` sạch toàn workspace. Không có migration/schema/OpenAPI nào đổi — thuần bổ sung test theo endpoint đã có, không sửa code nghiệp vụ.
- Cập nhật `docs/TASK.md` (đánh dấu S2-10 xong, Sprint 2 hoàn tất toàn bộ), `docs/CURRENT.md`.

## 2026-08-12 (15)

Fix — Web Lịch hẹn: chế độ Danh sách không lọc theo ngày đã chọn; UX — badge tổng số lịch hẹn, phím tắt F2 cho "Đặt lịch":

- **Bug thật phát hiện qua chủ dự án dùng thử**: `AppointmentListView` (S2-09) gọi `useAppointmentsListQuery()` không truyền `date` — chế độ Danh sách luôn tải toàn bộ lịch hẹn qua cursor, đổi ngày ở thanh điều hướng chung (dùng cho cả 2 chế độ) không có tác dụng gì với Danh sách, trong khi backend `GET /appointments` đã hỗ trợ sẵn kết hợp `date` + `cursor` từ S2-09. Sửa: `useAppointmentsListQuery(date)` thêm `date` vào cả `queryKey` và tham số gọi API; `AppointmentListView` nhận prop `date` từ `AppointmentSchedulePage` (cùng state `date` đã dùng cho chế độ Lưới).
- **UX theo yêu cầu chủ dự án**: badge "N lịch hẹn trong ngày" — trước đây nhỏ, màu xám, đặt cạnh tiêu đề trang và chỉ hiện ở chế độ Lưới — chuyển xuống ngang hàng nút "Hôm nay" trên thanh điều hướng ngày, đổi màu nền xanh nổi bật hơn, và hiện cho **cả 2 chế độ** (Lưới lẫn Danh sách) vì đều dùng chung state `date`/`dayQuery` của trang cha.
- **Phím tắt F2** cho nút "+ Đặt lịch" (mở panel đặt lịch nhanh) — `useEffect` đăng ký `keydown` ở `window`, bỏ qua khi đang gõ trong input/textarea/select. Thêm `title` (tooltip native) trên nút hướng dẫn phím tắt.
- **Đã xác minh thật qua trình duyệt** (Playwright, dữ liệu demo thật, không mock): đổi ngày ở chế độ Danh sách → số dòng + badge đổi đúng theo từng ngày (hôm nay 7, ngày mai 1, ngày kế 0 → hiện đúng EmptyState); F2 mở đúng panel đặt lịch nhanh; tooltip đúng nội dung. `pnpm -w typecheck` sạch toàn workspace. Không đổi schema/OpenAPI — thuần sửa lỗi + UX phía web, tái dùng nguyên endpoint/props đã có.

## 2026-08-12 (14)

Đổi mô hình đặt lịch sang "lead capture" — không tạo/gắn `patient` lúc đặt, mã đặt lịch, tra cứu theo SĐT + cảnh báo spam, check-in (docs/DECISIONS.md #032, docs/ERD.md v1.4):

- **Chủ dự án yêu cầu đổi ngay sau khi dùng thử S2-09** (mục changelog `(13)` bên dưới): đặt lịch **không** còn tạo/gắn hồ sơ `patient` — chỉ ghi Họ tên/SĐT/Lý do khám trực tiếp lên `appointment`. Việc tạo/khớp hồ sơ `patient` thật sự chuyển sang lúc Tiếp nhận (Sprint 3, chưa xây). Đây là thay đổi cấu trúc dữ liệu đã chốt (`docs/ERD.md`/`.claude/docs/data-model.md`) — đã hỏi rõ 4 điểm mơ hồ trước khi lên plan (check-in có sinh trạng thái mới không, hành vi nút Check-in khi Tiếp nhận chưa có, mức độ cảnh báo spam, số phận `PatientPicker.tsx`) rồi mới code.
- **Schema**: migration `20260812012715_appointment_lead_capture` (viết tay, không dùng `prisma migrate diff` — tránh trùng "drift giả" với exclusion constraint C2 raw-SQL-only đã gặp ở các migration trước). `appointment.patient_id` đổi **nullable** (để sẵn cho Tiếp nhận gắn sau); thêm cột `booking_code` (`UNIQUE (tenant_id, booking_code)`, prefix `LH`, sinh qua `code_sequence`/`formatDisplayCode()` đúng khuôn `patient_code`), `full_name`, `phone` (index `(tenant_id, phone)`), `reason`. Backfill dữ liệu dev cũ từ `patient` đã gắn; gặp lỗi thật `23505` lúc backfill `booking_code` (8 ký tự đầu UUID v7 sinh liên tiếp trong cùng mili-giây có thể trùng) — sửa dùng nguyên UUID đầy đủ.
- **Backend**: `packages/shared/src/appointment.ts` — bỏ `patientId` khỏi `createAppointmentRequestSchema` (thêm `fullName`/`phone`/`reason`), `appointmentSummarySchema` bỏ `patientFullName`/`patientCode` (denormalized-join thêm ở S2-09, không còn cần) thay bằng `fullName`/`phone`/`reason`/`bookingCode`, thêm `checkinAppointmentRequestSchema`/`appointmentPhoneLookupQuerySchema`/`ResponseSchema`. `AppointmentRepository.list()` bỏ hẳn join `patient` (đơn giản hoá lại phần vừa thêm ở S2-09 — không cần nữa vì giờ là cột riêng); thêm `findByPhone()` (tên gần nhất + đếm huỷ), `checkin()` (cùng khuôn `cancel()`/`reschedule()`). `AppointmentController` thêm `GET /appointments/lookup?phone=`, `POST /appointments/:id/checkin` (tái dùng quyền `appointment.update` có sẵn, không thêm permission mới).
- **Frontend**: `AppointmentQuickCreatePanel.tsx` bỏ hẳn `PatientPicker` (giữ nguyên file, chỉ bỏ nơi gọi — có thể cần lại ở Tiếp nhận S3), thay bằng 3 trường SĐT (debounce tra cứu tự điền tên + cảnh báo spam ≥5 lần huỷ, chỉ cảnh báo không chặn)/Họ tên/Lý do khám, và dialog xác nhận đặt lịch thành công đúng nguyên văn chủ dự án yêu cầu (tên khách, giờ+ngày, mã đặt lịch). `AppointmentDetailPanel.tsx` thêm nút "Check-in" (chuyển `SCHEDULED → CONVERTED`, không sinh trạng thái mới, chưa tạo `encounter`/gắn `patient_id`) + dialog "Đã ghi nhận khách đến". `AppointmentGridView.tsx`/`AppointmentListView.tsx` đổi hiển thị sang `fullName`/`bookingCode`.
- **1 bug bundler thật phát hiện + xử lý**: hằng số ngưỡng spam (`APPOINTMENT_SPAM_CANCELLED_THRESHOLD`, số nguyên thuần) không export được từ `packages/shared` qua `vite build` — Rollup báo "not exported" dù Node `require()` thấy đúng, kể cả sau khi đổi cách re-export (`__exportStar` lẫn `Object.defineProperty` tường minh đều fail giống nhau). Xác nhận: mọi hằng số giá trị thuần khác ở `packages/shared` từ trước tới giờ chưa từng được import vào `apps/web` (chỉ dùng ở `apps/api`) nên chưa từng lộ vấn đề. Xử lý bằng chuyển hằng số sang khai báo tại `apps/web/src/features/appointment/appointment-status.ts` (đúng vị trí kiến trúc thật — chỉ web so sánh để hiển thị banner, `apps/api` không tự chặn theo ngưỡng này — cùng khuôn `LATE_APPOINTMENT_THRESHOLD_MINUTES` đã có sẵn ở file đó). Ghi lại thành lưu ý cho lần sau ở `docs/DECISIONS.md` #032.
- **Đã cập nhật tài liệu chốt trong cùng lúc**: `docs/ERD.md` (v1.4), `.claude/docs/data-model.md`, `.claude/docs/clinical-workflow.md` mục "Đặt lịch".
- **Đã xác minh thật**: `appointment-http.spec.ts` viết lại toàn bộ theo body mới (50/50 pass, tổng 152 test `apps/api` không regress). `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua dữ liệu thật (seed qua API, không mock): đặt lịch mới → dialog xác nhận đúng nội dung + mã đặt lịch (`LH2608000007`); đặt lại đúng SĐT → tự điền đúng tên lần gần nhất; SĐT có 5 lần huỷ (seed qua API) → đúng banner cảnh báo vàng; check-in → dialog thông báo + trạng thái đổi "Đã chuyển khám" ngay trên lưới. Kiểm tra thêm bằng script cô lập xác nhận form tự làm sạch đúng khi mở lại panel (không phải bug, chỉ artefact của thứ tự click dồn dập trong 1 script test dài trước đó).

## 2026-08-12 (13)

S2-09 — Web Lịch hẹn: dạng lưới (bác sĩ × khung giờ) + dạng danh sách, đổi/dời lịch, cảnh báo trễ hẹn (docs/DECISIONS.md #030, #031):

- **Dựng mockup Artifact trước khi code, nhiều vòng hỏi/chốt lại**: chủ dự án gửi ảnh tham khảo (Zendenta), yêu cầu skill `ui-ux-pro-max` dựng 2 chế độ hiển thị. Hoà giải bố cục tham khảo với token màu/design đã chốt (`ui-guidelines.md`, không copy màu tím/pastel). Sau khi xem mockup, chốt thêm ngoài phạm vi hỏi ban đầu: đổi/dời lịch (ban đầu chốt "không sửa lịch đã có"), cảnh báo trễ hẹn, đường kẻ "bây giờ", gợi ý bác sĩ trống lịch; và xác nhận **không** làm "ca làm việc riêng từng bác sĩ" (cần bảng mới ngoài `docs/ERD.md` đã chốt — nhu cầu thật, hoãn lại).
- **Backend**: `packages/core/src/date/vietnam-day-range.ts` (util thuần, VN UTC+7 cố định — cùng kỹ thuật `format-display-code.ts`, dùng lọc `GET /appointments?date=`). Hai port mới `DoctorDirectoryPort`/`ClinicConfigReaderPort` (`packages/core/src/ports/`) — module `appointment` đọc dữ liệu thuộc `iam`/`clinic` qua port thay vì import chéo, đúng ví dụ `PatientReaderPort` đã nêu ở `.claude/docs/coding-standards.md`; adapter: `IamModule`/`ClinicModule` (đã `@Global()`/`imports`) cung cấp provider, `UserAccountRepository.listActiveDoctors()` mới, `ClinicSettingsService` thêm hàm bọc `getScheduleConfig()`.
  **Phát hiện lỗ hổng ma trận quyền thật lúc thiết kế**: lễ tân cần đọc danh sách bác sĩ + giờ làm việc nhưng ma trận mặc định chỉ cấp `user_account.read`/`clinic_config.read` cho `clinic_admin`/`system_admin`. Đã hỏi và chốt: **không** mở rộng ma trận — thêm 2 endpoint chiếu tối thiểu `GET /appointments/doctors|schedule-config`, gắn quyền `appointment.read` sẵn có.
  `AppointmentController` thêm `PATCH /appointments/:id/reschedule` (đổi/dời lịch — tái dùng khuôn `cancel()`, exclusion constraint C2 chặn trùng giờ mới, tái dùng `AppointmentSlotConflictError`/`AppointmentNotCancellableError` đổi message cho khớp cả hai ngữ cảnh). `GET /appointments` thêm filter `date` + join `patientFullName`/`patientCode` (chỉ ở `list()`, tránh N+1 cho lưới/danh sách).
- **Frontend**: `apps/web/src/features/appointment/` (feature module đầy đủ — `appointment.api.ts`/`queries.ts`, `AppointmentSchedulePage.tsx` toggle Lưới/Danh sách, `AppointmentGridView.tsx`, `AppointmentListView.tsx` theo List Screen Pattern, `AppointmentQuickCreatePanel.tsx`, `AppointmentDetailPanel.tsx`). `apps/web/src/features/patient/PatientPicker.tsx` (mới, dùng chung — tìm bệnh nhân debounce + tạo nhanh, tái dùng nguyên `PatientFormFields`/`useCreatePatientMutation`, không viết lại form). `Sidebar.tsx` thêm "Lịch hẹn" trong nhóm "Tiếp nhận và Đặt lịch"; `router.tsx` thêm route `/appointments`.
- **2 bug thật phát hiện + sửa lúc kiểm bằng trình duyệt** (không phải review code): (1) chế độ lưới gọi `limit=200` cho `GET /appointments` vượt trần `listAppointmentsQuerySchema.max(100)` đã có từ trước → 400, sửa hạ về 100 (đủ cho một ngày một tenant nhỏ); (2) đường kẻ "bây giờ" ban đầu cài làm 1 phần tử CSS Grid đặt vị trí tường minh cả hai trục (`gridRow`/`gridColumn`) để "phủ" lên toàn hàng thân bảng — theo đúng thuật toán CSS Grid, việc này khiến các cột giờ/bác sĩ (auto-placement, không khai vị trí tường minh) bị thuật toán né hàng đã chiếm và đẩy xuống hàng kế tiếp, lưới trông trống trơn dù DOM/chiều cao vẫn đúng (phát hiện bằng so `getBoundingClientRect()` qua Playwright, không đoán từ đọc code) — sửa bằng để mỗi cột tự vẽ đoạn kẻ riêng cùng toạ độ `top`, ghép lại thành một đường liền mạch. Bài học ghi thành quy tắc chung ở `docs/DECISIONS.md` #031 cho mọi overlay phủ nhiều cột CSS Grid sau này.
- **Đã xác minh thật**: `appointment-http.spec.ts` thêm 13 test (38/38) — tổng 140 test `apps/api` + 30 test `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. Playwright qua dữ liệu demo thật (seed 5 bác sĩ + nhiều lịch hẹn qua API, tránh mock): đăng nhập → lưới đúng cột/giờ/đường kẻ bây giờ → đặt lịch nhanh (tìm bệnh nhân có sẵn, gợi ý trống/bận) → chuyển Danh sách đúng dữ liệu → huỷ có lý do cập nhật ngay không cần tải lại → sửa/dời lịch thành công + demo đúng báo `APPOINTMENT_SLOT_CONFLICT` khi chọn giờ trùng.

## 2026-08-11 (12)

App Shell v2 — triển khai sidebar thu gọn, topbar/breadcrumb, List Screen Pattern cho Danh sách bệnh nhân (docs/DECISIONS.md #027, #029):

- **Thiết kế trước, duyệt qua mockup rồi mới code**: chủ dự án yêu cầu làm lại UI dựa trên tham khảo một sản phẩm HIS khác. Dựng mockup HTML tương tác (Artifact) đúng token màu/design đã có trong `ui-guidelines.md`, đánh dấu rõ 4 điểm khác với ảnh tham khảo cần chủ dự án xác nhận (giữ cursor pagination + đổi UX sang cuộn vô hạn thay vì "1-N trên tổng"; bỏ chuông thông báo; bỏ dòng "KHOA:"; điểm tạo bệnh nhân mới chuyển sang luồng Đặt lịch/Tiếp nhận) — cả 4 đều được duyệt, thêm quyết định dùng `@tanstack/react-virtual` sau khi giải thích rủi ro DOM phình to khi cuộn sâu không tìm kiếm.
- **`apps/web/src/shared/layout/`**: `Sidebar.tsx` viết lại — tự quản lý `<aside>` + trạng thái thu gọn (`w-60` ⇄ `w-16`), vùng logo riêng ở đỉnh, nhóm menu cha/con (accordion, "Tiếp nhận và Đặt lịch" chứa "Danh sách bệnh nhân"). `breadcrumb.context.tsx` (mới): `BreadcrumbProvider` + `useBreadcrumb(items)` — Context thay vì route `handle`/`useMatches()` vì cần đoạn động (tên bệnh nhân) mà route không có loader. `TopBar.tsx` (mới): breadcrumb dạng chip (icon Home mở đầu, `CaretRight` phân cách, đoạn cuối nổi bật nền xanh nhạt) thay nút "← Quay lại", lời chào + avatar viết tắt tên mở dropdown "Đăng xuất" — thay hẳn `UserMenu.tsx` (đã xoá, không dùng nữa). `AppShell.tsx`: bỏ `max-w`/`padding` mặc định (mỗi trang tự quyết định), đổi `min-h-dvh` → `h-dvh` (cố định đúng viewport, điều kiện bắt buộc để màn hình danh sách tự cuộn nội bộ đúng).
- **`PatientListPage.tsx`** viết lại theo List Screen Pattern (`ui-guidelines.md` mục 9): đổi tên "Bệnh nhân" → "Danh sách bệnh nhân"; bỏ hẳn nút "Thêm bệnh nhân" (tạo mới chuyển sang luồng Đặt lịch/Tiếp nhận, chưa xây); bảng dựng bằng CSS Grid (không phải `<table>`, để phối hợp được với `position: absolute` của virtualization) kèm `role` ARIA giữ ngữ nghĩa bảng; `@tanstack/react-virtual` + cuộn vô hạn (theo dõi hàng ảo cuối cùng trong `useEffect`, gọi `fetchNextPage()` khi gần hết — giữ cursor pagination, không offset); mở hồ sơ bằng double-click vào mã bệnh nhân (không phải click cả hàng) + phím tắt Tab/Enter tương đương. `PATIENT_LIST_LIMIT` 20→50.
- **`PatientDetailPage.tsx`/`PatientNewPage.tsx`**: bỏ link "← Quay lại danh sách" (breadcrumb thay thế), thêm `max-w-4xl` riêng (AppShell không còn áp `max-w` chung). Detail: nút "Sửa hồ sơ" chỉ hiện với vai trò có `patient.update` (`receptionist`, `clinic_admin` — mảng tĩnh cùng kiểu `Sidebar.tsx` đã dùng, không phát minh cơ chế phân quyền client mới).
- **2 bug hạ tầng dùng chung phát hiện qua chủ dự án dùng thử** (không phải review code), đã sửa + ghi thành quy tắc bắt buộc ở `ui-guidelines.md` mục 8.3: (1) breadcrumb tồn đọng (stale) ở `DashboardPage`/`AdminPage` vì quên gọi `useBreadcrumb` — `useBreadcrumb` cố ý không tự xoá khi unmount (tránh nháy trắng) nên trang quên khai báo sẽ hiện nguyên breadcrumb trang trước; (2) icon nhóm "Tiếp nhận và Đặt lịch" lúc sidebar thu gọn bấm không phản hồi (`onClick` có điều kiện chặn khi `collapsed`) — sửa để bấm sẽ mở lại sidebar và mở luôn nhóm. Kèm 1 bug nhỏ: avatar viết tắt tên sai (`"Quản trị viên (dev)"` → `"Q("`) do lấy chữ cái đầu từ hậu tố `(dev)` — sửa bằng lọc từ không bắt đầu bằng chữ cái.
- **Đã xác minh thật qua trình duyệt** (Playwright, chụp ảnh từng bước): đăng nhập → mở nhóm sidebar → vào Danh sách bệnh nhân (không có nút thêm, breadcrumb đúng) → thu gọn sidebar → bấm icon nhóm mở lại đúng (kèm nhóm tự mở) → double-click mã bệnh nhân mở chi tiết (breadcrumb có tên bệnh nhân, nút Sửa hồ sơ hiện đúng cho clinic_admin) → điều hướng Tổng quan ⇄ Danh sách bệnh nhân ⇄ Quản trị, breadcrumb đổi đúng từng trang. `pnpm -w lint/typecheck/build` sạch toàn workspace; 127 test `apps/api` pass, không regress (thuần thay đổi web, không chạm backend).
- Cập nhật `.claude/docs/ui-guidelines.md` mục 8 (đặc tả breadcrumb cụ thể, mục 8.3 mới), `docs/DECISIONS.md` (#029), `apps/web/package.json` (+`@tanstack/react-virtual`).

## 2026-08-11 (11)

Fix — Web: tự làm mới access token khi hết hạn (401), hết nháy giao diện khi tìm kiếm (docs/DECISIONS.md #028):

- **Bug 1 (để trang yên rồi thao tác lại → "Không tải được dữ liệu")**: `shared/api/client.ts` — `authMiddleware.onResponse` tự phát hiện 401, gọi `/auth/refresh` (cookie httpOnly) lấy token mới, thử lại đúng request cũ đúng 1 lần, không lộ lỗi ra UI. Clone request ngay ở `onRequest` (body chỉ đọc được 1 lần) để dùng lại lúc retry. Dedupe nhiều 401 đồng thời bằng `refreshPromise` dùng chung — chỉ gọi `/auth/refresh` một lần dù nhiều query cùng fetch lúc quay lại tab. Refresh cũng thất bại mới coi là hết phiên thật (`sessionExpiredHandler`, đăng ký từ `AppBootstrap.tsx`, không import thẳng `auth.store` vào `shared/`).
- **Bug 2 (gõ tìm kiếm nháy liên tục)**: `patient.queries.ts` — `usePatientsQuery` thêm `placeholderData: keepPreviousData` (TanStack Query v5), giữ kết quả cũ hiển thị (làm mờ nhẹ qua `isPlaceholderData`, `PatientListPage.tsx`) tới khi kết quả mới về hẳn, thay vì xoá trắng bảng mỗi lần từ khoá debounce đổi.
- **Đã xác minh thật qua trình duyệt** (Playwright, hạ tạm `ACCESS_TOKEN_TTL_SECONDS` xuống 6 giây để mô phỏng hết hạn nhanh): network log đúng chuỗi `401 → POST /auth/refresh 200 → retry 200`, không hiện banner lỗi; gõ "nguyen" từng chữ với khoảng nghỉ >300ms không lần nào bảng bị trắng. Đã khôi phục TTL về 15 phút, build lại `packages/core`. `pnpm -w lint/typecheck/build` sạch toàn workspace; 127 test `apps/api` pass, không regress (không đổi hành vi backend).
- Cập nhật `docs/DECISIONS.md` (#028). Không đổi schema/OpenAPI — thuần sửa hạ tầng phía web.

## 2026-08-11 (10)

S2-08 — Web: danh sách + tạo mới + chi tiết/sửa bệnh nhân (PAT-01→03):

- **Hỏi chốt workflow trước khi code** (theo yêu cầu chủ dự án, giống S1-08): hai điểm chưa có đặc tả — (1) sửa hồ sơ dùng modal hay trang riêng? Chốt: **sửa tại chỗ trên trang chi tiết** (`PatientDetailPage`, không modal) — cùng bố cục `PatientFormFields` cho cả xem lẫn sửa, chỉ khác `disabled`. (2) Chống trùng "mềm" (PAT-03) gọi lúc nào? Chốt: gọi `check-duplicate` LÚC bấm nút Lưu, không debounce theo từng ký tự gõ (khác tìm kiếm PAT-02) — nghi trùng thì chặn submit lần đầu, hiện cảnh báo kèm hai lựa chọn "Vẫn tạo mới"/"Huỷ", không tự động tạo.
- **`apps/web/src/features/patient/`**: `PatientListPage.tsx` (bảng + ô tìm kiếm debounce 300ms qua `useDebouncedValue`, tái dùng `q` đã có từ PAT-02; phân trang cursor bằng nút "Tải thêm" — không hiện "Đang xem 1-N trên tổng" như `ui-guidelines.md` mục 4.2 mô tả cho phân trang offset, vì cursor không có tổng số bản ghi để tính); `PatientNewPage.tsx` (gọi `checkPatientDuplicate()` trước `createPatient()`, disable toàn bộ form khi đang hiện cảnh báo trùng); `PatientDetailPage.tsx` (state `editing` cục bộ, `patientDetailToFormValues()` chuyển response API sang form phẳng); `PatientFormFields.tsx` (component dùng chung tạo/sửa, lưới 2 cột `md:grid-cols-2`, textarea ghi chú dị ứng `md:col-span-2`, theo `.claude/docs/ui-guidelines.md` mục 4.1); `patient.api.ts` (wrapper `openapi-fetch`), `patient.queries.ts` (`useInfiniteQuery`/`useQuery`/`useMutation`, cache key qua `queryKey(tenantId, 'patient', ...)` đã có sẵn từ S1-09), `patient-form.utils.ts` (form phẳng ↔ `address` lồng của API, chuỗi rỗng → `undefined` khớp field optional).
- **`apps/web/src/shared/ui/ErrorBanner.tsx`, `Skeleton.tsx`, `apps/web/src/shared/hooks/useDebouncedValue.ts`**: viết ở `shared/` ngay từ đầu (không đặt trong `features/patient/` rồi chờ trùng lặp lần hai mới tách) — theo `CLAUDE.md` mục "viết component tiện ích ở nơi dùng chung với chủ đích tái dùng ngay", vì đây là UI primitive rõ ràng sẽ cần lại ở mọi màn hình có fetch dữ liệu (lịch, tiếp nhận, khám bệnh...) chứ không phải logic nghiệp vụ đặc thù.
- **`Sidebar.tsx`**: thêm mục "Bệnh nhân" (`icon={Users}`), điều kiện hiện `PATIENT_ROLES = ['receptionist','nurse','doctor','clinic_admin']` — khớp đúng ma trận `patient.read` mặc định ở `.claude/docs/security-audit.md` (mọi vai trò trừ `system_admin`, vai trò này không có `patient.read` ở mức nào).
- **2 bug phát hiện + sửa lúc kiểm bằng trình duyệt thật** (không phải quyết định trước, phát hiện qua chạy tay):
  1. `Button.tsx`: override qua `className` thô (ví dụ muốn nút "Huỷ" nền trắng chữ đen) bị nuốt bởi class mặc định `bg-blue-600 text-white` — thứ tự thắng của CSS phụ thuộc thứ tự khai báo trong stylesheet đã build (Tailwind), không phải thứ tự chuỗi class nối trong HTML, nên nút hiện ra chữ trắng trên nền trắng, không đọc được. Sửa bằng thêm prop `variant: 'primary' | 'secondary' | 'danger'` — mỗi biến thể là một khối class trọn vẹn không xung đột, thay vì lắp ghép override.
  2. `App.tsx`: `QueryClient` mặc định (`retry: 3` có backoff) áp dụng cho MỌI lỗi kể cả `404 NOT_FOUND` rõ ràng — vào `/patients/:id` với id không tồn tại phải chờ vài giây (3 lần thử lại) mới hiện empty state "Không tìm thấy hồ sơ". Sửa bằng `defaultOptions.queries.retry` tuỳ chỉnh: không retry khi `error instanceof ApiError && error.code === 'NOT_FOUND'`, giữ nguyên retry 3 lần cho lỗi khác (mất mạng, 5xx).
- **Đã xác minh thật qua trình duyệt** (Playwright headless Chromium, cài tạm ở scratchpad — môi trường chưa có project skill `run`, đã cân nhắc dùng `chromium-cli` theo skill `run` nhưng không có sẵn nên dùng lại cách Playwright trực tiếp như các lần trước; chụp ảnh từng bước): đăng nhập bằng `dev.admin` → Dashboard; vào "Bệnh nhân" → bảng danh sách + ô tìm kiếm hiện đúng; gõ tìm kiếm → kết quả lọc đúng; "Thêm bệnh nhân" → điền form → Lưu → tạo thành công, điều hướng sang trang chi tiết đúng hồ sơ vừa tạo; tạo lại đúng tên + ngày sinh vừa dùng → cảnh báo "Có thể trùng với hồ sơ đã có" hiện đúng kèm link hồ sơ cũ và hai nút "Huỷ"/"Vẫn tạo mới"; bấm "Huỷ" → không tạo bản ghi mới, quay lại form trống cảnh báo; vào chi tiết hồ sơ đã tạo, bấm "Sửa hồ sơ" → đổi số điện thoại → "Lưu" → quay lại chế độ xem, giá trị mới hiển thị đúng (ảnh chụp xác nhận trực tiếp, không suy luận qua DOM text vì input value không nằm trong `textContent`); `console --errors` không có lỗi ngoài dự kiến (chỉ 401 ở lần `/auth/refresh` đầu tiên trước khi có phiên, hành vi đã biết từ S1-08/S1-09). `pnpm -w lint/typecheck/build` sạch toàn workspace; 127 test `apps/api` (không đổi) pass, không regress — S2-08 là web thuần, không chạm backend.
- Cập nhật `docs/TASK.md`, `docs/CURRENT.md`. Không đổi `.claude/docs/data-model.md`/`docs/ERD.md`/OpenAPI (không có endpoint hay schema mới, chỉ tiêu thụ API đã có từ S2-01→S2-03).

## 2026-08-11 (9)

S2-07 — Tài khoản + vai trò (ADM-01), cấu hình phòng khám (ADM-02, trừ mẫu in):

- **Hỏi chốt phạm vi trước khi code**: `docs/product/plan.md` gọi việc này là "Module `tenant`: tài khoản, vai trò, cấu hình phòng khám (giờ làm, slot, phòng)" và PRD ADM-02 gồm cả "mẫu in". Chủ dự án chọn: làm tài khoản+vai trò + phòng + giờ làm/slot, **bỏ mẫu in** (chưa cần tới khi chưa làm PRE-04/in đơn, Sprint 4).
- **Đúng ranh giới module đã chốt, không tạo module `tenant`**: `.claude/docs/architecture.md` đã định nghĩa rõ "iam | Tài khoản, vai trò, phiên đăng nhập, audit log" và "clinic | Tenant, cấu hình phòng khám, phòng, danh mục nội bộ" — tài khoản/vai trò thuộc `iam` (mở rộng module đã có từ S1-04), phòng/cấu hình thuộc `clinic` (module mới). Phát hiện thêm bằng chứng đã có sẵn trong code: comment trong `user-account-auth.repository.ts` (viết từ S1-04) đã ghi "CRUD tài khoản đầy đủ (tạo/sửa/gán vai trò) thuộc S2-07 — không lặp lại ở đây" — xác nhận đúng kế hoạch cũ.
- **Sửa schema trước khi code (phát hiện lúc thiết kế gán lại vai trò)**: `user_role` có `UNIQUE(tenant_id, user_id, role_id)` dạng THƯỜNG, không phải PARTIAL — nếu soft-delete một gán-vai-trò rồi gán lại đúng vai trò đó (ví dụ gỡ rồi thêm lại `nurse`) sẽ vi phạm unique vì dòng cũ đã xoá mềm vẫn tính. Migration `20260811073334_user_role_partial_unique_s2_07`: đổi thành `CREATE UNIQUE INDEX ... WHERE deleted_at IS NULL` — cùng kỹ thuật C3 (`patient.national_id_hash`, S2-01). Bỏ khai báo `@@unique` trong `schema.prisma` (Prisma không hỗ trợ điều kiện `WHERE`), chỉ còn raw SQL.
- **Bug fix đi kèm**: `UserAccountAuthRepository.findRoleNamesForUser()` (viết từ S1-08) thiếu `deletedAt: null` trong `WHERE` — vô hại từ trước tới nay vì chưa từng có đường nào soft-delete `user_role`, nhưng tính năng gán lại vai trò mới (S2-07) sẽ làm lộ ra (vai trò đã gỡ vẫn hiện trong `login`/`GET /auth/me`). Sửa trước khi triển khai tính năng, kèm test xác nhận.
- **`apps/api/src/modules/iam/` (mở rộng)**: `UserAccountRepository` (CRUD `user_account` + `user_role`, tách khỏi `UserAccountAuthRepository` vốn chỉ phục vụ đăng nhập), `UserAccountService`, `UserAccountController` — `POST/GET/GET:id/PATCH /api/v1/users`, `POST /api/v1/users/:id/reset-password` (đổi mật khẩu tách endpoint riêng khỏi sửa hồ sơ — thao tác nhạy cảm, audit action riêng `user_account.password_reset`). `roleNames` dùng tên 5 vai trò hệ thống cố định thay vì `roleId` thô (v1 mỗi tenant chỉ có đúng 5 vai trò mặc định, vai trò tuỳ biến là ADM-07 P1 chưa hiện thực — ghi rõ giới hạn này sẽ cần đổi API khi ADM-07 xong).
- **`SessionRepository.revokeAllForUser()` (viết sẵn từ S1-04, xem docs/DECISIONS.md #019) có nơi gọi thật đầu tiên**: `.claude/docs/security-audit.md` yêu cầu "Đổi vai trò hoặc chuyển tenant thu hồi toàn bộ phiên đang mở" — `UserAccountService.updateUserAccount()` gọi khi `isActive→false` (reason `account_disabled`, đã là giá trị hợp lệ có sẵn từ S1-04) hoặc đổi `roleNames` (reason mới `role_changed`); `resetPassword()` cũng gọi (reason mới `password_reset`) vì đổi mật khẩu mà không thu hồi phiên cũ thì máy đang giữ phiên cũ (ví dụ máy bị mất) vẫn dùng được tới khi access token 15 phút hết hạn.
- **`apps/api/src/modules/clinic/` (module mới)**: `RoomRepository`/`Service`/`Controller` (`POST/GET/PATCH /api/v1/rooms`, KHÔNG phân trang — quy mô phòng của phòng khám 1-3 bác sĩ rất nhỏ, thêm cursor pagination ở đây là dựng abstraction cho quy mô chưa từng xảy ra). `ClinicSettingsRepository`/`Service`/`Controller` (`GET/PATCH /api/v1/clinic-settings`) đọc/ghi `tenant_setting` 2 key riêng (`business_hours`, `slot_duration_minutes`) — cùng mẫu `BreakGlassRepository.getDurationMinutes()` đã có (đọc trực tiếp key, Zod parse, fallback mặc định nếu chưa cấu hình/dữ liệu hỏng). Giờ làm việc mô hình theo từng ngày trong tuần (`{open,close}` dạng `HH:mm` hoặc `null` = đóng cửa cả ngày) — chưa có đặc tả chính thức cho hình dạng này ở PRD/ERD, tự quyết theo lẽ thường phổ biến nhất, ghi rõ trong code. Room và clinic-settings dùng CHUNG quyền `clinic_config.read`/`clinic_config.update` (PRD ADM-02 gộp "giờ làm việc, độ dài slot, phòng, mẫu in" làm một yêu cầu, không có lý do tách quyền riêng cho "phòng" ở v1).
- **Cân nhắc nhưng KHÔNG làm**: nối `AppointmentService` đọc `slot_duration_minutes` từ `clinic-settings` làm mặc định `durationMinutes` thay cho hằng số tĩnh `DEFAULT_APPOINTMENT_DURATION_MINUTES` — cần một port (`packages/core/ports`) vì `appointment` và `clinic` là hai module khác nhau (`.claude/docs/coding-standards.md` cấm import chéo module). Chưa có yêu cầu cụ thể nào cần hành vi động này ngay — để dành, không dựng abstraction cho tình huống chưa xảy ra.
- `packages/shared/src/user-account.ts`, `packages/shared/src/clinic.ts` (schema mới, `businessHoursSchema` export riêng để repository tái dùng validate). `packages/core/src/errors/user-account-errors.ts`: `UserAccountDuplicateUsernameError` (409).
- `apps/api/src/testing/tenant-fixture.ts`: `cleanup()` thêm xoá `room`/`tenantSetting` (FK RESTRICT tới `tenant`, phải xoá trước khi xoá tenant).
- OpenAPI: 6 path mới (`/users`, `/users/{id}`, `/users/{id}/reset-password`, `/rooms`, `/rooms/{id}`, `/clinic-settings`). Sinh lại `openapi.json` + `openapi-schema.d.ts` (web).
- **Đã xác minh thật**: `user-account-http.spec.ts` (14/14 pass) — CRUD, trùng username → 409, optimistic lock, **thu hồi phiên xác minh bằng HTTP thật** (đăng nhập lấy refresh cookie thật → admin đổi vai trò/vô hiệu hoá/đặt lại mật khẩu → thử refresh bằng cookie cũ → 401; tài khoản bị vô hiệu hoá thì đăng nhập lại → 403 `AUTH_ACCOUNT_DISABLED`; mật khẩu cũ hết tác dụng, mật khẩu mới dùng được), gán lại đúng vai trò đã từng gỡ trước đó không lỗi (xác nhận migration partial unique đúng thiết kế, không phải suy luận), cách ly tenant. `clinic-http.spec.ts` (13/13 pass) — room CRUD + optimistic lock, settings GET mặc định (15 phút, businessHours null) → PATCH từng phần → GET phản ánh đúng, PATCH chỉ ghi field có mặt (field khác giữ nguyên), 2 tenant có cấu hình độc lập, permission, cách ly tenant, validate giờ sai định dạng → 400. Toàn bộ 127 test `apps/api` (100 cũ + 27 mới, không regress) pass. `pnpm -w lint/typecheck/build` sạch toàn workspace.
- Cập nhật `docs/TASK.md`, `docs/CURRENT.md`. Không đổi `.claude/docs/data-model.md`/`docs/ERD.md` cấu trúc cột nghiệp vụ (chỉ đổi kiểu unique index của `user_role`, đã ghi trong migration).

## 2026-08-11 (8)

S2-06 — Huỷ lịch có lý do + ghi audit (APP-04), walk-in tạo lịch (APP-06):

- **Hỏi chốt phạm vi trước khi code**: `docs/product/plan.md` mô tả S2-06 là "huỷ lịch có lý do, ghi audit; walk-in tạo lịch **+ check-in** trong một transaction" — nhưng check-in nghĩa là tạo `encounter`, bảng này thuộc Sprint 3 (chưa tồn tại), và quyết định đã chốt trước đó ("không đi đường tắt walk-in tối thiểu", ghi ở `docs/CURRENT.md` lúc S2-02) ngụ ý chưa làm check-in ngay. Chủ dự án xác nhận: S2-06 chỉ tạo `appointment` (kể cả walk-in), **không** tạo `encounter` — check-in thật là việc của module Tiếp nhận ở Sprint 3 (lễ tân nhập thông tin tiếp nhận, bấm lưu mới tạo `encounter`).
- **Walk-in (APP-06 phần "tạo lịch") không cần code mới** — `appointment.source` đã có giá trị `walk_in` từ S2-05, endpoint `POST /appointments` hiện có xử lý được thẳng (chỉ cần gửi `scheduledAt` là thời điểm hiện tại). Xác minh bằng 1 test mới thay vì viết thêm route.
- **`POST /api/v1/appointments/:id/cancel`** (APP-04): `packages/shared/src/appointment.ts` thêm `cancelAppointmentRequestSchema` (`cancelReason` bắt buộc non-empty + `version` optimistic locking, cùng quy ước `updatePatientRequestSchema`). `AppointmentRepository.cancel()`: `updateMany` với `WHERE tenant_id=? AND id=? AND version=? AND deleted_at IS NULL AND status='SCHEDULED'` — ghép cả điều kiện `status` vào `WHERE` để atomic, tránh race hai request huỷ cùng lịch gần như đồng thời (đơn giản hoá: coi race đó là `CONCURRENT_MODIFICATION` chung, không cố phân biệt nguyên nhân). `AppointmentService.cancelAppointment()`: `findById` trước để kiểm tồn tại + `data_scope=personal` (404 nếu ngoài phạm vi bác sĩ — cùng triết lý `getAppointment()` đã có từ S2-05), kiểm `status==='SCHEDULED'` riêng biệt trước khi update để trả lỗi rõ ràng (`AppointmentNotCancellableError`, phân biệt với race version), ghi audit `appointment.cancelled` trong cùng transaction.
- **`packages/core/src/errors/appointment-errors.ts`** thêm `AppointmentNotCancellableError` (409, map trong `domain-exception.filter.ts`) — lịch không còn `SCHEDULED` (đã `CANCELLED`/`NO_SHOW`/`CONVERTED`) thì không huỷ lại được qua endpoint này; luồng "bỏ về" của lượt khám (encounter `CHECKED_IN → CANCELLED`) là việc khác của module `encounter` ở S3.
- OpenAPI: `generate-openapi.ts` thêm path `POST /appointments/{id}/cancel`. Sinh lại `openapi.json` + `openapi-schema.d.ts` (web).
- **Đã xác minh thật**: `appointment-http.spec.ts` thêm 10 test (25/25 tổng pass, HTTP e2e qua toàn bộ stack) — thiếu token/quyền/`cancelReason` đúng mã lỗi; huỷ thành công đúng version tăng lên 2 và `cancelReason` được ghi; huỷ với version cũ → 409 `CONCURRENT_MODIFICATION`; huỷ lại lịch đã huỷ → 409 `APPOINTMENT_NOT_CANCELLABLE`; **huỷ xong thì khung giờ đó không còn bị exclusion constraint C2 chặn** — đặt lịch mới cùng giờ cùng bác sĩ thành công ngay (xác nhận đúng thiết kế `WHERE status='SCHEDULED'` của C2, không phải suy luận); bác sĩ (scope personal) không huỷ được lịch của bác sĩ khác → 404; tenant khác không huỷ được → 404; walk-in tạo lịch qua `POST /appointments` với `source=walk_in` → 200, chưa có `encounter` nào được tạo (đúng phạm vi đã chốt). Toàn bộ 100 test `apps/api` (90 cũ + 10 mới, không regress) pass. `pnpm -w lint/typecheck/build` sạch toàn workspace.
- Cập nhật `docs/TASK.md`, `docs/CURRENT.md`. Không đổi `.claude/docs/data-model.md`/`docs/ERD.md` (không có cột/bảng mới, `cancel_reason` đã có sẵn từ S2-05).

## 2026-08-11 (7)

S2-05 — Module `appointment` (APP-01/02/03: đặt lịch, chặn trùng khung giờ cùng bác sĩ ở tầng DB) — **đạt gate cuối Sprint 2**:

- **Migration `20260811064930_appointment_s2_05`** (sinh phần bảng/FK/index bằng `prisma migrate diff --from-schema-datasource ... --to-schema-datamodel ... --script`, chạy non-interactive vì môi trường không có TTY — thay cho `migrate dev --create-only` như các lần trước; diff thô kèm 3 dòng "drift giả" cùng loại đã gặp ở S2-02 — `DROP INDEX patient_tenant_id_phone_idx`, `DROP INDEX patient_tenant_id_search_key_trgm_idx`, `ALTER TABLE patient ALTER COLUMN search_key DROP DEFAULT` — đã bỏ, không đưa vào migration): bảng `appointment` đủ 8 cột bắt buộc + `patient_id`/`doctor_id`/`room_id` (nullable — xem `docs/DECISIONS.md` #026 điểm 1)/`scheduled_at`/`duration_minutes`/`status` (enum `appointment_status`: `SCHEDULED`/`CANCELLED`/`NO_SHOW`/`CONVERTED`)/`source` (enum `appointment_source`: `walk_in`/`phone`/`online`)/`cancel_reason`. Composite FK `(tenant_id, patient_id/doctor_id/room_id)` — `room` lần đầu bị composite FK tham chiếu tới nên thêm `UNIQUE(tenant_id, id)`.
- **C2 (docs/ERD.md mục 4)** — chống đặt trùng khung giờ cùng bác sĩ, kể cả hai lễ tân thao tác đồng thời (APP-03): `EXCLUDE USING gist (doctor_id WITH =, nexamed_appointment_slot_range(scheduled_at, duration_minutes) WITH &&) WHERE (status='SCHEDULED' AND deleted_at IS NULL)`. Hai trở ngại kỹ thuật gặp lúc viết, cả hai đã xác minh thật (không đoán) bằng cách chạy thử:
  1. Postgres từ chối `tstzrange(scheduled_at, scheduled_at + (duration_minutes * INTERVAL '1 minute'))` thẳng trong exclusion constraint — lỗi "functions in index expression must be marked IMMUTABLE" (toán tử `timestamptz + interval` đánh dấu `STABLE`). Bọc lại thành hàm SQL `nexamed_appointment_slot_range()` đánh dấu `IMMUTABLE` — cùng kỹ thuật `nexamed_unaccent_lower()` đã dùng ở migration `*_patient_search_s2_02`, an toàn vì interval ở đây luôn đơn vị phút (không tháng/ngày nên không có điểm mơ hồ do DST/lịch).
  2. Cần extension `btree_gist` — GiST thuần không có toán tử `=` cho kiểu `uuid` (`doctor_id`), cùng lý do dùng `btree_gin` cho GIN kết hợp uuid+trigram ở S2-02 nhưng đây là GiST.
  Migration lần đầu áp thất bại đúng ở bước tạo constraint (do trở ngại #1) — dùng `prisma migrate resolve --rolled-back` để đánh dấu lại, sửa SQL, áp lại thành công (chưa merge vào `main` nên không vi phạm quy tắc forward-only).
- **`AppointmentService.isExclusionViolation()`**: lỗi vi phạm exclusion constraint (SQLSTATE `23P01`) **không** rơi vào `PrismaClientKnownRequestError` với mã P-số như unique (`P2002`, patient) hay FK (`P2003`) — Prisma trả `PrismaClientUnknownRequestError` (message thô kèm nguyên văn lỗi Postgres) vì exclusion constraint không nằm trong bảng mã lỗi Prisma đã biết. Phát hiện thật qua chạy test đặt lịch trùng giờ đồng thời trước, không đoán từ tài liệu Prisma — kiểm `err instanceof Prisma.PrismaClientUnknownRequestError` + `message` chứa cả mã SQLSTATE lẫn tên constraint. Chi tiết đầy đủ cả 4 quyết định kỹ thuật của S2-05: `docs/DECISIONS.md` #026.
- **Lọc `data_scope=personal` THẬT lần đầu tiên của dự án** (`doctor.appointment.read/create/update/cancel = personal`, đã seed sẵn từ S1-04b) — khác `patient` (S2-01) coi `personal`/`department` tương đương `global` vì bảng đó không có "chủ sở hữu" (xem `docs/DECISIONS.md` #025). `appointment.doctor_id` là chủ sở hữu đúng nghĩa theo `.claude/docs/security-audit.md`: `create` chặn bác sĩ đặt hộ bác sĩ khác (403 `PERMISSION_DENIED`), `list` ép lọc `doctorId = actorId` bỏ qua query param client gửi, `getById` trả 404 (không phải 403) cho lịch hẹn ngoài phạm vi cá nhân — cùng triết lý "404 thay vì 403" đã dùng cho xuyên tenant (`.claude/docs/multi-tenancy.md`). Break-glass cho riêng tình huống này **chưa nối** vào `PermissionGuard` (guard hiện chỉ escalate khi scope=`none`) — để dành tới khi có yêu cầu nghiệp vụ cụ thể. Khuôn mẫu lọc thật này sẽ tái dùng cho `encounter`/`vital_sign`/`clinical_note` ở S3 (đúng dự đoán đã ghi ở `docs/DECISIONS.md` #025).
- **`packages/shared/src/appointment.ts`**: `appointmentStatusSchema`/`appointmentSourceSchema` (enum, mirror đúng Prisma enum theo `.claude/docs/coding-standards.md`), `createAppointmentRequestSchema` (mặc định `durationMinutes=15` theo PRD APP-02, chưa đọc `tenant_setting.slot_duration_minutes` vì module `tenant`/S2-07 chưa tồn tại), `appointmentSummarySchema`, `listAppointmentsQuerySchema`/`listAppointmentsResponseSchema` (cursor, không offset). `packages/core/src/errors/appointment-errors.ts`: `AppointmentSlotConflictError` (409, map trong `domain-exception.filter.ts`), `AppointmentInvalidReferenceError` (422 mặc định — FK violation khi `patientId`/`doctorId`/`roomId` không tồn tại hoặc thuộc tenant khác).
- **`apps/api/src/modules/appointment/`**: `AppointmentController` (`POST/GET/GET:id /api/v1/appointments`, `JwtAuthGuard`+`PermissionGuard`+`@RequirePermission('appointment', ...)`), `AppointmentService`, `AppointmentRepository` (chỗ duy nhất gọi Prisma cho bảng `appointment`, sắp `list()` theo `scheduledAt` thay vì `id` như `patient.list()` — mục đích phục vụ xem lịch, kèm `id` làm khoá phụ ổn định).
- **Test harness**: `apps/api/src/testing/tenant-fixture.ts` — `createTwoTenantFixture().cleanup()` thêm xoá bảng `appointment` (FK RESTRICT tới `patient`/`user_account`, phải xoá trước cả hai) — dùng chung cho mọi spec mới từ giờ.
- OpenAPI: `generate-openapi.ts` thêm 3 path `/appointments`, `/appointments/{id}`. Sinh lại `openapi.json` + `openapi-schema.d.ts` (web).
- **Đã xác minh thật** (`appointment-http.spec.ts`, 15/15 pass, HTTP e2e qua toàn bộ stack, testcontainers Postgres thật): **gate cuối Sprint 2** — hai request `POST /appointments` cùng payload gửi đồng thời qua `Promise.all` → đúng 1 trả `200`, 1 trả `409 APPOINTMENT_SLOT_CONFLICT` (không phải giả lập tuần tự); trùng giờ nhưng khác bác sĩ → cả hai tạo được; cùng bác sĩ nhưng giờ liền kề không chồng lấn → cả hai tạo được; bác sĩ (scope personal) tự đặt cho mình OK, đặt hộ bác sĩ khác → 403; bác sĩ chỉ thấy lịch của mình ở `list`/`getById` (lịch bác sĩ khác → 404); vai trò thiếu quyền (nurse) → 403; tenant B không thấy lịch hẹn tenant A → 404; `patientId` không tồn tại → 422 `APPOINTMENT_INVALID_REFERENCE`; thiếu trường bắt buộc → 400 `VALIDATION_ERROR`. Toàn bộ 90 test `apps/api` (75 cũ + 15 mới, không regress) + 25 test `packages/core` pass. `pnpm -w lint/typecheck/build` sạch toàn workspace.
- Cập nhật `docs/DECISIONS.md` (#026), `docs/TASK.md`, `docs/CURRENT.md`. Không đổi `.claude/docs/data-model.md`/`docs/ERD.md` (schema `appointment` dùng đúng những gì đã chốt, không có cột phát sinh thêm ngoài đó).

## 2026-08-11 (6)

S2-03 — Chống trùng "mềm" (PAT-03: cảnh báo trùng tên + ngày sinh khi tạo hồ sơ mới):

- **Hỏi chốt cách thiết kế API trước khi code** (PRD chỉ nói "cảnh báo, không chặn", không chốt cơ chế cụ thể): 2 phương án — (a) endpoint check riêng gọi trước khi tạo, hay (b) gộp cảnh báo vào response `POST /patients` sau khi đã tạo. Chủ dự án chọn (a) — không đổi contract `POST /patients` hiện có (đã test kỹ 16 test trước đó), tách rời bước "hỏi trước" khỏi bước "tạo thật".
- **`GET /api/v1/patients/check-duplicate?fullName=&dob=`**: `packages/shared/src/patient.ts` thêm `checkPatientDuplicateQuerySchema`/`checkPatientDuplicateResponseSchema` (`{ items: patientSummary[] }`). `PatientController.checkDuplicate()` khai báo **trước** `@Get(':id')` trong file — bắt buộc vì NestJS/Express khớp route theo thứ tự khai báo trong class, để sau thì `:id` sẽ nuốt mất `check-duplicate` (hiểu nhầm thành `id = "check-duplicate"`). Quyền yêu cầu `patient.read` (giống `list()`, không phải `patient.create`) vì về bản chất đây là một lượt tra cứu/lọc giống `GET /patients?q=`, không phải riêng cho hành động tạo.
- **`PatientRepository.findPossibleDuplicates()`**: khớp CHÍNH XÁC `searchKey` (không phải `contains` như tìm kiếm PAT-02/S2-02) + `dob` — mục đích là phát hiện hai hồ sơ tên gần như giống hệt nhau (đã bỏ dấu, viết thường) chứ không phải liệt kê mọi tên chứa từ khoá. Tái dùng nguyên cột generated `search_key` và hàm `stripVietnameseDiacritics` đã có từ S2-02, không viết logic chuẩn hoá tên lần hai. `take: 10` phòng vệ (nghiệp vụ thực tế số kết quả gần như luôn 0 hoặc 1).
- Endpoint chỉ đọc, không ghi gì — không tạo bản ghi, không audit (giống lý do `list()` không có `@AuditView`: đây không phải "xem 1 hồ sơ cụ thể", mà là một lượt tra cứu tổng hợp).
- Trùng CCCD **không** thuộc phạm vi việc này — đã chặn cứng bằng DB partial unique constraint (C3) từ S2-01, endpoint mới không xử lý lại.
- OpenAPI: `generate-openapi.ts` thêm `registry.registerPath` cho `/patients/check-duplicate`, đặt trước path `/patients/{id}` cho nhất quán với thứ tự controller. Sinh lại `openapi.json` + `openapi-schema.d.ts` (web).
- **Đã xác minh thật**: `patient-http.spec.ts` thêm 6 test (22/22 tổng pass) — trùng tên không dấu + không phân biệt hoa/thường + đúng ngày sinh → thấy hồ sơ đã có; đúng tên nhưng khác ngày sinh → không trùng; tên gần giống (thêm chữ) nhưng không khớp tuyệt đối → không trùng; gọi check-duplicate không tạo bản ghi mới nào (đếm số dòng trước/sau không đổi); cách ly tenant (tenant B không thấy bản ghi trùng của tenant A); thiếu `dob` → 400 `VALIDATION_ERROR`. Toàn bộ 75 test `apps/api` (69 cũ + 6 mới) + 25 test `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace.
- Cập nhật `docs/TASK.md`, `docs/CURRENT.md`. Không đổi `.claude/docs/data-model.md`/`docs/ERD.md` (không đổi schema, chỉ thêm 1 endpoint đọc trên bảng `patient` đã có).

## 2026-08-11 (5)

S2-02 — Tìm kiếm bệnh nhân (PAT-02: theo tên không dấu, số điện thoại, mã bệnh nhân):

- **`packages/core/src/search/strip-vietnamese-diacritics.ts`**: bỏ dấu tiếng Việt + viết thường ở tầng ứng dụng, dùng `String.prototype.normalize('NFD')` xoá combining diacritical mark (U+0300–U+036F) + tự thay riêng chữ "đ"/"Đ" (U+0111/U+0110 — không phải ký tự tổ hợp, `unaccent` chuẩn của Postgres bỏ sót). Viết bằng hex code point/`String.fromCharCode` thay vì gõ trực tiếp ký tự tổ hợp trong regex literal — phát hiện lúc viết: gõ trực tiếp ký tự combining mark vào regex character class bị vỡ mã hoá qua nhiều lớp công cụ (không phải lỗi TypeScript/Node, mà là lỗi truyền chuỗi trước khi tới trình biên dịch). Unit test 5/5, gồm đủ 5 thanh điệu + nguyên âm mở rộng.
- **Migration `20260811055006_patient_search_s2_02`**: bật extension `unaccent`, `pg_trgm`, `btree_gin` (`btree_gin` cần để một cột kiểu thường như `uuid` — `tenant_id` — tham gia cùng một GIN index với cột `gin_trgm_ops`, vì GIN thuần không có opclass cho `uuid`). Hàm SQL `nexamed_unaccent_lower(text)` bọc `unaccent()` (vốn `STABLE`, không dùng được trong generated column hay index biểu thức) thành `IMMUTABLE` — đánh đổi đã ghi rõ trong migration (chấp nhận vì dictionary `unaccent` gần như không đổi). Cột `patient.search_key TEXT GENERATED ALWAYS AS (nexamed_unaccent_lower(full_name)) STORED`. GIN kết hợp `(tenant_id, search_key gin_trgm_ops)` — đúng `docs/ERD.md` mục 5 "Index cần có từ đầu".
- **Sự cố phát hiện khi scaffold migration**: `prisma migrate dev --create-only` tự đề xuất `DROP INDEX patient_tenant_id_phone_idx` — Prisma coi index này là "drift" vì nó chỉ tồn tại ở raw SQL (migration S2-01), không có khai báo tương ứng trong `schema.prisma` (schema.prisma không có cú pháp cho partial index/GIN/generated column nên nhiều index của `patient` chỉ tồn tại ở raw SQL). Đã phát hiện và bỏ dòng DROP INDEX đó trước khi merge — bài học ghi vào comment migration: mọi `--create-only` sau này trên bảng có index raw-SQL-only đều phải soát kỹ, Prisma không "biết" những gì nó không quản lý qua schema.prisma.
- **Prisma quirk khác**: khai `searchKey` là `String` (không nullable) trong `schema.prisma` khiến `PatientUncheckedCreateInput` bắt buộc phải truyền field này lúc `create()` — vô lý với generated column (DB tự tính, ghi vào sẽ lỗi, và Prisma không có khái niệm "generated column chỉ đọc"). Sửa bằng khai `String?` dù thực tế DB không bao giờ null (đã ghi rõ lý do trong comment schema).
- **`PatientRepository.list()`/`PatientService.listPatients()`**: mở rộng nhận `search` — `q` gốc khớp `patientCode`/`phone` (`startsWith`), bản đã chuẩn hoá qua `stripVietnameseDiacritics` khớp `searchKey` (`contains`). `listPatientsQuerySchema` (`packages/shared`) thêm field `q` (optional, max 100 ký tự).
- **Đo hiệu năng thật với 50.000 bản ghi** (`apps/api/scripts/perf-patient-search.ts`, script thủ công — `pnpm --filter @nexamed/api run perf:patient-search`, không chạy trong CI vì seed 50k dòng tốn thời gian): bulk INSERT 50k dòng "nhiễu" tên tiếng Việt ngẫu nhiên qua `generate_series` (nhanh hơn nhiều lần so với gọi `PatientRepository.create()` 50k lần), cộng 1 dòng "kim" cần tìm. Cả 3 kiểu tìm (tên/SĐT/mã) đều dưới 35ms qua đúng role app (`DATABASE_URL`, có RLS) — đạt mục tiêu "<1 giây" của `docs/product/prd.md` mục 5 với biên độ lớn.
  - **Phát hiện đáng chú ý**: lần đo đầu tiên (ngay sau bulk insert, chưa `ANALYZE`) planner chọn `patient_tenant_id_id_active_idx` (btree) thay vì GIN — do thống kê bảng còn cũ (gần như rỗng) từ trước khi bulk insert. Thêm `ANALYZE patient` sau bulk insert để mô phỏng đúng trạng thái ổn định của dữ liệu thật — sau đó planner chuyển sang **Seq Scan**, vẫn không dùng GIN. Đây là quyết định ĐÚNG của Postgres cho quy mô 50k dòng/tenant (bảng đủ nhỏ để nằm gọn trong shared buffers — quét tuần tự rẻ hơn tra index ngẫu nhiên), không phải lỗi index. Xác nhận GIN trigram index tồn tại và dùng được thật bằng cách ép `SET LOCAL enable_seqscan = off` — `EXPLAIN ANALYZE` xác nhận đúng `Bitmap Index Scan` trên `patient_tenant_id_search_key_trgm_idx`. Planner sẽ tự chuyển sang dùng GIN khi dữ liệu đủ lớn (nhiều tenant cộng dồn hoặc một tenant lâu năm) mà không cần đổi code — script ném lỗi nếu GIN index không dùng được thật khi ép chọn, để lần đo sau tự phát hiện nếu index bị hỏng.
- **Đã xác minh thật**: `patient-http.spec.ts` thêm 6 test tìm kiếm (16/16 tổng pass) — tìm không dấu, tìm nguyên bản có dấu, tìm SĐT (prefix), tìm mã bệnh nhân (prefix, không phân biệt hoa/thường), không khớp gì → mảng rỗng không lỗi, cách ly tenant khi tìm kiếm (tenant B không thấy bệnh nhân tenant A dù cùng từ khoá). Testcontainers cũng áp migration mới thành công từ đầu (xác nhận 3 extension không phụ thuộc cài đặt thủ công trên máy dev). Toàn bộ 69 test `apps/api` + 25 test `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. OpenAPI (`openapi:generate`) + web type (`api:codegen`) đã sinh lại cho query param `q` mới.
- Cập nhật `docs/TASK.md`, `docs/CURRENT.md`. Không đổi `.claude/docs/data-model.md`/`docs/ERD.md` (không đổi cấu trúc cột nghiệp vụ, `search_key` đã nằm trong kế hoạch mục Index từ trước).
- **Kế hoạch Tiếp nhận (Sprint 3)**: đã trao đổi và chốt sơ bộ với chủ dự án (chưa code) — xây đủ module `appointment` (S2-05/06) + màn hình lịch (S2-09) trước Tiếp nhận, đúng thứ tự `docs/product/plan.md`, không đi đường tắt "chỉ walk-in tối thiểu". Snapshot thẻ BHYT lúc check-in tạm luôn `self_pay=true` tới khi làm S2-04 (lùi lại sau Sprint 3, không chặn tiến độ Tiếp nhận). Đã trình bày plan schema/UI dựa đúng theo `docs/ERD.md`/`.claude/docs/clinical-workflow.md` đã chốt, không đề xuất trường mới.

## 2026-08-11 (4)

S2-01 — Module `patient` (CRUD, mã hoá CCCD) + `PermissionGuard` thật đầu tiên (Sprint 1 → Sprint 2):

- **Trước khi viết migration**: phát hiện `docs/ERD.md` (sơ đồ mermaid) lệch với `.claude/docs/data-model.md` (mô tả chi tiết) cho bảng `patient` — thiếu `address_json` (PRD PAT-01, P0) và `identity_verified_at` (đi cùng `global_patient_ref`, liên-tenant v3+). Hỏi và xác nhận với chủ dự án trước khi tự chọn (theo `CLAUDE.md` — cấu trúc đã chốt lệch nhau thì dừng lại hỏi): thêm cả hai cột, sửa `docs/ERD.md` khớp lại (v1.3). Ghi `docs/DECISIONS.md` #024.
- **Migration `20260811024047_patient_s2_01`**: bảng `patient` đủ 8 cột bắt buộc + cột đặc thù (`patient_code`, `full_name`, `dob`, `gender`, `phone`, `national_id_enc`, `national_id_hash`, `address_json`, `allergy_note`, `merged_into_id`, `global_patient_ref`, `identity_verified_at`), RLS, `CHECK(version>=1)`, `UNIQUE(tenant_id, patient_code)`, partial `UNIQUE(tenant_id, national_id_hash) WHERE national_id_hash IS NOT NULL` (C3 — viết raw SQL, Prisma `@@unique` không biểu diễn được điều kiện `WHERE`), index `(tenant_id, phone)`, composite FK tự tham chiếu `merged_into_id` (chưa dùng, để sẵn PAT-04/S2-03).
- **Mã hoá PII**: `apps/api/src/infrastructure/crypto/pii-encryption.ts` — `encryptPii`/`decryptPii` (AES-256-GCM, khoá từ `ENCRYPTION_KEY` chuẩn hoá 32 byte qua SHA-256), `hashForLookup` (HMAC-SHA256, tra trùng chính xác không cần giải mã hàng loạt). Viết cho tái dùng ngay từ đầu — sẽ dùng lại cho `insurance_card.card_no` ở S2-04.
- **Mã hiển thị**: `apps/api/src/infrastructure/persistence/code-sequence.repository.ts` (đăng ký global trong `PersistenceModule`, dùng chung mọi domain sinh mã tuần tự) — cấp số bằng `INSERT ... ON CONFLICT DO UPDATE` atomic. `packages/core/src/code-sequence/format-display-code.ts` — định dạng `<prefix><yyMM><seq6>` (`BN2608000001`), tính tháng theo giờ Việt Nam (UTC+7 cố định, không dùng thư viện timezone) thay vì UTC server — tránh sai tháng ở khung nửa đêm tới 7h sáng UTC (đúng CLAUDE.md: "không dùng `new Date()` phía server để cắt mốc ngày").
- **`PermissionGuard` thật** (`apps/api/src/common/permission.guard.ts` + `require-permission.decorator.ts` + `infrastructure/persistence/permission-lookup.helper.ts`) — việc treo từ Sprint 1 (docs/TASK.md "Đang chờ"), giờ hiện thực vì `patient` là controller nghiệp vụ đầu tiên. Đọc `role_permission` gộp theo mọi vai trò user đang giữ (`packages/core/src/rbac/data-scope.ts` — `maxDataScope`, unit test 3/3); `none` → thử `BreakGlassService.tryConsume()` nếu route có `entityIdParam` (chỉ áp dụng route thao tác một bản ghi cụ thể), không có phiên hợp lệ thì `403 { error: { code: 'PERMISSION_DENIED', details: { breakGlassAvailable: true } } }`. `IamModule` đổi thành `@Global()` — lý do kỹ thuật: Nest resolve dependency của `PermissionGuard` (đặt ở `CommonModule`, cũng global) theo context của module ĐANG DÙNG guard (`PatientModule`), không phải module định nghĩa guard, nên `BreakGlassService` phải tự nó global thay vì chỉ "chảy qua" `CommonModule.imports` — phát hiện qua lỗi DI thật lúc chạy test lần đầu, không phải suy đoán trước.
- **`DomainExceptionFilter` mở rộng**: nhánh `HttpException` chung giờ gói field lạ ngoài `message`/`code`/`statusCode`/`error` mặc định của Nest vào `error.details` (phục vụ `breakGlassAvailable`) — không đổi hình dạng response cho exception không có field lạ (đã xác minh qua toàn bộ test cũ vẫn pass nguyên).
- **DTO** (`packages/shared/src/patient.ts`): `createPatientRequestSchema`, `updatePatientRequestSchema` (bắt buộc kèm `version`), `patientSummarySchema` (danh sách — không lộ CCCD, chỉ `hasNationalId`), `patientDetailSchema` (chi tiết — kèm CCCD đã giải mã), `listPatientsQuerySchema`/`listPatientsResponseSchema` (cursor, không offset — theo `.claude/docs/architecture.md`). `packages/core/src/errors/`: `PatientDuplicateNationalIdError` (409), `ConcurrentModificationError` (409, dùng chung mọi module, không riêng `patient`).
- **`PATCH /patients/:id`**: optimistic locking qua `updateMany` + kiểm `count` thay vì `update()` (Prisma `update()` chỉ nhận unique field làm điều kiện, không ghép thêm được `version` vào cùng `WHERE`).
- **API + web**: `PatientController` (`POST/GET/GET:id/PATCH:id /api/v1/patients`), tất cả qua `JwtAuthGuard` + `PermissionGuard` + `@RequirePermission()`; `GET :id` thêm `@AuditView('patient')` (lần dùng thật đầu tiên của `AuditViewInterceptor`, viết từ S1-05). OpenAPI (`apps/api/scripts/generate-openapi.ts`, S1-09) thêm 4 path — cần `extendZodWithOpenApi(z)` trước khi dùng `request.params`/`request.query` (khác `request.body`, không cần) mới phát hiện lúc chạy generator lần đầu cho path có param. Sinh lại `openapi.json` + `openapi-schema.d.ts` (web).
- **Đã xác minh thật** (không mock, Postgres thật qua testcontainers): `patient-http.spec.ts` (10/10, HTTP e2e qua toàn bộ stack) — tạo hồ sơ có/không CCCD, CCCD mã hoá thật ở DB (kiểm tra trực tiếp cột `national_id_enc` không chứa plaintext), trùng CCCD → 409, danh sách không lộ CCCD, optimistic lock (version cũ → 409 `CONCURRENT_MODIFICATION`, version đúng → 200), vai trò thiếu quyền (`doctor` thiếu `patient.create`, `system_admin` không có `patient.*` nào) → 403 kèm `breakGlassAvailable`, user tenant B (có `patient.read=global`) gọi ID của tenant A → 404 không phải 403 (đúng cả `PermissionGuard` lẫn RLS/lọc `tenantId` ở repository). `code-sequence.repository.spec.ts` (3/3 — tăng dần đúng, tenant riêng biệt không cộng dồn, cấp đồng thời 10 lần không trùng số). `pii-encryption.spec.ts` (5/5). `data-scope.spec.ts`, `format-display-code.spec.ts` (`packages/core`). Toàn bộ 63 test `apps/api` + 20 test `packages/core` pass, không regress test Sprint 1. `pnpm -w lint/typecheck/build` sạch toàn workspace.
- Cập nhật `docs/DECISIONS.md` (#024), `docs/TASK.md`, `docs/CURRENT.md`, `docs/ERD.md` (v1.3), `.claude/docs/data-model.md`, `.claude/docs/security-audit.md` (mục Data Scope — mô tả `PermissionGuard` đã hiện thực).

## 2026-08-11 (3)

S1-09 — API client sinh từ OpenAPI + TanStack Query cache-key infra (Sprint 1 hoàn tất toàn bộ 11 việc):

- `apps/api/scripts/generate-openapi.ts`: dựng `OpenAPIRegistry` (`@asteasolutions/zod-to-openapi@^7.3.0` — ghim bản 7 vì bản 9 mới nhất yêu cầu peer `zod@^4`, dự án đang ghim `zod@^3.23.8`) trực tiếp từ các Zod schema đã có ở `@nexamed/shared` (`loginRequestSchema`, `loginResponseSchema`, `refreshResponseSchema`, `logoutResponseSchema`, `meResponseSchema`, `breakGlassRequestSchema`, `breakGlassResponseSchema`) — không viết lại contract theo trí nhớ, đúng `.claude/docs/coding-standards.md`. Không dùng `@nestjs/swagger` (scan decorator trên DTO class) vì hệ thống validate bằng `schema.parse(body)` ngay trong controller, không có DTO class nào để scan. Script xuất `apps/api/openapi/openapi.json` (5 endpoint: `/auth/login|refresh|logout|me`, `/break-glass`, đúng envelope `{data,meta}`/`{error}` theo `architecture.md`, có `securitySchemes.bearerAuth`). Chạy: `pnpm --filter @nexamed/api run openapi:generate`.
- `packages/shared/src/auth.ts`: thêm `refreshResponseSchema`, `logoutResponseSchema` — hai response trước đây (`/auth/refresh`, `/auth/logout`) trả object ad-hoc trong controller, không có schema Zod tương ứng để làm nguồn cho registry.
- `apps/web`: `openapi-typescript` sinh `src/shared/api/openapi-schema.d.ts` từ `apps/api/openapi/openapi.json` (không cần API đang chạy — chạy: `pnpm --filter @nexamed/web run api:codegen`). `src/shared/api/client.ts` viết lại hoàn toàn bằng `openapi-fetch` — thay wrapper `fetch` tối giản tự viết ở S1-08: giữ nguyên `ApiError` và việc bóc envelope `{data,meta}`/`{error}` (hàm `unwrap()`), nhưng đính `Authorization: Bearer` qua `openapi-fetch` middleware (`setAccessToken()`) thay vì truyền tham số `accessToken` ở từng lời gọi như trước. `src/shared/api/query-keys.ts`: factory cache key `[tenantId, domain, ...]` đúng quy ước `.claude/docs/architecture.md` mục "Luồng dữ liệu phía web" — chưa có nơi dùng thật (auth vẫn là session state qua Zustand theo quyết định đã có ở S1-08, không phải server entity data), sẵn sàng cho hook `useQuery` của `patient`/`appointment` ở S2.
- `apps/web/src/features/auth/auth.api.ts` đổi chữ ký `getMe()` (bỏ tham số `accessToken`); `apps/web/src/app/AppBootstrap.tsx` cập nhật theo.
- **Quyết định commit cả `openapi.json` lẫn `openapi-schema.d.ts`** vào repo (không gitignore) — xem `docs/DECISIONS.md` #023: `apps/web` phải build được độc lập, không phụ thuộc `apps/api` đang chạy hay đã build trước (chưa có bước CI nào đảm bảo thứ tự đó).
- **Đã xác minh thật**: `pnpm -w lint/typecheck/build` sạch toàn workspace; 45 test `apps/api` + 14 test `packages/core` pass nguyên, không đổi hành vi backend (chỉ thêm 2 schema Zod). Chạy `pnpm dev` thật (api :3000 + web :5173) + Playwright (headless Chromium, cài tạm ở scratchpad — môi trường vẫn chưa có project skill `run`) xác nhận qua ảnh chụp màn hình: đăng nhập đúng → Dashboard; F5 (reload) giữ nguyên phiên qua đúng luồng `refresh` → `/auth/me` bằng client mới; đăng xuất → `/login`; kiểm `console --errors` không có lỗi ngoài dự kiến (chỉ 401 ở lần `/auth/refresh` đầu tiên khi trang chưa từng có phiên, giống hành vi đã ghi nhận ở S1-08).
- Cập nhật `docs/DECISIONS.md` (#023), `docs/TASK.md`, `docs/CURRENT.md` — Sprint 1 nay đã xong toàn bộ 11 việc (S1-01 → S1-09), sẵn sàng Sprint 2.

## 2026-08-11 (2)

S1-08 — Web app shell (router, luồng đăng nhập, layout, menu, design token):

- Trước khi code, hỏi chốt với chủ dự án về luồng/menu (yêu cầu mới, ghi vào memory): menu sidebar kết hợp theo luồng nghiệp vụ + ẩn/hiện theo vai trò; có Dashboard chung dạng empty-state; Quản trị là mục sidebar riêng (không gộp avatar) chỉ hiện với `clinic_admin`/`system_admin`; chỉ hiện menu module đã có backend thật; màu/token theo `.claude/docs/ui-guidelines.md`; `tenantId` nạp runtime qua `config.json`. Đọc `docs/design/UI_GUIDELINE.md` theo yêu cầu chủ dự án — dùng làm tham khảo cho cấu trúc menu/layout (sidebar cố định không collapse, không "3 card ngang bằng nhau"...), không dùng bảng màu/font riêng của tài liệu đó.
- **Backend mở rộng nhỏ** (`docs/DECISIONS.md` #022, tiền đề bắt buộc cho menu theo vai trò): `packages/shared/src/auth.ts` — `loginResponseSchema.user` thêm `roles: UserRole[]`, tách `currentUserSchema` dùng chung với response mới. `apps/api/src/modules/iam/`: `user-account-auth.repository.ts` thêm `findRoleNamesForUser()`; `auth.service.ts` thêm `getCurrentUser()`, `login()` trả kèm `roles`; `auth.controller.ts` thêm `GET /auth/me` (`JwtAuthGuard` — lần dùng thật thứ hai sau `POST /break-glass`) phục vụ khôi phục danh tính lúc web reload trang (chỉ còn refresh cookie). Không migration mới. Test mở rộng: `auth.spec.ts` (11/11, seed vai trò `doctor`), `auth-login-http.spec.ts` (5/5, seed vai trò `nurse` + test `/auth/me` có/không token).
- **Frontend** — `apps/web` từ bare Vite+React scaffold thành app shell đầy đủ:
  - Dependencies: `react-router-dom` v7, `@phosphor-icons/react` (khai báo tường minh, trước chỉ có ở root), Tailwind **v4** qua `@tailwindcss/vite` — phát hiện lúc cài rằng v4 không cần `tailwind.config.js`/`postcss.config.js` như giả định ban đầu (kế hoạch viết cho v3), chỉ cần `@import 'tailwindcss'` trong CSS + plugin Vite; cùng lớp class Tailwind utility nên không ảnh hưởng token đã chốt.
  - `apps/web/public/config.json` (gitignore) + `config.example.json`: runtime config `{apiBaseUrl, tenantId}`, Vite copy nguyên vào `dist/` — sửa trực tiếp lúc deploy on-prem không cần rebuild.
  - `apps/web/src/shared/api/client.ts`: wrapper `fetch` tối giản (`credentials:'include'` cho cookie refresh, parse envelope `{data,meta}`/`{error}`) — chỉ đủ cho auth flow, S1-09 thay hẳn bằng client sinh từ OpenAPI + TanStack Query.
  - `apps/web/src/features/auth/`: Zustand `auth.store.ts` (session state, không phải server entity — đúng `.claude/docs/architecture.md`), `auth.api.ts`, `LoginPage.tsx` (theo `ui-guidelines.md` mục 4.1: required `*` đỏ, focus ring, nút loading), `RequireAuth.tsx`.
  - `apps/web/src/app/AppBootstrap.tsx`: khôi phục phiên lúc app khởi động — gọi `/auth/refresh` (cookie) rồi `/auth/me` (roles).
  - `apps/web/src/shared/layout/`: `AppShell.tsx` (sidebar `w-60 bg-slate-900` cố định + nội dung `max-w-[1400px]`), `Sidebar.tsx` (active state `weight="fill"`, Quản trị lọc theo vai trò), `UserMenu.tsx`.
  - `apps/web/src/features/dashboard/DashboardPage.tsx`, `.../admin/AdminPage.tsx`: empty-state đúng `ui-guidelines.md` mục 3 (không bịa số liệu).
  - `apps/web/src/shared/ui/`: `Button.tsx`, `EmptyState.tsx` dùng chung.
  - Dùng skill `ui-ux-pro-max` (theo yêu cầu chủ dự án) tra cứu icon/layout/empty-state cho enterprise dashboard mật độ dữ liệu cao — chỉ lấy phần layout/spacing/icon, không lấy bảng màu (đã có token riêng).
- **Sự cố kỹ thuật gặp và xử lý lúc build**:
  - `@nestjs/testing` bị pnpm resolve nhầm bản `11.x` (dự án dùng NestJS 10.4) — gỡ, cài lại ghim `^10.4.15`.
  - `vite.config.ts` build lỗi `ESM-only` vì `@tailwindcss/vite` là gói ESM thuần nhưng Vite bundle config theo CJS — đổi tên file sang `vite.config.mts` (cách sửa chính thức của Vite).
  - `main.tsx`: TypeScript không narrow được `HTMLElement | null` xuyên qua closure của hàm `bootstrap()` khai báo riêng — sửa bằng truyền `rootElement` làm tham số thay vì đọc biến ngoài closure.
- **Phát hiện + sửa 1 race condition lúc kiểm bằng trình duyệt thật** (không phải bug nghiệp vụ, chỉ dev-only): React StrictMode gọi effect 2 lần khiến `AppBootstrap` bắn 2 request `/auth/refresh` gần đồng thời, có thể đụng cơ chế phát hiện reuse-token (S1-04, thu hồi toàn bộ phiên khi token đã rotate bị dùng lại) — chặn bằng `useRef` guard đảm bảo effect chỉ chạy thật một lần.
- **Xác minh thật bằng trình duyệt**: môi trường chưa có project skill `run` cho dự án này — tự cài Playwright + Chromium headless (npm trong thư mục scratchpad, không thêm vào dependencies của dự án), chạy `pnpm dev` thật (api :3000 + web :5173), tạo 1 tenant + user `clinic_admin` thử qua script tạm (xoá sau khi xong, không commit), lái trình duyệt qua toàn bộ luồng: chưa đăng nhập → `/login`; đăng nhập đúng → Dashboard đúng token màu, thấy đủ Tổng quan + Quản trị; empty-state đúng cả hai trang; F5 giữ nguyên phiên; đăng xuất → `/login`. Chụp ảnh màn hình xác nhận từng bước, kiểm `console --errors` không có lỗi thật (chỉ 401 dự kiến ở lần refresh đầu tiên khi chưa có phiên).
- `pnpm -w lint/typecheck/build` sạch toàn workspace; `apps/api` 45 test pass (11 auth.spec.ts + 5 auth-login-http.spec.ts, còn lại giữ nguyên).
- Cập nhật `docs/DECISIONS.md` (#022), `docs/TASK.md`, `docs/CURRENT.md`.

## 2026-08-11

S1-07 — Test harness cách ly tenant (testcontainers + HTTP e2e đầu tiên):

- `apps/api/src/testing/global-setup.ts`: Vitest `globalSetup` — dựng Postgres 18 tạm bằng `@testcontainers/postgresql`, chạy `prisma migrate deploy` thật lên đó, set `DATABASE_URL`/`MIGRATE_DATABASE_URL` TRƯỚC khi Vitest tách worker chạy từng file test. Vì 6 spec đã có (`tenant-isolation`, `rbac`, `auth`, `break-glass`, `audit-view.interceptor`, `jwt-auth.guard`) đều đọc hai biến này qua `process.env`/Prisma datasource mặc định, không phải sửa lại file nào — `pnpm test` giờ tự dựng DB sạch, không cần `docker compose up -d` trước. CI service Postgres (`db:check-schema`/`db:deploy`/`db:seed`) giữ nguyên, không đụng — vẫn xác minh migration/seed chạy được độc lập.
- `apps/api/src/testing/tenant-fixture.ts`: `createTwoTenantFixture()` — helper tạo 2 tenant + `cleanup()` đúng thứ tự FK, thay cho boilerplate lặp lại 4 lần ở các spec cũ (không retrofit spec cũ — dùng cho test mới từ S2 trở đi).
- `apps/api/src/modules/iam/auth-login-http.spec.ts`: test HTTP e2e thật đầu tiên của dự án — boot toàn bộ `AppModule` qua `@nestjs/testing` (đúng wiring của `main.ts`: prefix, cookie-parser, interceptor, filter) rồi gọi qua supertest, khác `auth.spec.ts` (S1-04) gọi thẳng service bỏ qua controller/guard/filter. Dùng làm template cho endpoint nghiệp vụ đầu tiên ở S2.
- Hai vấn đề hạ tầng test mới lộ ra khi viết test HTTP e2e đầu tiên (đã xác minh cả hai chỉ là artifact môi trường Vitest, KHÔNG phải bug thật — kiểm bằng `node dist/main.js` + `curl` trên production build):
  1. **DI âm thầm ra `undefined`**: Vitest transform TypeScript bằng esbuild mặc định, esbuild không emit `design:paramtypes` (decorator metadata) mà NestJS DI cần để suy ra kiểu tham số constructor không có `@Inject()` tường minh. `Test.createTestingModule` vẫn compile thành công nhưng inject `undefined` thay vì lỗi rõ ràng. Sửa bằng `unplugin-swc` + `apps/api/.swcrc` (`decoratorMetadata: true`) thay esbuild — đúng khuyến nghị chính thức NestJS (docs.nestjs.com/recipes/swc mục Vitest). Chỉ ảnh hưởng lúc test; build production vẫn `tsc`/`nest build` như cũ.
  2. **`exception instanceof ZodError` sai qua ranh giới package**: `loginRequestSchema` định nghĩa ở `@nexamed/shared` (build sẵn CommonJS), còn `domain-exception.filter.ts` ở `apps/api` (transform qua SWC/ESM lúc test) — hai bên nạp gói `zod` qua hai đường module khác nhau trong Vitest, khiến `instanceof` sai dù đúng là `ZodError`. Đổi `apps/api/src/common/domain-exception.filter.ts` sang `isZodError()` kiểm theo cấu trúc (`err.name === 'ZodError'` + `issues` là mảng) thay vì `instanceof` — bền vững hơn qua mọi ranh giới module/bundler, không chỉ vá cho môi trường test.
- Thêm devDependencies `apps/api`: `@testcontainers/postgresql`, `@nestjs/testing` (ghim đúng `^10.4.x` khớp NestJS 10.4 — lần đầu cài bị pnpm resolve nhầm `11.x`, đã sửa lại), `supertest`, `@types/supertest`, `unplugin-swc`, `@swc/core`.
- Cũng thêm `import 'reflect-metadata'` vào `vitest.setup.ts` (trước nay chỉ `main.ts` cần vì các test trước giờ đều `new Service(...)` tay, không qua NestJS DI container thật).
- **Đã xác minh thật**: toàn bộ 41 test `apps/api` pass (38 cũ giữ nguyên hành vi + 3 mới), chạy lại nhiều lần ổn định không flaky; lint/typecheck/build sạch toàn workspace.
- Cập nhật `docs/TASK.md`, `docs/CURRENT.md`.

## 2026-08-10 (5)

S1-06 — Hoàn thiện `packages/core` (khung entity + 5 port còn lại + đăng ký DI):

- `packages/core/src/entity/base-entity.ts`: `BaseEntity` (8 cột bắt buộc), `AppendOnlyEntity` (audit_log/break_glass_session), `SignableEntity` (dữ liệu lâm sàng ký được) + hàm thuần `isSigned()` — nguồn sự thật duy nhất cho quy tắc "bản ghi đã ký bất biến" (`.claude/docs/clinical-workflow.md`), unit test 2/2.
- `packages/core/src/ports/`: thêm 5 interface còn lại (`NotificationPort` đã có từ S1-04c) — `StoragePort`, `EventBusPort`, `SignaturePort`, `InsuranceGatewayPort`, `PatientIdentityPort`. Mỗi port có docstring trỏ về đúng mục trong `.claude/docs/project-structure.md`/`multi-tenancy.md`/`security-audit.md`/`clinical-workflow.md` giải thích lý do tồn tại và adapter v1 tương ứng.
- `apps/api/src/infrastructure/`: 5 adapter — `storage/local-disk.adapter.ts` (ghi/đọc/xoá file thật dưới `STORAGE_DIR/<tenantId>/<key>`, chặn key chứa `..`), `eventbus/in-memory.adapter.ts` (publish/subscribe đồng bộ, handler lỗi ném lên để service rollback transaction gốc — đúng quy ước ở `.claude/docs/coding-standards.md` mục Event), `signature/noop.adapter.ts` (chữ ký logic, `signaturePayload: null`), `insurance/noop.adapter.ts` (ném `NOT_IMPLEMENTED`, đúng bảng port/adapter đã chốt), `patient-identity/same-tenant.adapter.ts` (trả chính `patient.id`). `ports.module.ts` (`@Global()`) đăng ký DI cho cả 5 — không đụng `NOTIFICATION_PORT` (vẫn ở `IamModule` như S1-04c), wire vào `AppModule`.
- Thêm `STORAGE_DIR` vào `apps/api/src/config/env.schema.ts` (mặc định `./storage`) + `.env.example`; thêm `/storage/` vào `.gitignore`.
- Không có folder `patient-identity/` sẵn trong cây thư mục minh hoạ ở `project-structure.md` (chỉ liệt kê storage/eventbus/signature/insurance/notification) — tạo thêm đúng theo pattern "một thư mục cho mỗi port" đã có sẵn cho 5 port kia, không phải suy diễn lại cấu trúc đã chốt.
- Test thật (không mock): `base-entity.spec.ts` (2/2), `local-disk.adapter.spec.ts` (4/4, ghi/đọc/xoá file thật trên disk tạm + chặn path traversal), `in-memory.adapter.spec.ts` (3/3, đúng thứ tự handler + rollback khi handler lỗi). Build thật (`node dist/main.js`) xác nhận `PortsModule dependencies initialized` + `Nest application successfully started` qua NestJS DI container thật — khác các test trước chỉ `new Service(...)` tay bỏ qua Nest, nên đây là lần đầu DI thật của `AppModule` được xác minh chạy được từ đầu đến cuối.
- Toàn bộ 52 test trên workspace (14 `packages/core` + 38 `apps/api`, cộng 9 test mới trong đó) pass; lint/typecheck/build sạch toàn workspace.
- Cập nhật `docs/TASK.md`, `docs/CURRENT.md`.

## 2026-08-10 (4)

S1-05 — Audit log (interceptor + helper dùng chung):

- Không có gì mới ở bảng `audit_log`/quyền DB — đã xong từ S1-03 (append-only, `nexamed_app` chỉ `INSERT`/`SELECT`).
- Làm rõ và ghi lại một điểm kiến trúc quan trọng (`docs/DECISIONS.md` #021): một NestJS interceptor chạy ở tầng HTTP không thể tham gia vào transaction Prisma mà service tự mở/đóng kín bên trong chính nó (`UnitOfWorkService.runInTenantScope` là callback trọn vẹn) — nên "interceptor ghi audit cho mọi thao tác" theo cách đọc chữ nghĩa ban đầu của `docs/product/plan.md` S1-05 là không khả thi an toàn cho thao tác **ghi**. Chốt hai cơ chế: ghi → lời gọi tường minh trong transaction (đã dùng đúng ở S1-04/S1-04c); xem → interceptor thật (không có transaction nghiệp vụ nào để đồng bộ cùng nên an toàn).
- Dời `writeAuditLog` từ `apps/api/src/modules/iam/audit-log.helper.ts` sang `apps/api/src/infrastructure/persistence/audit-log.helper.ts` — đây là hạ tầng cross-cutting mọi module domain tương lai (patient/appointment/encounter/prescription, S2+) đều cần gọi, để trong `modules/iam/` buộc module khác import xuyên qua domain `iam`, sát ranh giới cấm trong `coding-standards.md`. Cập nhật import ở `auth.service.ts`, `break-glass.service.ts` — hành vi giữ nguyên, test cũ (`auth.spec.ts`, `break-glass.spec.ts`) pass y nguyên sau khi dời.
- `apps/api/src/common/`: `audit-view.decorator.ts` (`@AuditView(entityType, {paramName?})`), `audit-view.interceptor.ts` (`AuditViewInterceptor` — đọc `tenantId`/`actorId` từ `tenantContextStorage` đã có sẵn từ JWT, `entityId` từ route param; dùng `mergeMap` không phải `tap` để đợi ghi audit xong mới trả response và để lỗi ghi audit nổi lên; handler lỗi thì không audit; không có metadata `@AuditView` thì passthrough). Đăng ký vào `CommonModule` — **chưa áp vào controller nào** (chưa có controller nghiệp vụ, S2).
- Cập nhật `.claude/docs/security-audit.md` mục Audit log: ghi rõ quy ước hai cơ chế trên cho S2 trở đi theo, tránh mỗi module tự nghĩ cách khác nhau.
- Integration test thật trên Postgres (`audit-view.interceptor.spec.ts`, 3/3 pass, không mock): handler thành công + có `@AuditView` → ghi đúng 1 dòng `<entityType>.viewed`; handler ném lỗi → không ghi audit; không có `@AuditView` → passthrough không ghi audit.
- Toàn bộ 31 test trên `apps/api` (bao gồm 3 test mới) pass; typecheck/lint/build sạch toàn workspace.

## 2026-08-10 (3)

S1-04c — Break-glass (vượt quyền tạm thời):

- Không cần migration mới: `break_glass_session` đã đủ cột/RLS/revoke `UPDATE` từ migration `*_rbac_data_scope` (S1-04b).
- `packages/core/src/iam/break-glass.ts`: hằng số `DEFAULT_BREAK_GLASS_DURATION_MINUTES=120`, hàm thuần `computeExpiresAt`/`isSessionActive`, unit test 3/3. `packages/core/src/ports/notification.port.ts`: `NotificationPort` + DI token `NOTIFICATION_PORT` — chỉ 1/6 port của S1-06 được kéo lên trước (giống cách S1-04 đã kéo trước một phần `errors/`), 5 port còn lại vẫn để dành S1-06.
- `apps/api/src/infrastructure/notification/noop.adapter.ts`: adapter no-op (chỉ log `tenantId`/`type`, không log nội dung).
- `apps/api/src/modules/iam/`: `break-glass.repository.ts` (đọc/ghi `break_glass_session` + đọc `tenant_setting` key `break_glass_duration_minutes` với fallback mặc định), `break-glass.service.ts` (`request()`: xác thực lại mật khẩu — tái dùng `UserAccountAuthRepository.findById` + `InvalidCredentialsError` đã có từ S1-04, không tạo lỗi mới; `tryConsume()`: primitive kiểm tra phiên còn hiệu lực + ghi `audit_log` `break_glass.access`, tự mở transaction riêng vì một guard tương lai không có sẵn transaction của service nghiệp vụ), `break-glass.controller.ts` (`POST /break-glass`, `@UseGuards(JwtAuthGuard, ThrottlerGuard)` — lần đầu `JwtAuthGuard` viết ở S1-04 được dùng thật).
- `packages/shared/src/break-glass.ts`: Zod `breakGlassRequestSchema`/`breakGlassResponseSchema`.
- Integration test thật trên Postgres (`break-glass.spec.ts`, 6/6 pass, không mock — cùng pattern `auth.spec.ts`): đúng mật khẩu tạo phiên đúng hạn (mặc định 120 phút và theo `tenant_setting` tuỳ chỉnh), sai mật khẩu không tạo phiên/không gọi `NotificationPort`, `tryConsume` đúng entity trong hạn → `granted:true` + có dòng audit, entity khác/hết hạn → `granted:false`.
- Kiểm thử thủ công qua curl trên server thật: không có access token → 401; sai mật khẩu → 401 kèm rate-limit header; đúng mật khẩu → 200 kèm `expiresAt` đúng +120 phút.
- Cập nhật `docs/TASK.md`, `docs/CURRENT.md`. Không đổi `.claude/docs/data-model.md`/`docs/ERD.md` (không đổi schema).

## 2026-08-10 (2)

S1-04 — Auth (JWT + refresh rotation, Argon2id, khoá tài khoản):

- Migration `20260810150400_auth_sessions`: bảng mới `user_session` (refresh token đã hash, rotation chain qua `replaced_by_session_id`, RLS + `CHECK(version>=1)` + index `(tenant_id,user_id,expires_at DESC) WHERE deleted_at IS NULL`); `user_account` thêm `failed_login_count`/`last_failed_login_at`/`locked_until`. Xem `docs/DECISIONS.md` #019-#020 (2 quyết định đã hỏi và chốt với chủ dự án trước khi code: bảng session thay vì bộ đếm `token_version`, client gửi `tenantId` tường minh lúc đăng nhập).
- `packages/core/src/iam/`: `lockout.ts` (ngưỡng khoá 5 lần sai/15 phút, hàm thuần `isAccountLocked`/`recordFailedLogin`/`resetLoginAttempts`, unit test 9/9 — mọi cạnh của ngưỡng/cửa sổ) + `constants.ts` (TTL token). `packages/core/src/errors/`: `DomainError` (lớp gốc) + 5 lỗi auth cụ thể — khởi đầu tối thiểu của lớp lỗi nghiệp vụ mà `coding-standards.md` yêu cầu (S1-06 mở rộng thêm sau, không làm lại).
- `packages/shared/src/auth.ts`: Zod schema `loginRequestSchema`/`loginResponseSchema`/`jwtPayloadSchema` dùng chung controller và web (S1-09).
- `apps/api/src/modules/iam/`: `AuthController` (`POST /auth/login|refresh|logout`), `AuthService` (điều phối, mở transaction qua `UnitOfWorkService`), `TokenService` (ký/verify JWT, một `JWT_SECRET` dùng chung phân biệt bằng claim `typ`), `SessionRepository`, `UserAccountAuthRepository`, `audit-log.helper.ts` (ghi trực tiếp các sự kiện auth mà `security-audit.md` liệt kê tên rõ — chưa phải interceptor tổng quát, đó là S1-05).
- **Phát hiện quan trọng lúc viết test**: `prisma.$transaction(async (tx) => ...)` rollback toàn bộ nếu callback throw — kể cả khi throw đó là một kết quả nghiệp vụ bình thường (sai mật khẩu, phát hiện refresh token bị dùng lại) mà cần **commit** (đếm số lần sai, thu hồi phiên). Sửa bằng cách để callback trả về một outcome thay vì throw, transaction luôn commit, rồi throw `DomainError` tương ứng sau khi transaction đã xong — áp dụng cho cả `login()` và `refresh()`.
- `apps/api/src/common/`: `tenant-context.middleware.ts` đổi hẳn sang đọc JWT access token thật qua `Authorization: Bearer` (không còn header tạm `x-tenant-id`/`x-actor-id` — hoàn thành cam kết ở `docs/DECISIONS.md` #012, vì đây là controller thật đầu tiên nhận traffic). Thêm `jwt-auth.guard.ts` (xác thực tối thiểu, viết + test sẵn, chưa áp vào controller nào), `response.interceptor.ts` + `domain-exception.filter.ts` (response envelope `{data,meta}`/`{error}` theo `architecture.md` — lần đầu cần dùng tới).
- `main.ts`: `cookie-parser`, CORS (`WEB_ORIGIN`, `credentials:true` cho cookie refresh token), wire global interceptor/filter, đọc `PORT` qua `ConfigService` thay vì hard-code.
- Rate limit riêng cho `/auth/login` (`@nestjs/throttler`, 10 request/phút/IP) — không áp toàn cục.
- Integration test thật trên Postgres (`auth.spec.ts`, 9/9 pass, không mock — cùng pattern `rbac.spec.ts`/`tenant-isolation.spec.ts`): login đúng/sai mật khẩu, khoá tài khoản đúng ngưỡng, tài khoản vô hiệu hoá, sai `tenantId` bị coi như sai thông tin đăng nhập, refresh rotation, phát hiện reuse thu hồi toàn bộ phiên, logout, token hỏng. `jwt-auth.guard.spec.ts` (5/5 pass).
- Cập nhật `.claude/docs/data-model.md`, `docs/ERD.md` (v1.2) trong cùng lúc theo `coding-standards.md`.

## 2026-08-10

- Thêm `docs/product/future-modules-reference.md`: gom ý tưởng kiến trúc từ 3 tài liệu đặc tả HIS/EMR tổng quát do chủ dự án cung cấp (Dược/kho FEFO + BOM tiêu hao tự động, Viện phí/bảng giá đa đối tượng, danh mục dùng chung — địa giới hành chính/BHYT/ICD-10/đường dùng thuốc..., luồng khám mở rộng). Tài liệu **chưa chốt**, chỉ tham khảo cho v2/v2.1/v3 — đã ghi rõ 6 điểm mâu thuẫn với schema/state machine/quy ước đặt tên đã chốt ở v1 (state `encounter` khác, PascalCase vs snake_case, thiếu `tenant_id`/RLS, thiếu 8 cột bắt buộc, đề xuất module ngoài phạm vi v1, tiền dùng `Decimal` thay vì `bigint`) để không bị dùng nhầm làm nguồn đã chốt.

## 2026-08-08 (5)

- Thêm `docs/demo.md`: hướng dẫn chạy `pnpm dev` để xem giao diện lúc phát triển, ghi rõ trạng thái thật hiện nay (`apps/web` mới có bootstrap tối thiểu, chưa tới S1-08 nên chưa có màn hình nghiệp vụ để demo) và sự cố thường gặp.
- Thêm `docs/Deploy.md`: hướng dẫn triển khai on-premise (mục tiêu chính thức v1, dựa theo kiến trúc đã chốt ở `.claude/docs/project-structure.md`, ghi rõ S4-05 chưa bắt đầu nên đây là kế hoạch chứ chưa chạy được ngay) và cloud (v3+, chưa có quyết định — chỉ liệt kê phần kiến trúc port/adapter đã chuẩn bị sẵn).

## 2026-08-08 (4)

- `CLAUDE.md`: thêm `.claude/docs/ui-guidelines.md` vào danh sách tài liệu bắt buộc đọc trước khi bắt đầu bất kỳ công việc nào (trước đây chỉ bắt buộc đọc khi làm UI/UX) — đảm bảo agent nắm token/quy tắc thiết kế xuyên suốt, không chỉ khi chạm trực tiếp vào `apps/web`.

## 2026-08-08 (3)

- `CLAUDE.md`: thêm Tailwind CSS vào Tech Stack (`docs/DECISIONS.md` #017); thêm quy định bắt buộc đọc `.claude/docs/ui-guidelines.md` + `docs/design/*.md` trước khi thiết kế UI/UX, hỏi trước khi điều chỉnh, cập nhật tài liệu khi đổi quyết định thiết kế; thêm tham chiếu `docs/Hybrid Authorization.md` (định hướng platform/đa module v3+, chưa triển khai, chỉ tham khảo).
- Phát hiện và xử lý mâu thuẫn giữa `ui-guidelines.md` và `docs/design/AI_AVOID_RULES.md` (số lượng màu, độ đậm shadow) — chốt `ui-guidelines.md` thắng, ghi chú ngay trong `AI_AVOID_RULES.md` (`docs/DECISIONS.md` #018).

## 2026-08-08 (2)

S1-04b — Thay mô hình vai trò cứng bằng RBAC + Data Scope (theo yêu cầu chủ dự án, tài liệu `PhanQuyen.md`):

- Cập nhật tài liệu chốt trước khi code: `.claude/docs/security-audit.md` (mô hình mới, ma trận mặc định, break-glass), `.claude/docs/data-model.md`, `docs/ERD.md` (v1.1), `docs/product/prd.md` (v1.1 — ADM-06, ADM-07, R10), `docs/product/plan.md` (v1.1 — S1-04b/S1-04c, sprint 1 tăng 34→40 dev-day), `docs/DECISIONS.md` #013-#016.
- Schema: bỏ enum `UserRoleName` cứng, thêm `department`, `role` (theo tenant), `permission` (danh mục toàn hệ thống, 23 permission), `role_permission` (ma trận `role × permission → data_scope`), `break_glass_session`. Enum `data_scope`: `none`/`personal`/`department`/`global` — **không có `branch`** (đa chi nhánh hoãn, khớp PRD Q6).
- Migration `20260808015619_rbac_data_scope`: đổi `user_role` từ cột `role` enum sang `role_id` FK; RLS + `CHECK(version>=1)` cho bảng mới; `permission` read-only cho role app (`REVOKE INSERT, UPDATE`); `break_glass_session` không cho `UPDATE` (append-only).
- Gặp sự cố khi áp migration: bảng `_prisma_migrations` bị mất dấu vết dù dữ liệu bảng cũ vẫn còn — baseline lại 2 migration cũ bằng `prisma migrate resolve --applied` trước khi áp migration mới.
- Logic nghiệp vụ thuần (danh mục permission + ma trận mặc định) đặt ở `packages/core/src/rbac/permissions.ts` — nguồn sự thật duy nhất, dùng chung cho seed script, guard sau này, và có thể dùng lại ở web. `packages/shared/src/data-scope.ts`: enum Data Scope dùng chung web/api.
- `apps/api/src/infrastructure/persistence/seed-permissions.ts`, `seed-tenant-roles.ts`: seed danh mục + seed 5 vai trò/ma trận cho một tenant (`prisma/seed/index.ts` là entrypoint mỏng gọi vào, chạy bằng role đặc quyền vì `permission` đã revoke ghi từ role app).
- Integration test thật (`rbac.spec.ts`, 4/4 pass, không mock): seed đúng 5 vai trò/tenant; RLS cách ly `role`/`role_permission` theo tenant; `permission` đọc được nhưng không ghi được qua role app; `doctor.encounter.read = global` đúng thiết kế (không bắt break-glass cho tiền sử — khớp PRD ENC-01 P0, khác ví dụ minh hoạ chung của ngành).
- CI: thêm bước seed sau `migrate deploy` (không tự seed như `migrate dev`).

## 2026-08-08

S1-03 — Tenant context (RLS, unit of work):

- Migration `20260807170922_tenant_context`: role Postgres mới `nexamed_app` (không superuser/BYPASSRLS/CREATEDB/CREATEROLE), chỉ `SELECT`/`INSERT`/`UPDATE` (không `DELETE`; `audit_log` không cả `UPDATE`); `CHECK (version >= 1)` trên 6 bảng; Row Level Security thật (policy `USING`+`WITH CHECK`) trên 6 bảng có `tenant_id` (không áp cho `tenant`, xem `docs/DECISIONS.md` #011).
- **Phát hiện quan trọng**: role cũ `nexamed` là superuser nên **luôn bypass RLS** dù policy đúng — API runtime phải chuyển sang role mới `nexamed_app`. Tách `DATABASE_URL` (app, runtime) khỏi `MIGRATE_DATABASE_URL` (đặc quyền, chỉ migration) — xem `docs/DECISIONS.md` #010. Thêm `apps/api/scripts/with-migrate-url.mjs` chạy migrate bằng đúng role.
- `apps/api/src/infrastructure/persistence/`: `prisma.service.ts`, `unit-of-work.service.ts` (mở transaction + `SET LOCAL app.current_tenant_id`, validate UUID chống injection), `tenant-context.store.ts` (AsyncLocalStorage), `persistence.module.ts` — wire vào `AppModule`.
- `apps/api/src/common/tenant-context.middleware.ts`: tạm đọc `x-tenant-id`/`x-actor-id` từ header, chờ JWT thật ở S1-04 (xem `docs/DECISIONS.md` #012) — **không dùng khi có controller thật nhận traffic ngoài**.
- Integration test thật trên Postgres (`tenant-isolation.spec.ts`, 4/4 pass, không mock): cách ly tenant đúng; quên set tenant context → lỗi (fail closed, không rò dữ liệu); không xoá được; không insert xuyên tenant được (RLS `WITH CHECK`).
- `apps/api/vitest.config.ts` + `vitest.setup.ts`: tự nạp `.env` cho test, khớp luồng `pnpm test` đã ghi trong `CLAUDE.md`.
- CI: tách `DATABASE_URL`/`MIGRATE_DATABASE_URL`, thêm bước `Prisma generate` sau `migrate deploy` (trước đây thiếu — `migrate deploy` không tự generate client như `migrate dev`, có thể làm build/typecheck CI fail nếu chạy trên máy sạch).

## 2026-08-07 (5)

Xác minh migration trên Postgres thật (sau khi cài Docker lên máy dev):

- Phát hiện và sửa lỗi: image `postgres:18` đổi quy ước thư mục data (mount `/var/lib/postgresql`, không phải `.../data` như PG16) — `docker-compose.yml` mount sai sẽ khiến container restart-loop.
- Phát hiện máy dev có sẵn Postgres native (Windows service, không thuộc dự án) chiếm cổng 5432 — đổi cổng container Docker sang **5433** trong `docker-compose.yml`, `.env.example`, `.env`.
- Chạy `prisma migrate deploy` thật lên Postgres 18.4 — thành công. Insert thử xác nhận `id` sinh đúng UUID v7 (time-ordered).
- `docs/TASK.md`/`docs/CURRENT.md`: bỏ ghi chú "chưa xác minh trên DB thật".

## 2026-08-07 (4)

Chốt các điểm treo từ S1-02:

- Nâng PostgreSQL 16 → **18** trong `CLAUDE.md`, `docker-compose.yml`, `.github/workflows/ci.yml` để dùng `uuidv7()` built-in thay vì `gen_random_uuid()`. Sinh lại migration (`prisma/migrations/20260807090207_init`).
- Xác nhận cột `audit_log` (không có `created_at`/`created_by`/`updated_by`) và sửa câu tóm tắt mâu thuẫn trong `.claude/docs/data-model.md` cho khớp.
- `docs/DECISIONS.md`: thêm #009 (thay thế #006), xác nhận #007, ghi rõ #008 phải hoàn thiện đầy đủ khi tới S1-03.

## 2026-08-07 (3)

S1-02 — Prisma schema nền:

- `apps/api/prisma/schema.prisma`: 7 bảng nền tảng (`tenant`, `tenant_setting`, `room`, `user_account`, `user_role`, `code_sequence`, `audit_log`), đủ 8 cột bắt buộc trừ ngoại lệ đã ghi trong `.claude/docs/data-model.md`. Composite FK `(tenant_id, user_id)` cho `user_role` chống trỏ chéo tenant.
- Migration đầu tiên — sinh bằng `prisma migrate diff` (không có Docker trong môi trường build nên chưa áp thử lên Postgres thật).
- `scripts/check-mandatory-columns.mjs`: kiểm tra mọi model có đủ 8 cột bắt buộc, nối vào CI.
- `apps/api/src/config`: Zod env schema (`DATABASE_URL`, `JWT_SECRET`, `ENCRYPTION_KEY`, `PORT`, `NODE_ENV`), `ConfigModule` chặn khởi động khi thiếu biến — đã kiểm chứng cả hai chiều (thiếu → crash, đủ → chạy).
- `docker-compose.yml` (gốc, chỉ Postgres, cho local dev — khác với `deploy/on-prem/docker-compose.yml` đầy đủ ở S4-05), `apps/api/.env.example`.
- `packages/shared/src/roles.ts`: enum 5 vai trò (Zod + TS), khớp enum `UserRoleName` trong Prisma schema.
- CI (`ci.yml`): thêm service Postgres, bước `db:check-schema` và `prisma migrate deploy` trước lint/typecheck/build/test.
- 4 quyết định kỹ thuật ghi vào `docs/DECISIONS.md` (#005-#008): không FK cho `created_by`/`updated_by`, UUID v4 tạm thay UUID v7, cách xử lý mâu thuẫn nội bộ về cột `audit_log`, RLS/CHECK/DELETE-revoke để dành cho S1-03.
- Thêm quy định "viết component/hàm tiện ích hướng tái sử dụng ngay từ đầu" vào `CLAUDE.md`.

## 2026-08-07 (2)

S1-01 — Khởi tạo monorepo:

- pnpm workspace gốc: `package.json`, `pnpm-workspace.yaml`, `.npmrc`, `.gitignore`, `tsconfig.base.json`.
- `apps/api`: NestJS 10.4, bootstrap tối thiểu (`main.ts`, `app.module.ts`), cấu trúc thư mục `modules/`, `infrastructure/{persistence,storage,eventbus,signature,insurance,notification}`, `common/`, `config/`.
- `apps/web`: React 18.3 + Vite 5.4, bootstrap tối thiểu (`src/app/main.tsx`, `App.tsx`), cấu trúc `features/`, `shared/`, `app/`.
- `packages/core`, `packages/shared`: khung package, `ports/`, `errors/` (rỗng, chờ S1-06).
- ESLint flat config (`eslint.config.js`) với boundary rule chặn `packages/core` import `@nestjs/*`, `@prisma/*`, `react`, `axios`/HTTP client, và đọc `process.env` — đã kiểm chứng thật sự chặn được.
- CI: `.github/workflows/ci.yml` (install → lint → typecheck → build → test).
- `pnpm install`, `build`, `typecheck`, `lint`, `test` chạy sạch trên toàn workspace.
- Đổi tên `docs/product/PRD.md`/`PLAN.md` → `prd.md`/`plan.md` (chữ thường) để khớp đường dẫn tham chiếu trong `CLAUDE.md`.

## 2026-08-07

- Thêm `CLAUDE.md`.
- Thêm `.claude/docs/`: `project-structure.md`, `architecture.md`, `coding-standards.md`, `multi-tenancy.md`, `data-model.md`, `clinical-workflow.md`, `security-audit.md`.
- Thêm `docs/product/prd.md`, `docs/product/plan.md`.
- Thêm `docs/design/design-system.md`.
- Thêm `docs/CURRENT.md`, `docs/TASK.md`, `docs/CHANGELOG.md`, `docs/DECISIONS.md`.