# Task — NEXAMed

Danh sách công việc theo phase. Nguồn ưu tiên/mốc thời gian là `docs/product/plan.md` — file này chỉ theo dõi trạng thái từng việc cụ thể.

Quy ước trạng thái: `[ ]` chưa làm, `[~]` đang làm, `[x]` xong.

## Phase 0 — Thiết lập tài liệu

- [x] `CLAUDE.md`
- [x] `.claude/docs/*` (7 file)
- [x] `docs/product/prd.md`
- [x] `docs/product/plan.md`
- [x] `docs/ERD.md`
- [~] `docs/design/design-system.md` (chủ đích để trống, chờ bổ sung)

## Sprint 1 — Nền tảng (tuần 1-2, theo `docs/product/plan.md` mục 4)

- [x] S1-01 — Khởi tạo monorepo pnpm, cấu trúc thư mục theo `project-structure.md`, ESLint boundary rules, CI
- [x] S1-02 — Prisma schema nền: 8 cột bắt buộc, base model, quy ước migration, `code_sequence` (migration **đã áp thật lên Postgres 18.4** qua `prisma migrate deploy`, xác minh `uuidv7()` sinh đúng UUID v7)
- [x] S1-03 — Tenant context: middleware `app.current_tenant_id`, RLS policy, unit of work. RLS/CHECK/thu hồi DELETE **đã bật thật** trên Postgres, xác minh bằng integration test 4/4 pass. Middleware ban đầu đọc header tạm (chưa qua JWT — xem `docs/DECISIONS.md` #012) — đã thay bằng JWT thật ở S1-04
- [x] S1-04 — Auth: JWT (access 15 phút + refresh httpOnly cookie xoay vòng), Argon2id, khoá tài khoản tạm sau 5 lần sai/15 phút. Bảng mới `user_session` (rotation + reuse detection — xem `docs/DECISIONS.md` #019), `user_account` thêm 3 cột lockout. `TenantContextMiddleware` đổi sang đọc JWT access token thật (không còn header tạm). Module `iam` (`auth.controller/service`, `token.service`, `session.repository`) — **đã xác minh thật** (`auth.spec.ts`, 9/9 pass): login đúng/sai, khoá tài khoản, tài khoản vô hiệu hoá, sai tenantId, refresh rotation, phát hiện reuse thu hồi toàn bộ phiên, logout, token hỏng. `JwtAuthGuard` (xác thực tối thiểu, chưa áp controller nào) + `lockout.spec.ts` (9/9 pass, unit thuần). Response envelope `{data,meta}`/`{error}` + `DomainExceptionFilter` (theo `architecture.md`, chưa từng có controller nào cần tới trước S1-04)
- [x] S1-04b — RBAC schema: `role`/`permission`/`role_permission`/`department`, seed danh mục permission (23) + 5 vai trò mặc định + ma trận, guard đọc `role_permission` — **đã xác minh thật** (`rbac.spec.ts`, 4/4 pass). Guard NestJS thật áp vào controller là việc của S2 (chưa có controller)
- [x] S1-04c — Break-glass: `POST /api/v1/break-glass` (yêu cầu `JwtAuthGuard` — nơi dùng thật đầu tiên), xác thực lại mật khẩu + lý do bắt buộc, tạo `break_glass_session` (bảng đã có sẵn từ S1-04b, không cần migration mới), thời hạn đọc `tenant_setting` (`break_glass_duration_minutes`, mặc định 120 phút), ghi `audit_log` (`break_glass.request`), gọi `NotificationPort` (no-op — kéo trước 1 port của S1-06, xem `packages/core/src/ports/notification.port.ts`). Primitive `BreakGlassService.tryConsume()` (kiểm tra phiên còn hiệu lực + ghi `audit_log` `break_glass.access`) viết + test sẵn cho guard `data_scope` thật của S2 gọi — **đã xác minh thật** (`break-glass.spec.ts`, 6/6 pass; `break-glass.spec.ts` ở `packages/core`, 3/3 pass, unit thuần).
- [x] S1-05 — Audit log: bảng append-only + quyền DB đã xong từ S1-03. Hai cơ chế ghi (xem `docs/DECISIONS.md` #021): thao tác **ghi** → gọi tường minh `writeAuditLog(tx, ...)` trong transaction của service (dời từ `modules/iam/` sang `infrastructure/persistence/audit-log.helper.ts` — hạ tầng dùng chung, không phải riêng domain `iam`); thao tác **xem** → `AuditViewInterceptor` + decorator `@AuditView('entityType')` (`apps/api/src/common/`), tự mở transaction riêng sau khi handler thành công, không ghi khi handler lỗi. **Đã xác minh thật** (`audit-view.interceptor.spec.ts`, 3/3 pass) — chưa áp vào controller nào (chưa có controller nghiệp vụ, S2)
- [x] S1-06 — `packages/core`: khung entity (`BaseEntity`/`AppendOnlyEntity`/`SignableEntity` + `isSigned`), lớp lỗi + mã lỗi (đã có từ S1-04), `ports/` đủ 6 interface (5 port còn lại: Storage/EventBus/Signature/Insurance/PatientIdentity, cộng `NotificationPort` đã có từ S1-04c), adapter no-op/thật + đăng ký DI qua `PortsModule` (global) — **đã xác minh thật**: unit test `base-entity.spec.ts` (2/2), integration test `local-disk.adapter.spec.ts` (4/4), `in-memory.adapter.spec.ts` (3/3); build thật (`node dist/main.js`) log `PortsModule dependencies initialized` + `Nest application successfully started`, xác nhận DI wire đúng qua NestJS container thật (không chỉ new service tay như các test trước)
- [x] S1-07 — Test harness cách ly tenant: `src/testing/global-setup.ts` (Vitest globalSetup — testcontainers Postgres 18 tạm + `prisma migrate deploy`, set `DATABASE_URL`/`MIGRATE_DATABASE_URL` trước khi mọi spec chạy, không cần sửa 6 spec đã có), `src/testing/tenant-fixture.ts` (`createTwoTenantFixture` — helper tạo 2 tenant + cleanup đúng thứ tự FK, dùng cho test mới từ S2 trở đi), `src/modules/iam/auth-login-http.spec.ts` (template + test HTTP e2e thật đầu tiên — boot `AppModule` qua `@nestjs/testing` + supertest, xác minh cách ly tenant qua toàn bộ stack HTTP thay vì gọi thẳng service). Phát sinh và xử lý 2 vấn đề hạ tầng test mới lộ ra (không phải bug nghiệp vụ, xác minh lại bằng `node dist/main.js` + `curl` thật): (1) Vitest dùng esbuild mặc định không emit decorator metadata NestJS cần cho DI qua constructor — thêm `unplugin-swc`/`.swcrc` (đúng khuyến nghị chính thức NestJS); (2) `exception instanceof ZodError` sai qua ranh giới `@nexamed/shared` (dist CJS) ↔ `apps/api` (SWC/ESM lúc test) — đổi `domain-exception.filter.ts` sang kiểm tra theo cấu trúc (`.name === 'ZodError'` + `issues` mảng) thay vì `instanceof`, bền vững hơn qua mọi ranh giới module. **Đã xác minh thật**: toàn bộ 41 test `apps/api` pass (38 cũ không đổi gì + 3 mới), chạy lại ổn định không flaky.
- [x] S1-08 — Web: app shell, router, provider, luồng đăng nhập, layout, design token. Hỏi chốt trước khi làm (menu kết hợp luồng nghiệp vụ + vai trò, Dashboard rỗng empty-state, Quản trị là mục sidebar riêng, chỉ hiện mục có backend thật, `tenantId` nạp runtime qua `config.json`, màu theo `ui-guidelines.md`). Backend mở rộng nhỏ đi kèm (docs/DECISIONS.md #022): `loginResponseSchema.user` thêm `roles`, thêm `GET /auth/me`. Frontend: `react-router-dom` v7, `@phosphor-icons/react`, Tailwind v4 (`@tailwindcss/vite`, không cần postcss/config riêng), runtime config `public/config.json` (gitignore, có `config.example.json`), API client tối thiểu (`shared/api/client.ts`, thay hẳn ở S1-09), Zustand auth store, `LoginPage`/`RequireAuth`/`AppBootstrap` (khôi phục phiên qua refresh+`/me` lúc reload), `AppShell`/`Sidebar`/`UserMenu`, `DashboardPage`/`AdminPage` (empty state), `Button`/`EmptyState` dùng chung. Sửa 1 race điều kiện phát hiện lúc kiểm bằng trình duyệt thật: React StrictMode (dev) gọi effect 2 lần khiến `AppBootstrap` bắn 2 request `/auth/refresh` gần đồng thời, có thể đụng cơ chế phát hiện reuse-token (S1-04) — chặn bằng `useRef` guard. **Đã xác minh thật**: `pnpm -w lint/typecheck/build` sạch; 45 test `apps/api` pass; chạy `pnpm dev` thật + Playwright (headless Chromium, không có project skill `run` nên tự cài) xác nhận qua ảnh chụp màn hình: chưa đăng nhập → `/login`; đăng nhập đúng → Dashboard với sidebar đúng token (`bg-slate-900`), thấy cả Tổng quan lẫn Quản trị (user test có vai trò `clinic_admin`); Dashboard/Quản trị đều empty-state đúng chuẩn; F5 giữ nguyên phiên; đăng xuất → `/login`.
- [x] S1-09 — Web: api client sinh từ OpenAPI, TanStack Query setup với cache key có `tenantId`. Pipeline (xem `docs/DECISIONS.md` #023): `apps/api/scripts/generate-openapi.ts` (`@asteasolutions/zod-to-openapi` v7 dựng registry từ Zod schema có sẵn ở `@nexamed/shared`, không viết lại contract) → `apps/api/openapi/openapi.json` (commit) → `openapi-typescript` sinh `apps/web/src/shared/api/openapi-schema.d.ts` (commit) → `apps/web/src/shared/api/client.ts` dùng `openapi-fetch` thay wrapper `fetch` tự viết ở S1-08, vẫn bóc envelope `{data,meta}`/`{error}` đúng `architecture.md`. Thêm `packages/shared` `refreshResponseSchema`/`logoutResponseSchema` (nguồn cho registry). Thêm `apps/web/src/shared/api/query-keys.ts` — factory cache key `[tenantId, domain, ...]`, sẵn sàng cho `patient`/`appointment` ở S2 (auth vẫn là session state theo Zustand, không qua Query, quyết định đã có từ S1-08). **Đã xác minh thật**: `pnpm -w lint/typecheck/build` sạch; 45 test `apps/api` + 14 test `packages/core` pass nguyên; chạy `pnpm dev` thật + Playwright (headless Chromium, cài tạm ở scratchpad) xác nhận qua ảnh chụp: đăng nhập đúng → Dashboard; F5 giữ nguyên phiên (refresh + `/auth/me` qua client mới hoạt động); đăng xuất → `/login`; không lỗi console ngoài dự kiến (401 lần refresh đầu khi chưa có phiên).

**Gate cuối sprint 1** (từ `plan.md`):
- [x] CI fail khi `packages/core` import NestJS/Prisma/React (đã kiểm chứng thủ công với ESLint; CI thật chỉ chạy khi có remote GitHub)
- [x] CI fail khi migration tạo bảng thiếu một trong 8 cột bắt buộc (`scripts/check-mandatory-columns.mjs`, đã kiểm chứng chặn được lỗi thật lúc viết script)
- [x] Test cách ly tenant chạy được: đăng nhập tenant A gọi ID của tenant B trả 404 — xác minh đủ cả 3 tầng: repository (`tenant-isolation.spec.ts`, S1-03), HTTP đăng nhập sai tenant (`auth-login-http.spec.ts`, 401, S1-07), và giờ đã có **HTTP e2e qua controller nghiệp vụ đọc theo ID thật** (`patient-http.spec.ts`, S2-01: user tenant B có `patient.read=global` vẫn nhận 404 khi gọi `GET /patients/:id` của tenant A — đúng RLS + lọc `tenantId` tường minh ở repository, không phải 403)
- [x] Mọi thao tác ghi mẫu đều sinh dòng `audit_log`; rollback audit thì rollback cả thao tác — đúng cho các sự kiện auth/break-glass đã có (ghi trong cùng transaction); cơ chế dùng chung (`writeAuditLog` + `AuditViewInterceptor`) đã sẵn sàng cho mọi module ghi/xem từ S2 trở đi (S1-05), và giờ patient module (S2-01) đã dùng thật cả hai
- [x] Guard chặn đúng theo `data_scope` — **`PermissionGuard` thật đã hiện thực ở S2-01** (`apps/api/src/common/permission.guard.ts`), đọc `role_permission` gộp theo mọi vai trò user đang giữ (`maxDataScope`), `none`/không có dòng nào → thử break-glass (nếu route có `entityIdParam`) → không có phiên hợp lệ thì `403 { code: 'PERMISSION_DENIED', details: { breakGlassAvailable: true } }`; đã xác minh thật qua `patient-http.spec.ts` (vai trò không có `patient.create`/`patient.read` nào bị chặn đúng, có break-glass sẵn sàng nhưng chưa có phiên thì vẫn chặn). Giới hạn đã biết: `personal`/`department` coi tương đương `global` cho riêng `patient.*` vì bảng này không có khái niệm chủ sở hữu trong `security-audit.md` — xem comment trong `permission.guard.ts`

## Sprint 2 — Bệnh nhân + Đặt lịch (Tuần 3-4, theo `docs/product/plan.md` mục 5)

- [x] S2-01 — Module `patient`: CRUD (tạo/xem/sửa, chưa có xoá — không có yêu cầu PRD), mã hoá `national_id` (AES-256-GCM) + cột hash (HMAC-SHA256, tra trùng), `global_patient_ref`/`identity_verified_at` để null. Cũng là nơi hiện thực **`PermissionGuard` thật đầu tiên** (việc treo từ Sprint 1) vì đây là controller nghiệp vụ đầu tiên của dự án.
  - Vá lệch `docs/ERD.md` ↔ `.claude/docs/data-model.md` cho bảng `patient` trước khi viết migration (hỏi và xác nhận với chủ dự án — xem `docs/DECISIONS.md` #024): thêm `address_json` (PRD PAT-01, P0) và `identity_verified_at` (đi cùng `global_patient_ref`, liên-tenant v3+).
  - Migration `20260811024047_patient_s2_01`: bảng `patient` đủ 8 cột bắt buộc + cột đặc thù, RLS, `CHECK(version>=1)`, `UNIQUE(tenant_id, patient_code)`, partial `UNIQUE(tenant_id, national_id_hash) WHERE national_id_hash IS NOT NULL` (C3, viết raw SQL vì Prisma schema không biểu diễn được unique có điều kiện), index `(tenant_id, phone)`, composite FK tự tham chiếu `merged_into_id` (chưa dùng tới, để sẵn cho PAT-04/S2-03).
  - `apps/api/src/infrastructure/crypto/pii-encryption.ts`: `encryptPii`/`decryptPii` (AES-256-GCM, khoá từ `ENCRYPTION_KEY` chuẩn hoá qua SHA-256), `hashForLookup` (HMAC-SHA256) — dùng chung cho `national_id` hôm nay, `insurance_card.card_no` ở S2-04.
  - `apps/api/src/infrastructure/persistence/code-sequence.repository.ts` (đăng ký global trong `PersistenceModule`): cấp `patient_code` bằng `INSERT ... ON CONFLICT DO UPDATE` atomic (không phải `SELECT...FOR UPDATE` + `UPDATE` hai bước, cùng đạt atomicity nhưng gọn hơn). `packages/core/src/code-sequence/format-display-code.ts`: định dạng `<prefix><yyMM><seq6>`, tính đúng theo giờ Việt Nam (UTC+7 cố định) chứ không phải UTC server — tránh sai tháng quanh khung nửa đêm tới 7h sáng UTC.
  - `apps/api/src/common/permission.guard.ts` + `require-permission.decorator.ts` + `infrastructure/persistence/permission-lookup.helper.ts` + `packages/core/src/rbac/data-scope.ts` (`maxDataScope`, unit test 3/3): guard `data_scope` thật, tích hợp `BreakGlassService.tryConsume()` đã có sẵn từ S1-04c. `IamModule` đổi thành `@Global()` (để `BreakGlassService` resolve đúng context khi `PermissionGuard` được dùng từ module khác — xem comment trong file).
  - `apps/api/src/common/domain-exception.filter.ts` mở rộng: nhánh `HttpException` chung giờ gói field lạ ngoài `message`/`code` vào `error.details` (phục vụ `breakGlassAvailable`), không đổi hình dạng response cho exception không có field lạ.
  - `packages/shared/src/patient.ts`: `createPatientRequestSchema`/`updatePatientRequestSchema`/`patientSummarySchema`/`patientDetailSchema`/`listPatientsQuerySchema`/`listPatientsResponseSchema`. `packages/core/src/errors/`: `PatientDuplicateNationalIdError` (409), `ConcurrentModificationError` (409, dùng chung mọi module — không riêng patient).
  - `PATCH /patients/:id` bắt buộc kèm `version` — optimistic locking qua `updateMany` + kiểm `count` (Prisma `update()` không ghép thêm điều kiện `version` vào `WHERE` được).
  - **Đã xác minh thật**: `patient-http.spec.ts` (10/10 pass, HTTP e2e qua toàn bộ stack) — tạo hồ sơ (có/không CCCD), CCCD mã hoá thật ở DB (không lưu plaintext), trùng CCCD → 409, danh sách không lộ CCCD, optimistic lock (version cũ → 409, version đúng → 200), vai trò thiếu quyền → 403 kèm `breakGlassAvailable`, cách ly tenant qua permission guard + RLS → 404 (không phải 403); `code-sequence.repository.spec.ts` (3/3, tăng dần đúng, tenant riêng biệt, đồng thời không trùng số); `pii-encryption.spec.ts` (5/5); `data-scope.spec.ts`, `format-display-code.spec.ts` (`packages/core`, 3+3). Toàn bộ 63 test `apps/api` + 20 test `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch. OpenAPI (`openapi:generate` + `api:codegen`, S1-09) đã cập nhật 4 endpoint mới.
- [x] S2-02 — Tìm kiếm bệnh nhân (PAT-02): tìm theo tên (không dấu, không phân biệt hoa/thường), số điện thoại, mã bệnh nhân — cùng một query param `q` trên `GET /api/v1/patients`.
  - `packages/core/src/search/strip-vietnamese-diacritics.ts`: bỏ dấu tiếng Việt + viết thường ở tầng ứng dụng (chuẩn hoá `q` trước khi so khớp `search_key`), unit test 5/5 — xử lý riêng chữ "đ"/"Đ" (không phải ký tự tổ hợp Unicode, `unaccent` chuẩn của Postgres bỏ sót).
  - Migration `20260811055006_patient_search_s2_02`: bật extension `unaccent`, `pg_trgm`, `btree_gin` (cần cho GIN kết hợp cột `uuid` với cột `gin_trgm_ops` trong cùng index — GIN thuần không có opclass cho `uuid`). Hàm SQL `nexamed_unaccent_lower()` (bọc `unaccent()` — vốn STABLE — thành IMMUTABLE để dùng được trong generated column, đánh đổi đã ghi rõ trong migration). Cột `patient.search_key TEXT GENERATED ALWAYS AS (nexamed_unaccent_lower(full_name)) STORED` (khai báo `String?` ở Prisma dù DB không NULL trên thực tế — Prisma không có khái niệm "generated column chỉ đọc", khai `String` thường sẽ bắt buộc phải truyền field này lúc `create()`). GIN kết hợp `(tenant_id, search_key gin_trgm_ops)` đúng docs/ERD.md mục 5.
  - Lúc scaffold migration bằng `prisma migrate dev --create-only`, Prisma tự đề xuất `DROP INDEX patient_tenant_id_phone_idx` vì index đó chỉ tồn tại ở raw SQL (không khai báo trong schema.prisma) nên bị coi là "drift" — đã bỏ dòng đó, giữ nguyên index cũ. Bài học: mọi lần `--create-only` sau này trên bảng có index raw-SQL-only đều phải soát kỹ trước khi merge, không áp thẳng.
  - `PatientRepository.list()`/`PatientService.listPatients()` mở rộng nhận `search` (OR giữa `patientCode`/`phone` startsWith và `searchKey` contains bản đã chuẩn hoá).
  - **Đo hiệu năng thật với 50.000 bản ghi** (`apps/api/scripts/perf-patient-search.ts`, script thủ công không chạy trong CI — seed 50k dòng tốn thời gian): cả 3 kiểu tìm (tên/SĐT/mã) đều dưới 35ms, đạt mục tiêu "<1 giây" của PRD mục 5 với biên độ lớn. Phát hiện đáng chú ý: ở quy mô 50k dòng/tenant, planner Postgres chọn Seq Scan thay vì GIN trigram theo mặc định (bảng đủ nhỏ để nằm gọn trong shared buffers, quét tuần tự rẻ hơn tra index) — đây là quyết định ĐÚNG của Postgres, không phải lỗi. Xác nhận GIN index tồn tại và dùng được thật bằng cách ép `enable_seqscan=off` (EXPLAIN xác nhận `Bitmap Index Scan` trên đúng `patient_tenant_id_search_key_trgm_idx`) — planner sẽ tự chuyển sang dùng index này khi dữ liệu đủ lớn, không cần đổi code.
  - **Đã xác minh thật**: `patient-http.spec.ts` thêm 6 test tìm kiếm (16/16 tổng, pass) — tìm không dấu, tìm có dấu, tìm SĐT, tìm mã, không khớp → mảng rỗng, cách ly tenant khi tìm kiếm. `strip-vietnamese-diacritics.spec.ts` (5/5). Toàn bộ 69 test `apps/api` + 25 test `packages/core` pass. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại cho query param `q` mới.
- [x] S2-03 — Chống trùng "mềm" (PAT-03): `GET /api/v1/patients/check-duplicate?fullName=&dob=` — endpoint riêng gọi TRƯỚC khi tạo (đã hỏi và chốt với chủ dự án cách thiết kế, không suy diễn), khớp CHÍNH XÁC `searchKey` (tên đã bỏ dấu/viết thường, tái dùng `stripVietnameseDiacritics`/cột generated của S2-02) + `dob`, trả danh sách `patientSummary` nghi trùng, không chặn/không tự tạo gì. Trùng CCCD đã chặn cứng bằng DB constraint C3 từ S2-01, không cần endpoint này xử lý lại.
  - `packages/shared/src/patient.ts`: `checkPatientDuplicateQuerySchema`/`checkPatientDuplicateResponseSchema`.
  - `apps/api/src/modules/patient/`: `PatientController.checkDuplicate()` (`@Get('check-duplicate')`, khai báo TRƯỚC `@Get(':id')` — bắt buộc vì NestJS/Express khớp route theo thứ tự khai báo), `PatientService.checkDuplicates()`, `PatientRepository.findPossibleDuplicates()` (`take: 10` phòng vệ).
  - OpenAPI (`generate-openapi.ts` thêm path `/patients/check-duplicate`) + web type đã sinh lại.
  - **Đã xác minh thật**: `patient-http.spec.ts` thêm 6 test (22/22 tổng pass) — trùng tên không dấu + đúng dob → thấy hồ sơ cũ, đúng tên khác dob → không trùng, gần giống nhưng không khớp tuyệt đối → không trùng, gọi check-duplicate không tạo bản ghi mới, cách ly tenant, thiếu tham số → 400. Toàn bộ 75 test `apps/api` + 25 test `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch.
- [ ] S2-04 — Lưu thẻ BHYT (mã hoá số thẻ bằng `pii-encryption.ts` đã có), hiển thị, không tính chi trả
- [x] S2-05 — Module `appointment` (APP-01/02/03): exclusion constraint chống trùng giờ bác sĩ ở tầng DB (C2), service bắt lỗi constraint trả `APPOINTMENT_SLOT_CONFLICT`.
  - Migration `20260811064930_appointment_s2_05`: bảng `appointment` đủ 8 cột bắt buộc + `patient_id`/`doctor_id`/`room_id` (nullable, xem `docs/DECISIONS.md` #026)/`scheduled_at`/`duration_minutes`/`status` (enum `SCHEDULED`/`CANCELLED`/`NO_SHOW`/`CONVERTED`)/`source` (enum `walk_in`/`phone`/`online`)/`cancel_reason`, composite FK `(tenant_id, patient_id/doctor_id/room_id)`, RLS, `CHECK(version>=1)`. `room` thêm `UNIQUE(tenant_id, id)` (lần đầu bị composite FK tham chiếu tới). C2: `EXCLUDE USING gist (doctor_id WITH =, nexamed_appointment_slot_range(scheduled_at, duration_minutes) WITH &&) WHERE (status='SCHEDULED' AND deleted_at IS NULL)` — cần extension `btree_gist` + hàm SQL riêng đánh dấu `IMMUTABLE` bọc phép cộng timestamptz+interval (xem `docs/DECISIONS.md` #026 điểm 3). Index `(tenant_id, doctor_id, scheduled_at)` theo `docs/ERD.md` mục 5.
  - `packages/shared/src/appointment.ts`: `createAppointmentRequestSchema` (mặc định `durationMinutes=15`, PRD APP-02), `appointmentSummarySchema`, `listAppointmentsQuerySchema`/`listAppointmentsResponseSchema` (cursor). `packages/core/src/errors/appointment-errors.ts`: `AppointmentSlotConflictError` (409), `AppointmentInvalidReferenceError` (422, FK violation — bệnh nhân/bác sĩ/phòng không tồn tại).
  - `apps/api/src/modules/appointment/`: `AppointmentController` (`POST/GET/GET:id /api/v1/appointments`, `PermissionGuard` + `@RequirePermission('appointment', ...)`), `AppointmentService` (bắt cả exclusion violation lẫn FK violation — xem `docs/DECISIONS.md` #026 điểm 4 về cách nhận diện lỗi Prisma thật cho exclusion constraint), `AppointmentRepository`.
  - **Lọc `data_scope=personal` THẬT lần đầu tiên** (`doctor.appointment.* = personal`, đã seed từ S1-04b) — khác `patient` (S2-01) coi personal/department như global vì không có chủ sở hữu. Xem chi tiết `docs/DECISIONS.md` #026 điểm 2: tạo hộ bác sĩ khác → 403; list ép lọc theo actor; xem lịch của bác sĩ khác → 404.
  - `docs/TASK.md`/testing: `apps/api/src/testing/tenant-fixture.ts` thêm dọn bảng `appointment` (FK RESTRICT tới `patient`/`user_account`, phải xoá trước cả hai).
  - **Đã xác minh thật**: `appointment-http.spec.ts` (15/15 pass, HTTP e2e) — **gate cuối Sprint 2**: hai request đặt trùng khung giờ cùng bác sĩ gần như đồng thời (`Promise.all`) → đúng 1 thành công + 1 `409 APPOINTMENT_SLOT_CONFLICT`; trùng giờ khác bác sĩ → cả hai tạo được; cùng bác sĩ giờ liền kề không chồng → cả hai tạo được; scope personal (bác sĩ) tự đặt cho mình OK, đặt hộ bác sĩ khác → 403; scope personal chỉ thấy lịch của mình ở list/get; cách ly tenant → 404; permission thiếu → 403; tham chiếu bệnh nhân không tồn tại → 422; thiếu trường bắt buộc → 400. Toàn bộ 90 test `apps/api` (75 cũ + 15 mới) pass, không regress. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại cho 3 endpoint `/appointments*` mới.
- [x] S2-06 — Huỷ lịch có lý do, ghi audit (APP-04); walk-in tạo lịch (APP-06). Đã hỏi và chốt với chủ dự án: check-in thật (tạo `encounter`) KHÔNG làm ở đây — để dành cho module Tiếp nhận ở Sprint 3, đúng quyết định "không đi đường tắt walk-in tối thiểu" đã ghi ở `docs/CURRENT.md`. Walk-in không cần code mới — `source='walk_in'` đã có sẵn từ S2-05, dùng thẳng `POST /appointments`.
  - `POST /api/v1/appointments/:id/cancel`: `cancelAppointmentRequestSchema` (`cancelReason` bắt buộc + `version` optimistic locking, `packages/shared/src/appointment.ts`). `AppointmentRepository.cancel()`: `updateMany` với `WHERE version=? AND status='SCHEDULED'` (atomic, tránh race huỷ trùng). `AppointmentService.cancelAppointment()`: kiểm tồn tại + scope `personal` (404 nếu ngoài phạm vi, cùng triết lý getAppointment), kiểm `status==='SCHEDULED'` trước (lỗi rõ ràng `APPOINTMENT_NOT_CANCELLABLE`, 409) rồi mới update, ghi audit `appointment.cancelled`.
  - `packages/core/src/errors/appointment-errors.ts` thêm `AppointmentNotCancellableError` (409, map trong `domain-exception.filter.ts`) — lịch không còn `SCHEDULED` (đã huỷ/không đến/đã chuyển đổi) thì không huỷ lại được qua endpoint này.
  - **Đã xác minh thật**: `appointment-http.spec.ts` thêm 10 test (25/25 tổng pass) — 401/403/400 đúng chuẩn, huỷ thành công đúng version tăng, version cũ → 409 `CONCURRENT_MODIFICATION`, huỷ lại lịch đã huỷ → 409 `APPOINTMENT_NOT_CANCELLABLE`, huỷ xong thì khung giờ đó không còn bị exclusion constraint chặn (đặt lại được), scope personal/tenant cách ly đúng (404), walk-in tạo lịch qua `POST /appointments` thành công. Toàn bộ 100 test `apps/api` (90 cũ + 10 mới) pass, không regress. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại cho endpoint `/appointments/:id/cancel`.
- [x] S2-07 — Tài khoản+vai trò (ADM-01) + cấu hình phòng khám (ADM-02, trừ "mẫu in" — lùi tới S4-04/PRE-04). Hỏi và chốt phạm vi trước khi code. Theo `.claude/docs/architecture.md` (module `iam` sở hữu "tài khoản, vai trò"; module `clinic` sở hữu "cấu hình phòng khám, phòng") — không tạo module `tenant` riêng như tên gọi ở `plan.md`, cùng khớp với ghi chú sẵn có trong `user-account-auth.repository.ts`: "CRUD tài khoản đầy đủ... thuộc S2-07 — không lặp lại ở đây".
  - **Sửa schema trước khi code**: `user_role` UNIQUE(tenant_id, user_id, role_id) đổi từ thường sang PARTIAL (`WHERE deleted_at IS NULL`, migration `20260811073334_user_role_partial_unique_s2_07`) — phát hiện lúc thiết kế gán lại vai trò: unique thường sẽ chặn gán lại đúng vai trò đã từng bị gỡ (dòng cũ soft-delete vẫn tính). Cùng loại C3 (`patient.national_id_hash`).
  - **Bug fix đi kèm**: `UserAccountAuthRepository.findRoleNamesForUser()` thiếu filter `deletedAt: null` — chưa từng lộ vì trước S2-07 không có đường nào soft-delete `user_role`; sửa trước khi feature mới (gán lại vai trò) làm lộ ra (ảnh hưởng `login`/`GET /auth/me`).
  - **`apps/api/src/modules/iam/`** (mở rộng): `UserAccountController`/`Service`/`Repository` — `POST/GET/GET:id/PATCH /api/v1/users`, `POST /api/v1/users/:id/reset-password`. Quyền `user_account.read`/`user_account.manage` (đã seed từ S1-04b). Đổi mật khẩu tách endpoint riêng khỏi sửa hồ sơ (thao tác nhạy cảm, audit riêng).
  - **Thu hồi phiên đăng nhập THẬT lần đầu tiên** khi đổi vai trò/vô hiệu hoá/đặt lại mật khẩu (`SessionRepository.revokeAllForUser()` viết sẵn từ S1-04 chờ đúng chỗ gọi này — xem `docs/DECISIONS.md` #019) — đúng yêu cầu `.claude/docs/security-audit.md` "Đổi vai trò hoặc chuyển tenant thu hồi toàn bộ phiên đang mở".
  - **`apps/api/src/modules/clinic/`** (module mới): `RoomController`/`Service`/`Repository` (`POST/GET/PATCH /api/v1/rooms`, không phân trang — quy mô nhỏ), `ClinicSettingsController`/`Service`/`Repository` (`GET/PATCH /api/v1/clinic-settings` — giờ làm việc theo từng ngày trong tuần + độ dài slot, đọc/ghi `tenant_setting` 2 key `business_hours`/`slot_duration_minutes`, cùng mẫu `break_glass_duration_minutes`). Dùng chung quyền `clinic_config.read`/`clinic_config.update` cho cả phòng lẫn giờ làm/slot (PRD ADM-02 gộp chung một yêu cầu).
  - `packages/shared/src/user-account.ts`, `packages/shared/src/clinic.ts` (schema mới). `packages/core/src/errors/user-account-errors.ts`: `UserAccountDuplicateUsernameError` (409).
  - **Đã xác minh thật**: `user-account-http.spec.ts` (14/14 pass) — CRUD, trùng username → 409, optimistic lock, **thu hồi phiên thật** (đăng nhập lấy refresh cookie thật → admin đổi vai trò/vô hiệu hoá/đặt lại mật khẩu → refresh cookie cũ bị từ chối, xác nhận qua HTTP thật không phải suy luận), gán lại đúng vai trò đã gỡ trước đó không lỗi (xác nhận migration partial unique đúng), cách ly tenant. `clinic-http.spec.ts` (13/13 pass) — room CRUD, settings GET mặc định/PATCH từng phần/round-trip, cách ly tenant, permission. Toàn bộ 127 test `apps/api` (100 cũ + 27 mới) pass, không regress. `pnpm -w lint/typecheck/build` sạch. OpenAPI + web type đã sinh lại cho 6 endpoint mới.
- [x] S2-07 mở rộng (2026-08-20, ngoài kế hoạch, chủ dự án yêu cầu trực tiếp) — "Danh mục quản lý tài khoản" đầy đủ hồ sơ nhân sự + UI web đầu tiên cho `user_account`/`department` (`docs/DECISIONS.md` #063). `user_account` thêm 10 cột hồ sơ nhân sự (mã NV tự sinh, SĐT/2 email, học vị/chức danh/trạng thái+hình thức làm việc tham chiếu `reference_catalog`, được ký HSBA metadata, bắt buộc đổi mật khẩu lần đầu — enforce thật qua `POST /auth/change-password` + chặn điều hướng ở `RequireAuth.tsx`). `reference_catalog` thêm 4 category (`ACADEMIC_TITLE`/`STAFF_POSITION`/`EMPLOYMENT_STATUS`/`EMPLOYMENT_TYPE`, tự sinh `code`) + cột `deactivates_account` (chỉ `EMPLOYMENT_STATUS` — "Nghỉ việc" tự động vô hiệu hoá tài khoản, `resolveAccountActiveState` ở `packages/core`). Module `department`/`department_type` mới (`department` có bảng từ S1-04b nhưng chưa từng có UI) — phân loại 2 cấp tùy chọn, `code` tự sinh (prefix `KP`), UI 2 cột tham khảo "Tầng phòng". Web: `UserAccountPane`/`UserAccountFormDialog` (Boxed Section 3 khối, `MultiSelectCombobox` cho Vai trò, `PasswordInput`), `DepartmentPane` (master-detail), `ChangePasswordPage`, 4 pill danh mục mới trong "Vai trò & Phân quyền".
  - **Đã kiểm chứng RBAC thật bằng Playwright** (không chỉ suy luận từ code): tạo tài khoản chỉ gán 1 vai trò "Điều dưỡng" → đăng nhập bằng chính tài khoản đó → sidebar không hiện "Quản trị" → gọi thẳng URL trang quản trị bằng phiên thật → API trả `403` thật từ backend → vào trang được phép ("Hàng đợi khám") vẫn bình thường.
  - Rà soát mã hoá mật khẩu toàn hệ thống (yêu cầu chủ dự án): đã dùng Argon2id từ trước (S1-04), đạt chuẩn OWASP, không cần sửa.
  - **Đã xác minh thật**: `packages/core` `employment-status.spec.ts` (4/4), `generate-code.spec.ts` (2/2). `apps/api`: `auth-change-password-http.spec.ts` (4/4, mới), `user-account-hr-profile-http.spec.ts` (9/9, mới, tách riêng để không vượt rate-limit login), `reference-catalog-http.spec.ts` thêm test `deactivatesAccount`. Tổng 352 test `apps/api` (351 pass ổn định, 1 flake tiền-nhiệm không liên quan ở `appointment-http.spec.ts`). `pnpm -w typecheck/lint/build` sạch toàn workspace. Playwright qua Chrome thật xác nhận toàn bộ luồng chức năng + RBAC (xem trên).
- [x] S2-08 — Web: danh sách + form + chi tiết bệnh nhân (PAT-01→03). Hỏi chốt workflow trước khi code, theo yêu cầu chủ dự án: chi tiết sửa TẠI CHỖ (không modal), chống trùng kiểm tra LÚC bấm Lưu (không debounce theo ký tự) — nghi trùng chặn submit lần đầu, hiện cảnh báo kèm "Vẫn tạo mới"/"Huỷ".
  - `apps/web/src/features/patient/`: `PatientListPage.tsx` (bảng + ô tìm kiếm debounce 300ms dùng chung `q` của PAT-02, phân trang cursor bằng nút "Tải thêm" — không "Đang xem 1-N trên tổng" vì cursor không có tổng số), `PatientNewPage.tsx` (gọi `check-duplicate` trước khi `POST /patients`), `PatientDetailPage.tsx` (xem/sửa cùng một `PatientFormFields`, `disabled` khi không ở chế độ sửa), `PatientFormFields.tsx` (form dùng chung tạo/sửa, lưới 2 cột theo `ui-guidelines.md` mục 4.1), `patient.api.ts`/`patient.queries.ts` (TanStack Query, cache key có `tenantId`), `patient-form.utils.ts` (chuyển đổi form phẳng ↔ payload API có `address` lồng).
  - `apps/web/src/shared/ui/ErrorBanner.tsx`, `Skeleton.tsx`, `apps/web/src/shared/hooks/useDebouncedValue.ts` — viết ở `shared/` với chủ đích tái dùng ngay (không riêng cho patient), theo `CLAUDE.md`.
  - `Sidebar.tsx` thêm mục "Bệnh nhân" (`PATIENT_ROLES` khớp `patient.read` trong ma trận mặc định — mọi vai trò trừ `system_admin`).
  - **2 bug phát hiện + sửa lúc kiểm bằng trình duyệt thật**: (1) `Button.tsx` — override `className` thô (ví dụ nút "Huỷ" nền trắng) bị thua class mặc định `bg-blue-600`/`text-white` vì thứ tự thắng CSS phụ thuộc thứ tự trong stylesheet đã build, không phải thứ tự chuỗi class ở HTML → nút vô hình (chữ trắng nền trắng). Sửa bằng thêm `variant` (`primary`/`secondary`/`danger`) thay vì override `className`. (2) `App.tsx` — `QueryClient` mặc định retry 3 lần có backoff cho mọi lỗi kể cả `404 NOT_FOUND` (ví dụ vào `/patients/:id` với id không tồn tại), làm chậm hiển thị empty state vài giây không cần thiết → thêm `retry` tuỳ chỉnh: không retry khi `ApiError.code === 'NOT_FOUND'`.
  - **Đã xác minh thật qua trình duyệt** (Playwright headless Chromium, cài tạm ở scratchpad, chụp ảnh từng bước): đăng nhập → danh sách bệnh nhân hiện đúng bảng + tìm kiếm hoạt động; "Thêm bệnh nhân" tạo hồ sơ mới thành công → điều hướng sang trang chi tiết; tạo lại đúng tên + ngày sinh → cảnh báo trùng hiện đúng (link tới hồ sơ cũ, nút "Huỷ"/"Vẫn tạo mới"), bấm "Huỷ" không tạo bản ghi; vào chi tiết, bấm "Sửa hồ sơ", đổi số điện thoại, "Lưu" → quay lại chế độ xem với giá trị mới đã lưu; `console --errors` không có lỗi ngoài dự kiến (chỉ 401 ở lần `/auth/refresh` đầu tiên trước khi đăng nhập, hành vi đã biết từ S1-08/S1-09). `pnpm -w lint/typecheck/build` sạch toàn workspace; 127 test `apps/api` pass, không regress.
  - **Sửa 2 bug hạ tầng dùng chung sau khi dùng thử** (docs/DECISIONS.md #028): access token hết hạn giữa chừng (15 phút) trước đây báo lỗi "Không tải được dữ liệu" thay vì tự làm mới — `shared/api/client.ts` giờ tự phát hiện 401, gọi `/auth/refresh`, thử lại request cũ đúng 1 lần, không lộ lỗi ra UI; gõ tìm kiếm giao diện nháy liên tục — thêm `placeholderData: keepPreviousData` giữ kết quả cũ hiển thị tới khi kết quả mới về. Xác minh thật bằng cách hạ tạm TTL token xuống 6 giây rồi khôi phục.
  - **Redesign App Shell v2 sau khi dùng thử tiếp** (docs/DECISIONS.md #027, #029): đổi tên "Bệnh nhân" → "Danh sách bệnh nhân", gộp vào nhóm sidebar mới "Tiếp nhận và Đặt lịch"; bỏ nút "Thêm bệnh nhân" (tạo mới chuyển sang luồng Đặt lịch/Tiếp nhận); sidebar thêm vùng logo + thu gọn; thêm `TopBar` (breadcrumb thay nút "quay lại" + lời chào/avatar, thay `UserMenu.tsx` đã xoá); màn hình danh sách theo List Screen Pattern mới (`ui-guidelines.md` mục 9): rộng gần hết chiều ngang, bảng cố định chiều cao cuộn nội bộ, cuộn vô hạn (`PATIENT_LIST_LIMIT` 20→50) + `@tanstack/react-virtual`, mở hồ sơ bằng double-click mã bệnh nhân, nút Sửa hồ sơ chỉ hiện với vai trò có `patient.update`. Sửa thêm 2 bug phát hiện lúc dùng thử: breadcrumb tồn đọng ở Dashboard/Admin (quên gọi `useBreadcrumb`), icon nhóm sidebar lúc thu gọn bấm không phản hồi. Đã xác minh thật qua Playwright toàn bộ luồng điều hướng; `pnpm -w lint/typecheck/build` sạch; 127 test `apps/api` pass.
- [x] S2-09 — Web: lịch hẹn dạng lưới (bác sĩ × khung giờ) + dạng danh sách (APP-01). Hỏi chốt workflow trước khi code theo đúng thói quen dự án — nhiều vòng hỏi/chốt lại sau khi xem mockup Artifact (ảnh tham khảo Zendenta do chủ dự án gửi, đã hoà giải với `ui-guidelines.md` — xem `docs/DECISIONS.md` #030): kéo-chọn chỉ để đặt mới; tạo nhanh bệnh nhân ngay trong form khi không tìm ra; cột=bác sĩ/hàng=khung giờ (bước 30 phút) chọn 1 ngày; sau khi xem mockup bổ sung thêm đổi/dời lịch (nút "Sửa" trong panel chi tiết) + cảnh báo trễ hẹn (chỉ gợi ý thị giác, giữ ranh giới APP-05/Sprint 5) + đường kẻ "bây giờ" + gợi ý bác sĩ trống lịch trong form đặt nhanh; xác nhận KHÔNG làm "ca làm việc riêng từng bác sĩ" (cần bảng mới ngoài ERD đã chốt).
  - **Backend mở rộng**: `packages/core/src/date/vietnam-day-range.ts` (util thuần, cùng kỹ thuật UTC+7 cố định như `format-display-code.ts`). Hai port mới `DoctorDirectoryPort`/`ClinicConfigReaderPort` (`packages/core/src/ports/`) — module `appointment` đọc dữ liệu `iam`/`clinic` qua port thay vì import chéo, đúng ví dụ `PatientReaderPort` đã nêu ở `.claude/docs/coding-standards.md`. `AppointmentController` thêm `GET /appointments/doctors`, `GET /appointments/schedule-config` (2 endpoint chiếu tối thiểu, gắn quyền `appointment.read` đã có — giải quyết lỗ hổng thật: lễ tân không có `user_account.read`/`clinic_config.read`, xem `docs/DECISIONS.md` #030), `PATCH /appointments/:id/reschedule` (đổi/dời lịch, tái dùng khuôn `cancel()` + exclusion constraint C2 chặn trùng giờ mới). `GET /appointments` thêm filter `date` (dùng `vietnamDayRange()`) + join `patientFullName`/`patientCode` (chỉ ở `list()`, tránh N+1).
  - **`apps/web/src/features/appointment/`**: `appointment.api.ts`/`appointment.queries.ts` (theo khuôn `patient.*`), `AppointmentSchedulePage.tsx` (toggle Lưới/Danh sách), `AppointmentGridView.tsx` (cột sinh động theo số bác sĩ thật, tự cuộn ngang; đường kẻ "bây giờ" — mỗi cột tự vẽ đoạn riêng, xem bài học ở `docs/DECISIONS.md` #031), `AppointmentListView.tsx` (List Screen Pattern), `AppointmentQuickCreatePanel.tsx` (gợi ý bác sĩ trống/bận theo giờ vừa chọn, dựa dữ liệu đã tải sẵn trong ngày — không gọi API mới), `AppointmentDetailPanel.tsx` (xem/huỷ/sửa). `apps/web/src/features/patient/PatientPicker.tsx` (mới, dùng chung — tìm bệnh nhân debounce + tạo nhanh, tái dùng nguyên `PatientFormFields`/`useCreatePatientMutation`).
  - `Sidebar.tsx` thêm "Lịch hẹn" trong nhóm "Tiếp nhận và Đặt lịch" (`APPOINTMENT_ROLES` khớp `appointment.read`: receptionist/clinic_admin/doctor). `router.tsx` thêm route `/appointments`.
  - **Đã xác minh thật**: `appointment-http.spec.ts` thêm 13 test (38/38 tổng) — filter `date` đúng biên giờ VN, `patientFullName`/`patientCode` trong response list, `GET /appointments/doctors` đúng danh sách active + không lộ nurse/receptionist, lễ tân/bác sĩ gọi được 2 endpoint chiếu mới dù không có `user_account.read`/`clinic_config.read`, `reschedule` đủ cạnh (thành công, trùng giờ → 409, đổi hộ bác sĩ khác scope personal → 403, lịch không còn `SCHEDULED` → 409, version cũ → 409). Toàn bộ 140 test `apps/api` + 30 test `packages/core` pass, không regress. `pnpm -w lint/typecheck/build` sạch toàn workspace. OpenAPI + web type đã sinh lại.
  - **Đã xác minh thật qua trình duyệt** (Playwright, dữ liệu demo thật qua API — 5 bác sĩ, nhiều lịch hẹn): lưới hiện đúng cột bác sĩ + khung giờ + đường kẻ bây giờ; đặt lịch nhanh (tìm bệnh nhân có sẵn, gợi ý trống/bận theo giờ); chuyển Danh sách thấy đúng dữ liệu; huỷ có lý do → trạng thái cập nhật ngay không cần tải lại; sửa/dời lịch thành công + demo đúng báo trùng giờ khi chọn giờ đã có lịch khác. 1 bug thật phát hiện + sửa lúc kiểm (giới hạn `limit` chế độ lưới vượt trần `listAppointmentsQuerySchema.max(100)` → 400) và 1 bug CSS Grid thật (đường kẻ bây giờ làm trống cả lưới — xem `docs/DECISIONS.md` #031).
- [x] S2-09b — Đổi mô hình đặt lịch sang "lead capture" (`docs/DECISIONS.md` #032, chủ dự án yêu cầu ngay sau khi dùng thử S2-09 ở trên): **không** tạo/gắn `patient` lúc đặt — chỉ ghi Họ tên/SĐT/Lý do khám trực tiếp lên `appointment` (`full_name`/`phone`/`reason` cột mới, `patient_id` đổi nullable, để sẵn cho Tiếp nhận thật gắn sau — Sprint 3, chưa xây). Mỗi lịch có `booking_code` (mã đặt lịch, sinh qua `code_sequence`/`formatDisplayCode()` đúng khuôn `patient_code`, prefix `LH`). Tra cứu `GET /appointments/lookup?phone=` tự điền tên theo lần đặt gần nhất + đếm số lần huỷ, cảnh báo spam khi ≥5 lần (chỉ cảnh báo, không chặn — ngưỡng đặt ở `apps/web` không phải `packages/shared`, xem lý do kỹ thuật ở #032). Check-in (`POST /appointments/:id/checkin`, tái dùng quyền `appointment.update`) chuyển thẳng `SCHEDULED → CONVERTED`, không sinh trạng thái mới, chưa tạo `encounter`/gắn `patient_id` (Tiếp nhận thật là Sprint 3) — chỉ đổi trạng thái + hiện thông báo đã ghi nhận.
  - Migration `20260812012715_appointment_lead_capture` (viết tay, không dùng `prisma migrate diff` — tránh trùng "drift giả" với exclusion constraint C2 raw-SQL-only). `AppointmentQuickCreatePanel.tsx` bỏ hẳn `PatientPicker` (giữ nguyên file, không xoá — có thể cần lại ở Tiếp nhận S3), thay bằng 3 trường SĐT/Họ tên/Lý do khám + banner cảnh báo spam + dialog xác nhận đặt lịch thành công (nội dung đúng chữ chủ dự án yêu cầu, có mã đặt lịch). `AppointmentDetailPanel.tsx` thêm nút "Check-in" + dialog thông báo thành công.
  - Cập nhật `docs/ERD.md` (v1.4), `.claude/docs/data-model.md`, `.claude/docs/clinical-workflow.md` mục "Đặt lịch" trong cùng lúc (thay đổi cấu trúc đã chốt).
  - **Đã xác minh thật**: `appointment-http.spec.ts` viết lại toàn bộ theo body mới (50/50 pass, tổng 152 test `apps/api` không regress). `pnpm -w lint/typecheck/build` sạch. Playwright qua dữ liệu thật: đặt lịch mới → dialog xác nhận đúng "Đã đặt lịch thành công cho khách hàng [Tên] vào lúc [Giờ] · [Ngày] với mã số là: [Mã]"; đặt lại cùng SĐT → tự điền đúng tên lần gần nhất; SĐT có 5 lần huỷ → đúng banner cảnh báo; check-in → dialog "Đã ghi nhận khách đến" + trạng thái chuyển "Đã chuyển khám" trên lưới.
  - **1 bug bundler thật phát hiện + xử lý** (không phải lỗi logic, xem chi tiết ở `docs/DECISIONS.md` #032): hằng số ngưỡng spam không export được từ `packages/shared` qua `vite build` (Rollup không dò được named export là hằng số giá trị thuần qua `__exportStar`, dù Node `require()` thấy đúng) — chuyển hằng số này sang khai báo tại `apps/web` (đúng vị trí kiến trúc thật: chỉ web dùng để hiển thị, `apps/api` không so sánh/chặn theo ngưỡng này).
- [x] S2-10 — Test cách ly tenant cho toàn bộ endpoint mới + vá lỗi. Rà soát `appointment-http.spec.ts`/`user-account-http.spec.ts`/`clinic-http.spec.ts` (S2-05→S2-09b, S2-07) theo `.claude/docs/multi-tenancy.md` ("mỗi endpoint chạm dữ liệu... phải có test cách ly tenant: tenant A gọi ID tenant B → 404"). `clinic-http.spec.ts` đã đủ từ S2-07 (room list/PATCH, clinic-settings độc lập theo tenant), không thêm gì. Bổ sung 7 test còn thiếu — **không tìm thấy bug thật, chỉ thiếu xác nhận** (code repository/service đã lọc `tenantId` đúng từ đầu, `findById`/`updateIfVersionMatches` luôn ghép `tenantId` của actor vào `WHERE`):
  - `appointment` (+4, `appointment-http.spec.ts`): `GET /appointments/doctors` tenant B không thấy bác sĩ tenant A; `GET /appointments/schedule-config` tenant B độc lập (không lẫn cấu hình tenant A); `GET /appointments/lookup?phone=` tenant B tra đúng SĐT đã đặt ở tenant A → không lộ tên/lịch sử (trả như chưa từng đặt) — điểm đáng chú ý nhất vì đây là tra cứu chỉ theo SĐT (không theo ID cụ thể), rủi ro lộ PII nếu quên lọc tenant; `GET /appointments` (danh sách, scope global) tenant B không thấy lịch hẹn tenant A.
  - `user-account` (+3, `user-account-http.spec.ts`): `GET /users` danh sách tenant B không thấy tài khoản tenant A; `PATCH /users/:id` tenant B sửa tài khoản tenant A → 404 (không phải 403 — trước đó chỉ có test cho `GET /users/:id`, chưa có cho `PATCH`/`reset-password`); `POST /users/:id/reset-password` tenant B đặt lại mật khẩu tài khoản tenant A → 404, mật khẩu cũ vẫn dùng được (xác nhận không có tác dụng phụ nào lọt qua).
  - **Đã xác minh thật**: 159/159 test `apps/api` pass (152 cũ không regress + 7 mới), `pnpm -w typecheck` sạch toàn workspace. Không có migration/schema/OpenAPI nào đổi — thuần bổ sung test theo endpoint đã có.

**Gate cuối sprint 2** (từ `plan.md`):
- [x] Hai phiên đặt lịch đồng thời cùng khung giờ: một thành công, một trả `APPOINTMENT_SLOT_CONFLICT` — xác minh thật bằng `Promise.all` gửi 2 request đồng thời (S2-05, `appointment-http.spec.ts`), không chỉ chạy tay
- [x] Tìm kiếm bệnh nhân trên 50.000 bản ghi trả về dưới 1 giây — đo thật, cả 3 kiểu tìm dưới 35ms (S2-02, xem `apps/api/scripts/perf-patient-search.ts`)
- [x] Không endpoint nào trả danh sách bệnh nhân thiếu lọc `tenant_id` — `PatientRepository.list()` luôn lọc `tenantId` (S2-01)

**Ngoài kế hoạch — mở rộng PAT-01 (`docs/DECISIONS.md` #034)**: Sprint 3 tạm hoãn (S3-01 chặn vì thiếu file danh mục ICD-10 chính thức), chủ dự án yêu cầu quay lại hiệu chỉnh form Thêm/Sửa hồ sơ bệnh nhân trước — thêm ảnh đại diện (upload thật qua `StoragePort` + signed URL, lần đầu hệ thống phục vụ file), CCCD ngày/nơi cấp, dân tộc/quốc tịch/nghề nghiệp, số bảo hiểm, Khu phố, nhóm "Thông tin người thân" (1 bộ/hồ sơ). Migration `20260812150000_patient_profile_fields`. Chi tiết đầy đủ ở `docs/CURRENT.md` và `docs/DECISIONS.md` #034. Đã xác minh: `patient-http.spec.ts` 33/33 (tổng 176 test `apps/api`), `signed-url.spec.ts` 6/6, `sniff-image-extension.spec.ts` 5/5 (`packages/core`), Playwright qua trình duyệt thật.

**Ngoài kế hoạch — danh mục dùng chung `reference_catalog` (`docs/DECISIONS.md` #037)**: đảo ngược #034 cho riêng `ethnicity`/`nationality` (`occupation` không đổi) — chủ dự án tự cung cấp 2 file dữ liệu thật (54 dân tộc, 30 quốc tịch), muốn xây thành master data **quản lý được** qua UI (không chỉ xem). Bảng mới `reference_catalog` (toàn hệ thống, không tenant_id/version, "xoá" là soft `is_active=false` vì role DB không có quyền `DELETE`). Module `apps/api/src/modules/reference-catalog/` (CRUD đầy đủ, permission mới `reference_catalog.read`/`.manage` đúng chuẩn `PermissionGuard`). Web: `ReferenceCatalogAdminPage.tsx` (tab theo category, CRUD qua modal), `Sidebar.tsx` "Quản trị" đổi thành nhóm cha/con (`/admin/reference-catalog`), `PatientFormFields.tsx` 2 input text → dropdown (lưu `code`, giữ nguyên giá trị cũ không khớp danh mục qua `withLegacyValueOption`). Xoá `AdminPage.tsx`/route `/admin` cũ (không còn nội dung thật). Chi tiết đầy đủ ở `docs/CURRENT.md` và `docs/DECISIONS.md` #037. Đã xác minh: `reference-catalog-http.spec.ts` 10/10 (tổng 191 test `apps/api`), `data.spec.ts` 2/2 (tổng 42 test `packages/core`), Playwright qua trình duyệt thật (CRUD đầy đủ, legacy value giữ nguyên).

**Ngoài kế hoạch — danh mục hành chính `province`/`ward` (`docs/DECISIONS.md` #038)**: đảo ngược tiếp phần Tỉnh/Xã của #034 — chủ dự án gửi file dữ liệu chính thức Bộ Nội vụ (34 tỉnh + 3321 phường/xã, sáp nhập hành chính 2025), chốt 2 bảng chuẩn hoá + **read-only** (khác `reference_catalog` — không quản lý qua UI) trước khi code. Bảng mới `province`/`ward` (toàn hệ thống, không tenant_id/version/is_active, REVOKE INSERT/UPDATE khỏi `nexamed_app`, cùng bản chất `icd10_catalog`). Module `apps/api/src/modules/geo/` (`GET /geo/provinces`, `GET /geo/wards?provinceCode=` — bỏ trống trả toàn bộ, dùng dựng bảng tra code→tên). Quyền dùng lại `patient.read`, không thêm permission mới. Web: `PatientFormFields.tsx` 2 input text → Combobox cascading (đổi Tỉnh tự xoá Phường/Xã); `withLegacyValueOption()` tách ra `shared/ui/Combobox.tsx` dùng chung. Sửa 4 tên tỉnh viết tắt "Tp" → "Thành phố" theo yêu cầu chủ dự án lúc rà lại dữ liệu. Chi tiết đầy đủ ở `docs/CURRENT.md` và `docs/DECISIONS.md` #038. Đã xác minh: `geo-http.spec.ts` 7/7 (tổng 198 test `apps/api`), `geo/data.spec.ts` 4/4 (tổng 46 test `packages/core`), Playwright qua trình duyệt thật (Combobox cascading đúng, cột Địa chỉ ở Danh sách bệnh nhân hiện đúng tên đã resolve từ mã).

**Ngoài kế hoạch — trang "Danh mục" + "Cấu hình hệ thống", mẫu Two-panel Config Screen (`docs/DECISIONS.md` #039, #040)**: chủ dự án gửi 2 ảnh tham khảo cho phong cách trang Cấu hình, dựng mockup Artifact 2 kiểu trước khi code, chốt Kiểu 2 (thanh nhóm + thẻ) — không mặc định cho mọi trang Quản trị tương lai. Route `/admin/reference-catalog` → `/admin/catalog` ("Danh mục"), gộp `reference_catalog` + `geo` (Tỉnh/Thành, Phường/Xã — vẫn chỉ đọc đúng #038) chung 1 pill. `ClinicHoursPane` (giờ làm việc + slot, dùng `GET/PATCH /clinic-settings` có sẵn từ S2-07, lần đầu có UI web) tách sang mục sidebar mới "Cấu hình hệ thống" (`/admin/system-config`) sau nhiều vòng chỉnh trực tiếp trên bản chạy thật. Trích `apps/web/src/shared/ui/ConfigScreenShell.tsx` (lần dùng thứ hai của khuôn pill+list+content) để 2 trang cấu hình luôn đồng nhất bố cục. Điều tra báo cáo "lưới lịch hẹn không áp đúng giờ đã cấu hình" — xác minh **không có bug thật** (dữ liệu lệch tạm thời do chính kịch bản test Playwright của phiên này, đã khôi phục); việc lưới cần cuộn nhiều để thấy hết 1 ngày dài là hành vi có sẵn từ S2-09, ghi vào `docs/CURRENT.md` mục "Đang chờ" để hỏi chủ dự án. Không migration/schema/API contract nào đổi. Chi tiết đầy đủ ở `docs/CURRENT.md` và `docs/DECISIONS.md` #039/#040. Đã xác minh: `pnpm -w typecheck/lint/build` sạch, Playwright qua trình duyệt thật (dữ liệu thật, không mock) — 2 trang cấu hình đồng nhất, CRUD/browse đúng, giờ làm việc lưu đúng và phản ánh ngay trên lưới lịch hẹn.

**Ngoài kế hoạch — backfill `role_permission` cho tenant cũ + tối ưu khoảng trống lưới lịch hẹn**: giải quyết 2 việc treo trong `docs/CURRENT.md` mục "Đang chờ", đã hỏi và chốt hướng trước khi code. (1) `apps/api/src/infrastructure/persistence/sync-role-permissions.ts` — đồng bộ `role_permission` còn thiếu cho tenant đã tồn tại, chạy tự động lúc API khởi động (`main.ts`), chỉ thêm dòng thiếu theo `DEFAULT_ROLE_PERMISSIONS` cho vai trò `is_system_default=true`, không đụng vai trò tuỳ biến. (2) `AppointmentSchedulePage.tsx`/`AppointmentGridView.tsx` — gộp 2 hàng công cụ thành 1, giảm padding header lưới; giữ nguyên `ROW_HEIGHT_PX`/bước 30 phút theo đúng quyết định đã chốt lúc S2-09 (không tự đổi mật độ dữ liệu). Không migration/schema/API contract nào đổi. Chi tiết đầy đủ ở `docs/CURRENT.md`. Đã xác minh: `sync-role-permissions.spec.ts` 4/4 (tổng 202 test `apps/api`), kiểm thật trên DB dev (xoá 1 dòng role_permission → khởi động API → tự vá lại đúng), `pnpm -w typecheck/lint/build` sạch, Playwright qua trình duyệt thật (layout lưới lịch hẹn không vỡ ở 1366×768, các thao tác vẫn đúng).

**Ngoài kế hoạch — trang "Thông tin phòng khám" (`docs/DECISIONS.md` #041)**: mục con mới trong pill "Cấu hình phòng khám" (`/admin/system-config`), đặt trước "Giờ làm việc & Slot", mặc định mở đầu tiên — theo yêu cầu chủ dự án. Dùng EnterPlanMode + hỏi 4 điểm trước khi code (đụng schema `tenant`): mở rộng cột trên `tenant` (không dùng `tenant_setting`), `currency`/`timezone` chỉ lưu chưa nối logic, kích thước logo chỉ khuyến nghị. Migration `20260813120000_tenant_profile_fields` thêm `phone`/`email`/`currency`/`timezone`/`logo_key`/`print_logo_key`, không permission mới (dùng lại `clinic_config.*`). `apps/api/src/modules/clinic/clinic-profile.{controller,service,repository}.ts` (mới, cùng khuôn upload ảnh đại diện bệnh nhân #034). `packages/shared/src/currency.ts`/`timezone.ts`, `packages/core/src/errors/clinic-errors.ts` (mới). `apps/web/src/features/clinic/ClinicInfoPane.tsx` (mới).
  **2 bug phát hiện lúc dùng thử, đã sửa**: (1) ảnh vỡ do `<img>` dùng đường dẫn tương đối phân giải sai origin — sửa bằng `resolveApiUrl()` dùng chung (`shared/api/client.ts`), áp dụng lại cho `PatientAvatarUpload.tsx` (bug có sẵn từ #034); (2) upload ảnh hoạt động độc lập với chế độ Sửa gây khó hiểu — đổi sang ẩn nút "Chọn ảnh" tới khi bấm "Sửa" ở cả patient avatar lẫn clinic logo, ghi quy tắc bắt buộc vào `.claude/docs/ui-guidelines.md` mục 9b. Chi tiết đầy đủ ở `docs/CURRENT.md` và `docs/DECISIONS.md` #041. Đã xác minh: `clinic-profile-http.spec.ts` 18/18 (tổng 220 test `apps/api`), `pnpm -w typecheck/lint/build` sạch, Playwright qua trình duyệt thật (CRUD đầy đủ, upload logo hiển thị đúng ảnh thật, gating Sửa đúng ở 2 màn hình).

## Sprint 3 — Tiếp nhận + Khám bệnh (Tuần 5-6, theo `docs/product/plan.md` mục 6)

S3-01 (danh mục ICD-10) **đã xong, mở khoá một phần** (2026-08-19, `docs/DECISIONS.md` #056) — xem `docs/CURRENT.md`. Phần "Tiếp nhận" (REC-01→03) không phụ thuộc ICD-10 nên làm trước được, xem `docs/DECISIONS.md` #042 → #043 (thiết kế lại 3 màn hình sau 3 vòng góp ý) → #044 (dồn thao tác ra khỏi "Danh sách tiếp nhận", hợp nhất biểu mẫu tiếp nhận). S3-05→07 (phần "Khám bệnh" thật) vẫn cần thêm bộ chọn chẩn đoán, chưa xây.

- [x] S3-01 — Nhập và chuẩn hoá danh mục ICD-10 — **mở khoá một phần**: seed ĐỦ Chương I-XXII (15.844 mã). Migration `20260819110000_icd10_catalog`, `packages/core/src/icd10/data.ts` (sinh từ `docs/data/icd10-chuong-*.md`, 22 file — nhúng JSON + `JSON.parse` vì mảng literal 15.844 phần tử làm TS lỗi `TS2590`), `packages/core/src/icd10/roman-numeral.ts` (`romanToInt`, sắp đúng thứ tự Chương thật — `chapterCode` là số La Mã, không sắp được theo thứ tự chuỗi). Module `apps/api/src/modules/icd10/` (4 endpoint: chapters/groups/codes/tìm kiếm theo tên không dấu, tái dùng `nexamed_unaccent_lower()`). Trang tra cứu web `apps/web/src/features/catalog-clinical/` (`/admin/catalog-clinical`, `ConfigScreenShell` pill phẳng, 3 cột cascade). Đã xác minh: `data.spec.ts` 9/9 + `roman-numeral.spec.ts` 3/3 (`packages/core`), `icd10-http.spec.ts` 11/11 (tổng 296 test `apps/api`), `pnpm -w lint/typecheck/build` sạch, Playwright qua Chrome thật. Hạn chế đã biết, chủ động để nguyên theo yêu cầu chủ dự án: tìm nhanh từ khoá ngắn có thể khớp chuỗi con không liên quan ngữ nghĩa (xem `docs/CURRENT.md`).
- [x] S3-02 (một phần) — State machine `encounter` trong `packages/core` + unit test cho mọi cạnh hợp lệ và không hợp lệ. `packages/core/src/encounter/encounter-state-machine.ts` (`canTransitionEncounter`/`assertEncounterTransition`), test ma trận đầy đủ 6×6 (`encounter-state-machine.spec.ts`, 5/5).
- [x] S3-03 — Check-in (2 luồng, dùng chung 1 biểu mẫu web `docs/DECISIONS.md` #044): (1) từ lịch hẹn có sẵn — `POST /reception/check-in` (atomic: tạo `encounter` + gắn `patient` + chuyển `appointment→CONVERTED`), popup đầy đủ trên panel chi tiết Lịch hẹn; (2) "Tiếp nhận bệnh nhân" (khách không qua đặt lịch) — `POST /reception/direct`, tạo thẳng `encounter` với `appointment_id=NULL`. Cả hai đều lưu snapshot loại khám (`exam_type_code/name/price`) + nguồn khách (`patient_source_code`, tuỳ chọn — 2 danh mục mới, tái dùng `reference_catalog`) và kèm sinh hiệu tuỳ chọn (tạo `vital_sign` cùng transaction nếu có nhập). Snapshot thẻ BHYT luôn `{selfPay:true}` (S2-04 chưa làm). `apps/api/src/modules/reception/`, `apps/api/src/modules/encounter/` (mới, `POST /encounters/:id/start|cancel`, không thuộc `reception`).
- [x] S3-04 — Sinh hiệu: nhập ngay lúc tiếp nhận (trên biểu mẫu, `docs/DECISIONS.md` #044) hoặc bổ sung sau qua `POST /reception/encounters/:encounterId/vital-signs` (ngưỡng cảnh báo theo nhóm tuổi — quyết định khác plan.md gốc "cấu hình được", `docs/DECISIONS.md` #042), lưu số nguyên (gram, mm, phần mười độ C). `packages/core/src/vital-sign/vital-sign-thresholds.ts`.
- [x] S3-05 — API màn hình khám: gộp tiền sử, dị ứng, sinh hiệu trong một request (`docs/DECISIONS.md` #059). Bảng `diagnosis`/`clinical_note` hiện thực (migration `20260820120000_clinical_diagnosis_note`, đúng khuôn đã đặc tả). 4 endpoint mới `apps/api/src/modules/encounter/`: `GET .../consultation`, `PUT .../diagnoses`, `PUT .../clinical-note`, `POST .../complete` (Hoàn tất khám không đợi Kê đơn Sprint 4). Đã xác minh: `encounter-http.spec.ts` 27/27 (thêm 17), tổng 333/333 test `apps/api` pass, `pnpm -w typecheck` sạch, OpenAPI + web codegen đã sinh lại.
- [x] S3-06 — Web: màn hình khám (`/encounters/:id`, `apps/web/src/features/encounter/`) đúng bố cục mockup đã chốt (panel trái tiền sử cố định, dải sinh hiệu + BMI + cảnh báo dị ứng dưới topbar, 4 tab neo cuộn — chỉ tab 1 "Khám & Chẩn đoán" nội dung thật, tab 2-4 "Sắp ra mắt"). Vào từ "Hàng đợi khám" (nút "Bắt đầu khám"/"Tiếp tục khám" cho dòng `IN_CONSULTATION`). Sidebar tự thu gọn khi vào trang (`shared/layout/sidebar.context.tsx`).
- [x] S3-07 — Web: ghi chú lâm sàng + chọn chẩn đoán ICD-10 (`Icd10DiagnosisPicker.tsx`, tái dùng `useIcd10SearchQuery`) — lưu chẩn đoán tự động mỗi lần thêm/xoá/đổi bệnh chính (giữ bất biến đúng 1 PRIMARY), ghi chú lưu thủ công qua "Lưu nháp". **Đã đổi khỏi 4 mục SOAP sang 2 nhóm "Tiền sử"/"Thăm khám" (8 mục, migration `20260820150000_clinical_note_sections_v2`) theo yêu cầu chủ dự án** — xem `docs/DECISIONS.md` #060: "Tiền sử dị ứng" ghi thẳng `patient.allergyNote` (không phải section riêng, ripple `doctor.patient.update=global`); "Lý do khám"/"Chuẩn đoán" bắt buộc. Sinh hiệu bổ sung/đo lại được ngay trong màn khám (`VitalSignsDialog.tsx`, nới guard `recordVitalSigns` sang cả `IN_CONSULTATION`); ngưỡng cảnh báo nhiệt độ 38.5°C, BMI phân loại theo chuẩn WHO Châu Á - TBD. "Hoàn tất khám" gọi `POST .../complete`. Đã xác minh qua Playwright thật + 335/335 test `apps/api` pass, `pnpm -w typecheck/lint/build` sạch toàn workspace.
- [x] S3-08 (một phần) — Web: 3 màn hình tách biệt (`docs/DECISIONS.md` #043/#044), `apps/web/src/features/reception/` — `ReceptionListPage.tsx` ("Danh sách tiếp nhận", route `/reception`, THUẦN theo dõi trạng thái, không thao tác nào), `ReceptionRegisterPage.tsx` ("Tiếp nhận bệnh nhân", route `/reception/new`), `ReceptionDoctorQueuePage.tsx` ("Hàng đợi khám", route `/reception/doctor-queue`, riêng cho bác sĩ — nút "Bắt đầu khám" thật). `ReceptionIntakeForm.tsx` (mới) — biểu mẫu tiếp nhận DÙNG CHUNG cho cả trang "Tiếp nhận bệnh nhân" và popup check-in trên `AppointmentDetailPanel.tsx` (`ReceptionIntakeDialog.tsx`, thay `ReceptionCheckInDialog.tsx` cũ).
- [x] S3-09 — Test + đo hiệu năng màn hình khám. Bổ sung 3 test cách ly/scope còn thiếu ở `encounter-http.spec.ts` (`.claude/docs/multi-tenancy.md`: mỗi endpoint chạm dữ liệu phải có test tenant A/B) — `PUT .../clinical-note`: "bác sĩ khác (scope personal) → 404", "tenant B → 404"; `POST .../complete`: "bác sĩ khác (scope personal) → 404" (3 endpoint còn lại của S3-05 đã có sẵn từ trước). `apps/api/scripts/perf-consultation.ts` (mới, thủ công không chạy CI, cùng khuôn `perf-patient-search.ts` S2-02) — đo đúng 4 truy vấn tuần tự của `EncounterService.getConsultationDetail()` qua role app + RLS thật, bệnh nhân có 20 lượt khám cũ (PRD mục 5/plan.md S3-09). Đã xác minh thật: 338/338 test `apps/api` pass (30/30 `encounter-http.spec.ts`, thêm 3), `pnpm -w typecheck/build` sạch. Đo thật trên dev DB: tổng 4 truy vấn 33ms — ĐẠT xa ngưỡng "dưới 2 giây" (ENC-01, gate cuối Sprint 3).

**Bổ sung giữa phiên theo yêu cầu chủ dự án (ngoài plan.md gốc)**:
- Số điện thoại được phép trùng thật sự — `GET /patients/by-phone` (module `patient`) phục vụ cảnh báo mềm ở form Thêm/Sửa khách hàng (`PhoneDuplicateWarning.tsx`, không chặn lưu). **Đã đổi (2026-08-18, `docs/DECISIONS.md` #052)**: chọn khách hàng lúc tiếp nhận không còn qua `PatientPicker` (đã bỏ khỏi màn Tiếp nhận, giữ file cho nơi khác nếu cần) — nay qua `PatientMatchDialog.tsx` (popup) sau khi gõ SĐT/CCCD tự tra trùng trực tiếp trên `PatientFormFields`.
- 2 danh mục cấu hình mới "Nguồn khách hàng"/"Loại khám" (`reference_catalog` category `PATIENT_SOURCE`/`EXAM_TYPE` + cột `price`) — quản lý ở trang "Danh mục" (`/admin/catalog`), phục vụ "Tiếp nhận bệnh nhân". Giá chỉ lưu để hiển thị, không tính viện phí (ngoài phạm vi v1).
- **Thiết kế lại "Tiếp nhận bệnh nhân" (2026-08-18, `docs/DECISIONS.md` #052)** — mockup đã duyệt: hiện đủ field bệnh nhân + khoá riêng 4 field định danh khi khớp trùng, thêm Loại tiếp nhận/Hình thức khám/Ưu tiên khám (2 danh mục mới `RECEPTION_TYPE`/`EXAM_FORM` + `PRIORITY_REASON`), "Chỉ định dịch vụ khám" (Loại giá dịch vụ `PRICE_TYPE` + Đơn vị/Số lượng, chỉ lưu hiển thị). `encounter` +7 cột, `reference_catalog` +`unit`. Endpoint mới `GET /patients/by-national-id`. Chi tiết đầy đủ ở `docs/CURRENT.md`/`docs/DECISIONS.md` #052. Đã xác minh: 258/258 test `apps/api`, `pnpm -w typecheck/lint/build` sạch, Playwright qua trình duyệt thật.

**Quyết định kiến trúc mới**: `reception`/`encounter`/`appointment` chia sẻ Repository qua Nest `exports` trong cùng 1 transaction (không dùng port — port tự mở transaction riêng, phá vỡ tính atomic của check-in). Vá 3 lỗ hổng ma trận RBAC seed từ S1-04b (`encounter.create/update/cancel` mới, `nurse.encounter.read`/`vital_sign.create` personal→global). Xem đầy đủ `docs/DECISIONS.md` #042.

**Ngoài kế hoạch, sau Sprint 3 — danh mục "Nghề nghiệp" (`docs/DECISIONS.md` #061)**: chủ dự án yêu cầu trực tiếp, đúng khuôn `PATIENT_SOURCE`/`EXAM_TYPE` đã có — thêm category `OCCUPATION` vào `reference_catalog` (migration `20260820160000_reference_catalog_occupation`, không seed cứng vì thiếu nguồn dữ liệu chính thức, `clinic_admin` tự thêm qua UI). Đảo ngược tiếp phần `occupation` của #034 (trước đây cố ý để text tự do). Pill "Nghề nghiệp" mới ở trang "Danh mục hành chính". `patient.occupation` (cột đã có sẵn, không đổi kiểu) đổi từ input text tự do sang `Combobox` load từ danh mục này ở CẢ HAI nơi dùng chung `PatientFormFields.tsx`: form Thêm/Sửa bệnh nhân và form Tiếp nhận bệnh nhân (`ReceptionIntakeForm.tsx`). Đã xác minh thật: `pnpm -w typecheck/lint/build` sạch toàn workspace (lint chỉ còn 3 lỗi cấu hình có sẵn từ trước), 338 test `apps/api` không regress. Playwright qua Chrome thật: pill "Nghề nghiệp" CRUD thêm/hiện đúng; Combobox ở cả 2 form load đúng danh mục vừa tạo, chọn được, hiển thị đúng label sau khi chọn.

**Đã xác minh thật**: `encounter-state-machine.spec.ts` (5/5), `vital-sign-thresholds.spec.ts` (10/10, `packages/core`), `reception-http.spec.ts` (19/19), `encounter-http.spec.ts` (9/9), `patient-http.spec.ts` +4 test `by-phone` — tổng **246/246 test `apps/api` pass** (chạy lặp lại 3 lần liên tiếp để xác nhận hết flaky sau khi sửa 1 race condition thật trong `sync-role-permissions.ts`, không liên quan trực tiếp module mới nhưng lộ ra do ma trận RBAC lớn hơn). `pnpm -w lint/typecheck/build` sạch toàn workspace (trừ 1 lỗi ESLint có sẵn từ trước, không liên quan). Chi tiết đầy đủ ở `docs/CURRENT.md` và `docs/DECISIONS.md` #042.

**Ngoài kế hoạch, sau Sprint 3 — "Hàng đợi ảo" (Virtual Queue theo Bác sĩ/Khoa, `docs/DECISIONS.md` #064)**: kiến trúc chốt qua mockup ở phiên trước, code + kiểm chứng thật ở phiên này. `encounter.doctorId` → nullable + `departmentId` bắt buộc (migration `20260820210000_encounter_virtual_queue`, seed "Khoa chung" mỗi tenant). "Hàng đợi khám" (`ReceptionDoctorQueuePage.tsx`) viết lại thành board 3 cột, gộp "của tôi" ∪ "hàng chờ chung Khoa" qua `includeDepartmentPool`. "Nhận ca" (mở rộng `POST /encounters/:id/start`) set `doctorId` lúc pull atomic, chống trùng fallback (`ENCOUNTER_ALREADY_CLAIMED`). `ReceptionIntakeForm.tsx` thêm khu vực Điều phối Bác sĩ/Khoa; `EncounterConsultationPage.tsx` thêm dải hàng chờ thu gọn. Đã xác minh: 368/368 test `apps/api` (28 file), `pnpm -w typecheck/lint/build` sạch, Playwright qua Chrome thật (2 bác sĩ cùng Khoa xác nhận claim + ẩn ticket đúng). Chi tiết đầy đủ ở `docs/CURRENT.md` và `docs/DECISIONS.md` #064.
## Sprint 4 — Kê đơn (Tuần 7-8, theo `docs/product/plan.md` mục 7)

Trước khi bắt đầu, chủ dự án đề xuất tách "Đơn thuốc y khoa" khỏi "Hoá đơn bán hàng/kho thuốc" (2-Tab thuốc nội bộ/thuốc ngoài, luồng thu ngân) — đối chiếu `CLAUDE.md` thì vượt phạm vi v1 (dược/kho, hoá đơn thuốc đều ngoài v1). Đã hỏi qua `AskUserQuestion`, chốt **giữ đúng phạm vi v1** ("Trường hợp A" — ghi nhận + in, không kho). Toàn bộ đề xuất lưu vào `docs/product/future-modules-reference.md` §2.2.1 cho v2.1.

- [x] S4-01 — Module `prescription`: kê đơn, liều, số lần/ngày, số ngày, số lượng, hướng dẫn dùng (PRE-01). Bảng `drug` (theo tenant, phòng khám tự nhập)/`prescription`/`prescription_item` mới (migration `20260825090000_prescription`). `prescription` là `SignableEntity` ĐẦU TIÊN trong dự án thật sự dùng logic ký (gọi `SignaturePort`) — **C8 (chặn UPDATE nội dung đã ký) lần đầu hiện thực bằng DB trigger thật** (`clinical_note` có sẵn cột nhưng chưa từng có trigger). Đơn NHÁP (`signedAt=null`) sửa tự do qua bulk-replace (`PUT /encounters/:id/prescription-items`, đúng khuôn `DiagnosisRepository.replaceForEncounter()`); ký xong bất biến, sửa = đính chính (`POST .../prescription/amend`, tạo bản mới ĐÃ KÝ NGAY, `supersedesId` trỏ về bản cũ, bản cũ soft-delete).
- [x] S4-02 — Cảnh báo trùng hoạt chất (PRE-02) + đối chiếu dị ứng (PRE-03). Hàm thuần `findDuplicateActiveIngredients`/`findAllergyMatches` (`packages/core/src/prescription/warnings.ts`, 10 unit test) — CHỈ cảnh báo mềm, KHÔNG chặn ký (đã hỏi và chốt: v1 không có nguồn dữ liệu chống chỉ định/liều theo tuổi, PRE-06 hoãn P2 theo #072). **Phát hiện + chốt qua `AskUserQuestion`**: đã có sẵn danh mục "Dị nguyên" (`allergen_catalog`, #069) nhưng chưa từng liên kết bệnh nhân — thêm bảng nối `patient_allergen` (migration `20260825100000_patient_allergen`, đủ 8 cột bắt buộc) thay vì chỉ so khớp `patient.allergyNote` tự do; `allergyNote` GIỮ NGUYÊN làm ghi chú bổ sung, không xoá/đổi ý nghĩa.
- [x] S4-03 — Danh mục thuốc: cấu trúc bảng, màn hình quản lý. `drug` THEO TENANT (PRD mục 8: "phòng khám tự nhập"), không tồn kho/giá bán. Trang "Danh mục thuốc" (`/admin/catalog-pharmacy`, thay `ComingSoonPage`, đổi nhãn sidebar từ "Danh mục Dược và Vật tư" — v1 không quản lý vật tư/kho) — CRUD đúng khuôn `RoomPane.tsx` (PATCH kèm `isActive`+`version`, không endpoint deactivate/reactivate riêng).
- [x] S4-04 — In đơn (PRE-04): mẫu **CHƯA chính thức** (plan.md T5 còn treo) — dựng bố cục chuẩn tạm theo thông tin bắt buộc thường thấy trên đơn thuốc VN, chờ mẫu thật để điều chỉnh. Hạ tầng in ấn DÙNG CHUNG lần đầu (`@media print` + kỹ thuật `visibility:hidden` cho toàn trang trừ `.print-area`, `apps/web/src/app/index.css`) — chủ đích tái dùng cho BIL-02 (Sprint 5/6, `docs/DECISIONS.md` #072) mà không phải viết lại.

**Backend**: `apps/api/src/modules/drug/` (mới, `DrugController/Service/Repository`, export `DrugRepository`); `apps/api/src/modules/encounter/prescription.repository.ts` + mở rộng `EncounterService`/`EncounterController` (4 endpoint mới: `PUT .../prescription-items`, `POST .../prescription/sign|print|amend`); `apps/api/src/modules/patient/patient-allergen.repository.ts` (mới, export qua `PatientModule`, `EncounterModule` dùng chung trong transaction — cùng tinh thần `EncounterRepository`/`ReceptionModule`). Permission mới `drug.read`/`drug.manage` (`packages/core/src/rbac/permissions.ts`) — `prescription.create/sign/print` đã seed sẵn từ S1-04b, chưa từng dùng tới lượt này mới có consumer thật. `patient.allergyNote` giữ nguyên; `PATCH /patients/:id` mở rộng `allergenIds?: string[]` (bulk-replace).

**Frontend**: `apps/web/src/features/drug/` (mới, `DrugCatalogPane.tsx` + api/queries); `apps/web/src/features/encounter/` thêm `PrescriptionPanel.tsx`/`DrugPicker.tsx`/`PrescriptionPrintView.tsx`, gắn vào tab 3 "Kê đơn thuốc" của `EncounterConsultationPage.tsx` (trước là "Sắp ra mắt"). `PatientFormFields.tsx` thêm `MultiSelectCombobox` "Dị nguyên đã biết" (danh mục có sẵn) cạnh "Ghi chú dị ứng khác" (đổi nhãn từ "Tiền sử dị ứng"). Banner đầu trang màn khám thêm chip "DỊ NGUYÊN: ..." tách khỏi "GHI CHÚ DỊ ỨNG: ...". Route `/admin/catalog-pharmacy` lazy theo đúng mẫu #073.

**Đã xác minh thật**: `packages/core` `warnings.spec.ts` (10/10, tổng 99 test); `apps/api` `drug-http.spec.ts` (8/8), `prescription-http.spec.ts` (8/8, gồm test DB trigger C8 chặn `UPDATE` thẳng qua SQL không qua service), `patient-http.spec.ts` +4 test `allergenIds` — tổng **406/406 test `apps/api` pass** (1 lần chạy dính flake tiền-nhiệm đã biết ở `sync-role-permissions.ts`/race điều kiện seed role_permission song song nhiều file test, chạy lại sạch). Vá `tenant-fixture.ts` (cleanup `prescription`/`prescription_item`/`patient_allergen`/`drug` đúng thứ tự FK — self-reference `prescription.supersedesId` KHÔNG null-hoá-rồi-xoá được như `patient.mergedIntoId` vì trigger C8 chặn cả sửa cột này trên bản đã ký, phải xoá theo tầng). `pnpm -w typecheck/lint/build` sạch toàn workspace (lint chỉ còn 4 lỗi cấu hình ESLint plugin có sẵn từ trước, không liên quan; chunk khởi động web vẫn 440 kB, không đổi — `DrugCatalogPane`/`EncounterConsultationPage` đều lazy). Playwright qua Chrome thật (tài khoản/dữ liệu tạo riêng cho lần kiểm qua HTTP API, không phải mock): CRUD Danh mục thuốc (thêm/sửa) → gán dị nguyên cho bệnh nhân qua `MultiSelectCombobox` (dùng đúng danh mục thật đã seed) → màn khám hiện đúng banner dị nguyên → kê 2 thuốc trùng hoạt chất (khác hoa/thường) → cảnh báo PRE-02 hiện đúng → thêm thuốc trùng dị nguyên đã gán → cảnh báo PRE-03 hiện đúng → ký đơn → form khoá lại, chỉ còn "Sửa đơn"/"In đơn" → in đơn không lỗi → "Sửa đơn" tạo bản đính chính đúng (badge "bản đính chính", `supersedesId` đúng, dữ liệu giữ nguyên qua đính chính) — không lỗi console ngoài dự kiến.

**Còn treo — chưa làm trong lần này**: S4-05 (đóng gói on-prem), S4-06 (dữ liệu pilot + giáo án), S4-07 (cài đặt tại pilot), S4-08 (đệm lỗi) — việc vận hành/triển khai riêng biệt, lập kế hoạch khi tới lượt. PRE-05 (sao chép đơn từ lần khám trước, P1) chưa làm.

**Ngoài kế hoạch, sau Sprint 4 (25/08/2026)** — Tiền sử bản thân/gia đình chuyển sang dữ liệu có cấu trúc (`patient_condition`/`patient_family_history` mới) + popup "Tìm kiếm khách hàng" ở Tiếp nhận, mockup Artifact duyệt 2 vòng trước khi code. Permission mới `allergen_catalog.create`. Sửa 1 bug thật (circular import gây `ReferenceError` chỉ lộ ở `pnpm dev`, không lộ ở build). Đã xác minh: 405/412 test `apps/api` (7 fail là flake `geo-http.spec.ts` đã biết), `pnpm -w typecheck/lint/build` sạch, Playwright qua Chrome thật. Chi tiết đầy đủ ở `docs/CURRENT.md` và `docs/CHANGELOG.md` 2026-08-25 (3).

**Ngoài kế hoạch, sau Sprint 4 (26/08/2026)** — "Chỉ định dịch vụ khám" ở Tiếp nhận đổi từ 1 dịch vụ/lượt khám sang danh sách nhiều dịch vụ + cascade giá thật theo `exam_type_price`, đảo ngược `docs/DECISIONS.md` #052 điểm 6 (`docs/DECISIONS.md` #080). Bảng mới `encounter_service_item` (module `reception`, đúng khuôn `vital_sign`); 6 cột `exam_type_*` cũ trên `encounter` chuyển deprecated. Ghi nhận (chưa code) ý nghĩa thật của checkbox "Thanh toán sau" cho lúc làm Thu ngân cơ bản (Sprint 5/6) — xem #080, ảnh hưởng trực tiếp cách tính tổng tiền ở #072. Tiện sửa 1 bug tiền nhiệm không liên quan (`check-mandatory-columns.mjs` thiếu exemption `AllergenGroup`/`Allergen`). Đã xác minh: `reception-http.spec.ts` +2 test, 411/418 test `apps/api` pass (7 skip do flake `geo-http.spec.ts` đã biết), `pnpm -w typecheck/lint/build` sạch. Chi tiết đầy đủ ở `docs/CURRENT.md` và `docs/CHANGELOG.md` 2026-08-26 (4).
